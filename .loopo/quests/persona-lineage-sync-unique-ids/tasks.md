# Quest

- objective: Implement the unified lineage-based persona sync model with unique local thread IDs per persona and additive idempotent sync.
- scope: Dedicated track for tasks `T107`–`T126`, split from shared and VM quest files to isolate unique-ID lineage-sync implementation work.
- constraints: Preserve task IDs/status/acceptance text; keep cross-quest dependency traceability; avoid changing task semantics during migration.

## Tasks

| id   | title                                                                                  | type    | status | dependencies     | scope_files                                                                                                                                                                                                         | owner | branch_ref | worktree_path | acceptance                                                                                                                                                                                                                                                                                                                                                                                                            |
| ---- | -------------------------------------------------------------------------------------- | ------- | ------ | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----- | ---------- | ------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T107 | [H01] Define lineage-sync invariants and additive uniqueness contract                  | general | done   | T106             | packages/codex-rotate/crates/codex-rotate-runtime/src/rotation_hygiene.rs                                                                                                                                           | main  | -          | -             | The contract explicitly states that the same logical conversation across personas must use different local thread IDs while preserving continuity. ; The contract explicitly states additive sync means one local thread per lineage per persona with no duplicate logical conversations on repeated sync. ; The contract explicitly states host and VM must execute the same sync semantics through a shared engine. |
| T108 | [H02] Add rotate-owned conversation sync store schema                                  | coding  | done   | T107             | packages/codex-rotate/crates/codex-rotate-runtime/src/rotation_hygiene.rs; packages/codex-rotate/crates/codex-rotate-runtime/src/daemon.rs                                                                          | main  | -          | -             | The runtime creates and migrates a rotate-owned SQLite store under `~/.codex-rotate` with lineage, binding, and per-target watermark tables. ; Bindings support `(account_id, local_thread_id) <-> lineage_id` lookups for deterministic import/update decisions. ; Schema bootstrap and migrations are idempotent and safe to run repeatedly.                                                                        |
| T109 | [H03] Add ConversationSyncStore APIs and transactional guards                          | coding  | done   | T108             | packages/codex-rotate/crates/codex-rotate-runtime/src/rotation_hygiene.rs                                                                                                                                           | main  | -          | -             | Store APIs expose lineage resolution, binding upsert/read, and watermark update/read operations with explicit transaction boundaries. ; Concurrent sync attempts cannot produce conflicting bindings for the same `(lineage_id, account_id)` pair. ; Failure paths roll back partial store writes and leave a coherent retry state.                                                                                   |
| T110 | [H04] Introduce a shared ConversationTransport abstraction                             | coding  | done   | T107             | packages/codex-rotate/crates/codex-rotate-runtime/src/rotation_hygiene.rs; packages/codex-rotate/crates/codex-rotate-runtime/src/thread-recovery.rs                                                                 | main  | -          | -             | A single transport trait covers `list_threads`, `read_thread`, `start_thread`, `inject_items`, and `start_turn`. ; The sync engine consumes only this transport abstraction rather than backend-specific file/storage details. ; The abstraction supports both host-local API calls and guest-bridge API calls without changing sync semantics.                                                                       |
| T111 | [H05] Implement host transport via native Codex app APIs                               | coding  | done   | T110             | packages/codex-rotate/crates/codex-rotate-runtime/src/rotation_hygiene.rs                                                                                                                                           | main  | -          | -             | Host sync uses only Codex app API methods for conversation materialization (`thread/start`, `thread/inject_items`, `turn/start`). ; Host sync does not write `state_*.sqlite`, `session_index.jsonl`, `sessions/`, or `archived_sessions/` directly for conversation materialization. ; Host transport error handling returns deterministic retryable versus terminal outcomes to the sync engine.                    |
| T112 | [H06] Implement VM transport parity through guest bridge                               | coding  | done   | T110             | packages/codex-rotate/crates/codex-rotate-runtime/src/rotation_hygiene.rs; packages/codex-rotate/crates/codex-rotate-runtime/src/daemon.rs                                                                          | main  | -          | -             | VM sync uses the same transport operations and payload model as host sync, routed through the guest bridge. ; Guest bridge request/response contracts carry lineage and watermark metadata required by the shared sync engine. ; Host and VM paths differ only by transport wiring and not by dedupe/identity rules.                                                                                                  |
| T113 | [H07] Make handoff payload lineage-aware with source watermark metadata                | coding  | done   | T109, T110       | packages/codex-rotate/crates/codex-rotate-runtime/src/rotation_hygiene.rs                                                                                                                                           | main  | -          | -             | Exported sync payloads include lineage identity and source watermark metadata for incremental idempotent sync. ; Payload mapping remains compatible with existing item mapping for user/agent continuity. ; Payload generation remains bounded and deterministic for large conversation histories.                                                                                                                    |
| T114 | [H08] Implement lineage-first import resolution and binding persistence                | coding  | done   | T109, T113       | packages/codex-rotate/crates/codex-rotate-runtime/src/rotation_hygiene.rs                                                                                                                                           | main  | -          | -             | Import resolves `(lineage_id, target_account_id)` before materialization and updates the existing bound thread when present. ; Import creates exactly one new target-local thread when binding is absent and persists the new binding atomically. ; Repeated imports of the same lineage into the same persona do not create additional local threads.                                                                |
| T115 | [H09] Enforce unique local-ID invariant across personas                                | coding  | done   | T114             | packages/codex-rotate/crates/codex-rotate-runtime/src/rotation_hygiene.rs; packages/codex-rotate/crates/codex-rotate-cli/tests/live_host_e2e.rs                                                                     | main  | -          | -             | The sync engine guarantees that source and target local thread IDs differ for the same lineage after cross-persona sync. ; Sync logic never reuses source local IDs as target IDs during materialization. ; Invariant checks are covered by hermetic and live acceptance assertions.                                                                                                                                  |
| T116 | [H10] Replace host file-copy conversation mirroring with import-based lineage sync     | coding  | done   | T111, T114, T115 | packages/codex-rotate/crates/codex-rotate-runtime/src/rotation_hygiene.rs                                                                                                                                           | main  | -          | -             | Rotation continuity no longer depends on direct host conversation file mirroring for active/archived thread state. ; Host rotation continuity path executes the shared lineage sync engine and transport APIs. ; Existing non-conversation host persona provisioning/symlink/auth behavior remains intact.                                                                                                            |
| T117 | [H11] Align VM rotation continuity to the same lineage sync engine                     | coding  | done   | T112, T114, T115 | packages/codex-rotate/crates/codex-rotate-runtime/src/rotation_hygiene.rs                                                                                                                                           | main  | -          | -             | VM rotation uses the shared lineage sync flow and store semantics instead of a VM-specific handoff path. ; Host and VM produce equivalent lineage/binding/watermark outcomes for equivalent scenarios. ; VM continuity retains existing VM lifecycle controls while replacing conversation-specific divergence.                                                                                                       |
| T118 | [H12] Add recovery-event translation from source local IDs to target local IDs         | coding  | done   | T114             | packages/codex-rotate/crates/codex-rotate-runtime/src/watch.rs; packages/codex-rotate/crates/codex-rotate-runtime/src/thread-recovery.rs; packages/codex-rotate/crates/codex-rotate-runtime/src/rotation_hygiene.rs | main  | -          | -             | Pending interrupted-thread events are translated using lineage bindings from source local thread IDs to target local thread IDs after switch. ; If a target binding is missing, sync materialization occurs before enqueueing translated recovery events. ; Source pending recovery entries are cleared only after target translation/persistence succeeds.                                                           |
| T119 | [H13] Rework `repair-host-history` into lineage-based one-time recovery                | coding  | done   | T109, T111, T114 | packages/codex-rotate/crates/codex-rotate-cli/src/main.rs; packages/codex-rotate/crates/codex-rotate-runtime/src/rotation_hygiene.rs                                                                                | main  | -          | -             | `repair-host-history` dry-run reports discovered source conversations, existing bindings, and planned creates/updates per target. ; `repair-host-history --apply` imports source corpus into targets via app APIs, creating target-local IDs and persisting bindings. ; Re-running apply is idempotent and does not create duplicate logical conversations per target persona.                                        |
| T120 | [H14] Add direct-write guardrails for conversation storage paths                       | coding  | done   | T116             | packages/codex-rotate/crates/codex-rotate-runtime/src/rotation_hygiene.rs                                                                                                                                           | main  | -          | -             | Conversation materialization paths no longer write persona-local `state_*.sqlite`, `session_index.jsonl`, `sessions/`, or `archived_sessions/` directly. ; Any fallback persona-local reads are isolated behind adapters and never used for direct conversation writes. ; Guardrail tests fail if new code reintroduces direct conversation file mutation in sync paths.                                              |
| T121 | [H15] Add hermetic tests for idempotent additive sync and uniqueness                   | coding  | done   | T115, T116, T118 | packages/codex-rotate/crates/codex-rotate-runtime/src/rotation_hygiene.rs; packages/codex-rotate/crates/codex-rotate-cli/tests/live_host_e2e.rs                                                                     | main  | -          | -             | A->B->A->B without new source content produces no duplicate logical conversation in B. ; Re-syncing the same lineage into the same target updates one bound thread instead of creating another. ; Tests assert same lineage across personas maps to different local thread IDs.                                                                                                                                       |
| T122 | [H16] Add host live acceptance for full trigger/restart/sync/recovery flow             | coding  | done   | T118, T121       | packages/codex-rotate/crates/codex-rotate-cli/tests/live_host_e2e.rs                                                                                                                                                | main  | -          | -             | Live host tests cover tray/watch-triggered rotation flow end-to-end with restart and lineage sync completion checks. ; Live host tests verify interrupted-thread auto-resume occurs after sync completion and app restart using target-local IDs. ; Live host tests assert no duplicate logical thread appears on repeated rotations without new source content.                                                      |
| T123 | [H17] Add VM live parity acceptance for the same lineage-sync invariants               | coding  | done   | T117, T121, T084 | packages/codex-rotate/crates/codex-rotate-cli/tests/live_vm_e2e.rs                                                                                                                                                  | main  | -          | -             | Live VM tests cover the same additive idempotency, unique-local-ID, and translated recovery invariants as host. ; VM live results demonstrate one shared sync model rather than VM-specific continuity semantics. ; The task is implemented at the code level, awaiting real UTM infrastructure for full live execution.                                                                                              |
| T124 | [H18] Add one-time original-single-profile recovery runbook and verification checklist | general | done   | T119             | docs/; .loopo/quests/shared-persona-rotation-e2e/tasks.md                                                                                                                                                           | main  | -          | -             | The runbook defines how to execute dry-run/apply recovery from original single-profile history and verify target-local IDs were created. ; The checklist includes post-run validation for additive no-duplicate behavior across repeated sync cycles. ; The runbook documents that v1 prevents new duplicates but does not auto-delete historical duplicates.                                                         |
| T125 | [H19] Add resilience tests for schema drift and adapter fallback                       | coding  | done   | T120, T121       | packages/codex-rotate/crates/codex-rotate-runtime/src/rotation_hygiene.rs; packages/codex-rotate/crates/codex-rotate-cli/tests/live_host_e2e.rs                                                                     | main  | -          | -             | Sync continues for known bindings and active threads when local persona file schema/read fallbacks change. ; Adapter failures do not crash rotation and instead surface actionable, bounded errors. ; Regression tests cover both host and VM adapter pathways for fallback behavior.                                                                                                                                 |
| T126 | [H20] Add duplicate-observability reporting without destructive cleanup in v1          | coding  | done   | T121             | packages/codex-rotate/crates/codex-rotate-cli/src/main.rs; packages/codex-rotate/crates/codex-rotate-runtime/src/rotation_hygiene.rs                                                                                | main  | -          | -             | Runtime/CLI can report potential pre-existing duplicate logical conversations per persona for operator visibility. ; Reporting is read-only and does not delete or mutate existing persona conversations in v1. ; Output distinguishes historical duplicates from newly prevented duplicates under lineage-sync behavior.                                                                                             |
