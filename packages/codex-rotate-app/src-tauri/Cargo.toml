[package]
name = "codex-rotate-tray"
version = "1.0.0"
description = "Codex Rotate Tray"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2.0.1", features = [] }

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tauri = { version = "2.0.1", features = ["tray-icon"] }

[features]
default = []
