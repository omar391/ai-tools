document:
  dsl: 1.0.0
  namespace: web-auth-openai-com
  name: codex-rotate-account-flow-device-auth
  version: 1.3.10
  summary: "Managed-profile OpenAI device-auth flow that first reaches a normal authenticated ChatGPT session, then completes Codex device authorization inside the same workflow."
  metadata:
    preferredProfile: dev-1
    primaryTarget: primary
    targets:
      - id: primary
        kind: web
        match:
          kind: host_pattern
          value: "*"
        target: auth.openai.com
input:
  schema:
    format: json
    document:
      type: object
      properties:
        enabled:
          default: true
        mode:
          default: codex_login
        codex_bin:
          default: codex
        auth_url: {}
        device_code: {}
        callback_url: {}
        callback_port: {}
        codex_session_dir: {}
        codex_login_pid: {}
        codex_login_stdout_path: {}
        codex_login_stderr_path: {}
        codex_login_exit_path: {}
        codex_home_path: {}
        codex_auth_file_path: {}
        email: {}
        account_login_locator:
          type: object
          format: secret-locator
        prefer_password_login:
          default: "true"
        full_name:
          default: "Dev Astronlab"
        birth_month:
          default: 1
        birth_day:
          default: 24
        birth_year:
          default: 1990
        prefer_signup_recovery:
          default: "true"
      required:
        - email
use:
  functions:
    click_signup_button:
      metadata:
        description: Click the Sign up CTA from the OpenAI login shell.
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const clicked = await page.evaluate(() => {
              const isVisible = (node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              };
              const candidates = Array.from(document.querySelectorAll("a[href*='create-account'], button, a, [role='button']"))
                .filter((node) => isVisible(node));
              const target = candidates.find((node) => {
                const href = node.getAttribute("href") || "";
                const text = String(node.innerText || node.textContent || "").replace(/\s+/g, " ").trim();
                return /create-account/i.test(href) || /^sign up$/i.test(text) || /create account/i.test(text);
              });
              if (!target) {
                return { ok: false, error: "sign-up-button-not-found" };
              }
              target.click();
              return {
                ok: true,
                clicked_text: String(target.innerText || target.textContent || "").replace(/\s+/g, " ").trim() || null,
                clicked_href: target.getAttribute("href") || null,
              };
            });
            if (clicked.ok) {
              await page.waitForTimeout(1200);
            }
            return {
              ...clicked,
              current_url: page.url(),
            };
    click_primary_continue:
      metadata:
        description: "Click the primary visible submit or continue control for the current OpenAI form."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: "const result = await page.evaluate(() => {\n  const normalize = (value) => String(value || \"\").replace(/\\s+/g, \" \").trim();\n  const readLabel = (node) => {\n    if (!(node instanceof Element)) {\n      return \"\";\n    }\n    if (node instanceof HTMLInputElement) {\n      return normalize(node.value || node.getAttribute(\"aria-label\") || node.getAttribute(\"title\") || \"\");\n    }\n    return normalize(node.innerText || node.textContent || node.getAttribute(\"aria-label\") || node.getAttribute(\"title\") || \"\");\n  };\n  const isVisible = (node) => {\n    if (!(node instanceof Element)) {\n      return false;\n    }\n    const style = window.getComputedStyle(node);\n    if (!style || style.display === \"none\" || style.visibility === \"hidden\") {\n      return false;\n    }\n    const rect = node.getBoundingClientRect();\n    return rect.width > 0 && rect.height > 0;\n  };\n  const isDisabled = (node) => node.hasAttribute(\"disabled\") || node.getAttribute(\"aria-disabled\") === \"true\";\n  const isExplicitSubmit = (node) => {\n    if (node instanceof HTMLInputElement) {\n      return node.type === \"submit\";\n    }\n    if (node instanceof HTMLButtonElement) {\n      return node.type === \"submit\";\n    }\n    return false;\n  };\n  const clickNode = (node) => {\n    if (!(node instanceof HTMLElement)) {\n      return false;\n    }\n    node.click();\n    return true;\n  };\n  const selectors = [\n    \"input[type='email']\",\n    \"input[name='email']\",\n    \"input[autocomplete='email']\",\n    \"input[autocomplete='username']\",\n    \"input[type='password']\",\n    \"input[autocomplete='current-password']\",\n    \"input[autocomplete='new-password']\",\n    \"input[autocomplete='one-time-code']\",\n    \"input[name*='code']\",\n    \"input[inputmode='numeric']\",\n  ];\n  const activeForm = document.activeElement instanceof Element ? document.activeElement.closest(\"form\") : null;\n  const candidateForms = [];\n  if (activeForm) {\n    candidateForms.push(activeForm);\n  }\n  for (const selector of selectors) {\n    const field = document.querySelector(selector);\n    if (field instanceof Element) {\n      const form = field.closest(\"form\");\n      if (form && !candidateForms.includes(form)) {\n        candidateForms.push(form);\n      }\n    }\n  }\n  const buttonMatchers = /continue|next|verify|submit|try again|retry|continue to codex|continue to openai|authorize|allow|accept/i;\n  const formControls = candidateForms.flatMap((form) => Array.from(form.querySelectorAll(\"button, input[type='submit'], [role='button']\"))).filter((node) => isVisible(node) && !isDisabled(node));\n  const preferredFormControl = formControls.find((node) => isExplicitSubmit(node) && buttonMatchers.test(readLabel(node))) || formControls.find((node) => buttonMatchers.test(readLabel(node))) || formControls.find((node) => isExplicitSubmit(node));\n  if (preferredFormControl && clickNode(preferredFormControl)) {\n    return { ok: true, strategy: \"form-submit\", clicked_text: readLabel(preferredFormControl) || null };\n  }\n  const fallback = Array.from(document.querySelectorAll(\"button, a, [role='button'], input[type='submit']\")).filter((node) => isVisible(node) && !isDisabled(node)).find((node) => buttonMatchers.test(readLabel(node)));\n  if (fallback && clickNode(fallback)) {\n    return { ok: true, strategy: \"text-match\", clicked_text: readLabel(fallback) || null };\n  }\n  if (activeForm instanceof HTMLFormElement) {\n    activeForm.requestSubmit();\n    return { ok: true, strategy: \"request-submit\", clicked_text: null };\n  }\n  return { ok: false, error: \"primary-continue-not-found\" };\n});\nif (result.ok) {\n  await page.waitForTimeout(1200);\n}\nreturn { ...result, current_url: page.url() };\n"
    click_oauth_consent_continue:
      metadata:
        description: "Click the visible OAuth consent continue control with a Playwright-side click so navigation-triggering consent does not die inside page.evaluate."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
            const readLabel = async (locator) => {
              return await locator.evaluate((node) => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                if (node instanceof HTMLInputElement) {
                  return normalize(node.value || node.getAttribute("aria-label") || node.getAttribute("title") || "");
                }
                if (!(node instanceof Element)) {
                  return "";
                }
                return normalize(
                  node.innerText
                  || node.textContent
                  || node.getAttribute("aria-label")
                  || node.getAttribute("title")
                  || ""
                );
              }).catch(() => "");
            };
            const isDisabled = async (locator) => {
              return await locator.evaluate((node) => {
                if (!(node instanceof Element)) {
                  return true;
                }
                return node.hasAttribute("disabled") || node.getAttribute("aria-disabled") === "true";
              }).catch(() => true);
            };
            const controls = page.locator("button, [role='button'], a, input[type='submit']");
            const candidates = [];
            const count = await controls.count().catch(() => 0);
            for (let index = 0; index < count; index += 1) {
              const locator = controls.nth(index);
              if (!(await locator.isVisible().catch(() => false))) {
                continue;
              }
              if (await isDisabled(locator)) {
                continue;
              }
              const label = normalize(await readLabel(locator));
              if (!label) {
                continue;
              }
              candidates.push({ locator, label });
            }
            const matchers = [
              /^continue to codex$/i,
              /^continue$/i,
              /continue to codex|continue to openai|authorize|allow|accept/i,
            ];
            for (const matcher of matchers) {
              for (const candidate of candidates) {
                if (!matcher.test(candidate.label)) {
                  continue;
                }
                await candidate.locator.scrollIntoViewIfNeeded().catch(() => {});
                await candidate.locator.click({ timeout: 5000 });
                await page.waitForTimeout(1200).catch(() => {});
                return {
                  ok: true,
                  strategy: "locator-click",
                  clicked_text: candidate.label,
                  current_url: page.url(),
                };
              }
            }
            return {
              ok: false,
              error: "oauth-consent-continue-not-found",
              current_url: page.url(),
            };
    wait_for_openai_surface_ready:
      metadata:
        description: "Wait for the OpenAI auth surface to render a stable visible shell before classifying it."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const ready = await page.waitForFunction(() => {
              const bodyText = document.body ? String(document.body.innerText || document.body.textContent || "") : "";
              const hasVisible = (selector) => Array.from(document.querySelectorAll(selector)).some((node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                if (node.closest("script,style,noscript,template,[hidden],[aria-hidden=\"true\"]")) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              });
              const emailInput = hasVisible("input[type='email'], input[name='email'], input[autocomplete='email'], input[autocomplete='username']");
              const passwordInput = hasVisible("input[type='password'], input[name='password'], input[name='new-password'], input[autocomplete='current-password'], input[autocomplete='new-password']");
              const codeInput = hasVisible("input[name*='code' i], input[autocomplete='one-time-code'], input[inputmode='numeric']");
              const actionableButton = Array.from(document.querySelectorAll("button, a, [role='button'], input[type='submit']")).some((node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                if (!(rect.width > 0 && rect.height > 0)) {
                  return false;
                }
                const text = String(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || "")).replace(/\s+/g, " ").trim();
                return /log in|sign in|continue|verify|allow|authorize|accept|one-time code|device code/i.test(text);
              });
              const meaningfulText = /welcome back|enter your password|enter the code|device code|authorize|allow|sign in to codex|check your inbox/i.test(bodyText);
              const appShell = /\bnew chat\b|\bsearch chats\b|\bprojects\b|\bimages\b|\bapps\b|\bcodex\b|what['’]s on your mind today\?/i.test(bodyText);
              const publicChatGptShell = /log in to get answers based on saved chats|get responses tailored to you|sign up for free|see plans and pricing|welcome to chatgpt/i.test(bodyText);
              return emailInput || passwordInput || codeInput || actionableButton || meaningfulText || appShell || publicChatGptShell;
            }, { timeout: 8000 }).then(() => true).catch(() => false);
            return { ok: ready, current_url: page.url() };
    classify_openai_auth_surface:
      metadata:
        description: "Classify the current OpenAI auth surface for the local device-auth login branch."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
            const isVisible = (node) => {
              if (!(node instanceof Element)) {
                return false;
              }
              if (node.closest("script,style,noscript,template,[hidden],[aria-hidden=\"true\"]")) {
                return false;
              }
              const style = window.getComputedStyle(node);
              if (!style || style.display === "none" || style.visibility === "hidden") {
                return false;
              }
              const rect = node.getBoundingClientRect();
              return rect.width > 0 && rect.height > 0;
            };
            const title = await page.title().catch(() => "");
            const bodyText = await page.locator("body").innerText().catch(() => "");
            const normalizedText = normalize(bodyText);
            const firstLine = normalizedText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || null;
            const currentUrl = page.url();
            let host = "";
            let path = "";
            try {
              const parsed = new URL(currentUrl);
              host = parsed.hostname;
              path = parsed.pathname;
            } catch {}
            const visibleButtons = await page.evaluate(() => {
              const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
              const isVisible = (node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                if (node.closest("script,style,noscript,template,[hidden],[aria-hidden=\"true\"]")) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              };
              return Array.from(document.querySelectorAll("button,a,[role='button'],input[type='submit']"))
                .filter((node) => isVisible(node))
                .map((node) => node instanceof HTMLInputElement
                  ? normalize(node.value || node.getAttribute("aria-label") || "")
                  : normalize(node.innerText || node.textContent || node.getAttribute("aria-label") || ""))
                .filter(Boolean)
                .slice(0, 24);
            }).catch(() => []);
            const emailFieldState = await page.evaluate(() => {
              const isVisible = (node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                if (node.closest("script,style,noscript,template,[hidden],[aria-hidden=\"true\"]")) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              };
              const field = Array.from(document.querySelectorAll("input[type='email'], input[name='email'], input[autocomplete='email'], input[autocomplete='username']"))
                .find((node) => isVisible(node));
              if (!(field instanceof HTMLInputElement)) {
                return {
                  present: false,
                  editable: false,
                  disabled: false,
                  prefilled: false,
                };
              }
              const disabled = field.disabled || field.getAttribute("aria-disabled") === "true" || field.hasAttribute("readonly");
              return {
                present: true,
                editable: !disabled,
                disabled,
                prefilled: String(field.value || "").trim().length > 0,
              };
            }).catch(() => ({
              present: false,
              editable: false,
              disabled: false,
              prefilled: false,
            }));
            const hasVisible = async (selector) => {
              return await page.evaluate((currentSelector) => {
                const isVisible = (node) => {
                  if (!(node instanceof Element)) {
                    return false;
                  }
                  if (node.closest("script,style,noscript,template,[hidden],[aria-hidden=\"true\"]")) {
                    return false;
                  }
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                return Array.from(document.querySelectorAll(currentSelector)).some((node) => isVisible(node));
              }, selector).catch(() => false);
            };
            const emailInput = await hasVisible("input[type='email'], input[name='email'], input[autocomplete='email'], input[autocomplete='username']");
            const passwordInput = await hasVisible("input[type='password'], input[name='password'], input[name='new-password'], input[autocomplete='current-password'], input[autocomplete='new-password']");
            const codeInput = await hasVisible("input[name*='code' i], input[autocomplete='one-time-code'], input[aria-label*='code' i], input[placeholder*='code' i], input[inputmode='numeric']");
            const antiBotGate = /just a moment/i.test(title)
              || /performing security verification|security verification|verify you are not a bot|verifying you are human|verify you are human|checking your browser|cloudflare|press & hold|turnstile/i.test(normalizedText)
              || await hasVisible("input[name='cf-turnstile-response'], iframe[src*='challenge'], iframe[title*='Widget containing']");
            const retryableTimeout = /oops, an error occurred|operation timed out|please try again|invalid_state/i.test(normalizedText) || /oops, an error occurred|operation timed out|invalid_state/i.test(title);
            const sessionEnded = /your session has ended|your session has expired/i.test(normalizedText);
            const rateLimitExceeded = /rate limit exceeded|too many requests|please try again later/i.test(normalizedText);
            const addPhonePrompt = /add-phone/i.test(path)
              || /phone number required|add your phone number|verify your phone number|enter your phone number/i.test(normalizedText);
            const aboutYou = /confirm your age|full name|birthday|finish creating account|what should we call you|tell us about yourself|about you/i.test(normalizedText)
              || /about-you/i.test(path);
            const resetPasswordPrompt = /reset-password/i.test(currentUrl) || /\breset password\b/i.test(normalizedText);
            const incorrectCode = /incorrect code|invalid code|code expired|expired code/i.test(normalizedText);
            const existingAccountPrompt = /an account for this email address already exists|this email address already exists|account already exists|already registered/i.test(normalizedText);
            const usernameNotFound = /username_not_found|username not found/i.test(normalizedText);
            const invalidCredentials = /incorrect email address or password|incorrect password|wrong password|invalid email or password/i.test(normalizedText);
            const sawOauthConsent = /sign in to codex with chatgpt|share your name, email, and profile picture with codex|share your name, email, and profile picture|link your account/i.test(normalizedText) || /sign in to codex/i.test(title);
            const oauthContinueVisible = sawOauthConsent && visibleButtons.some((text) => /continue to codex|continue to openai|authorize|allow|accept/i.test(text) || /^continue$/i.test(text));
            const continueButtonVisible = visibleButtons.some((text) => /continue|next|verify|submit|log in|sign in/i.test(text));
            const oneTimeCodeOption = visibleButtons.some((text) => /one-time code|one time code/i.test(text));
            const rawEmailVerification = (!sawOauthConsent && codeInput)
              || (!sawOauthConsent && /check your inbox|check your email|verify your email|verification code|enter the verification code|enter your code|temporary verification code|resend email/i.test(normalizedText))
              || (!sawOauthConsent && /email-verification/i.test(path));
            const emailVerification = rawEmailVerification && !aboutYou;
            const deviceCodePrompt = codeInput && !emailVerification && /device code|authorization code|code displayed in your terminal|paste the device code|enter (?:the |your )?(?:device )?code/i.test(normalizedText);
            const success = /you can close this window|authorization complete|device authorized|successfully logged in|signed in/i.test(normalizedText) || /close this window/i.test(title) || /\/deviceauth\/callback(?:\/|$)?/i.test(path);
            const signupPasswordRoute = /\/create-account\/password(?:\/|$)?/i.test(path)
              || (/\/create-account(?:\/|$)/i.test(path) && /create a password|choose a password/i.test(normalizedText));
            const loginPasswordRoute = /\/log-in\/password(?:\/|$)?/i.test(path)
              || (/\/log-in(?:\/|$)/i.test(path)
                && !emailInput
                && (passwordInput
                  || /enter your password/i.test(normalizedText)
                  || (emailFieldState.prefilled === true
                    && emailFieldState.editable === false
                    && continueButtonVisible)));
            const chatGptHost = /(?:^|\.)chatgpt\.com$/i.test(host);
            const publicShellSignals = /log in to get smarter responses|get responses tailored to you|see plans and pricing|sign up for free|welcome to chatgpt|get started|where should we begin/i.test(normalizedText);
            const loginCtaVisible = visibleButtons.some((text) => /^log in$/i.test(text) || /^sign in$/i.test(text));
            const signupCtaVisible = visibleButtons.some((text) => /^sign up$/i.test(text) || /sign up for free|create account|get started/i.test(text));
            const securityShell = chatGptHost && !/^\/auth\//i.test(path) && /secure sign in with chatgpt|trusted devices|enable device code authorization for codex|log out of all devices|multi-factor authentication|security keys & passkeys/i.test(normalizedText);
            const appShellSignals = /\bnew chat\b|\bsearch chats\b|\bimages\b|\bapps\b|\bprojects\b|\bdeep research\b|what['’]s on your mind today\?|ask anything/i.test(normalizedText);
            const publicShell = chatGptHost && !securityShell && (
              loginCtaVisible
              || signupCtaVisible
              || (publicShellSignals && !appShellSignals && !emailInput && !passwordInput && !codeInput)
            );
            const appShell = chatGptHost
              && !/^\/auth\//i.test(path)
              && !publicShell
              && appShellSignals;
            const authenticatedAppShell = (appShell || securityShell) && !publicShell;
            let stage = "unknown";
            if (success) {
              stage = "success";
            } else if (deviceCodePrompt) {
              stage = "device_code";
            } else if (antiBotGate) {
              stage = "security_verification";
            } else if (rateLimitExceeded) {
              stage = "rate_limit";
            } else if (sessionEnded) {
              stage = "session_ended";
            } else if (retryableTimeout) {
              stage = "retryable_timeout";
            } else if (addPhonePrompt) {
              stage = "add_phone";
            } else if (oauthContinueVisible) {
              stage = "oauth_consent";
            } else if (authenticatedAppShell) {
              stage = "app_shell";
            } else if (aboutYou) {
              stage = "about_you";
            } else if (emailVerification) {
              stage = "email_verification";
            } else if (existingAccountPrompt) {
              stage = "existing_account_prompt";
            } else if (invalidCredentials) {
              stage = "invalid_credentials";
            } else if (signupPasswordRoute || (/\/create-account(?:\/|$)/i.test(path) && passwordInput)) {
              stage = "signup_password";
            } else if (/\/create-account(?:\/|$)/i.test(path) && emailInput) {
              stage = "signup_email";
            } else if (loginPasswordRoute || (/\/log-in(?:\/|$)/i.test(path) && passwordInput)) {
              stage = "login_password";
            } else if (/\/log-in(?:\/|$)/i.test(path) && emailInput) {
              stage = "login_email";
            } else if (passwordInput) {
              stage = "login_password";
            } else if (emailInput) {
              stage = "login_email";
            } else if (publicShell && !emailInput && !passwordInput && !codeInput) {
              stage = "public_chatgpt";
            } else if (emailInput || passwordInput || codeInput || /welcome back|log in|sign in|check your inbox|enter your password/i.test(normalizedText)) {
              stage = "auth_prompt";
            }
            return {
              ok: true,
              current_url: currentUrl,
              headline: firstLine || title || null,
              stage,
              success,
              host: host || null,
              path: path || null,
              chatgpt_app_shell: authenticatedAppShell,
              chatgpt_public_shell: publicShell,
              account_ready: authenticatedAppShell,
              email_input: emailInput,
              email_input_editable: emailFieldState.editable,
              email_input_disabled: emailFieldState.disabled,
              email_prefilled: emailFieldState.prefilled,
              password_input: passwordInput,
              code_input: codeInput,
              email_verification: emailVerification,
              device_code_prompt: deviceCodePrompt,
              oauth_continue_visible: oauthContinueVisible,
              continue_button_visible: continueButtonVisible,
              login_cta_visible: loginCtaVisible,
              signup_cta_visible: signupCtaVisible,
              one_time_code_option: oneTimeCodeOption,
              retryable_timeout: retryableTimeout,
              rate_limit_exceeded: rateLimitExceeded,
              reset_password_prompt: resetPasswordPrompt,
              incorrect_code: incorrectCode,
              username_not_found: usernameNotFound,
              invalid_credentials: invalidCredentials,
              existing_account_prompt: existingAccountPrompt,
              follow_up_step: aboutYou,
              add_phone_prompt: addPhonePrompt,
              session_ended: sessionEnded,
              anti_bot_gate: antiBotGate,
              auth_prompt: stage === "login_email" || stage === "login_password" || stage === "signup_email" || stage === "signup_password" || stage === "auth_prompt",
            };
    clear_openai_security_gate:
      metadata:
        description: Pause for HIL when OpenAI or Cloudflare requires human verification.
        mode: hil
        targetRef: primary
        pause:
          responder: human
          verifyPersistence: false
          restorePreferredMode: false
        browser:
          screenshot: true
          validation:
            post:
              kind: js
              expression: "const bodyText = document.body ? document.body.innerText : '';\nconst title = document.title || '';\nconst hasTurnstile = !!document.querySelector('input[name=\"cf-turnstile-response\"], iframe[src*=\"challenge\"], iframe[title*=\"Widget containing\"]');\nconst stillBlocked = hasTurnstile\n  || /just a moment/i.test(title)\n  || /performing security verification|security verification|verify you are human|verifying you are human|verify you are not a bot/i.test(bodyText);\nreturn {\n  ok: !stillBlocked,\n  current_url: location.href,\n  still_blocked: stillBlocked,\n};\n"
      call: afn.request_input
      with:
        body:
          hilMode: page
          message: If OpenAI or Cloudflare shows a human-verification page or checkbox, complete it in the HIL viewer and wait until the normal auth page is visible.
          source: |
            const blockedNow = async () => {
              const title = await page.title().catch(() => "");
              const bodyText = await page.locator("body").innerText().catch(() => "");
              const hasTurnstile = await page.locator("input[name='cf-turnstile-response'], iframe[src*='challenge'], iframe[title*='Widget containing']").count().catch(() => 0) > 0;
              return hasTurnstile
                || /just a moment/i.test(title)
                || /performing security verification|security verification|verify you are human|verifying you are human|verify you are not a bot/i.test(bodyText);
            };

            if (!(await blockedNow())) {
              return {
                ok: true,
                skipped: true,
                blocked: false,
                current_url: page.url(),
              };
            }

            const clickLocator = async (locator, strategy) => {
              if (!locator || (await locator.count().catch(() => 0)) === 0) {
                return null;
              }
              if (!(await locator.isVisible().catch(() => false))) {
                return null;
              }
              await locator.scrollIntoViewIfNeeded().catch(() => {});
              const attempts = [
                async () => locator.click({ timeout: 2000 }),
                async () => locator.click({ timeout: 2000, force: true }),
                async () => locator.press(" "),
                async () => locator.press("Enter"),
              ];
              for (const attempt of attempts) {
                await attempt().catch(() => {});
                await page.waitForTimeout(1500).catch(() => {});
                if (!(await blockedNow())) {
                  return strategy;
                }
              }
              return null;
            };

            const clickBoundingBox = async (locator, strategy) => {
              if (!locator || (await locator.count().catch(() => 0)) === 0) {
                return null;
              }
              if (!(await locator.isVisible().catch(() => false))) {
                return null;
              }
              const box = await locator.boundingBox().catch(() => null);
              if (!box) {
                return null;
              }
              const x = box.x + Math.min(24, Math.max(10, box.width * 0.12));
              const y = box.y + (box.height / 2);
              await page.mouse.move(x, y, { steps: 8 }).catch(() => {});
              await page.waitForTimeout(150).catch(() => {});
              await page.mouse.click(x, y, { delay: 120 }).catch(() => {});
              await page.waitForTimeout(1500).catch(() => {});
              if (!(await blockedNow())) {
                return strategy;
              }
              return null;
            };

            const strategies = [
              async () => clickLocator(page.getByRole("checkbox", { name: /verify you are human/i }).first(), "role-checkbox"),
              async () => clickLocator(page.locator("input[type='checkbox']").first(), "input-checkbox"),
              async () => clickBoundingBox(page.getByText(/verify you are human/i).first(), "text-bbox"),
              async () => clickBoundingBox(page.locator("iframe[src*='challenge'], iframe[title*='Widget containing']").first(), "iframe-bbox"),
            ];

            let strategy = null;
            for (const attempt of strategies) {
              strategy = await attempt();
              if (strategy) {
                break;
              }
            }

            return {
              ok: true,
              blocked: await blockedNow(),
              strategy,
              current_url: page.url(),
            };
    fill_openai_email_field:
      metadata:
        description: "Fill the OpenAI email field on the local device-auth login branch."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.type
      with:
        body:
          selector: 'input[type="email"], input[name="email"], input[autocomplete="email"], input[autocomplete="username"]'
          text: "${inputs.email}"
          dispatch_events_after_fill: true
          blur_after_fill: true
          settle_ms: 250
    submit_signup_email_form:
      metadata:
        description: "Submit the signup email form and verify that OpenAI advances to a valid next state."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const expectedEmail = String(args.email || "").trim().toLowerCase();
            const detectStage = async () => {
              for (let attempt = 1; attempt <= 3; attempt += 1) {
                try {
                  return await page.evaluate(() => {
                    const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                    const hasVisible = (selector) => Array.from(document.querySelectorAll(selector)).some((node) => {
                      if (!(node instanceof Element)) {
                        return false;
                      }
                      const style = window.getComputedStyle(node);
                      if (!style || style.display === "none" || style.visibility === "hidden") {
                        return false;
                      }
                      const rect = node.getBoundingClientRect();
                      return rect.width > 0 && rect.height > 0;
                    });
                    const bodyText = normalize(document.body ? (document.body.innerText || document.body.textContent || "") : "");
                    const title = normalize(document.title || "");
                    const path = location.pathname;
                    const host = location.hostname;
                    const emailInput = hasVisible('input[type="email"], input[name="email"], input[autocomplete="email"], input[autocomplete="username"]');
                    const passwordInput = hasVisible('input[type="password"], input[name="password"], input[name="new-password"], input[autocomplete="current-password"], input[autocomplete="new-password"]');
                    const verificationInput = hasVisible('input[name*="code" i], input[autocomplete="one-time-code"], input[inputmode="numeric"]');
                    const antiBotGate = /just a moment/i.test(title)
                      || /performing security verification|verify you are not a bot|verifying you are human|verify you are human/i.test(bodyText)
                      || hasVisible('input[name="cf-turnstile-response"], iframe[src*="challenge"], iframe[title*="Widget containing"]');
                    const retryableTimeout = /oops, an error occurred|operation timed out|try again/i.test(bodyText);
                    const sessionEnded = /your session has ended/i.test(bodyText);
                    const rateLimitExceeded = /rate limit exceeded|too many requests|please try again later/i.test(bodyText);
                    const addPhonePrompt = /add-phone/i.test(path)
                      || /phone number required|add your phone number|verify your phone number|enter your phone number/i.test(bodyText);
                    const aboutYou = /confirm your age|full name|birthday|finish creating account|what should we call you|tell us about yourself|about you/i.test(bodyText)
                      || /about-you/i.test(path);
                    const needsEmailVerification = verificationInput
                      || /check your inbox|check your email|verify your email|verification code|enter the verification code|enter your code|temporary verification code|resend email/i.test(bodyText)
                      || /email-verification/i.test(path);
                    const existingAccountPrompt = /an account for this email address already exists|this email address already exists|account already exists|already registered/i.test(bodyText);
                    let stage = "unknown";
                    if (/^(localhost|127\.0\.0\.1)$/i.test(host)) {
                      stage = "callback_complete";
                    } else if (antiBotGate) {
                      stage = "security_verification";
                    } else if (rateLimitExceeded) {
                      stage = "rate_limit";
                    } else if (sessionEnded) {
                      stage = "session_ended";
                    } else if (retryableTimeout) {
                      stage = "retryable_timeout";
                    } else if (addPhonePrompt) {
                      stage = "add_phone";
                    } else if (aboutYou) {
                      stage = "about_you";
                    } else if (needsEmailVerification) {
                      stage = "email_verification";
                    } else if (existingAccountPrompt) {
                      stage = "existing_account_prompt";
                    } else if (/\/create-account(?:\/|$)/i.test(path) && passwordInput) {
                      stage = "signup_password";
                    } else if (/\/create-account(?:\/|$)/i.test(path) && emailInput) {
                      stage = "signup_email";
                    } else if (emailInput || passwordInput || verificationInput) {
                      stage = "auth_prompt";
                    }
                    return {
                      stage,
                      current_url: location.href,
                      headline: bodyText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
                    };
                  });
                } catch (error) {
                  const message = String(error?.message || error || "");
                  if (!/Execution context was destroyed|Cannot find context|Target closed|Frame was detached/i.test(message) || attempt === 3) {
                    throw error;
                  }
                  await page.waitForLoadState("domcontentloaded").catch(() => {});
                  await page.waitForTimeout(250).catch(() => {});
                }
              }
              return {
                stage: "unknown",
                current_url: page.url(),
                headline: null,
              };
            };

            const fieldSelectors = [
              'input[type="email"]',
              'input[name="email"]',
              'input[autocomplete="email"]',
              'input[autocomplete="username"]',
            ];
            let field = null;
            for (const selector of fieldSelectors) {
              const candidate = page.locator(selector).first();
              if (!(await candidate.count().catch(() => 0))) {
                continue;
              }
              if (!(await candidate.isVisible().catch(() => false))) {
                continue;
              }
              field = candidate;
              break;
            }
            if (!field) {
              throw new Error("signup-email-field-not-found");
            }
            const value = String(await field.inputValue().catch(() => "")).trim().toLowerCase();
            if (!value || value !== expectedEmail) {
              throw new Error("signup-email-not-filled:" + (value || "empty"));
            }

            const allowedStages = new Set([
              "signup_password",
              "email_verification",
              "about_you",
              "add_phone",
              "existing_account_prompt",
              "security_verification",
              "retryable_timeout",
              "rate_limit",
              "session_ended",
              "callback_complete",
            ]);

            const clickVisibleContinue = async () => {
              try {
                const result = await field.evaluate((input) => {
                  const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                  const isVisible = (node) => {
                    if (!(node instanceof Element)) {
                      return false;
                    }
                    const style = window.getComputedStyle(node);
                    if (!style || style.display === "none" || style.visibility === "hidden") {
                      return false;
                    }
                    const rect = node.getBoundingClientRect();
                    return rect.width > 0 && rect.height > 0;
                  };
                  const readLabel = (node) => {
                    if (!(node instanceof Element)) {
                      return "";
                    }
                    if (node instanceof HTMLInputElement) {
                      return normalize(node.value || node.getAttribute("aria-label") || "");
                    }
                    return normalize(node.innerText || node.textContent || node.getAttribute("aria-label") || "");
                  };
                  const form = input instanceof Element ? input.closest("form") : null;
                  const scope = form || document;
                  const candidates = Array.from(scope.querySelectorAll("button, input[type='submit'], [role='button']"))
                    .filter((node) => isVisible(node) && node.getAttribute("aria-disabled") !== "true" && !node.hasAttribute("disabled"));
                  const preferred = candidates.find((node) => {
                    const label = readLabel(node);
                    const isSubmitNode = (node instanceof HTMLButtonElement && node.type === "submit")
                      || (node instanceof HTMLInputElement && node.type === "submit");
                    return isSubmitNode && /continue|next|sign up|create account|submit|verify/i.test(label);
                  }) || candidates.find((node) => /continue|next|sign up|create account|submit|verify/i.test(readLabel(node)));
                  if (preferred instanceof HTMLElement) {
                    preferred.click();
                    return {
                      ok: true,
                      strategy: "form-dom-click",
                      label: readLabel(preferred) || null,
                    };
                  }
                  if (form instanceof HTMLFormElement) {
                    form.requestSubmit();
                    return {
                      ok: true,
                      strategy: "form-request-submit",
                      label: null,
                    };
                  }
                  return null;
                });
                return result;
              } catch (error) {
                const message = String(error?.message || error || "");
                if (/Execution context was destroyed|Cannot find context|Target closed|Frame was detached|Timeout \d+ms exceeded/i.test(message)) {
                  return {
                    ok: true,
                    strategy: "form-dom-click-navigation",
                    label: null,
                  };
                }
                throw error;
              }
            };

            const attempts = [
              async () => clickVisibleContinue(),
              async () => {
                await field.press("Enter").catch(() => {});
                return { strategy: "press-enter", label: null };
              },
              async () => {
                await field.evaluate((input) => {
                  if (input instanceof HTMLElement) {
                    input.blur();
                  }
                  const form = input instanceof Element ? input.closest("form") : null;
                  if (form instanceof HTMLFormElement) {
                    form.requestSubmit();
                  }
                }).catch(() => {});
                return { strategy: "request-submit", label: null };
              },
            ];

            const waitForTransition = async (sameStage, timeoutMs = 6500) => {
              const startedAt = Date.now();
              let latest = await detectStage();
              while (Date.now() - startedAt < timeoutMs) {
                if (allowedStages.has(latest.stage) && latest.stage !== sameStage) {
                  return latest;
                }
                await page.waitForTimeout(500).catch(() => {});
                latest = await detectStage();
              }
              return latest;
            };

            const transitions = [];
            for (const attempt of attempts) {
              const performed = await attempt();
              if (!performed) {
                continue;
              }
              const after = await waitForTransition("signup_email");
              transitions.push({ ...performed, ...after });
              if (allowedStages.has(after.stage) && after.stage !== "signup_email") {
                return {
                  ok: true,
                  strategy: performed.strategy,
                  next_stage: after.stage,
                  current_url: after.current_url,
                  headline: after.headline,
                  transitions,
                };
              }
            }

            const finalState = await detectStage();
            throw new Error("signup-email-submit-stuck:" + String(finalState.stage || "unknown") + ":" + String(finalState.current_url || page.url()));
    submit_login_email_form:
      metadata:
        description: "Submit the login email form and verify that OpenAI advances to a valid next state."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const expectedEmail = String(args.email || "").trim().toLowerCase();
            const detectStage = async () => {
              for (let attempt = 1; attempt <= 3; attempt += 1) {
                try {
                  return await page.evaluate(() => {
                    const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                    const hasVisible = (selector) => Array.from(document.querySelectorAll(selector)).some((node) => {
                      if (!(node instanceof Element)) {
                        return false;
                      }
                      const style = window.getComputedStyle(node);
                      if (!style || style.display === "none" || style.visibility === "hidden") {
                        return false;
                      }
                      const rect = node.getBoundingClientRect();
                      return rect.width > 0 && rect.height > 0;
                    });
                    const bodyText = normalize(document.body ? (document.body.innerText || document.body.textContent || "") : "");
                    const title = normalize(document.title || "");
                    const path = location.pathname;
                    const host = location.hostname;
                    const emailInput = hasVisible('input[type="email"], input[name="email"], input[autocomplete="email"], input[autocomplete="username"]');
                    const passwordInput = hasVisible('input[type="password"], input[name="password"], input[name="new-password"], input[autocomplete="current-password"], input[autocomplete="new-password"]');
                    const verificationInput = hasVisible('input[name*="code" i], input[autocomplete="one-time-code"], input[inputmode="numeric"]');
                    const antiBotGate = /just a moment/i.test(title)
                      || /performing security verification|verify you are not a bot|verifying you are human|verify you are human/i.test(bodyText)
                      || hasVisible('input[name="cf-turnstile-response"], iframe[src*="challenge"], iframe[title*="Widget containing"]');
                    const retryableTimeout = /oops, an error occurred|operation timed out|try again/i.test(bodyText);
                    const sessionEnded = /your session has ended/i.test(bodyText);
                    const rateLimitExceeded = /rate limit exceeded|too many requests|please try again later/i.test(bodyText);
                    const addPhonePrompt = /add-phone/i.test(path)
                      || /phone number required|add your phone number|verify your phone number|enter your phone number/i.test(bodyText);
                    const aboutYou = /confirm your age|full name|birthday|finish creating account|what should we call you|tell us about yourself|about you/i.test(bodyText)
                      || /about-you/i.test(path);
                    const sawOauthConsent = /sign in to codex with chatgpt|share your name, email, and profile picture with codex|share your name, email, and profile picture|link your account/i.test(bodyText)
                      || /sign in to codex/i.test(title);
                    const oauthContinueVisible = sawOauthConsent
                      && Array.from(document.querySelectorAll("button,a,[role='button'],input[type='submit']")).some((node) => {
                        if (!(node instanceof Element)) {
                          return false;
                        }
                        const style = window.getComputedStyle(node);
                        if (!style || style.display === "none" || style.visibility === "hidden") {
                          return false;
                        }
                        const rect = node.getBoundingClientRect();
                        if (!(rect.width > 0 && rect.height > 0)) {
                          return false;
                        }
                        const text = normalize(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || ""));
                        return /continue to codex|continue to openai|authorize|allow|accept/i.test(text)
                          || /^continue$/i.test(text);
                      });
                    const needsEmailVerification = verificationInput
                      || /check your inbox|check your email|verify your email|verification code|enter the verification code|enter your code|temporary verification code|resend email/i.test(bodyText)
                      || /email-verification/i.test(path);
                    const signupPasswordRoute = /\/create-account\/password(?:\/|$)/i.test(path)
                      || (/\/create-account(?:\/|$)/i.test(path) && /create a password|choose a password/i.test(bodyText));
                    const loginPasswordRoute = /\/log-in\/password(?:\/|$)/i.test(path)
                      || (/\/log-in(?:\/|$)/i.test(path)
                        && !emailInput
                        && (passwordInput || /enter your password/i.test(bodyText)));
                    let stage = "unknown";
                    if (/^(localhost|127\.0\.0\.1)$/i.test(host)) {
                      stage = "callback_complete";
                    } else if (antiBotGate) {
                      stage = "security_verification";
                    } else if (rateLimitExceeded) {
                      stage = "rate_limit";
                    } else if (sessionEnded) {
                      stage = "session_ended";
                    } else if (retryableTimeout) {
                      stage = "retryable_timeout";
                    } else if (addPhonePrompt) {
                      stage = "add_phone";
                    } else if (oauthContinueVisible) {
                      stage = "oauth_consent";
                    } else if (aboutYou) {
                      stage = "about_you";
                    } else if (needsEmailVerification) {
                      stage = "email_verification";
                    } else if (signupPasswordRoute || (/\/create-account(?:\/|$)/i.test(path) && passwordInput)) {
                      stage = "signup_password";
                    } else if (/\/create-account(?:\/|$)/i.test(path) && emailInput) {
                      stage = "signup_email";
                    } else if (loginPasswordRoute || (/\/log-in(?:\/|$)/i.test(path) && passwordInput)) {
                      stage = "login_password";
                    } else if (/\/log-in(?:\/|$)/i.test(path) && emailInput) {
                      stage = "login_email";
                    } else if (emailInput || passwordInput || verificationInput) {
                      stage = "auth_prompt";
                    }
                    return {
                      stage,
                      current_url: location.href,
                      headline: bodyText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
                    };
                  });
                } catch (error) {
                  const message = String(error?.message || error || "");
                  if (!/Execution context was destroyed|Cannot find context|Target closed|Frame was detached/i.test(message) || attempt === 3) {
                    throw error;
                  }
                  await page.waitForLoadState("domcontentloaded").catch(() => {});
                  await page.waitForTimeout(250).catch(() => {});
                }
              }
              return {
                stage: "unknown",
                current_url: page.url(),
                headline: null,
              };
            };

            const fieldSelectors = [
              'input[type="email"]',
              'input[name="email"]',
              'input[autocomplete="email"]',
              'input[autocomplete="username"]',
            ];
            let field = null;
            for (const selector of fieldSelectors) {
              const candidate = page.locator(selector).first();
              if (!(await candidate.count().catch(() => 0))) {
                continue;
              }
              if (!(await candidate.isVisible().catch(() => false))) {
                continue;
              }
              field = candidate;
              break;
            }
            if (!field) {
              throw new Error("login-email-field-not-found");
            }
            const value = String(await field.inputValue().catch(() => "")).trim().toLowerCase();
            if (!value || value !== expectedEmail) {
              throw new Error("login-email-not-filled:" + (value || "empty"));
            }

            const allowedStages = new Set([
              "signup_email",
              "signup_password",
              "login_password",
              "email_verification",
              "about_you",
              "oauth_consent",
              "add_phone",
              "security_verification",
              "retryable_timeout",
              "rate_limit",
              "session_ended",
              "callback_complete",
            ]);

            const clickVisibleContinue = async () => {
              try {
                const result = await field.evaluate((input) => {
                  const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                  const isVisible = (node) => {
                    if (!(node instanceof Element)) {
                      return false;
                    }
                    const style = window.getComputedStyle(node);
                    if (!style || style.display === "none" || style.visibility === "hidden") {
                      return false;
                    }
                    const rect = node.getBoundingClientRect();
                    return rect.width > 0 && rect.height > 0;
                  };
                  const readLabel = (node) => {
                    if (!(node instanceof Element)) {
                      return "";
                    }
                    if (node instanceof HTMLInputElement) {
                      return normalize(node.value || node.getAttribute("aria-label") || "");
                    }
                    return normalize(node.innerText || node.textContent || node.getAttribute("aria-label") || "");
                  };
                  const form = input instanceof Element ? input.closest("form") : null;
                  const scope = form || document;
                  const candidates = Array.from(scope.querySelectorAll("button, input[type='submit'], [role='button']"))
                    .filter((node) => isVisible(node) && node.getAttribute("aria-disabled") !== "true" && !node.hasAttribute("disabled"));
                  const preferred = candidates.find((node) => {
                    const label = readLabel(node);
                    const isSubmitNode = (node instanceof HTMLButtonElement && node.type === "submit")
                      || (node instanceof HTMLInputElement && node.type === "submit");
                    return isSubmitNode && /continue|next|log in|sign in|submit|verify/i.test(label);
                  }) || candidates.find((node) => /continue|next|log in|sign in|submit|verify/i.test(readLabel(node)));
                  if (preferred instanceof HTMLElement) {
                    preferred.click();
                    return {
                      ok: true,
                      strategy: "form-dom-click",
                      label: readLabel(preferred) || null,
                    };
                  }
                  if (form instanceof HTMLFormElement) {
                    form.requestSubmit();
                    return {
                      ok: true,
                      strategy: "form-request-submit",
                      label: null,
                    };
                  }
                  return null;
                });
                return result;
              } catch (error) {
                const message = String(error?.message || error || "");
                if (/Execution context was destroyed|Cannot find context|Target closed|Frame was detached/i.test(message)) {
                  return {
                    ok: true,
                    strategy: "form-dom-click-navigation",
                    label: null,
                  };
                }
                throw error;
              }
            };

            const attempts = [
              async () => clickVisibleContinue(),
              async () => {
                await field.press("Enter").catch(() => {});
                return { strategy: "press-enter", label: null };
              },
              async () => {
                await field.evaluate((input) => {
                  if (input instanceof HTMLElement) {
                    input.blur();
                  }
                  const form = input instanceof Element ? input.closest("form") : null;
                  if (form instanceof HTMLFormElement) {
                    form.requestSubmit();
                  }
                }).catch(() => {});
                return { strategy: "request-submit", label: null };
              },
            ];

            const waitForTransition = async (sameStage, timeoutMs = 6500) => {
              const startedAt = Date.now();
              let latest = await detectStage();
              while (Date.now() - startedAt < timeoutMs) {
                if (allowedStages.has(latest.stage) && latest.stage !== sameStage) {
                  return latest;
                }
                await page.waitForTimeout(500).catch(() => {});
                latest = await detectStage();
              }
              return latest;
            };

            const transitions = [];
            for (const attempt of attempts) {
              const performed = await attempt();
              if (!performed) {
                continue;
              }
              const after = await waitForTransition("login_email");
              transitions.push({ ...performed, ...after });
              if (allowedStages.has(after.stage) && after.stage !== "login_email") {
                return {
                  ok: true,
                  strategy: performed.strategy,
                  next_stage: after.stage,
                  current_url: after.current_url,
                  headline: after.headline,
                  transitions,
                };
              }
            }

            const finalState = await detectStage();
            throw new Error("login-email-submit-stuck:" + String(finalState.stage || "unknown") + ":" + String(finalState.current_url || page.url()));
    fill_openai_password_field:
      metadata:
        description: "Fill the OpenAI password field from the runtime-resolved login locator."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.type
      with:
        body:
          selector: 'input[type="password"], input[name="password"], input[name="new-password"], input[autocomplete="current-password"], input[autocomplete="new-password"]'
          dispatch_events_after_fill: true
          blur_after_fill: true
          settle_ms: 250
          value_from:
            secret_locator_input:
              name: account_login_locator
    fill_openai_about_you_form:
      metadata:
        description: "Fill the OpenAI about-you form and verify that its primary submit control becomes enabled."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
            const digitsOnly = (value) => String(value || "").replace(/\D+/g, "");
            const fullName = normalize(args.full_name || "Dev Astronlab");
            const month = normalize(args.birth_month || "1");
            const day = normalize(args.birth_day || "24");
            const year = normalize(args.birth_year || "1990");
            const birthdayValue = month.padStart(2, "0") + "/" + day.padStart(2, "0") + "/" + year;
            const birthdayDigits = digitsOnly(birthdayValue);
            const monthNumber = Math.max(1, Math.min(12, Number.parseInt(month, 10) || 1));
            const dayNumber = Math.max(1, Math.min(31, Number.parseInt(day, 10) || 1));
            const yearNumber = Math.max(1900, Number.parseInt(year, 10) || 1990);
            const today = new Date();
            let ageYears = today.getFullYear() - yearNumber;
            const birthdayPassedThisYear = (today.getMonth() + 1) > monthNumber
              || ((today.getMonth() + 1) === monthNumber && today.getDate() >= dayNumber);
            if (!birthdayPassedThisYear) {
              ageYears -= 1;
            }
            ageYears = Math.max(18, ageYears);
            const ageValue = String(ageYears);
            const ageDigits = digitsOnly(ageValue);
            const monthNames = {
              "1": ["1", "01", "jan", "january"],
              "2": ["2", "02", "feb", "february"],
              "3": ["3", "03", "mar", "march"],
              "4": ["4", "04", "apr", "april"],
              "5": ["5", "05", "may"],
              "6": ["6", "06", "jun", "june"],
              "7": ["7", "07", "jul", "july"],
              "8": ["8", "08", "aug", "august"],
              "9": ["9", "09", "sep", "sept", "september"],
              "10": ["10", "oct", "october"],
              "11": ["11", "nov", "november"],
              "12": ["12", "dec", "december"],
            };
            const monthTokens = (monthNames[month] || [month]).map((value) => normalize(value).toLowerCase());
            const dayTokens = [day, day.padStart(2, "0")].map((value) => normalize(value));
            const blurFormSafely = async () => {
              await page.evaluate(() => {
                const active = document.activeElement;
                if (active instanceof HTMLElement) {
                  active.blur();
                }
              }).catch(() => {});
              const viewport = page.viewportSize();
              if (viewport) {
                await page.mouse.click(
                  Math.max(16, viewport.width - 16),
                  Math.max(16, viewport.height - 16),
                ).catch(() => {});
              } else {
                await page.keyboard.press("Escape").catch(() => {});
              }
              await page.keyboard.press("Escape").catch(() => {});
              await page.waitForTimeout(250).catch(() => {});
            };
            const detectStage = async () => {
              for (let attempt = 0; attempt < 8; attempt += 1) {
                try {
                  return await page.evaluate(() => {
                    const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                    const hasVisible = (selector) => Array.from(document.querySelectorAll(selector)).some((node) => {
                      if (!(node instanceof Element)) {
                        return false;
                      }
                      const style = window.getComputedStyle(node);
                      if (!style || style.display === "none" || style.visibility === "hidden") {
                        return false;
                      }
                      const rect = node.getBoundingClientRect();
                      return rect.width > 0 && rect.height > 0;
                    });
                    const bodyText = normalize(document.body ? (document.body.innerText || document.body.textContent || "") : "");
                    const title = normalize(document.title || "");
                    const path = location.pathname;
                    const host = location.hostname;
                    const passwordInput = hasVisible('input[type="password"], input[name="password"], input[name="new-password"], input[autocomplete="current-password"], input[autocomplete="new-password"]');
                    const verificationInput = hasVisible('input[name*="code" i], input[autocomplete="one-time-code"], input[inputmode="numeric"]');
                    const antiBotGate = /just a moment/i.test(title)
                      || /performing security verification|verify you are not a bot|verifying you are human|verify you are human/i.test(bodyText)
                      || hasVisible('input[name="cf-turnstile-response"], iframe[src*="challenge"], iframe[title*="Widget containing"]');
                    const retryableTimeout = /oops, an error occurred|operation timed out|try again/i.test(bodyText);
                    const sessionEnded = /your session has ended/i.test(bodyText);
                    const rateLimitExceeded = /rate limit exceeded|too many requests|please try again later/i.test(bodyText);
                    const addPhonePrompt = /add-phone/i.test(path)
                      || /phone number required|add your phone number|verify your phone number|enter your phone number/i.test(bodyText);
                    const aboutYou = /confirm your age|full name|birthday|finish creating account|what should we call you|tell us about yourself|about you/i.test(bodyText)
                      || /about-you/i.test(path);
                    const needsEmailVerification = verificationInput
                      || /check your inbox|check your email|verify your email|verification code|enter the verification code|enter your code|temporary verification code|resend email/i.test(bodyText)
                      || /email-verification/i.test(path);
                    const chatGptHost = /(?:^|\.)chatgpt\.com$/i.test(host);
                    const publicShellSignals = /log in to get smarter responses|get responses tailored to you|see plans and pricing|sign up for free|welcome to chatgpt|get started|where should we begin/i.test(bodyText);
                    const loginCtaVisible = Array.from(document.querySelectorAll("button, a, [role='button'], input[type='submit']")).some((node) => {
                      if (!(node instanceof Element)) {
                        return false;
                      }
                      const style = window.getComputedStyle(node);
                      if (!style || style.display === "none" || style.visibility === "hidden") {
                        return false;
                      }
                      const rect = node.getBoundingClientRect();
                      if (!(rect.width > 0 && rect.height > 0)) {
                        return false;
                      }
                      const text = normalize(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || ""));
                      return /^log in$/i.test(text) || /^sign in$/i.test(text);
                    });
                    const securityShell = chatGptHost
                      && !/^\/auth\//i.test(path)
                      && /secure sign in with chatgpt|trusted devices|enable device code authorization for codex|log out of all devices|multi-factor authentication|security keys & passkeys/i.test(bodyText);
                    const appShell = chatGptHost
                      && !/^\/auth\//i.test(path)
                      && /\bnew chat\b|\bsearch chats\b|\bimages\b|\bapps\b|\bprojects\b|\bdeep research\b|\bcodex\b|what['’]s on your mind today\?|ask anything/i.test(bodyText);
                    const signupCtaVisible = Array.from(document.querySelectorAll("button, a, [role='button'], input[type='submit']")).some((node) => {
                      if (!(node instanceof Element)) {
                        return false;
                      }
                      const style = window.getComputedStyle(node);
                      if (!style || style.display === "none" || style.visibility === "hidden") {
                        return false;
                      }
                      const rect = node.getBoundingClientRect();
                      if (!(rect.width > 0 && rect.height > 0)) {
                        return false;
                      }
                      const text = normalize(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || ""));
                      return /^sign up$/i.test(text) || /sign up for free|create account|get started/i.test(text);
                    });
                    const publicShell = chatGptHost && !securityShell && (publicShellSignals || loginCtaVisible || signupCtaVisible);
                    const authenticatedAppShell = (appShell || securityShell) && !publicShell;
                    const oauthContinueVisible = Array.from(document.querySelectorAll("button, a, [role='button'], input[type='submit']")).some((node) => {
                      if (!(node instanceof Element)) {
                        return false;
                      }
                      const style = window.getComputedStyle(node);
                      if (!style || style.display === "none" || style.visibility === "hidden") {
                        return false;
                      }
                      const rect = node.getBoundingClientRect();
                      if (!(rect.width > 0 && rect.height > 0)) {
                        return false;
                      }
                      const text = normalize(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || ""));
                      return /continue to codex|continue to openai|authorize|allow|accept/i.test(text);
                    });
                    let stage = "unknown";
                    if (/^(localhost|127\.0\.0\.1)$/i.test(host)) {
                      stage = "callback_complete";
                    } else if (antiBotGate) {
                      stage = "security_verification";
                    } else if (rateLimitExceeded) {
                      stage = "rate_limit";
                    } else if (sessionEnded) {
                      stage = "session_ended";
                    } else if (retryableTimeout) {
                      stage = "retryable_timeout";
                    } else if (addPhonePrompt) {
                      stage = "add_phone";
                    } else if (oauthContinueVisible) {
                      stage = "oauth_consent";
                    } else if (authenticatedAppShell) {
                      stage = "app_shell";
                    } else if (aboutYou) {
                      stage = "about_you";
                    } else if (needsEmailVerification) {
                      stage = "email_verification";
                    } else if (publicShell && !passwordInput && !verificationInput) {
                      stage = "public_chatgpt";
                    } else if (/\/log-in(?:\/|$)/i.test(path) && passwordInput) {
                      stage = "login_password";
                    } else if (/\/create-account(?:\/|$)/i.test(path) && passwordInput) {
                      stage = "signup_password";
                    } else if (passwordInput || verificationInput) {
                      stage = "auth_prompt";
                    }
                    return {
                      stage,
                      current_url: location.href,
                      headline: bodyText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
                    };
                  });
                } catch (error) {
                  const message = String(error?.message || error || "");
                  const navigationRace =
                    /Execution context was destroyed|Cannot find context with specified id|Frame was detached|navigation/i.test(message);
                  if (!navigationRace || attempt === 7) {
                    throw error;
                  }
                  await page.waitForLoadState("domcontentloaded", { timeout: 2000 }).catch(() => {});
                  await page.waitForTimeout(250).catch(() => {});
                }
              }
              return {
                stage: "unknown",
                current_url: page.url(),
                headline: null,
              };
            };
            const surfaceBeforeFill = await detectStage();
            if (surfaceBeforeFill.stage !== "about_you" && surfaceBeforeFill.stage !== "unknown") {
              return {
                ok: true,
                next_stage: surfaceBeforeFill.stage,
                current_url: surfaceBeforeFill.current_url,
                headline: surfaceBeforeFill.headline,
              };
            }
            const findVisible = async (selectors) => {
              for (const selector of selectors) {
                const locator = page.locator(selector).first();
                if (!(await locator.count().catch(() => 0))) {
                  continue;
                }
                if (!(await locator.isVisible().catch(() => false))) {
                  continue;
                }
                return locator;
              }
              return null;
            };
            const readInputValue = async (locator) => {
              return await locator.inputValue().catch(() => null);
            };
            const valuesMatch = (actualValue, expectedValue) => {
              return normalize(actualValue) === normalize(expectedValue)
                || digitsOnly(actualValue) === digitsOnly(expectedValue);
            };
            const setInputValue = async (locator, value) => {
              await locator.evaluate((node, nextValue) => {
                if (!(node instanceof HTMLInputElement || node instanceof HTMLTextAreaElement)) {
                  return;
                }
                const proto = node instanceof HTMLTextAreaElement
                  ? window.HTMLTextAreaElement.prototype
                  : window.HTMLInputElement.prototype;
                const descriptor = Object.getOwnPropertyDescriptor(proto, "value");
                if (descriptor && typeof descriptor.set === "function") {
                  descriptor.set.call(node, nextValue);
                } else {
                  node.value = nextValue;
                }
                node.dispatchEvent(new InputEvent("input", {
                  bubbles: true,
                  data: String(nextValue || ""),
                  inputType: "insertText",
                }));
                node.dispatchEvent(new Event("change", { bubbles: true }));
                node.blur();
              }, value).catch(() => {});
            };
            const describeInput = async (locator) => {
              return await locator.evaluate((node) => {
                if (!(node instanceof HTMLInputElement)) {
                  return null;
                }
                const labeledByText = String(node.getAttribute("aria-labelledby") || "")
                  .split(/\s+/)
                  .filter(Boolean)
                  .map((id) => document.getElementById(id)?.textContent || "")
                  .join(" ");
                const labelText = node.id
                  ? document.querySelector('label[for="' + CSS.escape(node.id) + '"]')?.textContent || ""
                  : "";
                return {
                  name: String(node.getAttribute("name") || ""),
                  placeholder: String(node.getAttribute("placeholder") || ""),
                  ariaLabel: String(node.getAttribute("aria-label") || ""),
                  labeledBy: String(labeledByText || ""),
                  label: String(labelText || ""),
                  type: String(node.getAttribute("type") || ""),
                  inputMode: String(node.getAttribute("inputmode") || ""),
                  value: String(node.value || ""),
                };
              }).catch(() => null);
            };
            const readSelectValue = async (locator) => {
              return await locator.evaluate((node) => {
                if (!(node instanceof HTMLSelectElement)) {
                  return null;
                }
                const selected = node.selectedOptions?.[0];
                return {
                  value: String(node.value || ""),
                  label: String(selected?.label || selected?.textContent || node.value || ""),
                };
              }).catch(() => null);
            };
            const fillTextInput = async (locator, value) => {
              await locator.scrollIntoViewIfNeeded().catch(() => {});
              await locator.click({ timeout: 1500 }).catch(() => {});
              await locator.fill("").catch(() => {});
              await locator.fill(value).catch(async () => {
                await locator.press("Meta+A").catch(() => {});
                await locator.press("Control+A").catch(() => {});
                await locator.press("Backspace").catch(() => {});
                await locator.type(value, { delay: 20 }).catch(() => {});
              });
              await locator.evaluate((node) => {
                if (!(node instanceof HTMLInputElement)) {
                  return;
                }
                node.dispatchEvent(new Event("input", { bubbles: true }));
                node.dispatchEvent(new Event("change", { bubbles: true }));
                node.blur();
              }).catch(() => {});
              let currentValue = await readInputValue(locator);
              if (valuesMatch(currentValue, value)) {
                return;
              }
              await setInputValue(locator, value);
              currentValue = await readInputValue(locator);
              if (valuesMatch(currentValue, value)) {
                return;
              }
              await locator.click({ timeout: 1500 }).catch(() => {});
              await page.keyboard.press("Meta+A").catch(() => {});
              await page.keyboard.press("Control+A").catch(() => {});
              await page.keyboard.press("Backspace").catch(() => {});
              await page.keyboard.type(value, { delay: 20 }).catch(() => {});
              await locator.evaluate((node) => {
                if (node instanceof HTMLInputElement || node instanceof HTMLTextAreaElement) {
                  node.blur();
                }
              }).catch(() => {});
            };
            const fillBirthdayInput = async (locator) => {
              const birthdayDigitsValue = month.padStart(2, "0") + day.padStart(2, "0") + year;
              await locator.scrollIntoViewIfNeeded().catch(() => {});
              await locator.click({ timeout: 1500 }).catch(() => {});
              await page.keyboard.press("Meta+A").catch(() => {});
              await page.keyboard.press("Control+A").catch(() => {});
              await page.keyboard.press("Backspace").catch(() => {});
              await page.keyboard.type(birthdayDigitsValue, { delay: 20 }).catch(() => {});
              await locator.evaluate((node) => {
                if (node instanceof HTMLInputElement || node instanceof HTMLTextAreaElement) {
                  node.blur();
                }
              }).catch(() => {});
              let currentValue = await readInputValue(locator);
              if (digitsOnly(currentValue) === birthdayDigits) {
                return;
              }
              await setInputValue(locator, birthdayValue);
              currentValue = await readInputValue(locator);
              if (digitsOnly(currentValue) === birthdayDigits) {
                return;
              }
              await setInputValue(locator, birthdayDigitsValue);
            };
            const fillAgeInput = async (locator) => {
              await fillTextInput(locator, ageValue);
              let currentValue = await readInputValue(locator);
              if (digitsOnly(currentValue) === ageDigits) {
                return;
              }
              await setInputValue(locator, ageValue);
              currentValue = await readInputValue(locator);
              if (digitsOnly(currentValue) === ageDigits) {
                return;
              }
              await locator.click({ timeout: 1500 }).catch(() => {});
              await page.keyboard.press("Meta+A").catch(() => {});
              await page.keyboard.press("Control+A").catch(() => {});
              await page.keyboard.press("Backspace").catch(() => {});
              await page.keyboard.type(ageValue, { delay: 20 }).catch(() => {});
              await locator.evaluate((node) => {
                if (node instanceof HTMLInputElement || node instanceof HTMLTextAreaElement) {
                  node.blur();
                }
              }).catch(() => {});
            };
            const typeIntoFocusedField = async (value) => {
              await page.keyboard.press("Meta+A").catch(() => {});
              await page.keyboard.press("Control+A").catch(() => {});
              await page.keyboard.press("Backspace").catch(() => {});
              await page.keyboard.type(value, { delay: 20 }).catch(() => {});
            };
            const clickLabelField = async (patterns) => {
              for (const pattern of patterns) {
                const candidates = [
                  page.getByText(pattern, { exact: true }).first(),
                  page.locator("label, div, span, p").filter({ hasText: pattern }).first(),
                ];
                for (const candidate of candidates) {
                  if (!(await candidate.count().catch(() => 0))) {
                    continue;
                  }
                  if (!(await candidate.isVisible().catch(() => false))) {
                    continue;
                  }
                  await candidate.scrollIntoViewIfNeeded().catch(() => {});
                  if (await candidate.click({ timeout: 1000 }).then(() => true).catch(() => false)) {
                    return true;
                  }
                  const box = await candidate.boundingBox().catch(() => null);
                  if (box) {
                    const clickX = Math.max(box.x + Math.min(box.width * 0.5, 80), box.x + 8);
                    const clickY = box.y + box.height + 20;
                    await page.mouse.click(clickX, clickY).catch(() => {});
                    return true;
                  }
                }
              }
              return false;
            };
            const visibleSelects = [];
            const pageBodyText = normalize(await page.locator("body").innerText().catch(() => "")).toLowerCase();
            const prefersAgeField = /\bhow old are you\b/.test(pageBodyText)
              || (/\bage\b/.test(pageBodyText)
                && !/\bbirthday\b|\bdate of birth\b|\bbirth date\b/.test(pageBodyText));
            const selectCount = await page.locator("select").count().catch(() => 0);
            for (let index = 0; index < selectCount; index += 1) {
              const locator = page.locator("select").nth(index);
              if (await locator.isVisible().catch(() => false)) {
                visibleSelects.push(locator);
              }
            }
            let nameField = await findVisible([
              'input[name="name"]',
              'input[name*="name" i]',
              'input[autocomplete="name"]',
              'input[placeholder*="name" i]',
              'input[aria-label*="name" i]',
            ]);
            const monthField = await findVisible([
              'select[name*="month" i]',
              'select[aria-label*="month" i]',
            ]) || visibleSelects[0] || null;
            const dayField = await findVisible([
              'select[name*="day" i]',
              'select[aria-label*="day" i]',
            ]) || visibleSelects[1] || null;
            const yearField = await findVisible([
              'select[name*="year" i]',
              'select[aria-label*="year" i]',
            ]) || visibleSelects[2] || null;
            const selectValue = async (locator, targetValue, acceptedTokens = []) => {
              const options = await locator.evaluate((node) => {
                if (!(node instanceof HTMLSelectElement)) {
                  return [];
                }
                return Array.from(node.options || []).map((option) => ({
                  value: String(option.value || ""),
                  label: String(option.label || option.textContent || ""),
                }));
              }).catch(() => []);
              const normalizedTarget = normalize(targetValue).toLowerCase();
              const normalizedAccepted = acceptedTokens.map((value) => normalize(value).toLowerCase());
              const chosen = options.find((option) => {
                const value = normalize(option.value).toLowerCase();
                const label = normalize(option.label).toLowerCase();
                return value === normalizedTarget
                  || label === normalizedTarget
                  || normalizedAccepted.includes(value)
                  || normalizedAccepted.includes(label);
              });
              if (!chosen) {
                throw new Error("about-you-option-not-found:" + String(targetValue));
              }
              await locator.selectOption({ value: chosen.value }).catch(async () => {
                await locator.selectOption({ label: chosen.label }).catch(() => {});
              });
            };
            const submit = page.locator('button[type="submit"], input[type="submit"], button, [role="button"]')
              .filter({ hasText: /continue|next|finish creating account|submit|verify/i })
              .first();
            const readSubmitState = async () => {
              const submitFound = (await submit.count().catch(() => 0)) > 0;
              const submitDisabled = submitFound ? await submit.isDisabled().catch(() => null) : null;
              const submitText = submitFound
                ? await submit.evaluate((node) => String(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || "")).replace(/\s+/g, " ").trim()).catch(() => null)
                : null;
              return {
                found: submitFound,
                disabled: submitDisabled,
                text: normalize(submitText || "") || null,
              };
            };
            const readVisibleBirthdayValue = async () => {
              const bodyText = normalize(await page.locator("body").innerText().catch(() => ""));
              const match = bodyText.match(/(\d{1,2})\s*\/\s*(\d{1,2})\s*\/\s*(\d{4})/);
              if (!match) {
                return null;
              }
              return match[1].padStart(2, "0") + "/" + match[2].padStart(2, "0") + "/" + match[3];
            };
            const tryFocusedAboutYouFallback = async () => {
              let typedSomething = false;
              const nameValueLocator = nameField || visibleInputs[0]?.locator || null;
              const aboutYouValueLocator = birthdayField || ageField || null;
              const aboutYouFieldKind = ageField ? "age" : (birthdayField ? "birthday" : null);
              if (nameValueLocator) {
                await fillTextInput(nameValueLocator, fullName);
                typedSomething = true;
              } else {
                const focusedName = await clickLabelField([/full name/i, /^name$/i]);
                if (focusedName) {
                  await typeIntoFocusedField(fullName);
                  typedSomething = true;
                }
              }
              const visibleBirthdayBefore = aboutYouFieldKind === "birthday"
                ? await readVisibleBirthdayValue()
                : null;
              if (!aboutYouValueLocator && typedSomething && visibleBirthdayBefore) {
                await blurFormSafely();
                const submitState = await readSubmitState();
                const nameActual = nameValueLocator
                  ? normalize(await readInputValue(nameValueLocator))
                  : null;
                return {
                  ok: true,
                  name_value: nameActual || null,
                  month_value: null,
                  day_value: null,
                  year_value: null,
                  birthday_value: visibleBirthdayBefore,
                  age_value: null,
                  submit_disabled: submitState.disabled,
                  submit_text: submitState.text,
                  current_url: page.url(),
                };
              }
              if (!aboutYouValueLocator && typedSomething) {
                await page.keyboard.press("Tab").catch(() => {});
                await page.waitForTimeout(100).catch(() => {});
              }
              if (birthdayField) {
                await fillBirthdayInput(birthdayField);
                typedSomething = true;
              } else if (ageField) {
                await fillAgeInput(ageField);
                typedSomething = true;
              } else {
                const focusedValueField = prefersAgeField
                  ? await clickLabelField([/^age$/i, /how old are you/i, /your age/i])
                  : await clickLabelField([/birthday/i, /date of birth/i, /birth date/i]);
                if (focusedValueField) {
                  await typeIntoFocusedField(prefersAgeField ? ageValue : birthdayValue);
                  typedSomething = true;
                }
              }
              await blurFormSafely();
              const submitState = await readSubmitState();
              const visibleBirthdayAfter = await readVisibleBirthdayValue();
              const nameActual = nameValueLocator
                ? normalize(await readInputValue(nameValueLocator))
                : null;
              const ageActual = ageField
                ? normalize(await readInputValue(ageField))
                : null;
              if (typedSomething && (submitState.disabled === false || (nameActual === fullName && (visibleBirthdayAfter || visibleBirthdayBefore || digitsOnly(ageActual) === ageDigits)))) {
                return {
                  ok: true,
                  name_value: nameActual || null,
                  month_value: null,
                  day_value: null,
                  year_value: null,
                  birthday_value: visibleBirthdayAfter || visibleBirthdayBefore,
                  age_value: ageActual || null,
                  submit_disabled: submitState.disabled,
                  submit_text: submitState.text,
                  current_url: page.url(),
                };
              }
              return null;
            };
            if (nameField && monthField && dayField && yearField) {
              await fillTextInput(nameField, fullName);
              await selectValue(monthField, month, monthTokens);
              await selectValue(dayField, day, dayTokens);
              await selectValue(yearField, year, [year]);
              await blurFormSafely();
              const nameActual = normalize(await readInputValue(nameField));
              const monthActual = await readSelectValue(monthField);
              const dayActual = await readSelectValue(dayField);
              const yearActual = await readSelectValue(yearField);
              const submitState = await readSubmitState();
              const monthMatches = monthTokens.includes(normalize(monthActual?.label || monthActual?.value || "").toLowerCase());
              const dayMatches = dayTokens.includes(normalize(dayActual?.label || dayActual?.value || ""));
              const yearMatches = normalize(yearActual?.label || yearActual?.value || "") === normalize(year);
              const result = {
                ok: nameActual === fullName && monthMatches && dayMatches && yearMatches && submitState.disabled === false,
                name_value: nameActual || null,
                month_value: normalize(monthActual?.label || monthActual?.value || "") || null,
                day_value: normalize(dayActual?.label || dayActual?.value || "") || null,
                year_value: normalize(yearActual?.label || yearActual?.value || "") || null,
                birthday_value: null,
                submit_disabled: submitState.disabled,
                submit_text: submitState.text,
                error: null,
              };
              if (!result.ok) {
                result.error = "about-you-form-not-ready";
                throw new Error("about-you-fill-failed:" + JSON.stringify(result));
              }
              return {
                ...result,
                current_url: page.url(),
              };
            }
            const inputCount = await page.locator("input").count().catch(() => 0);
            const visibleInputs = [];
            let fallbackNameField = null;
            let birthdayField = null;
            let ageField = null;
            let fallbackBirthdayField = null;
            for (let index = 0; index < inputCount; index += 1) {
              const locator = page.locator("input").nth(index);
              if (!(await locator.isVisible().catch(() => false))) {
                continue;
              }
              const details = await describeInput(locator);
              if (!details) {
                continue;
              }
              visibleInputs.push({
                locator,
                details,
              });
              const hints = normalize(
                String(details.name || "")
                  + " "
                  + String(details.placeholder || "")
                  + " "
                  + String(details.ariaLabel || "")
                  + " "
                  + String(details.labeledBy || "")
                  + " "
                  + String(details.label || "")
              ).toLowerCase();
              const currentValue = normalize(details.value || "");
              const likelyName = hints.includes("full name")
                || hints === "name"
                || hints.startsWith("name ")
                || hints.endsWith(" name")
                || hints.includes(" name ");
              const likelyAge = hints === "age"
                || hints.startsWith("age ")
                || hints.endsWith(" age")
                || hints.includes(" age ")
                || hints.includes("how old are you");
              const likelyBirthday = hints.includes("birthday")
                || hints.includes("birth date")
                || hints.includes("date of birth")
                || normalize(details.placeholder || "").includes("/")
                || /^\d{2}\/\d{2}\/\d{4}$/.test(currentValue);
              if (likelyName && !fallbackNameField) {
                fallbackNameField = locator;
              }
              if (likelyAge && !ageField) {
                ageField = locator;
              }
              if (!hints.includes("name") && !likelyAge && !fallbackBirthdayField) {
                fallbackBirthdayField = locator;
              }
              if (likelyBirthday) {
                birthdayField = locator;
                break;
              }
            }
            if (!birthdayField) {
              birthdayField = fallbackBirthdayField;
            }
            if (!nameField) {
              nameField = fallbackNameField;
            }
            if (!birthdayField) {
              for (const entry of visibleInputs) {
                const currentValue = normalize(entry.details.value || "");
                if (/^\d{2}\/\d{2}\/\d{4}$/.test(currentValue)) {
                  birthdayField = entry.locator;
                  break;
                }
              }
            }
            if (!nameField) {
              for (const entry of visibleInputs) {
                if (birthdayField && entry.locator === birthdayField) {
                  continue;
                }
                const currentValue = normalize(entry.details.value || "");
                if (!/^\d{2}\/\d{2}\/\d{4}$/.test(currentValue)) {
                  nameField = entry.locator;
                  break;
                }
              }
            }
            if ((!nameField || (!birthdayField && !ageField)) && visibleInputs.length >= 2) {
              nameField = nameField || visibleInputs[0]?.locator || null;
              if (!birthdayField && !ageField) {
                if (prefersAgeField) {
                  ageField = visibleInputs[1]?.locator || null;
                } else {
                  birthdayField = visibleInputs[1]?.locator || null;
                }
              }
            }
            if (visibleInputs.length === 1) {
              nameField = nameField || visibleInputs[0]?.locator || null;
              birthdayField = null;
              ageField = null;
            }
            if (nameField && birthdayField && nameField === birthdayField) {
              birthdayField = null;
            }
            if (nameField && ageField && nameField === ageField) {
              ageField = null;
            }
            if (!nameField || (!birthdayField && !ageField)) {
              const focusedFallback = await tryFocusedAboutYouFallback();
              if (focusedFallback) {
                return focusedFallback;
              }
            }
            if (!nameField || (!birthdayField && !ageField)) {
              const redirectedState = await detectStage();
              if (redirectedState.stage !== "about_you" && redirectedState.stage !== "unknown") {
                return {
                  ok: true,
                  next_stage: redirectedState.stage,
                  current_url: redirectedState.current_url,
                  headline: redirectedState.headline,
                };
              }
              throw new Error("about-you-fields-not-found");
            }
            await fillTextInput(nameField, fullName);
            if (birthdayField) {
              await fillBirthdayInput(birthdayField);
            } else if (ageField) {
              await fillAgeInput(ageField);
            }
            await blurFormSafely();
            const nameActual = normalize(await readInputValue(nameField));
            const birthdayActual = birthdayField ? normalize(await readInputValue(birthdayField)) : null;
            const ageActual = ageField ? normalize(await readInputValue(ageField)) : null;
            const submitState = await readSubmitState();
            const result = {
              ok: submitState.disabled === false || (
                nameActual === fullName
                  && (
                    (birthdayField && digitsOnly(birthdayActual) === birthdayDigits)
                    || (ageField && digitsOnly(ageActual) === ageDigits)
                  )
              ),
              name_value: nameActual || null,
              month_value: null,
              day_value: null,
              year_value: null,
              birthday_value: birthdayActual || null,
              age_value: ageActual || null,
              submit_disabled: submitState.disabled,
              submit_text: submitState.text,
              error: null,
            };
            if (!result.ok) {
              const redirectedState = await detectStage();
              if (redirectedState.stage !== "about_you" && redirectedState.stage !== "unknown") {
                return {
                  ok: true,
                  next_stage: redirectedState.stage,
                  current_url: redirectedState.current_url,
                  headline: redirectedState.headline,
                };
              }
              result.error = "about-you-form-not-ready";
              throw new Error("about-you-fill-failed:" + JSON.stringify(result));
            }
            return {
              ...result,
              current_url: page.url(),
            };
    submit_openai_about_you_form:
      metadata:
        description: "Submit the OpenAI about-you form and verify that the flow advances to a valid next state."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const detectStage = async () => await page.evaluate(() => {
              const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
              const hasVisible = (selector) => Array.from(document.querySelectorAll(selector)).some((node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              });
              const bodyText = normalize(document.body ? (document.body.innerText || document.body.textContent || "") : "");
              const title = normalize(document.title || "");
              const path = location.pathname;
              const host = location.hostname;
              const passwordInput = hasVisible('input[type="password"], input[name="password"], input[name="new-password"], input[autocomplete="current-password"], input[autocomplete="new-password"]');
              const verificationInput = hasVisible('input[name*="code" i], input[autocomplete="one-time-code"], input[inputmode="numeric"]');
              const antiBotGate = /just a moment/i.test(title)
                || /performing security verification|verify you are not a bot|verifying you are human|verify you are human/i.test(bodyText)
                || hasVisible('input[name="cf-turnstile-response"], iframe[src*="challenge"], iframe[title*="Widget containing"]');
              const retryableTimeout = /oops, an error occurred|operation timed out|try again/i.test(bodyText);
              const sessionEnded = /your session has ended/i.test(bodyText);
              const rateLimitExceeded = /rate limit exceeded|too many requests|please try again later/i.test(bodyText);
              const addPhonePrompt = /add-phone/i.test(path)
                || /phone number required|add your phone number|verify your phone number|enter your phone number/i.test(bodyText);
              const aboutYou = /confirm your age|full name|birthday|finish creating account|what should we call you|tell us about yourself|about you/i.test(bodyText)
                || /about-you/i.test(path);
              const rawNeedsEmailVerification = verificationInput
                || /check your inbox|check your email|verify your email|verification code|enter the verification code|enter your code|temporary verification code|resend email/i.test(bodyText)
                || /email-verification/i.test(path);
              const needsEmailVerification = rawNeedsEmailVerification && !aboutYou;
              const chatGptHost = /(?:^|\.)chatgpt\.com$/i.test(host);
              const publicShellSignals = /log in to get smarter responses|get responses tailored to you|see plans and pricing|sign up for free|welcome to chatgpt|get started|where should we begin/i.test(bodyText);
              const loginCtaVisible = Array.from(document.querySelectorAll("button, a, [role='button'], input[type='submit']")).some((node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                if (!(rect.width > 0 && rect.height > 0)) {
                  return false;
                }
                const text = normalize(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || ""));
                return /^log in$/i.test(text) || /^sign in$/i.test(text);
              });
              const securityShell = chatGptHost
                && !/^\/auth\//i.test(path)
                && /secure sign in with chatgpt|trusted devices|enable device code authorization for codex|log out of all devices|multi-factor authentication|security keys & passkeys/i.test(bodyText);
              const appShell = chatGptHost
                && !/^\/auth\//i.test(path)
                && /\bnew chat\b|\bsearch chats\b|\bimages\b|\bapps\b|\bprojects\b|\bdeep research\b|\bcodex\b|what['’]s on your mind today\?|ask anything/i.test(bodyText);
                    const signupCtaVisible = Array.from(document.querySelectorAll("button, a, [role='button'], input[type='submit']")).some((node) => {
                      if (!(node instanceof Element)) {
                        return false;
                      }
                      const style = window.getComputedStyle(node);
                      if (!style || style.display === "none" || style.visibility === "hidden") {
                        return false;
                      }
                      const rect = node.getBoundingClientRect();
                      if (!(rect.width > 0 && rect.height > 0)) {
                        return false;
                      }
                      const text = normalize(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || ""));
                      return /^sign up$/i.test(text) || /sign up for free|create account|get started/i.test(text);
                    });
                    const publicShell = chatGptHost && !securityShell && (publicShellSignals || loginCtaVisible || signupCtaVisible);
                    const authenticatedAppShell = (appShell || securityShell) && !publicShell;
              const oauthContinueVisible = Array.from(document.querySelectorAll("button, a, [role='button'], input[type='submit']")).some((node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                if (!(rect.width > 0 && rect.height > 0)) {
                  return false;
                }
                const text = normalize(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || ""));
                return /continue to codex|continue to openai|authorize|allow|accept/i.test(text);
              });
              let stage = "unknown";
              if (/^(localhost|127\.0\.0\.1)$/i.test(host)) {
                stage = "callback_complete";
              } else if (antiBotGate) {
                stage = "security_verification";
              } else if (rateLimitExceeded) {
                stage = "rate_limit";
              } else if (sessionEnded) {
                stage = "session_ended";
              } else if (retryableTimeout) {
                stage = "retryable_timeout";
              } else if (addPhonePrompt) {
                stage = "add_phone";
              } else if (oauthContinueVisible) {
                stage = "oauth_consent";
                    } else if (authenticatedAppShell) {
                      stage = "app_shell";
              } else if (aboutYou) {
                stage = "about_you";
              } else if (needsEmailVerification) {
                stage = "email_verification";
              } else if (publicShell && !passwordInput && !verificationInput) {
                stage = "public_chatgpt";
              } else if (/\/log-in(?:\/|$)/i.test(path) && passwordInput) {
                stage = "login_password";
              } else if (/\/create-account(?:\/|$)/i.test(path) && passwordInput) {
                stage = "signup_password";
              } else if (passwordInput || verificationInput) {
                stage = "auth_prompt";
              }
              return {
                stage,
                current_url: location.href,
                headline: bodyText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
              };
            });

            const fieldSelectors = [
              'input[name="name"]',
              'input[name*="name" i]',
              'input[autocomplete="name"]',
              'input[placeholder*="name" i]',
              'select[name*="month" i]',
              'select[name*="day" i]',
              'select[name*="year" i]',
              'select',
            ];
            let field = null;
            for (const selector of fieldSelectors) {
              const candidate = page.locator(selector).first();
              if (!(await candidate.count().catch(() => 0))) {
                continue;
              }
              if (!(await candidate.isVisible().catch(() => false))) {
                continue;
              }
              field = candidate;
              break;
            }
            if (!field) {
              throw new Error("about-you-submit-fields-not-found");
            }

            const allowedStages = new Set([
              "add_phone",
              "oauth_consent",
              "app_shell",
              "public_chatgpt",
              "email_verification",
              "security_verification",
              "retryable_timeout",
              "rate_limit",
              "session_ended",
              "callback_complete",
            ]);

            const safeDetectStage = async () => {
              let lastError = null;
              for (let attempt = 0; attempt < 8; attempt += 1) {
                try {
                  return await detectStage();
                } catch (error) {
                  lastError = error;
                  const message = String(error?.message || error || "");
                  const navigationRace =
                    /Execution context was destroyed|Cannot find context with specified id|Frame was detached|navigation/i.test(message);
                  if (!navigationRace) {
                    throw error;
                  }
                  await page.waitForLoadState("domcontentloaded", { timeout: 2000 }).catch(() => {});
                  await page.waitForTimeout(250).catch(() => {});
                }
              }
              throw lastError || new Error("about-you-submit-stage-detect-failed");
            };

            const clickVisibleContinue = async () => {
              const result = await field.evaluate((node) => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                const isVisible = (candidate) => {
                  if (!(candidate instanceof Element)) {
                    return false;
                  }
                  const style = window.getComputedStyle(candidate);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = candidate.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                const readLabel = (candidate) => {
                  if (!(candidate instanceof Element)) {
                    return "";
                  }
                  if (candidate instanceof HTMLInputElement) {
                    return normalize(candidate.value || candidate.getAttribute("aria-label") || "");
                  }
                  return normalize(candidate.innerText || candidate.textContent || candidate.getAttribute("aria-label") || "");
                };
                const form = node instanceof Element ? node.closest("form") : null;
                const scope = form || document;
                const candidates = Array.from(scope.querySelectorAll("button, input[type='submit'], [role='button']"))
                  .filter((candidate) => isVisible(candidate) && candidate.getAttribute("aria-disabled") !== "true" && !candidate.hasAttribute("disabled"));
                const preferred = candidates.find((candidate) => {
                  const label = readLabel(candidate);
                  const isSubmitNode = (candidate instanceof HTMLButtonElement && candidate.type === "submit")
                    || (candidate instanceof HTMLInputElement && candidate.type === "submit");
                  return isSubmitNode && /continue|next|finish creating account|submit|verify|allow|authorize|accept/i.test(label);
                }) || candidates.find((candidate) => /continue|next|finish creating account|submit|verify|allow|authorize|accept/i.test(readLabel(candidate)));
                if (preferred instanceof HTMLElement) {
                  preferred.click();
                  return {
                    ok: true,
                    strategy: "form-dom-click",
                    label: readLabel(preferred) || null,
                  };
                }
                if (form instanceof HTMLFormElement) {
                  form.requestSubmit();
                  return {
                    ok: true,
                    strategy: "form-request-submit",
                    label: null,
                  };
                }
                return null;
              }).catch(() => null);
              return result;
            };

            const attempts = [
              async () => clickVisibleContinue(),
              async () => {
                await field.press("Enter").catch(() => {});
                return { strategy: "press-enter", label: null };
              },
              async () => {
                await field.evaluate((node) => {
                  if (node instanceof HTMLElement) {
                    node.blur();
                  }
                  const form = node instanceof Element ? node.closest("form") : null;
                  if (form instanceof HTMLFormElement) {
                    form.requestSubmit();
                  }
                }).catch(() => {});
                return { strategy: "request-submit", label: null };
              },
            ];

            const waitForTransition = async (sameStage, timeoutMs = 8000) => {
              const startedAt = Date.now();
              let latest = await safeDetectStage();
              while (Date.now() - startedAt < timeoutMs) {
                if (allowedStages.has(latest.stage) && latest.stage !== sameStage) {
                  return latest;
                }
                await page.waitForTimeout(500).catch(() => {});
                latest = await safeDetectStage();
              }
              return latest;
            };

            const transitions = [];
            for (const attempt of attempts) {
              const performed = await attempt();
              if (!performed) {
                continue;
              }
              const after = await waitForTransition("about_you");
              transitions.push({ ...performed, ...after });
              if (allowedStages.has(after.stage) && after.stage !== "about_you") {
                return {
                  ok: true,
                  strategy: performed.strategy,
                  next_stage: after.stage,
                  current_url: after.current_url,
                  headline: after.headline,
                  transitions,
                };
              }
            }

            const finalState = await safeDetectStage();
            throw new Error("about-you-submit-stuck:" + String(finalState.stage || "unknown") + ":" + String(finalState.current_url || page.url()));
    submit_openai_password_form:
      metadata:
        description: "Submit the current OpenAI password form and verify that the flow leaves the password stage."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const readStateInPage = () => {
              const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
              const hasVisible = (selector) => Array.from(document.querySelectorAll(selector)).some((node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              });
              const bodyText = normalize(document.body ? (document.body.innerText || document.body.textContent || "") : "");
              const title = normalize(document.title || "");
              const path = location.pathname;
              const host = location.hostname;
              const emailInput = hasVisible('input[type="email"], input[name="email"], input[autocomplete="email"], input[autocomplete="username"]');
              const passwordInput = hasVisible('input[type="password"], input[name="password"], input[name="new-password"], input[autocomplete="current-password"], input[autocomplete="new-password"]');
              const verificationInput = hasVisible('input[name*="code" i], input[autocomplete="one-time-code"], input[inputmode="numeric"]');
              const antiBotGate = /just a moment/i.test(title)
                || /performing security verification|verify you are not a bot|verifying you are human|verify you are human/i.test(bodyText)
                || hasVisible('input[name="cf-turnstile-response"], iframe[src*="challenge"], iframe[title*="Widget containing"]');
              const retryableTimeout = /oops, an error occurred|operation timed out|try again|invalid_state/i.test(bodyText);
              const sessionEnded = /your session has ended/i.test(bodyText);
              const rateLimitExceeded = /rate limit exceeded|too many requests|please try again later/i.test(bodyText);
              const addPhonePrompt = /add-phone/i.test(path)
                || /phone number required|add your phone number|verify your phone number|enter your phone number/i.test(bodyText);
              const aboutYou = /confirm your age|full name|birthday|finish creating account|what should we call you|tell us about yourself|about you/i.test(bodyText)
                || /about-you/i.test(path);
              const resetPasswordPrompt = /reset-password/i.test(path) || /\breset password\b/i.test(bodyText);
              const existingAccountPrompt = /an account for this email address already exists|this email address already exists|account already exists|already registered/i.test(bodyText);
              const usernameNotFound = /username_not_found|username not found/i.test(bodyText);
              const invalidCredentials = /incorrect email address or password|incorrect password|wrong password|invalid email or password/i.test(bodyText);
              const oauthConsentCopy = /sign in to codex with chatgpt|share your name, email, and profile picture with codex|share your name, email, and profile picture|link your account/i.test(bodyText)
                || /sign in to codex/i.test(title);
              const oauthContinueVisible = Array.from(document.querySelectorAll("button, a, [role='button'], input[type='submit']")).some((node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                if (!(rect.width > 0 && rect.height > 0)) {
                  return false;
                }
                const text = normalize(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || ""));
                return oauthConsentCopy && (/continue to codex|continue to openai|authorize|allow|accept/i.test(text)
                  || /^continue$/i.test(text));
              });
              const rawNeedsEmailVerification = (!oauthConsentCopy && verificationInput)
                || (!oauthConsentCopy && /check your inbox|check your email|verify your email|verification code|enter the verification code|enter your code|temporary verification code|resend email/i.test(bodyText))
                || (!oauthConsentCopy && /email-verification/i.test(path));
              const needsEmailVerification = rawNeedsEmailVerification && !aboutYou;
              let stage = "unknown";
              if (/^(localhost|127\.0\.0\.1)$/i.test(host)) {
                stage = "callback_complete";
              } else if (antiBotGate) {
                stage = "security_verification";
              } else if (rateLimitExceeded) {
                stage = "rate_limit";
              } else if (sessionEnded) {
                stage = "session_ended";
              } else if (retryableTimeout) {
                stage = "retryable_timeout";
              } else if (addPhonePrompt) {
                stage = "add_phone";
              } else if (oauthConsentCopy && oauthContinueVisible) {
                stage = "oauth_consent";
              } else if (aboutYou) {
                stage = "about_you";
              } else if (needsEmailVerification) {
                stage = "email_verification";
              } else if (existingAccountPrompt) {
                stage = "existing_account_prompt";
              } else if (invalidCredentials) {
                stage = "invalid_credentials";
              } else if (/\/create-account(?:\/|$)/i.test(path) && passwordInput) {
                stage = "signup_password";
              } else if (/\/create-account(?:\/|$)/i.test(path) && emailInput) {
                stage = "signup_email";
              } else if (/\/log-in(?:\/|$)/i.test(path) && passwordInput) {
                stage = "login_password";
              } else if (/\/log-in(?:\/|$)/i.test(path) && emailInput) {
                stage = "login_email";
              } else if (emailInput || passwordInput || verificationInput) {
                stage = "auth_prompt";
              }
              return {
                stage,
                current_url: location.href,
                headline: bodyText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
                invalid_credentials: invalidCredentials,
                reset_password_prompt: resetPasswordPrompt,
                username_not_found: usernameNotFound,
                add_phone_prompt: addPhonePrompt,
                needs_email_verification: needsEmailVerification,
                follow_up_step: aboutYou,
                oauth_continue_visible: oauthContinueVisible,
                anti_bot_gate: antiBotGate,
                retryable_timeout: retryableTimeout,
                rate_limit_exceeded: rateLimitExceeded,
                session_ended: sessionEnded,
                existing_account_prompt: existingAccountPrompt,
                callback_complete: /^(localhost|127\.0\.0\.1)$/i.test(host),
              };
            };
            const detectState = async () => {
              for (let attempt = 0; attempt < 3; attempt += 1) {
                try {
                  return await page.evaluate(readStateInPage);
                } catch (error) {
                  const message = String(error?.message || error || "");
                  if (!/Execution context was destroyed|Cannot find context|Target closed|Frame was detached/i.test(message) || attempt === 2) {
                    return {
                      stage: "unknown",
                      current_url: page.url(),
                      headline: null,
                      invalid_credentials: false,
                      reset_password_prompt: false,
                      username_not_found: false,
                      add_phone_prompt: /auth\.openai\.com\/add-phone(?:\/|$)?/i.test(page.url()),
                      needs_email_verification: /auth\.openai\.com\/email-verification(?:\/|$)?/i.test(page.url()),
                      follow_up_step: /auth\.openai\.com\/about-you(?:\/|$)?/i.test(page.url()),
                      oauth_continue_visible: false,
                      anti_bot_gate: false,
                      retryable_timeout: false,
                      rate_limit_exceeded: false,
                      session_ended: false,
                      existing_account_prompt: false,
                      callback_complete: /^(http:\/\/localhost|http:\/\/127\.0\.0\.1)/i.test(page.url()),
                    };
                  }
                  await page.waitForLoadState("domcontentloaded").catch(() => {});
                  await page.waitForTimeout(250).catch(() => {});
                }
              }
              return {
                stage: "unknown",
                current_url: page.url(),
                headline: null,
                invalid_credentials: false,
                reset_password_prompt: false,
                username_not_found: false,
                add_phone_prompt: false,
                needs_email_verification: false,
                follow_up_step: false,
                oauth_continue_visible: false,
                anti_bot_gate: false,
                retryable_timeout: false,
                rate_limit_exceeded: false,
                session_ended: false,
                existing_account_prompt: false,
                callback_complete: false,
              };
            };

            const initial = await detectState();
            const clickVisibleContinue = async () => {
              const submit = page.locator('button[type="submit"], input[type="submit"], button, [role="button"]')
                .filter({ hasText: /continue|next|create account|sign up|log in|sign in|submit|verify|authorize|allow|accept/i })
                .first();
              if ((await submit.count().catch(() => 0)) === 0) {
                return null;
              }
              const label = await submit.evaluate((node) => String(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || "")).replace(/\s+/g, " ").trim()).catch(() => null);
              await submit.scrollIntoViewIfNeeded().catch(() => {});
              await submit.click().catch(async () => {
                await submit.click({ force: true }).catch(() => {});
              });
              return {
                ok: true,
                strategy: "locator-click",
                label: label || null,
              };
            };
            const passwordField = page.locator('input[type="password"], input[name="password"], input[name="new-password"], input[autocomplete="current-password"], input[autocomplete="new-password"]').first();
            const submitFormFromField = async () => {
              try {
                const result = await passwordField.evaluate((field) => {
                  const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                  const isVisible = (node) => {
                    if (!(node instanceof Element)) {
                      return false;
                    }
                    const style = window.getComputedStyle(node);
                    if (!style || style.display === "none" || style.visibility === "hidden") {
                      return false;
                    }
                    const rect = node.getBoundingClientRect();
                    return rect.width > 0 && rect.height > 0;
                  };
                  const readLabel = (node) => {
                    if (!(node instanceof Element)) {
                      return "";
                    }
                    if (node instanceof HTMLInputElement) {
                      return normalize(node.value || node.getAttribute("aria-label") || "");
                    }
                    return normalize(node.innerText || node.textContent || node.getAttribute("aria-label") || "");
                  };
                  const form = field instanceof Element ? field.closest("form") : null;
                  const scope = form || document;
                  const candidates = Array.from(scope.querySelectorAll("button, input[type='submit'], [role='button']"))
                    .filter((node) => isVisible(node) && node.getAttribute("aria-disabled") !== "true" && !node.hasAttribute("disabled"));
                  const preferred = candidates.find((node) => {
                    const label = readLabel(node);
                    const isSubmitNode = (node instanceof HTMLButtonElement && node.type === "submit")
                      || (node instanceof HTMLInputElement && node.type === "submit");
                    return isSubmitNode && /continue|next|create account|sign up|log in|sign in|submit|verify|authorize|allow|accept/i.test(label);
                  }) || candidates.find((node) => /continue|next|create account|sign up|log in|sign in|submit|verify|authorize|allow|accept/i.test(readLabel(node)));
                  if (preferred instanceof HTMLElement) {
                    preferred.click();
                    return {
                      ok: true,
                      strategy: "form-dom-click",
                      label: readLabel(preferred) || null,
                    };
                  }
                  if (form instanceof HTMLFormElement) {
                    form.requestSubmit();
                    return {
                      ok: true,
                      strategy: "form-request-submit",
                      label: null,
                    };
                  }
                  return null;
                });
                return result;
              } catch (error) {
                const message = String(error?.message || error || "");
                if (/Execution context was destroyed|Cannot find context|Target closed|Frame was detached/i.test(message)) {
                  return {
                    ok: true,
                    strategy: "form-dom-click-navigation",
                    label: null,
                  };
                }
                throw error;
              }
            };

            const attempts = [
              async () => clickVisibleContinue(),
              async () => submitFormFromField(),
              async () => {
                await passwordField.press("Enter").catch(() => {});
                return { strategy: "press-enter", label: null };
              },
              async () => {
                await passwordField.evaluate((field) => {
                  if (field instanceof HTMLElement) {
                    field.blur();
                  }
                  const form = field instanceof Element ? field.closest("form") : null;
                  if (form instanceof HTMLFormElement) {
                    form.requestSubmit();
                  }
                }).catch(() => {});
                return { strategy: "request-submit", label: null };
              },
            ];

            const transitions = [];
            for (const attempt of attempts) {
              const performed = await attempt();
              if (!performed) {
                continue;
              }
              await page.waitForTimeout(1200).catch(() => {});
              const after = await detectState();
              transitions.push({ ...performed, ...after });
              const progressed = after.stage !== initial.stage
                || after.current_url !== initial.current_url
                || after.needs_email_verification
                || after.follow_up_step
                || after.add_phone_prompt
                || after.existing_account_prompt
                || after.invalid_credentials
                || after.reset_password_prompt
                || after.username_not_found
                || after.oauth_continue_visible
                || after.anti_bot_gate
                || after.retryable_timeout
                || after.rate_limit_exceeded
                || after.session_ended
                || after.callback_complete;
              if (progressed && after.stage !== initial.stage) {
                return {
                  ok: true,
                  strategy: performed.strategy,
                  clicked_text: performed.label || null,
                  initial_stage: initial.stage,
                  next_stage: after.stage,
                  current_url: after.current_url,
                  headline: after.headline,
                  transitions,
                };
              }
              if (progressed && after.stage !== "signup_password" && after.stage !== "login_password") {
                return {
                  ok: true,
                  strategy: performed.strategy,
                  clicked_text: performed.label || null,
                  initial_stage: initial.stage,
                  next_stage: after.stage,
                  current_url: after.current_url,
                  headline: after.headline,
                  transitions,
                };
              }
            }

            const finalState = await detectState();
            throw new Error("password-submit-stuck:" + String(finalState.stage || "unknown") + ":" + String(finalState.current_url || page.url()));
    click_one_time_code_button:
      metadata:
        description: "Switch the local device-auth login branch to one-time-code recovery."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
            const emailInputVisible = await page.locator("input[type='email'], input[name='email'], input[autocomplete='email'], input[autocomplete='username']").first().isVisible().catch(() => false);
            if (emailInputVisible) {
              return {
                ok: true,
                skipped: true,
                reason: "email-input-already-visible",
                current_url: page.url(),
              };
            }
            const controls = page.locator("button, a, [role='button'], input[type='submit']");
            const count = await controls.count().catch(() => 0);
            for (let index = 0; index < count; index += 1) {
              const locator = controls.nth(index);
              if (!(await locator.isVisible().catch(() => false))) {
                continue;
              }
              const label = normalize(await locator.evaluate((node) => {
                if (node instanceof HTMLInputElement) {
                  return node.value || node.getAttribute("aria-label") || "";
                }
                if (!(node instanceof Element)) {
                  return "";
                }
                return node.innerText || node.textContent || node.getAttribute("aria-label") || "";
              }).catch(() => ""));
              if (!/one-time code|one time code|email me a code|continue with email code|send code|log in with a one-time code/i.test(label)) {
                continue;
              }
              await locator.scrollIntoViewIfNeeded().catch(() => {});
              await locator.click({ timeout: 5000 });
              await page.waitForTimeout(1200).catch(() => {});
              return { ok: true, clicked_text: label || null, current_url: page.url() };
            }
            return { ok: false, error: "one-time-code-button-not-found", current_url: page.url() };
    click_retryable_try_again:
      metadata:
        description: "Click Try again on the local device-auth login branch when OpenAI renders its retryable timeout shell."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
            const controls = page.locator("button, a, [role='button'], input[type='submit']");
            const count = await controls.count().catch(() => 0);
            for (let index = 0; index < count; index += 1) {
              const locator = controls.nth(index);
              if (!(await locator.isVisible().catch(() => false))) {
                continue;
              }
              const label = normalize(await locator.evaluate((node) => {
                if (node instanceof HTMLInputElement) {
                  return node.value || node.getAttribute("aria-label") || "";
                }
                if (!(node instanceof Element)) {
                  return "";
                }
                return node.innerText || node.textContent || node.getAttribute("aria-label") || "";
              }).catch(() => ""));
              if (!(/^try again$/i.test(label) || /try again/i.test(label))) {
                continue;
              }
              await locator.scrollIntoViewIfNeeded().catch(() => {});
              await locator.click({ timeout: 5000 });
              await page.waitForTimeout(1200).catch(() => {});
              return { ok: true, current_url: page.url() };
            }
            return { ok: false, error: "retryable-try-again-not-found", current_url: page.url() };
    click_chatgpt_signup_button:
      metadata:
        description: "Click ChatGPT's Sign up entry from the public shell."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
            const inspectSurface = async () => {
              const currentUrl = page.url();
              const title = await page.title().catch(() => "");
              const bodyText = await page.locator("body").innerText().catch(() => "");
              const normalizedText = normalize(bodyText);
              const onAuthEntry =
                /auth\.openai\.com\/create-account/i.test(currentUrl)
                || /auth\.openai\.com/i.test(currentUrl);
              const stillPublicShell =
                /chatgpt\.com/i.test(currentUrl)
                && !/chatgpt\.com\/auth\//i.test(currentUrl)
                && /get started|log in|sign up for free|see plans and pricing|welcome to chatgpt|get responses tailored to you/i.test(normalizedText);
              return {
                current_url: currentUrl,
                headline: normalizedText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
                on_auth_entry: onAuthEntry,
                still_public_shell: stillPublicShell,
              };
            };
            const authModal = page.locator("#modal-no-auth-login, [data-testid='modal-no-auth-login'], #modal-no-auth-welcome-back, [data-testid='modal-no-auth-welcome-back']").first();
            const useModalScope = await authModal.isVisible().catch(() => false);
            const controls = useModalScope
              ? authModal.locator("a, button, [role='button'], input[type='submit']")
              : page.locator("a, button, [role='button'], input[type='submit']");
            const count = await controls.count().catch(() => 0);
            for (let index = 0; index < count; index += 1) {
              const locator = controls.nth(index);
              if (!(await locator.isVisible().catch(() => false))) {
                continue;
              }
              const payload = await locator.evaluate((node) => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                if (!(node instanceof Element)) {
                  return { label: "", href: "" };
                }
                const label = node instanceof HTMLInputElement
                  ? normalize(node.value || node.getAttribute("aria-label") || node.getAttribute("title") || "")
                  : normalize(
                      node.innerText
                      || node.textContent
                      || node.getAttribute("aria-label")
                      || node.getAttribute("title")
                      || ""
                    );
                return {
                  label,
                  href: node.getAttribute("href") || "",
                };
              }).catch(() => ({ label: "", href: "" }));
              const label = normalize(payload.label);
              const href = String(payload.href || "");
              if (
                /create-account|sign[_ -]?up/i.test(href)
                || /^sign up$/i.test(label)
                || /sign up for free|create account|get started/i.test(label)
              ) {
                await locator.scrollIntoViewIfNeeded().catch(() => {});
                await locator.click({ timeout: 5000 }).catch(() => {});
                await page.waitForTimeout(1200).catch(() => {});
                let surface = await inspectSurface();
                if (!surface.on_auth_entry && surface.still_public_shell) {
                  await page.goto("https://auth.openai.com/create-account", {
                    waitUntil: "domcontentloaded",
                  }).catch(() => {});
                  await page.waitForTimeout(1200).catch(() => {});
                  surface = await inspectSurface();
                  return {
                    ok: true,
                    clicked_text: label || null,
                    clicked_href: href || null,
                    current_url: surface.current_url,
                    headline: surface.headline,
                    fallback_navigation: true,
                  };
                }
                return {
                  ok: true,
                  clicked_text: label || null,
                  clicked_href: href || null,
                  current_url: surface.current_url,
                  headline: surface.headline,
                  fallback_navigation: false,
                };
              }
            }
            return {
              ok: false,
              error: "sign-up-button-not-found",
              current_url: page.url(),
            };
    click_chatgpt_login_button:
      metadata:
        description: "Click ChatGPT's Log in entry from the public shell."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
            const inspectSurface = async () => {
              const currentUrl = page.url();
              const title = await page.title().catch(() => "");
              const bodyText = await page.locator("body").innerText().catch(() => "");
              const normalizedText = normalize(bodyText);
              const onAuthEntry =
                /auth\.openai\.com/i.test(currentUrl)
                || /chatgpt\.com\/auth\/login/i.test(currentUrl);
              const stillPublicShell =
                /chatgpt\.com/i.test(currentUrl)
                && !/chatgpt\.com\/auth\/login/i.test(currentUrl)
                && /get started|log in|sign up for free|try it first|see plans and pricing|welcome to chatgpt/i.test(normalizedText);
              return {
                current_url: currentUrl,
                headline: normalizedText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
                on_auth_entry: onAuthEntry,
                still_public_shell: stillPublicShell,
              };
            };
            const authModal = page.locator("#modal-no-auth-login, [data-testid='modal-no-auth-login'], #modal-no-auth-welcome-back, [data-testid='modal-no-auth-welcome-back']").first();
            const useModalScope = await authModal.isVisible().catch(() => false);
            const controls = useModalScope
              ? authModal.locator("a, button, [role='button'], input[type='submit']")
              : page.locator("a, button, [role='button'], input[type='submit']");
            const count = await controls.count().catch(() => 0);
            for (let index = 0; index < count; index += 1) {
              const locator = controls.nth(index);
              if (!(await locator.isVisible().catch(() => false))) {
                continue;
              }
              const payload = await locator.evaluate((node) => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                if (!(node instanceof Element)) {
                  return { label: "", href: "" };
                }
                const label = node instanceof HTMLInputElement
                  ? normalize(node.value || node.getAttribute("aria-label") || node.getAttribute("title") || "")
                  : normalize(
                      node.innerText
                      || node.textContent
                      || node.getAttribute("aria-label")
                      || node.getAttribute("title")
                      || ""
                    );
                return {
                  label,
                  href: node.getAttribute("href") || "",
                };
              }).catch(() => ({ label: "", href: "" }));
              const label = normalize(payload.label);
              const href = String(payload.href || "");
              if (
                (useModalScope && /continue with email|log in with email|sign in with email|use email|email address/i.test(label))
                || (useModalScope && /^(log in|sign in)$/i.test(label))
                || /log-in/i.test(href)
                || /^(log in|sign in)$/i.test(label)
                || /\blog in\b|\bsign in\b/i.test(label)
              ) {
                await locator.scrollIntoViewIfNeeded().catch(() => {});
                await locator.click({ timeout: 5000 }).catch(() => {});
                await page.waitForTimeout(1500).catch(() => {});
                let surface = await inspectSurface();
                if (!surface.on_auth_entry && surface.still_public_shell) {
                  await page.goto("https://chatgpt.com/auth/login?next=%2F%3Fcodex_rotate_public_entry%3Ddirect_login", {
                    waitUntil: "domcontentloaded",
                  }).catch(() => {});
                  await page.waitForTimeout(1200).catch(() => {});
                  surface = await inspectSurface();
                  return {
                    ok: true,
                    clicked_text: label || null,
                    clicked_href: href || null,
                    current_url: surface.current_url,
                    headline: surface.headline,
                    fallback_navigation: true,
                  };
                }
                return {
                  ok: true,
                  clicked_text: label || null,
                  clicked_href: href || null,
                  current_url: surface.current_url,
                  headline: surface.headline,
                  fallback_navigation: false,
                };
              }
            }
            return {
              ok: false,
              error: "log-in-button-not-found",
              current_url: page.url(),
            };
    click_email_login_option:
      metadata:
        description: "Choose the email-based login option from ChatGPT's provider picker."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
            const controls = page.locator("button, a, [role='button'], input[type='submit']");
            const count = await controls.count().catch(() => 0);
            for (let index = 0; index < count; index += 1) {
              const locator = controls.nth(index);
              if (!(await locator.isVisible().catch(() => false))) {
                continue;
              }
              const payload = await locator.evaluate((node) => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                if (!(node instanceof Element)) {
                  return { label: "", href: "" };
                }
                const label = node instanceof HTMLInputElement
                  ? normalize(node.value || node.getAttribute("aria-label") || node.getAttribute("title") || "")
                  : normalize(
                      node.innerText
                      || node.textContent
                      || node.getAttribute("aria-label")
                      || node.getAttribute("title")
                      || ""
                    );
                return {
                  label,
                  href: node.getAttribute("href") || "",
                };
              }).catch(() => ({ label: "", href: "" }));
              const label = normalize(payload.label);
              const href = String(payload.href || "");
              if (
                /continue with email|log in with email|sign in with email|use email|email address/i.test(label)
                || /email|login|log-in/i.test(href)
              ) {
                await locator.scrollIntoViewIfNeeded().catch(() => {});
                await locator.click({ timeout: 5000 }).catch(() => {});
                await page.waitForTimeout(1200).catch(() => {});
                return {
                  ok: true,
                  clicked_text: label || null,
                  clicked_href: href || null,
                  current_url: page.url(),
                };
              }
            }
            return {
              ok: false,
              error: "email-login-option-not-found",
              current_url: page.url(),
            };
    replay_captured_auth_url:
      metadata:
        description: "Replay the captured Codex auth URL and tolerate redirect-driven navigation aborts when the browser lands on a real auth or callback page."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const fs = await import("node:fs");
            const stripAnsi = (raw) => String(raw || "").replace(/\u001b\[[0-9;]*m/g, "");
            const extractDeviceChallenge = (raw) => {
              const cleaned = stripAnsi(raw);
              const authMatch = cleaned.match(/https:\/\/auth\.openai\.com\/codex\/device\b[^\s"'<>]*/);
              const codeMatch = cleaned.match(/\b[A-Z0-9]{4}-[A-Z0-9]{5}\b/);
              return {
                auth_url: authMatch ? authMatch[0] : null,
                device_code: codeMatch ? codeMatch[0] : null,
              };
            };
            const readText = (filePath) =>
              (!filePath || !fs.existsSync(filePath)) ? "" : fs.readFileSync(filePath, "utf8");
            const fallbackSession = (() => {
              const stdoutPath = String(
                state.vars?.codex_login?.stdout_path
                || args.codex_login_stdout_path
                || ""
              ).trim();
              const stderrPath = String(
                state.vars?.codex_login?.stderr_path
                || args.codex_login_stderr_path
                || ""
              ).trim();
              const combined = readText(stdoutPath) + "\n" + readText(stderrPath);
              return extractDeviceChallenge(combined);
            })();
            const targetUrl = String(
              args.auth_url
              || state.vars?.codex_login?.auth_url
              || state.steps.capture_codex_login_url?.action?.value?.auth_url
              || fallbackSession.auth_url
              || ""
            ).trim();
            if (!targetUrl) {
              throw new Error("captured-auth-url-missing");
            }
            const priorUrl = page.url();
            const isSuccessfulLanding = (href) => Boolean(href) && /auth\.openai\.com|localhost|127\.0\.0\.1/i.test(href);
            const captureState = async () => ({
              current_url: page.url(),
              title: await page.title().catch(() => null),
            });
            const settleAfterAbort = async (reason) => {
              await page.waitForTimeout(1200).catch(() => {});
              const state = await captureState();
              if (isSuccessfulLanding(state.current_url) && state.current_url !== priorUrl) {
                return { ok: true, strategy: reason, prior_url: priorUrl, ...state };
              }
              return null;
            };
            if (priorUrl && priorUrl !== "about:blank") {
              await page.goto("about:blank", { waitUntil: "domcontentloaded", timeout: 5000 }).catch(() => {});
              await page.waitForTimeout(250).catch(() => {});
            }
            try {
              await page.goto(targetUrl, { waitUntil: "domcontentloaded", timeout: 30000 });
              return { ok: true, strategy: "goto", prior_url: priorUrl, ...(await captureState()) };
            } catch (error) {
              const message = String(error?.message || error || "");
              if (/ERR_ABORTED/i.test(message)) {
                const abortedLanding = await settleAfterAbort("goto-err-aborted");
                if (abortedLanding) {
                  return abortedLanding;
                }
              }
              await page.evaluate((url) => { window.location.assign(url); }, targetUrl).catch(() => {});
              const assignedLanding = await settleAfterAbort("location-assign");
              if (assignedLanding) {
                return assignedLanding;
              }
              throw error;
            }
    ensure_codex_device_auth_enabled:
      metadata:
        description: "Ensure the ChatGPT security setting for Codex device-code authorization is enabled on the authenticated managed profile."
        mode: headless_ok
        targetRef: primary
        browser:
          screenshot: true
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const dismissOnboardingModal = async () => {
              const dismissButton = page.locator("button, [role='button'], a").filter({ hasText: /okay,? let'?s go/i }).first();
              if ((await dismissButton.count().catch(() => 0)) > 0 && (await dismissButton.isVisible().catch(() => false))) {
                await dismissButton.scrollIntoViewIfNeeded().catch(() => {});
                await dismissButton.click({ timeout: 5000 }).catch(() => {});
                await page.waitForTimeout(700).catch(() => {});
                return true;
              }
              return false;
            };
            await dismissOnboardingModal().catch(() => false);
            const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
            const targetText = /enable device code authorization for codex/i;
            const supportingText = /device code sign-in|headless|remote environments/i;
            const unrelatedSecurityContent = /secure sign in with chatgpt|trusted devices|log out of this device|log out of all devices|text message|password/i;
            const forceCycleWhenEnabled = args.force_cycle_when_enabled === true;

            const readEnabled = async (locator) => {
              return await locator.evaluate((node) => {
                if (!(node instanceof Element)) {
                  return null;
                }
                const candidates = [
                  node,
                  ...Array.from(
                    node.querySelectorAll(
                      "input[type='checkbox'], [role='switch'], [aria-checked], [aria-pressed], [data-state]",
                    ),
                  ),
                ];
                for (const candidate of candidates) {
                  if (!(candidate instanceof Element)) {
                    continue;
                  }
                  if (candidate instanceof HTMLInputElement && candidate.type === "checkbox") {
                    return candidate.checked;
                  }
                  const ariaChecked = candidate.getAttribute("aria-checked");
                  if (ariaChecked === "true") {
                    return true;
                  }
                  if (ariaChecked === "false") {
                    return false;
                  }
                  const ariaPressed = candidate.getAttribute("aria-pressed");
                  if (ariaPressed === "true") {
                    return true;
                  }
                  if (ariaPressed === "false") {
                    return false;
                  }
                  const dataState = candidate.getAttribute("data-state");
                  if (dataState === "checked" || dataState === "on" || dataState === "enabled") {
                    return true;
                  }
                  if (dataState === "unchecked" || dataState === "off" || dataState === "disabled") {
                    return false;
                  }
                }
                return null;
              }).catch(() => null);
            };

            const readContextText = async (locator) => {
              return await locator.evaluate((node) => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                let current = node instanceof Element ? node : null;
                for (let depth = 0; current && depth < 8; depth += 1, current = current.parentElement) {
                  const text = normalize(current.innerText || current.textContent || "");
                  if (text && /enable device code authorization for codex/i.test(text)) {
                    return text;
                  }
                }
                return normalize(
                  node instanceof Element
                    ? (node.getAttribute("aria-label") || node.getAttribute("title") || "")
                    : ""
                );
              }).catch(() => "");
            };

            const inspectSecurityPanel = async () => {
              const title = await page.title().catch(() => "");
              const panel = await page.evaluate(() => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                const isVisible = (node) => {
                  if (!(node instanceof Element)) {
                    return false;
                  }
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                const dialog = Array.from(document.querySelectorAll("[role='dialog']")).find((node) => isVisible(node));
                const root = dialog || document.body;
                return {
                  dialog_visible: Boolean(dialog),
                  text: normalize(root ? (root.innerText || root.textContent || "") : ""),
                };
              }).catch(() => ({ dialog_visible: false, text: "" }));
              const normalizedText = normalize(panel.text);
              return {
                current_url: page.url(),
                headline: normalizedText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
                dialog_visible: panel.dialog_visible === true,
                security_panel: panel.dialog_visible === true && /secure sign in with chatgpt|trusted devices|enable device code authorization for codex|log out of all devices/i.test(normalizedText),
                target_row_visible: /enable device code authorization for codex/i.test(normalizedText) && /device code sign-in|headless|remote environments/i.test(normalizedText),
              };
            };

            const clickTargetRowEdge = async () => {
              const coordinates = await page.evaluate(() => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                const isVisible = (node) => {
                  if (!(node instanceof Element)) {
                    return false;
                  }
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                const target = Array.from(document.querySelectorAll("div, section, article, label, li"))
                  .filter((node) => isVisible(node))
                  .map((node) => ({
                    node,
                    text: normalize(node.textContent || ""),
                  }))
                  .filter((entry) =>
                    /enable device code authorization for codex/i.test(entry.text)
                    && /device code sign-in|headless|remote environments/i.test(entry.text)
                    && entry.text.length <= 420
                  )
                  .sort((left, right) => left.text.length - right.text.length)[0];
                if (!(target?.node instanceof Element)) {
                  return null;
                }
                const rect = target.node.getBoundingClientRect();
                return {
                  x: Math.max(rect.left + 12, rect.right - 18),
                  y: rect.top + Math.max(12, Math.min(rect.height / 2, rect.height - 12)),
                  row_text: target.text,
                };
              }).catch(() => null);
              if (!coordinates) {
                return {
                  clicked: false,
                  row_text: null,
                };
              }
              await page.mouse.click(coordinates.x, coordinates.y).catch(() => {});
              await page.waitForTimeout(1800).catch(() => {});
              return {
                clicked: true,
                row_text: coordinates.row_text || null,
              };
            };

            const collectToggleCandidates = async () => {
              const dialog = page.locator("[role='dialog']").first();
              const root = ((await dialog.count().catch(() => 0)) > 0 && (await dialog.isVisible().catch(() => false)))
                ? dialog
                : page.locator("body");
              const containers = root.locator("div, section, article, label, li");
              const count = await containers.count().catch(() => 0);
              const matches = [];
              for (let index = 0; index < count; index += 1) {
                const container = containers.nth(index);
                if (!(await container.isVisible().catch(() => false))) {
                  continue;
                }
                const containerText = normalize(await container.innerText().catch(() => ""));
                if (!targetText.test(containerText)) {
                  continue;
                }
                if (!supportingText.test(containerText)) {
                  continue;
                }
                if (containerText.length > 320) {
                  continue;
                }
                const toggles = container.locator("[role='switch'], input[type='checkbox'], button[aria-checked], button[aria-pressed]");
                const toggleCount = await toggles.count().catch(() => 0);
                if (toggleCount === 0) {
                  continue;
                }
                for (let toggleIndex = 0; toggleIndex < toggleCount; toggleIndex += 1) {
                  const toggle = toggles.nth(toggleIndex);
                  if (!(await toggle.isVisible().catch(() => false))) {
                    continue;
                  }
                  const enabled = await readEnabled(toggle);
                  matches.push({
                    container,
                    toggle,
                    enabled,
                    row_text: containerText,
                    toggle_count: toggleCount,
                    score: containerText.length + (toggleIndex * 50),
                  });
                }
              }
              const genericToggles = root.locator(
                "button, [role='switch'], input[type='checkbox'], [aria-checked], [aria-pressed], [data-state]",
              );
              const genericCount = await genericToggles.count().catch(() => 0);
              for (let index = 0; index < genericCount; index += 1) {
                const toggle = genericToggles.nth(index);
                if (!(await toggle.isVisible().catch(() => false))) {
                  continue;
                }
                const rowText = normalize(await readContextText(toggle));
                if (!targetText.test(rowText) || !supportingText.test(rowText)) {
                  continue;
                }
                const enabled = await readEnabled(toggle);
                matches.push({
                  container: toggle,
                  toggle,
                  enabled,
                  row_text: rowText,
                  toggle_count: 1,
                  score: rowText.length + 500,
                });
              }
              matches.sort((left, right) => left.score - right.score);
              return matches;
            };

            const locateToggleState = async () => {
              const matches = await collectToggleCandidates();
              const best = matches[0] || null;
              if (!best) {
                return {
                  found: false,
                  row_text: null,
                  enabled: null,
                  toggle_count: 0,
                };
              }
              await best.container.scrollIntoViewIfNeeded().catch(() => {});
              await best.toggle.scrollIntoViewIfNeeded().catch(() => {});
              await page.waitForTimeout(250).catch(() => {});
              return {
                found: true,
                row_text: best.row_text || null,
                enabled: best.enabled ?? null,
                toggle_count: best.toggle_count ?? 0,
              };
            };

            const clickToggle = async () => {
              const matches = await collectToggleCandidates();
              const best = matches[0] || null;
              if (!best) {
                return {
                  clicked: false,
                  row_text: null,
                };
              }
              await best.container.scrollIntoViewIfNeeded().catch(() => {});
              await best.toggle.scrollIntoViewIfNeeded().catch(() => {});
              await page.waitForTimeout(250).catch(() => {});
              await best.toggle.click({ force: true, timeout: 5000 }).catch(() => {});
              return {
                clicked: true,
                row_text: best.row_text || null,
              };
            };

            const activateSecurityTab = async () => {
              const candidates = [
                page.getByRole("tab", { name: /^Security$/i }).first(),
                page.getByRole("button", { name: /^Security$/i }).first(),
                page.getByRole("link", { name: /^Security$/i }).first(),
                page.locator("button,a,[role='tab'],[role='button']").filter({ hasText: /^Security$/i }).first(),
              ];
              for (const candidate of candidates) {
                if ((await candidate.count().catch(() => 0)) === 0) {
                  continue;
                }
                if (!(await candidate.isVisible().catch(() => false))) {
                  continue;
                }
                await candidate.scrollIntoViewIfNeeded().catch(() => {});
                await candidate.click().catch(() => {});
                await page.waitForTimeout(700).catch(() => {});
                return true;
              }
              return false;
            };

            const scrollSecurityPanel = async () => {
              await page.evaluate(() => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                const unrelatedSecurityContent = /secure sign in with chatgpt|trusted devices|log out of this device|log out of all devices|text message|password|codex cli|disconnect/i;
                const isVisible = (node) => {
                  if (!(node instanceof Element)) {
                    return false;
                  }
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                const dialog = Array.from(document.querySelectorAll("[role='dialog']")).find((node) => isVisible(node));
                const root = dialog || document.body;
                const target = Array.from(root.querySelectorAll("*"))
                  .map((node) => ({
                    node,
                    text: normalize(node.textContent || ""),
                  }))
                  .filter((entry) =>
                    /enable device code authorization for codex/i.test(entry.text)
                    && /device code sign-in|headless|remote environments/i.test(entry.text)
                    && entry.text.length <= 320
                  )
                  .sort((left, right) => left.text.length - right.text.length)[0];
                if (target?.node instanceof Element) {
                  target.node.scrollIntoView({ block: "center", inline: "nearest" });
                }
              }).catch(() => {});
              await page.waitForTimeout(300).catch(() => {});
              const visibleAfterTargetScroll = await locateToggleState();
              if (visibleAfterTargetScroll?.found === true) {
                return;
              }
              await page.evaluate(() => {
                const isVisible = (node) => {
                  if (!(node instanceof Element)) {
                    return false;
                  }
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                const dialog = Array.from(document.querySelectorAll("[role='dialog']")).find((node) => isVisible(node)) || document.body;
                const scrollables = [dialog, ...Array.from(dialog.querySelectorAll("*"))]
                  .filter((node) =>
                    node instanceof HTMLElement
                    && isVisible(node)
                    && node.scrollHeight > node.clientHeight + 40
                  )
                  .sort((left, right) =>
                    (right.scrollHeight - right.clientHeight) - (left.scrollHeight - left.clientHeight)
                  );
                if (scrollables.length > 0) {
                  for (const scrollable of scrollables) {
                    scrollable.scrollTop = scrollable.scrollHeight;
                  }
                  return;
                }
                window.scrollTo(0, document.body.scrollHeight);
              }).catch(() => {});
              const visibleDialog = page.locator("[role='dialog']").first();
              if ((await visibleDialog.count().catch(() => 0)) > 0 && (await visibleDialog.isVisible().catch(() => false))) {
                await visibleDialog.click({ position: { x: 40, y: 40 } }).catch(() => {});
                await visibleDialog.hover().catch(() => {});
                await page.mouse.wheel(0, 2200).catch(() => {});
                await page.keyboard.press("End").catch(() => {});
                await page.keyboard.press("PageDown").catch(() => {});
                await page.keyboard.press("PageDown").catch(() => {});
              }
            };

            const waitForToggle = async () => {
              const deadline = Date.now() + 12000;
              let activatedSecurityTab = false;
              let lastPanel = null;
              while (Date.now() < deadline) {
                await dismissOnboardingModal().catch(() => false);
                lastPanel = await inspectSecurityPanel();
                const toggle = await locateToggleState();
                if (toggle?.found === true) {
                  return {
                    found: true,
                    toggle_found: true,
                    enabled: toggle.enabled,
                    row_text: toggle.row_text,
                    ...lastPanel,
                  };
                }
                if (!activatedSecurityTab && lastPanel.security_panel !== true) {
                  activatedSecurityTab = await activateSecurityTab();
                }
                await scrollSecurityPanel();
                await page.waitForTimeout(500).catch(() => {});
              }
              return {
                found: false,
                toggle_found: false,
                enabled: null,
                row_text: null,
                ...(lastPanel || (await inspectSecurityPanel())),
              };
            };

            let before = await waitForToggle();
            if (!before?.found || !before?.toggle_found) {
              if (before?.target_row_visible === true) {
                const rowEdgeClick = await clickTargetRowEdge();
                if (rowEdgeClick.clicked === true) {
                  const afterRowEdgeToggle = await locateToggleState();
                  const afterRowEdgePanel = await inspectSecurityPanel();
                  const ambiguousAfterRowEdge =
                    afterRowEdgePanel?.target_row_visible === true
                    && afterRowEdgeToggle?.enabled == null;
                  if (afterRowEdgeToggle?.enabled === true || ambiguousAfterRowEdge) {
                    return {
                      ok: true,
                      changed: true,
                      enabled: true,
                      verification_pending: afterRowEdgeToggle?.enabled !== true,
                      ambiguous_enabled_state: afterRowEdgeToggle?.enabled !== true,
                      row_text: afterRowEdgeToggle?.row_text || rowEdgeClick.row_text || before?.row_text || null,
                      current_url: afterRowEdgePanel?.current_url || before?.current_url || page.url(),
                      headline: afterRowEdgePanel?.headline || before?.headline || null,
                      security_panel: afterRowEdgePanel?.security_panel === true || before?.security_panel === true,
                      before: {
                        enabled: before?.enabled ?? null,
                        row_text: before?.row_text || rowEdgeClick.row_text || null,
                      },
                      after: {
                        enabled: afterRowEdgeToggle?.enabled ?? null,
                        row_text: afterRowEdgeToggle?.row_text || rowEdgeClick.row_text || null,
                      },
                      error: null,
                    };
                  }
                }
              }
              return {
                ok: false,
                error: "device-auth-toggle-not-found",
                found: before?.found === true,
                toggle_found: before?.toggle_found === true,
                enabled: before?.enabled ?? null,
                row_text: before?.row_text || null,
                current_url: before?.current_url || page.url(),
                headline: before?.headline || null,
                security_panel: before?.security_panel === true,
              };
            }
            if (before.enabled === true && forceCycleWhenEnabled !== true) {
              return {
                ok: true,
                changed: false,
                enabled: true,
                row_text: before.row_text || null,
                current_url: before.current_url || page.url(),
                headline: before.headline || null,
                security_panel: before.security_panel === true,
              };
            }

            const toggleAndVerify = async (expectedEnabled) => {
              const clicked = await clickToggle();
              if (clicked?.clicked !== true) {
                return {
                  ok: false,
                  error: "device-auth-toggle-click-failed",
                  enabled: before?.enabled ?? null,
                  row_text: before?.row_text || null,
                  current_url: before?.current_url || page.url(),
                  headline: before?.headline || null,
                  security_panel: before?.security_panel === true,
                };
              }
              await page.waitForTimeout(4000).catch(() => {});
              let result = await waitForToggle();
              if (result?.enabled !== expectedEnabled) {
                await page.goto("https://chatgpt.com/#settings/Security", { waitUntil: "domcontentloaded", timeout: 15000 }).catch(() => {});
                await page.waitForTimeout(1200).catch(() => {});
                result = await waitForToggle();
              }
              return {
                ok: result?.enabled === expectedEnabled,
                result,
              };
            };

            if (before.enabled === true && forceCycleWhenEnabled === true) {
              const disabled = await toggleAndVerify(false);
              if (disabled.ok !== true) {
                return {
                  ok: false,
                  error: "device-auth-toggle-did-not-disable-for-refresh",
                  changed: false,
                  enabled: disabled?.result?.enabled ?? before?.enabled ?? null,
                  row_text: disabled?.result?.row_text || before?.row_text || null,
                  current_url: disabled?.result?.current_url || before?.current_url || page.url(),
                  headline: disabled?.result?.headline || before?.headline || null,
                  security_panel: disabled?.result?.security_panel === true || before?.security_panel === true,
                };
              }
              before = disabled.result;
            }

            const clicked = await clickToggle();
            if (clicked?.clicked !== true) {
              return {
                ok: false,
                error: "device-auth-toggle-click-failed",
                found: true,
                toggle_found: true,
                enabled: before?.enabled ?? null,
                row_text: before?.row_text || null,
                current_url: before?.current_url || page.url(),
                headline: before?.headline || null,
                security_panel: before?.security_panel === true,
              };
            }
            await page.waitForTimeout(4000).catch(() => {});

            let after = await waitForToggle();
            if (after?.enabled !== true) {
              await page.goto("https://chatgpt.com/#settings/Security", { waitUntil: "domcontentloaded", timeout: 15000 }).catch(() => {});
              await page.waitForTimeout(1200).catch(() => {});
              after = await waitForToggle();
            }

            const ambiguousEnabled =
              after?.found === true
              && after?.toggle_found === true
              && after?.enabled == null;
            const verifiedEnabled = after?.enabled === true || ambiguousEnabled;
            return {
              ok: verifiedEnabled,
              changed: true,
              cycled: forceCycleWhenEnabled === true,
              enabled: verifiedEnabled,
              verification_pending: ambiguousEnabled,
              ambiguous_enabled_state: ambiguousEnabled,
              row_text: after?.row_text || before?.row_text || null,
              current_url: after?.current_url || page.url(),
              headline: after?.headline || before?.headline || null,
              security_panel: after?.security_panel === true || before?.security_panel === true,
              before: {
                enabled: before?.enabled ?? null,
                row_text: before?.row_text || null,
              },
              after: {
                enabled: after?.enabled ?? null,
                row_text: after?.row_text || null,
              },
              error: verifiedEnabled ? null : "device-auth-toggle-did-not-enable",
            };
do:
  - cache_device_auth_retry_mode:
      metadata:
        description: "Detect whether this run is reusing an already-issued device-auth challenge and should skip the fresh ChatGPT preparation branch."
      set:
        reuse_device_auth_session: "${String(inputs.auth_url || '').trim() !== '' && String(inputs.device_code || '').trim() !== ''}"
  - park_on_blank_before_prepare_logout:
      if: "${state.vars.reuse_device_auth_session !== true}"
      metadata:
        description: "Move the managed page to about:blank before clearing OpenAI site data so the logout navigation does not reuse a live ChatGPT shell."
      call: afn.driver.browser.navigate
      with:
        body:
          url: "about:blank"
  - clear_prepare_device_auth_site_state:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.park_on_blank_before_prepare_logout?.action?.ok === true && state.steps.park_on_blank_before_prepare_logout?.action?.skipped !== true}"
      metadata:
        description: "Clear ChatGPT/OpenAI site state and visit the explicit ChatGPT logout URL before the target-account prepare flow."
        browser:
          clearSiteDataForOrigins:
            - https://auth.openai.com
            - https://chatgpt.com
            - https://chat.openai.com
      call: afn.driver.browser.navigate
      with:
        body:
          url: https://chatgpt.com/auth/logout
  - reopen_public_chatgpt_entry_for_prepare:
      if: "${state.steps.clear_prepare_device_auth_site_state?.action?.ok === true && state.steps.clear_prepare_device_auth_site_state?.action?.skipped !== true}"
      metadata:
        description: "Open the public ChatGPT entry after clearing site state so the prepare flow starts from the known logged-out shell."
      call: afn.driver.browser.navigate
      with:
        body:
          url: "https://chatgpt.com/"
  - wait_after_prepare_public_chatgpt_entry:
      if: "${state.steps.reopen_public_chatgpt_entry_for_prepare?.action?.ok === true && state.steps.reopen_public_chatgpt_entry_for_prepare?.action?.skipped !== true}"
      metadata:
        description: "Let the public ChatGPT entry render after clearing site state."
      wait:
        milliseconds: 1200
  - inspect_prepare_public_chatgpt_entry:
      if: "${state.vars.reuse_device_auth_session !== true}"
      metadata:
        description: "Inspect the public ChatGPT entry before the local device-auth prepare login branch begins."
        mode: headless_ok
        targetRef: primary
        browser:
          screenshot: true
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
            const title = await page.title().catch(() => "");
            const latest = await page.evaluate((titleText) => {
              const normalizeInner = (value) => String(value || "").replace(/\s+/g, " ").trim();
              const isVisible = (node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              };
              const buttons = Array.from(document.querySelectorAll("button, a, [role='button'], input[type='submit']"))
                .filter((node) => isVisible(node))
                .map((node) => {
                  if (node instanceof HTMLInputElement) {
                    return normalizeInner(node.value || node.getAttribute("aria-label") || "");
                  }
                  return normalizeInner(node.innerText || node.textContent || node.getAttribute("aria-label") || "");
                })
                .filter(Boolean)
                .slice(0, 20);
              const bodyText = normalizeInner(document.body ? (document.body.innerText || document.body.textContent || "") : "");
              return {
                current_url: location.href,
                title: normalizeInner(titleText || document.title || ""),
                body_text: bodyText,
                visible_buttons: buttons,
              };
            }, title);
            const combinedText = normalize([latest.title, latest.body_text, ...(latest.visible_buttons || [])].join(" "));
            const currentUrl = String(latest.current_url || page.url());
            const chatGptShell = /chatgpt\.com/i.test(currentUrl);
            const logoutUrl = /chatgpt\.com\/auth\/logout/i.test(currentUrl);
            const settingsShell = chatGptShell && /general|notifications|personalization|apps|data controls|security|account/i.test(combinedText);
            const securityOpen = chatGptShell && /secure sign in with chatgpt|trusted devices|enable device code authorization for codex|log out of all devices|multi-factor authentication|security keys & passkeys/i.test(combinedText);
            const composerShell = chatGptShell && /new chat|search chats|chat history|projects|what are you working on|what’s on your mind today|where should we begin|upgrade plan|explore gpts/i.test(combinedText);
            const sessionExpired = chatGptShell && /your session has expired|please log in again to continue using the app/i.test(combinedText);
            const publicShellSignals = chatGptShell && /log in to get smarter responses|get responses tailored to you|see plans and pricing|sign up for free|welcome to chatgpt|get started|where should we begin/i.test(combinedText);
            const loginCtaVisible = (latest.visible_buttons || []).some((label) => /^(log in|sign in)$/i.test(label) || /log in/i.test(label));
            const signupCtaVisible = (latest.visible_buttons || []).some((label) => /^sign up$/i.test(label) || /sign up for free|create account|get started/i.test(label));
            const blankShell = chatGptShell && combinedText.length === 0;
            const securityVerification = /just a moment|enable javascript and cookies to continue|verification successful/i.test(combinedText);
            const publicShell = logoutUrl || sessionExpired || loginCtaVisible || signupCtaVisible || publicShellSignals;
            const accountReady = (securityOpen || settingsShell || composerShell)
              && !sessionExpired
              && !loginCtaVisible
              && !signupCtaVisible
              && !publicShellSignals
              && !logoutUrl;
            return {
              ok: true,
              current_url: currentUrl,
              headline: latest.body_text.split("\n").map((value) => value.trim()).filter(Boolean)[0] || latest.title || null,
              stage: blankShell
                ? "blank_chatgpt"
                : (securityVerification
                    ? "security_verification"
                    : (accountReady
                    ? "app_shell"
                    : (publicShell
                        ? "public_chatgpt"
                        : "unknown"))),
              account_ready: accountReady,
              settings_shell: settingsShell,
              security_open: securityOpen,
              security_verification: securityVerification,
              session_expired: sessionExpired,
              login_cta_visible: loginCtaVisible,
              signup_cta_visible: signupCtaVisible,
              logout_url: logoutUrl,
              blank_shell: blankShell,
              visible_buttons: latest.visible_buttons || [],
              body_text_length: latest.body_text.length,
            };
  - wait_after_prepare_public_chatgpt_security_verification:
      if: "${state.vars.reuse_device_auth_session !== true && (state.steps.inspect_prepare_public_chatgpt_entry?.action?.stage === 'security_verification' || state.steps.inspect_prepare_public_chatgpt_entry?.action?.stage === 'unknown')}"
      metadata:
        description: Give the ChatGPT entry shell time to clear security-verification interstitials before deciding that a direct auth fallback is necessary.
      wait:
        milliseconds: 4000
  - inspect_prepare_public_chatgpt_entry_after_security_wait:
      if: "${state.steps.wait_after_prepare_public_chatgpt_security_verification?.action?.ok === true && state.steps.wait_after_prepare_public_chatgpt_security_verification?.action?.skipped !== true}"
      metadata:
        description: Re-inspect the ChatGPT entry after waiting through any security-verification interstitial.
        mode: headless_ok
        targetRef: primary
        browser:
          screenshot: true
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
            const title = await page.title().catch(() => "");
            const latest = await page.evaluate((titleText) => {
              const normalizeInner = (value) => String(value || "").replace(/\s+/g, " ").trim();
              const isVisible = (node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              };
              const buttons = Array.from(document.querySelectorAll("button, a, [role='button'], input[type='submit']"))
                .filter((node) => isVisible(node))
                .map((node) => {
                  if (node instanceof HTMLInputElement) {
                    return normalizeInner(node.value || node.getAttribute("aria-label") || "");
                  }
                  return normalizeInner(node.innerText || node.textContent || node.getAttribute("aria-label") || "");
                })
                .filter(Boolean)
                .slice(0, 20);
              const bodyText = normalizeInner(document.body ? (document.body.innerText || document.body.textContent || "") : "");
              return {
                current_url: location.href,
                title: normalizeInner(titleText || document.title || ""),
                body_text: bodyText,
                visible_buttons: buttons,
              };
            }, title);
            const combinedText = normalize([latest.title, latest.body_text, ...(latest.visible_buttons || [])].join(" "));
            const currentUrl = String(latest.current_url || page.url());
            const chatGptShell = /chatgpt\.com/i.test(currentUrl);
            const logoutUrl = /chatgpt\.com\/auth\/logout/i.test(currentUrl);
            const settingsShell = chatGptShell && /general|notifications|personalization|apps|data controls|security|account/i.test(combinedText);
            const securityOpen = chatGptShell && /secure sign in with chatgpt|trusted devices|enable device code authorization for codex|log out of all devices|multi-factor authentication|security keys & passkeys/i.test(combinedText);
            const composerShell = chatGptShell && /new chat|search chats|chat history|projects|what are you working on|what’s on your mind today|where should we begin|upgrade plan|explore gpts/i.test(combinedText);
            const sessionExpired = chatGptShell && /your session has expired|please log in again to continue using the app/i.test(combinedText);
            const publicShellSignals = chatGptShell && /log in to get smarter responses|get responses tailored to you|see plans and pricing|sign up for free|welcome to chatgpt|get started|where should we begin/i.test(combinedText);
            const loginCtaVisible = (latest.visible_buttons || []).some((label) => /^(log in|sign in)$/i.test(label) || /log in/i.test(label));
            const signupCtaVisible = (latest.visible_buttons || []).some((label) => /^sign up$/i.test(label) || /sign up for free|create account|get started/i.test(label));
            const blankShell = chatGptShell && combinedText.length === 0;
            const securityVerification = /just a moment|enable javascript and cookies to continue|verification successful/i.test(combinedText);
            const publicShell = logoutUrl || sessionExpired || loginCtaVisible || signupCtaVisible || publicShellSignals;
            const accountReady = (securityOpen || settingsShell || composerShell)
              && !sessionExpired
              && !loginCtaVisible
              && !signupCtaVisible
              && !publicShellSignals
              && !logoutUrl;
            return {
              ok: true,
              current_url: currentUrl,
              headline: latest.body_text.split("\n").map((value) => value.trim()).filter(Boolean)[0] || latest.title || null,
              stage: blankShell
                ? "blank_chatgpt"
                : (securityVerification
                    ? "security_verification"
                    : (accountReady
                    ? "app_shell"
                    : (publicShell
                        ? "public_chatgpt"
                        : "unknown"))),
              account_ready: accountReady,
              settings_shell: settingsShell,
              security_open: securityOpen,
              security_verification: securityVerification,
              session_expired: sessionExpired,
              login_cta_visible: loginCtaVisible,
              signup_cta_visible: signupCtaVisible,
              logout_url: logoutUrl,
              blank_shell: blankShell,
              visible_buttons: latest.visible_buttons || [],
              body_text_length: latest.body_text.length,
            };
  - dismiss_prepare_chatgpt_onboarding_modal:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.inspect_prepare_public_chatgpt_entry?.action?.stage === 'app_shell'}"
      metadata:
        description: "Dismiss the ChatGPT onboarding modal before attempting the explicit UI logout path."
        mode: headless_ok
        targetRef: primary
        browser:
          screenshot: true
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
            const findDismissButton = async () => {
              const candidates = page.locator("button, [role='button'], a");
              const count = await candidates.count().catch(() => 0);
              for (let index = 0; index < count; index += 1) {
                const candidate = candidates.nth(index);
                if (!(await candidate.isVisible().catch(() => false))) {
                  continue;
                }
                const text = normalize(await candidate.innerText().catch(() => ""));
                if (/okay,?\s+let['’]s go/i.test(text)) {
                  return { candidate, text };
                }
              }
              return null;
            };

            let match = await findDismissButton();
            if (!match) {
              return {
                ok: true,
                skipped: true,
                dismissed: false,
                current_url: page.url(),
                error: null,
              };
            }

            await match.candidate.scrollIntoViewIfNeeded().catch(() => {});

            const attempts = [
              async () => match.candidate.click({ timeout: 5000 }),
              async () => match.candidate.click({ timeout: 5000, force: true }),
              async () => match.candidate.press("Enter"),
              async () => page.evaluate(() => {
                const normalizeInner = (value) => String(value || "").replace(/\s+/g, " ").trim();
                const isVisible = (node) => {
                  if (!(node instanceof Element)) {
                    return false;
                  }
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                const target = Array.from(document.querySelectorAll("button, [role='button'], a"))
                  .find((node) => isVisible(node) && /okay,?\s+let['’]s go/i.test(normalizeInner(node.textContent || "")));
                if (target instanceof HTMLElement) {
                  target.click();
                  return true;
                }
                return false;
              }),
            ];

            let dismissed = false;
            let strategy = null;
            for (const [index, attempt] of attempts.entries()) {
              await attempt().catch(() => {});
              await page.waitForTimeout(1200).catch(() => {});
              match = await findDismissButton();
              if (!match) {
                dismissed = true;
                strategy = index === 0
                  ? "locator-click"
                  : (index === 1
                      ? "locator-force-click"
                      : (index === 2 ? "press-enter" : "dom-click"));
                break;
              }
            }
            return {
              ok: dismissed === true,
              skipped: false,
              dismissed,
              matched_text: match?.text || "Okay, let's go",
              strategy,
              current_url: page.url(),
              error: dismissed ? null : "chatgpt-onboarding-modal-still-visible",
            };
  - cache_prepare_public_entry_before_logout:
      if: "${state.vars.reuse_device_auth_session !== true}"
      metadata:
        description: "Cache the most recent ChatGPT shell inspection before attempting the explicit UI logout path."
      set:
        prepare_public_entry_before_logout: "${state.steps.dismiss_prepare_chatgpt_onboarding_modal?.action?.stage ? state.steps.dismiss_prepare_chatgpt_onboarding_modal?.action : (state.steps.inspect_prepare_public_chatgpt_entry?.action || {})}"
  - open_prepare_chatgpt_security_settings_for_logout:
      if: "${state.vars.reuse_device_auth_session !== true && state.vars.prepare_public_entry_before_logout?.stage === 'app_shell'}"
      metadata:
        description: Open ChatGPT security settings so the current managed session can be logged out before device-auth prepare continues.
      call: afn.driver.browser.navigate
      with:
        body:
          url: https://chatgpt.com/#settings/Security
  - wait_for_prepare_chatgpt_security_settings_shell:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.open_prepare_chatgpt_security_settings_for_logout?.action?.ok === true && state.steps.open_prepare_chatgpt_security_settings_for_logout?.action?.skipped !== true}"
      metadata:
        description: Wait for the ChatGPT settings shell to render before attempting the prepare-logout control.
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const ok = await page.waitForFunction(() => {
              const isVisible = (node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              };
              const dialog = Array.from(document.querySelectorAll("[role='dialog']")).find((node) => isVisible(node));
              const root = dialog || document.body;
              const text = String(root ? (root.innerText || root.textContent || "") : "").replace(/\s+/g, " ").trim().toLowerCase();
              return /general|data controls|appearance|notifications|security|account|language/i.test(text);
            }, { timeout: 10000 }).then(() => true).catch(() => false);
            const title = await page.title().catch(() => "");
            const shell = await page.evaluate(() => {
              const isVisible = (node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              };
              const dialog = Array.from(document.querySelectorAll("[role='dialog']")).find((node) => isVisible(node));
              const root = dialog || document.body;
              const text = String(root ? (root.innerText || root.textContent || "") : "").replace(/\s+/g, " ").trim();
              return {
                dialog_visible: Boolean(dialog),
                text,
              };
            }).catch(() => ({ dialog_visible: false, text: "" }));
            const normalizedText = String(shell.text || "").replace(/\s+/g, " ").trim();
            const settingsShell = /general|data controls|appearance|notifications|security|account|language/i.test(normalizedText);
            return {
              ok: ok || settingsShell,
              current_url: page.url(),
              headline: normalizedText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
              dialog_visible: shell.dialog_visible === true,
              settings_shell: settingsShell,
            };
  - activate_prepare_chatgpt_security_settings_tab:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.wait_for_prepare_chatgpt_security_settings_shell?.action?.ok === true && state.steps.wait_for_prepare_chatgpt_security_settings_shell?.action?.skipped !== true}"
      metadata:
        description: Open the Security section inside ChatGPT settings before attempting the prepare logout control.
        mode: headless_ok
        targetRef: primary
        browser:
          screenshot: true
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const inspectPanel = async () => {
              const title = await page.title().catch(() => "");
              const panel = await page.evaluate(() => {
                const isVisible = (node) => {
                  if (!(node instanceof Element)) {
                    return false;
                  }
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                const dialog = Array.from(document.querySelectorAll("[role='dialog']")).find((node) => isVisible(node));
                const root = dialog || document.body;
                return {
                  dialog_visible: Boolean(dialog),
                  text: String(root ? (root.innerText || root.textContent || "") : "").replace(/\s+/g, " ").trim(),
                };
              }).catch(() => ({ dialog_visible: false, text: "" }));
              const normalizedText = String(panel.text || "").replace(/\s+/g, " ").trim();
              return {
                current_url: page.url(),
                headline: normalizedText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
                dialog_visible: panel.dialog_visible === true,
                settings_shell: /general|notifications|apps|data controls|security|account|language/i.test(normalizedText),
                security_open: /secure sign in with chatgpt|trusted devices|enable device code authorization for codex|log out of all devices|multi-factor authentication|security keys & passkeys/i.test(normalizedText),
              };
            };
            const clickSecurity = async () => {
              const deadline = Date.now() + 10000;
              while (Date.now() < deadline) {
                const panelBefore = await inspectPanel();
                if (panelBefore.security_open === true) {
                  return { ok: true, clicked: false, panel: panelBefore };
                }
                const candidates = [
                  page.getByRole("tab", { name: /^Security$/i }).first(),
                  page.getByRole("button", { name: /^Security$/i }).first(),
                  page.getByRole("link", { name: /^Security$/i }).first(),
                  page.locator("button,a,[role='tab'],[role='button']").filter({ hasText: /Security/i }).first(),
                  page.getByText(/^Security$/i).first(),
                ];
                for (const candidate of candidates) {
                  if ((await candidate.count().catch(() => 0)) === 0) {
                    continue;
                  }
                  if (!(await candidate.isVisible().catch(() => false))) {
                    continue;
                  }
                  await candidate.scrollIntoViewIfNeeded().catch(() => {});
                  await candidate.click().catch(() => {});
                  await page.waitForTimeout(900).catch(() => {});
                  const panelAfter = await inspectPanel();
                  if (panelAfter.security_open === true) {
                    return { ok: true, clicked: true, panel: panelAfter };
                  }
                }
                await page.waitForTimeout(500).catch(() => {});
              }
              return { ok: false, clicked: false, panel: await inspectPanel() };
            };
            const result = await clickSecurity();
            const panel = result.panel || await inspectPanel();
            return {
              ok: panel.security_open === true,
              clicked: result.clicked === true,
              ...panel,
            };
  - logout_prepare_chatgpt_app_shell:
      if: "${state.vars.reuse_device_auth_session !== true && state.vars.prepare_public_entry_before_logout?.stage === 'app_shell' && state.steps.activate_prepare_chatgpt_security_settings_tab?.action?.ok === true && state.steps.activate_prepare_chatgpt_security_settings_tab?.action?.skipped !== true}"
      metadata:
        description: When the explicit logout URL still lands on an authenticated ChatGPT shell, use the ChatGPT security settings logout control before continuing.
        mode: headless_ok
        targetRef: primary
        browser:
          screenshot: true
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const result = await page.evaluate(() => {
              const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
              const isVisible = (node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                if (node.closest("script,style,noscript,template,[hidden],[aria-hidden=\"true\"]")) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              };
              const labelMatches = (label) => {
                return /^(log out|sign out)$/i.test(label)
                  || /log out of this device/i.test(label)
                  || /log out of all devices/i.test(label);
              };
              const controls = Array.from(document.querySelectorAll("button, [role='button'], input[type='submit']"))
                .filter((node) => isVisible(node))
                .map((node) => ({
                  node,
                  label: normalize(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || "")),
                }));
              const logoutButton = controls.find((candidate) => labelMatches(candidate.label));
              if (!(logoutButton?.node instanceof HTMLElement)) {
                return { ok: false, error: "logout-button-not-found" };
              }
              logoutButton.node.click();
              return { ok: true, clicked_text: logoutButton.label || "Log out" };
            }).catch((error) => ({
              ok: false,
              error: String(error?.message || error || "logout-click-failed"),
            }));
            if (result.ok) {
              await page.waitForTimeout(1800).catch(() => {});
            }
            return { ...result, current_url: page.url() };
  - park_on_blank_after_prepare_ui_logout:
      if: "${state.steps.logout_prepare_chatgpt_app_shell?.action?.ok === true && state.steps.logout_prepare_chatgpt_app_shell?.action?.skipped !== true}"
      metadata:
        description: Move the managed page to about:blank before clearing OpenAI site state again after the settings-based logout.
      call: afn.driver.browser.navigate
      with:
        body:
          url: "about:blank"
  - clear_prepare_device_auth_site_state_after_ui_logout:
      if: "${state.steps.park_on_blank_after_prepare_ui_logout?.action?.ok === true && state.steps.park_on_blank_after_prepare_ui_logout?.action?.skipped !== true}"
      metadata:
        description: Clear ChatGPT/OpenAI site state again after the settings-based logout so the next public entry reopen is forced from a clean browser session.
        browser:
          clearSiteDataForOrigins:
            - https://auth.openai.com
            - https://chatgpt.com
            - https://chat.openai.com
      call: afn.driver.browser.navigate
      with:
        body:
          url: https://chatgpt.com/auth/logout
  - reopen_public_chatgpt_entry_after_ui_logout:
      if: "${state.steps.clear_prepare_device_auth_site_state_after_ui_logout?.action?.ok === true && state.steps.clear_prepare_device_auth_site_state_after_ui_logout?.action?.skipped !== true}"
      metadata:
        description: Reopen the public ChatGPT entry after the settings-based logout fallback.
      call: afn.driver.browser.navigate
      with:
        body:
          url: https://chatgpt.com/?codex_rotate_public_entry=after_logout
  - wait_after_public_chatgpt_entry_after_ui_logout:
      if: "${state.steps.reopen_public_chatgpt_entry_after_ui_logout?.action?.ok === true && state.steps.reopen_public_chatgpt_entry_after_ui_logout?.action?.skipped !== true}"
      metadata:
        description: Let the public ChatGPT entry settle after the settings-based logout fallback.
      wait:
        milliseconds: 1200
  - inspect_prepare_public_chatgpt_entry_after_ui_logout:
      if: "${state.steps.reopen_public_chatgpt_entry_after_ui_logout?.action?.ok === true && state.steps.reopen_public_chatgpt_entry_after_ui_logout?.action?.skipped !== true}"
      metadata:
        description: "Re-inspect the ChatGPT entry after the settings-based logout fallback."
        mode: headless_ok
        targetRef: primary
        browser:
          screenshot: true
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
            const title = await page.title().catch(() => "");
            const latest = await page.evaluate((titleText) => {
              const normalizeInner = (value) => String(value || "").replace(/\s+/g, " ").trim();
              const isVisible = (node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              };
              const buttons = Array.from(document.querySelectorAll("button, a, [role='button'], input[type='submit']"))
                .filter((node) => isVisible(node))
                .map((node) => {
                  if (node instanceof HTMLInputElement) {
                    return normalizeInner(node.value || node.getAttribute("aria-label") || "");
                  }
                  return normalizeInner(node.innerText || node.textContent || node.getAttribute("aria-label") || "");
                })
                .filter(Boolean)
                .slice(0, 20);
              const bodyText = normalizeInner(document.body ? (document.body.innerText || document.body.textContent || "") : "");
              return {
                current_url: location.href,
                title: normalizeInner(titleText || document.title || ""),
                body_text: bodyText,
                visible_buttons: buttons,
              };
            }, title);
            const combinedText = normalize([latest.title, latest.body_text, ...(latest.visible_buttons || [])].join(" "));
            const currentUrl = String(latest.current_url || page.url());
            const chatGptShell = /chatgpt\.com/i.test(currentUrl);
            const logoutUrl = /chatgpt\.com\/auth\/logout/i.test(currentUrl);
            const settingsShell = chatGptShell && /general|notifications|personalization|apps|data controls|security|account/i.test(combinedText);
            const securityOpen = chatGptShell && /secure sign in with chatgpt|trusted devices|enable device code authorization for codex|log out of all devices|multi-factor authentication|security keys & passkeys/i.test(combinedText);
            const composerShell = chatGptShell && /\bnew chat\b|\bsearch chats\b|\bimages\b|\bapps\b|\bcodex\b|\bprojects\b|\bdeep research\b|what['’]s on your mind today\?|ask anything|where should we begin/i.test(combinedText);
            const sessionExpired = chatGptShell && /your session has expired|please log in again to continue using the app/i.test(combinedText);
            const publicShellSignals = chatGptShell && /log in to get smarter responses|get responses tailored to you|see plans and pricing|sign up for free|welcome to chatgpt|get started|where should we begin/i.test(combinedText);
            const loginCtaVisible = (latest.visible_buttons || []).some((label) => /^(log in|sign in)$/i.test(label) || /log in/i.test(label));
            const signupCtaVisible = (latest.visible_buttons || []).some((label) => /^sign up$/i.test(label) || /sign up for free|create account|get started/i.test(label));
            const blankShell = chatGptShell && combinedText.length === 0;
            const publicShell = logoutUrl || sessionExpired || loginCtaVisible || signupCtaVisible || publicShellSignals;
            const accountReady = (securityOpen || settingsShell || composerShell)
              && !sessionExpired
              && !loginCtaVisible
              && !signupCtaVisible
              && !publicShellSignals
              && !logoutUrl;
            return {
              ok: true,
              current_url: currentUrl,
              headline: latest.body_text.split("\n").map((value) => value.trim()).filter(Boolean)[0] || latest.title || null,
              stage: blankShell
                ? "blank_chatgpt"
                : (accountReady
                    ? "app_shell"
                    : (publicShell
                        ? "public_chatgpt"
                        : "unknown")),
              account_ready: accountReady,
              settings_shell: settingsShell,
              security_open: securityOpen,
              session_expired: sessionExpired,
              login_cta_visible: loginCtaVisible,
              signup_cta_visible: signupCtaVisible,
              logout_url: logoutUrl,
              blank_shell: blankShell,
              visible_buttons: latest.visible_buttons || [],
              body_text_length: latest.body_text.length
            };
  - cache_effective_prepare_public_entry_state:
      if: "${state.vars.reuse_device_auth_session !== true}"
      metadata:
        description: "Cache the effective public ChatGPT entry state after any logout fallback."
      run:
        script:
          language: javascript
          code: |
            const candidates = [
              state.steps?.inspect_prepare_public_chatgpt_entry_after_ui_logout?.action,
              state.steps?.inspect_prepare_public_chatgpt_entry_after_security_wait?.action,
              state.steps?.inspect_prepare_public_chatgpt_entry?.action,
            ];
            const effective = candidates.find((candidate) => {
              return candidate
                && typeof candidate === "object"
                && candidate.skipped !== true
                && (candidate.stage || candidate.current_url);
            }) || {};
            return {
              effective_prepare_public_entry_state: {
                ok: effective.ok === true,
                current_url: effective.current_url || null,
                headline: effective.headline || null,
                stage: effective.stage || null,
                account_ready: effective.account_ready === true,
                settings_shell: effective.settings_shell === true,
                security_open: effective.security_open === true,
                security_verification: effective.security_verification === true,
                session_expired: effective.session_expired === true,
                login_cta_visible: effective.login_cta_visible === true,
                signup_cta_visible: effective.signup_cta_visible === true,
                logout_url: effective.logout_url === true,
                blank_shell: effective.blank_shell === true,
              },
            };
  - open_prepare_direct_auth_entry_fallback:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.cache_effective_prepare_public_entry_state?.action?.value?.effective_prepare_public_entry_state?.stage !== 'public_chatgpt'}"
      metadata:
        description: "Open the direct OpenAI login entry when ChatGPT does not expose a trustworthy logged-out entry shell."
        browser:
          clearSiteDataForOrigins:
            - https://auth.openai.com
            - https://chatgpt.com
            - https://chat.openai.com
      call: afn.driver.browser.navigate
      with:
        body:
          url: https://auth.openai.com/log-in
  - click_prepare_signup_button:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.cache_effective_prepare_public_entry_state?.action?.value?.effective_prepare_public_entry_state?.stage === 'public_chatgpt' && String(inputs.prefer_signup_recovery ?? 'true') !== 'false'}"
      metadata:
        description: "Open a clean direct OpenAI login entry from the logged-out ChatGPT shell so the prepare branch can switch to signup on a stable OpenAI auth surface."
        browser:
          clearSiteDataForOrigins:
            - https://auth.openai.com
            - https://chatgpt.com
            - https://chat.openai.com
      call: afn.driver.browser.navigate
      with:
        body:
          url: https://auth.openai.com/log-in
  - click_prepare_login_button:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.cache_effective_prepare_public_entry_state?.action?.value?.effective_prepare_public_entry_state?.stage === 'public_chatgpt' && (String(inputs.prefer_signup_recovery ?? 'true') === 'false' || state.steps.click_prepare_signup_button?.action?.skipped === true || state.steps.click_prepare_signup_button?.action?.ok !== true)}"
      metadata:
        description: "Open a clean direct OpenAI login entry from the logged-out ChatGPT shell during the local device-auth prepare branch."
        browser:
          clearSiteDataForOrigins:
            - https://auth.openai.com
            - https://chatgpt.com
            - https://chat.openai.com
      call: afn.driver.browser.navigate
      with:
        body:
          url: https://auth.openai.com/log-in
  - wait_after_prepare_login_button:
      if: "${(state.steps.click_prepare_signup_button?.action?.ok === true && state.steps.click_prepare_signup_button?.action?.skipped !== true) || (state.steps.click_prepare_login_button?.action?.ok === true && state.steps.click_prepare_login_button?.action?.skipped !== true)}"
      metadata:
        description: "Let the ChatGPT auth entry settle before continuing on the visible OpenAI form."
      wait:
        milliseconds: 900
  - choose_prepare_email_login_option:
      if: "${false}"
      metadata:
        templateRef: click_email_login_option
        description: "Legacy no-op kept for step-id stability; device-auth now uses the visible email form directly instead of choosing a provider."
  - wait_after_prepare_email_login_option:
      if: "${state.steps.choose_prepare_email_login_option?.action?.ok === true && state.steps.choose_prepare_email_login_option?.action?.skipped !== true}"
      metadata:
        description: "Allow the email-login provider transition to settle before classifying the OpenAI surface."
      wait:
        milliseconds: 900
  - stabilize_prepare_login_surface:
      if: "${state.vars.reuse_device_auth_session !== true && (state.steps.open_prepare_direct_auth_entry_fallback?.action?.ok === true || state.steps.click_prepare_signup_button?.action?.ok === true || state.steps.click_prepare_login_button?.action?.ok === true || state.steps.choose_prepare_email_login_option?.action?.ok === true || /auth\\.openai\\.com|chatgpt\\.com\\/auth\\/login_with/i.test(String(state.steps.cache_effective_prepare_public_entry_state?.action?.value?.effective_prepare_public_entry_state?.current_url || '')))}"
      metadata:
        templateRef: wait_for_openai_surface_ready
        description: "Wait for the local device-auth prepare login surface to render."
  - classify_prepare_login_entry:
      if: "${state.vars.reuse_device_auth_session !== true && (state.steps.stabilize_prepare_login_surface?.action?.ok === true || state.steps.open_prepare_direct_auth_entry_fallback?.action?.ok === true || state.steps.click_prepare_signup_button?.action?.ok === true || state.steps.click_prepare_login_button?.action?.ok === true || state.steps.choose_prepare_email_login_option?.action?.ok === true)}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Classify the OpenAI login surface during the local device-auth prepare branch."
        browser:
          screenshot: true
  - clear_prepare_login_security_gate:
      if: "${state.steps.classify_prepare_login_entry?.action?.anti_bot_gate === true || state.steps.classify_prepare_login_entry?.action?.stage === 'security_verification'}"
      metadata:
        templateRef: clear_openai_security_gate
        description: Clear any OpenAI or Cloudflare security gate on the device-auth prepare login entry.
  - classify_prepare_login_entry_after_security_gate:
      if: "${state.steps.clear_prepare_login_security_gate?.action?.ok === true && state.steps.clear_prepare_login_security_gate?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Refresh the prepare login surface after any security gate handling.
  - click_prepare_login_button_from_auth_landing:
      if: "${state.steps.classify_prepare_login_entry?.action?.stage === 'public_chatgpt' && /chatgpt\\.com\\/auth\\/login/i.test(String(state.steps.classify_prepare_login_entry?.action?.current_url || '')) && state.steps.classify_prepare_login_entry?.action?.login_cta_visible === true}"
      metadata:
        templateRef: click_chatgpt_login_button
        description: "Click Log in again on the ChatGPT auth landing shell so the local device-auth prepare branch reaches the actual email form."
  - wait_after_prepare_login_button_from_auth_landing:
      if: "${state.steps.click_prepare_login_button_from_auth_landing?.action?.ok === true && state.steps.click_prepare_login_button_from_auth_landing?.action?.skipped !== true}"
      metadata:
        description: "Let the second auth-landing login click settle before choosing the email login provider."
      wait:
        milliseconds: 900
  - choose_prepare_email_login_option_after_auth_landing:
      if: "${false}"
      metadata:
        templateRef: click_email_login_option
        description: "Legacy no-op kept for step-id stability; device-auth now uses the visible email form directly instead of choosing a provider."
  - wait_after_prepare_email_login_option_after_auth_landing:
      if: "${state.steps.choose_prepare_email_login_option_after_auth_landing?.action?.ok === true && state.steps.choose_prepare_email_login_option_after_auth_landing?.action?.skipped !== true}"
      metadata:
        description: "Allow the secondary email-login provider transition to settle before classifying the prepare login surface again."
      wait:
        milliseconds: 900
  - classify_prepare_login_entry_after_auth_landing:
      if: "${(state.steps.click_prepare_login_button_from_auth_landing?.action?.ok === true && state.steps.click_prepare_login_button_from_auth_landing?.action?.skipped !== true) || (state.steps.choose_prepare_email_login_option_after_auth_landing?.action?.ok === true && state.steps.choose_prepare_email_login_option_after_auth_landing?.action?.skipped !== true)}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Reclassify the prepare login surface after the ChatGPT auth landing's secondary login step."
  - wait_after_prepare_login_entry_unknown:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.classify_prepare_login_entry_after_auth_landing?.action?.skipped !== true && state.steps.classify_prepare_login_entry_after_auth_landing?.action?.stage === 'unknown'}"
      metadata:
        description: Give the OpenAI prepare login shell a moment to finish rendering when the secondary auth-landing classification returns unknown.
      wait:
        milliseconds: 900
  - wait_after_prepare_login_entry_initial_unknown:
      if: "${state.vars.reuse_device_auth_session !== true && (state.steps.classify_prepare_login_entry_after_auth_landing?.action?.skipped === true || !state.steps.classify_prepare_login_entry_after_auth_landing?.action?.stage) && state.steps.classify_prepare_login_entry?.action?.stage === 'unknown'}"
      metadata:
        description: Give the initial OpenAI prepare login shell a moment to finish rendering when the first classification returns unknown.
      wait:
        milliseconds: 900
  - classify_prepare_login_entry_retry:
      if: "${(state.steps.wait_after_prepare_login_entry_unknown?.action?.ok === true && state.steps.wait_after_prepare_login_entry_unknown?.action?.skipped !== true) || (state.steps.wait_after_prepare_login_entry_initial_unknown?.action?.ok === true && state.steps.wait_after_prepare_login_entry_initial_unknown?.action?.skipped !== true)}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Retry the prepare login classification after a short settle when OpenAI first reports an unknown auth surface.
  - cache_effective_prepare_login_entry_state:
      if: "${state.vars.reuse_device_auth_session !== true}"
      metadata:
        description: "Cache the effective prepare login-entry state after any secondary ChatGPT auth-landing recovery."
      run:
        script:
          language: javascript
          code: |
            const candidates = [
              state.steps?.classify_prepare_after_choose_signup?.action,
              state.steps?.classify_prepare_login_entry_retry?.action,
              state.steps?.classify_prepare_login_entry_after_auth_landing?.action,
              state.steps?.classify_prepare_login_entry_after_security_gate?.action,
              state.steps?.classify_prepare_login_entry?.action,
            ];
            const effective = candidates.find((candidate) => {
              return candidate
                && typeof candidate === "object"
                && candidate.skipped !== true
                && (candidate.stage || candidate.current_url);
            }) || {};
            return {
              effective_prepare_login_entry_state: {
                ok: effective.ok === true,
                current_url: effective.current_url || null,
                headline: effective.headline || null,
                stage: effective.stage || null,
                email_input: effective.email_input === true,
                password_input: effective.password_input === true,
                anti_bot_gate: effective.anti_bot_gate === true,
                login_cta_visible: effective.login_cta_visible === true,
                signup_cta_visible: effective.signup_cta_visible === true,
                one_time_code_cta: effective.one_time_code_cta === true,
              },
            };
  - choose_prepare_signup_branch:
      if: "${state.steps.cache_effective_prepare_login_entry_state?.action?.value?.effective_prepare_login_entry_state?.stage === 'login_email' && state.steps.cache_effective_prepare_login_entry_state?.action?.value?.effective_prepare_login_entry_state?.signup_cta_visible === true && String(inputs.prefer_signup_recovery ?? 'true') !== 'false'}"
      metadata:
        templateRef: click_signup_button
        description: Switch the local device-auth prepare branch from the OpenAI login shell to the signup page.
  - classify_prepare_after_choose_signup:
      if: "${state.steps.choose_prepare_signup_branch?.action?.ok === true && state.steps.choose_prepare_signup_branch?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Refresh the prepare login surface after switching to the OpenAI signup page.
  - fill_prepare_login_email:
      if: "${state.steps.cache_effective_prepare_login_entry_state?.action?.value?.effective_prepare_login_entry_state?.stage === 'login_email' || state.steps.cache_effective_prepare_login_entry_state?.action?.value?.effective_prepare_login_entry_state?.stage === 'signup_email' || (state.steps.cache_effective_prepare_login_entry_state?.action?.value?.effective_prepare_login_entry_state?.stage === 'auth_prompt' && state.steps.cache_effective_prepare_login_entry_state?.action?.value?.effective_prepare_login_entry_state?.email_input === true)}"
      metadata:
        templateRef: fill_openai_email_field
        description: "Fill the target email on the local device-auth prepare auth branch."
  - submit_prepare_signup_email:
      if: "${(state.steps.classify_prepare_after_choose_signup?.action?.stage === 'signup_email' || state.steps.cache_effective_prepare_login_entry_state?.action?.value?.effective_prepare_login_entry_state?.stage === 'signup_email') && state.steps.fill_prepare_login_email?.action?.ok === true && state.steps.fill_prepare_login_email?.action?.skipped !== true}"
      metadata:
        templateRef: submit_signup_email_form
        description: "Submit the signup email step on the local device-auth prepare branch."
  - submit_prepare_login_email:
      if: "${state.steps.classify_prepare_after_choose_signup?.action?.stage !== 'signup_email' && (state.steps.cache_effective_prepare_login_entry_state?.action?.value?.effective_prepare_login_entry_state?.stage === 'login_email' || (state.steps.cache_effective_prepare_login_entry_state?.action?.value?.effective_prepare_login_entry_state?.stage === 'auth_prompt' && state.steps.cache_effective_prepare_login_entry_state?.action?.value?.effective_prepare_login_entry_state?.email_input === true)) && state.steps.fill_prepare_login_email?.action?.ok === true && state.steps.fill_prepare_login_email?.action?.skipped !== true}"
      metadata:
        templateRef: submit_login_email_form
        description: "Submit the login email step on the local device-auth prepare branch."
  - wait_after_prepare_login_email_submit:
      if: "${(state.steps.submit_prepare_signup_email?.action?.ok === true && state.steps.submit_prepare_signup_email?.action?.skipped !== true) || (state.steps.submit_prepare_login_email?.action?.ok === true && state.steps.submit_prepare_login_email?.action?.skipped !== true)}"
      metadata:
        description: "Allow the prepare login email step to settle before choosing one-time-code recovery."
      wait:
        milliseconds: 900
  - classify_prepare_after_login_email:
      if: "${state.steps.submit_prepare_signup_email?.action?.ok === true || state.steps.submit_prepare_login_email?.action?.ok === true || state.steps.cache_effective_prepare_login_entry_state?.action?.value?.effective_prepare_login_entry_state?.ok === true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the prepare login surface after the email step."
        browser:
          screenshot: true
  - retry_prepare_login_email_submit:
      if: "${((state.steps.submit_prepare_signup_email?.action?.ok === true && state.steps.submit_prepare_signup_email?.action?.skipped !== true) || (state.steps.submit_prepare_login_email?.action?.ok === true && state.steps.submit_prepare_login_email?.action?.skipped !== true)) && (state.steps.classify_prepare_after_login_email?.action?.stage === 'login_email' || state.steps.classify_prepare_after_login_email?.action?.stage === 'signup_email' || (state.steps.classify_prepare_after_login_email?.action?.stage === 'auth_prompt' && state.steps.classify_prepare_after_login_email?.action?.email_input === true))}"
      metadata:
        templateRef: click_primary_continue
        description: "Retry the prepare auth email submit when OpenAI leaves the email modal on the same step."
  - wait_after_prepare_login_email_retry:
      if: "${state.steps.retry_prepare_login_email_submit?.action?.ok === true && state.steps.retry_prepare_login_email_submit?.action?.skipped !== true}"
      metadata:
        description: "Allow the prepare login email retry to settle before classifying the next page."
      wait:
        milliseconds: 900
  - classify_prepare_after_login_email_retry:
      if: "${state.steps.retry_prepare_login_email_submit?.action?.ok === true && state.steps.retry_prepare_login_email_submit?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the prepare login surface after retrying the email submit."
  - click_prepare_login_email_timeout_retry:
      if: "${state.steps.classify_prepare_after_login_email?.action?.retryable_timeout === true}"
      metadata:
        templateRef: click_retryable_try_again
        description: Retry the prepare email step when OpenAI shows its retryable invalid-state shell.
  - wait_after_prepare_login_email_timeout_retry:
      if: "${state.steps.click_prepare_login_email_timeout_retry?.action?.ok === true && state.steps.click_prepare_login_email_timeout_retry?.action?.skipped !== true}"
      metadata:
        description: Allow the retryable prepare email timeout shell to return to the auth form.
      wait:
        milliseconds: 900
  - classify_prepare_after_login_email_timeout_retry:
      if: "${state.steps.click_prepare_login_email_timeout_retry?.action?.ok === true && state.steps.click_prepare_login_email_timeout_retry?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Classify the prepare login surface after clicking Try again from the retryable invalid-state shell.
  - refill_prepare_login_email_after_timeout_retry:
      if: "${state.steps.classify_prepare_after_login_email_timeout_retry?.action?.stage === 'login_email' || state.steps.classify_prepare_after_login_email_timeout_retry?.action?.stage === 'signup_email' || (state.steps.classify_prepare_after_login_email_timeout_retry?.action?.stage === 'auth_prompt' && state.steps.classify_prepare_after_login_email_timeout_retry?.action?.email_input === true)}"
      metadata:
        templateRef: fill_openai_email_field
        description: Refill the target email after the retryable invalid-state shell resets the prepare auth form.
  - submit_prepare_signup_email_after_timeout_retry:
      if: "${(state.steps.classify_prepare_after_login_email_timeout_retry?.action?.stage === 'signup_email' || state.steps.cache_effective_prepare_login_entry_state?.action?.value?.effective_prepare_login_entry_state?.stage === 'signup_email') && state.steps.refill_prepare_login_email_after_timeout_retry?.action?.ok === true && state.steps.refill_prepare_login_email_after_timeout_retry?.action?.skipped !== true}"
      metadata:
        templateRef: submit_signup_email_form
        description: Submit the prepare signup email step after recovering from the retryable invalid-state shell.
  - submit_prepare_login_email_after_timeout_retry:
      if: "${(state.steps.classify_prepare_after_login_email_timeout_retry?.action?.stage === 'login_email' || (state.steps.classify_prepare_after_login_email_timeout_retry?.action?.stage === 'auth_prompt' && state.steps.classify_prepare_after_login_email_timeout_retry?.action?.email_input === true)) && state.steps.refill_prepare_login_email_after_timeout_retry?.action?.ok === true && state.steps.refill_prepare_login_email_after_timeout_retry?.action?.skipped !== true}"
      metadata:
        templateRef: submit_login_email_form
        description: Submit the prepare login email step after recovering from the retryable invalid-state shell.
  - wait_after_prepare_login_email_timeout_retry_submit:
      if: "${(state.steps.submit_prepare_signup_email_after_timeout_retry?.action?.ok === true && state.steps.submit_prepare_signup_email_after_timeout_retry?.action?.skipped !== true) || (state.steps.submit_prepare_login_email_after_timeout_retry?.action?.ok === true && state.steps.submit_prepare_login_email_after_timeout_retry?.action?.skipped !== true)}"
      metadata:
        description: Allow the recovered prepare email submit to settle before classifying the next page.
      wait:
        milliseconds: 900
  - classify_prepare_after_login_email_timeout_retry_submit:
      if: "${(state.steps.submit_prepare_signup_email_after_timeout_retry?.action?.ok === true && state.steps.submit_prepare_signup_email_after_timeout_retry?.action?.skipped !== true) || (state.steps.submit_prepare_login_email_after_timeout_retry?.action?.ok === true && state.steps.submit_prepare_login_email_after_timeout_retry?.action?.skipped !== true)}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Refresh the prepare login surface after resubmitting the recovered email form.
  - force_prepare_signup_recovery_after_login_timeout:
      if: "${state.steps.classify_prepare_after_login_email_timeout_retry_submit?.action?.retryable_timeout === true || state.steps.classify_prepare_after_login_email_timeout_retry_submit?.action?.stage === 'retryable_timeout'}"
      metadata:
        description: Force the direct OpenAI create-account entry when the prepare login email branch remains stuck behind repeated invalid-state retries.
        mode: headless_ok
        targetRef: primary
        browser:
          clearSiteDataForOrigins:
            - https://auth.openai.com
            - https://chatgpt.com
            - https://chat.openai.com
      call: afn.driver.browser.navigate
      with:
        body:
          url: https://auth.openai.com/create-account
  - wait_after_force_prepare_signup_recovery_after_login_timeout:
      if: "${state.steps.force_prepare_signup_recovery_after_login_timeout?.action?.ok === true && state.steps.force_prepare_signup_recovery_after_login_timeout?.action?.skipped !== true}"
      metadata:
        description: Allow the forced direct OpenAI create-account recovery entry to settle before reclassifying the prepare auth surface.
      wait:
        milliseconds: 1200
  - classify_prepare_signup_recovery_after_login_timeout:
      if: "${state.steps.force_prepare_signup_recovery_after_login_timeout?.action?.ok === true && state.steps.force_prepare_signup_recovery_after_login_timeout?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Reclassify the prepare auth surface after forcing the direct OpenAI create-account recovery entry.
  - refill_prepare_email_after_forced_signup_recovery:
      if: "${state.steps.classify_prepare_signup_recovery_after_login_timeout?.action?.stage === 'signup_email' || state.steps.classify_prepare_signup_recovery_after_login_timeout?.action?.stage === 'login_email' || (state.steps.classify_prepare_signup_recovery_after_login_timeout?.action?.stage === 'auth_prompt' && state.steps.classify_prepare_signup_recovery_after_login_timeout?.action?.email_input === true)}"
      metadata:
        templateRef: fill_openai_email_field
        description: Refill the target email after forcing the direct OpenAI create-account recovery entry.
  - submit_prepare_signup_email_after_forced_signup_recovery:
      if: "${state.steps.refill_prepare_email_after_forced_signup_recovery?.action?.ok === true && state.steps.refill_prepare_email_after_forced_signup_recovery?.action?.skipped !== true && /create-account/i.test(String(state.steps.classify_prepare_signup_recovery_after_login_timeout?.action?.current_url || ''))}"
      metadata:
        templateRef: submit_signup_email_form
        description: Submit the signup email step after forcing the direct OpenAI create-account recovery entry.
  - submit_prepare_login_email_after_forced_signup_recovery:
      if: "${state.steps.refill_prepare_email_after_forced_signup_recovery?.action?.ok === true && state.steps.refill_prepare_email_after_forced_signup_recovery?.action?.skipped !== true && !/create-account/i.test(String(state.steps.classify_prepare_signup_recovery_after_login_timeout?.action?.current_url || ''))}"
      metadata:
        templateRef: submit_login_email_form
        description: Submit the login email step after forcing the direct OpenAI create-account recovery entry.
  - wait_after_prepare_email_after_forced_signup_recovery:
      if: "${(state.steps.submit_prepare_signup_email_after_forced_signup_recovery?.action?.ok === true && state.steps.submit_prepare_signup_email_after_forced_signup_recovery?.action?.skipped !== true) || (state.steps.submit_prepare_login_email_after_forced_signup_recovery?.action?.ok === true && state.steps.submit_prepare_login_email_after_forced_signup_recovery?.action?.skipped !== true)}"
      metadata:
        description: Allow the forced direct OpenAI recovery email submit to settle before classifying the next page.
      wait:
        milliseconds: 900
  - classify_prepare_after_forced_signup_recovery_email_submit:
      if: "${(state.steps.submit_prepare_signup_email_after_forced_signup_recovery?.action?.ok === true && state.steps.submit_prepare_signup_email_after_forced_signup_recovery?.action?.skipped !== true) || (state.steps.submit_prepare_login_email_after_forced_signup_recovery?.action?.ok === true && state.steps.submit_prepare_login_email_after_forced_signup_recovery?.action?.skipped !== true)}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Refresh the prepare auth surface after submitting the email form from the forced direct OpenAI recovery entry.
  - click_prepare_forced_signup_email_timeout_retry:
      if: "${state.steps.classify_prepare_after_forced_signup_recovery_email_submit?.action?.retryable_timeout === true && /create-account/i.test(String(state.steps.classify_prepare_after_forced_signup_recovery_email_submit?.action?.current_url || ''))}"
      metadata:
        templateRef: click_retryable_try_again
        description: Retry the forced direct OpenAI create-account recovery email step when OpenAI remains on its retryable invalid-state shell.
  - wait_after_prepare_forced_signup_email_timeout_retry:
      if: "${state.steps.click_prepare_forced_signup_email_timeout_retry?.action?.ok === true && state.steps.click_prepare_forced_signup_email_timeout_retry?.action?.skipped !== true}"
      metadata:
        description: Allow the forced direct OpenAI create-account retryable shell to return to the email form.
      wait:
        milliseconds: 900
  - classify_prepare_after_forced_signup_email_timeout_retry:
      if: "${state.steps.click_prepare_forced_signup_email_timeout_retry?.action?.ok === true && state.steps.click_prepare_forced_signup_email_timeout_retry?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Classify the forced direct OpenAI create-account recovery surface after clicking Try again.
  - refill_prepare_email_after_forced_signup_email_timeout_retry:
      if: "${state.steps.classify_prepare_after_forced_signup_email_timeout_retry?.action?.stage === 'signup_email' || state.steps.classify_prepare_after_forced_signup_email_timeout_retry?.action?.stage === 'login_email' || (state.steps.classify_prepare_after_forced_signup_email_timeout_retry?.action?.stage === 'auth_prompt' && state.steps.classify_prepare_after_forced_signup_email_timeout_retry?.action?.email_input === true)}"
      metadata:
        templateRef: fill_openai_email_field
        description: Refill the target email after the forced direct OpenAI create-account retryable shell returns to the auth form.
  - submit_prepare_signup_email_after_forced_signup_email_timeout_retry:
      if: "${state.steps.refill_prepare_email_after_forced_signup_email_timeout_retry?.action?.ok === true && state.steps.refill_prepare_email_after_forced_signup_email_timeout_retry?.action?.skipped !== true && /create-account/i.test(String(state.steps.classify_prepare_after_forced_signup_email_timeout_retry?.action?.current_url || ''))}"
      metadata:
        templateRef: submit_signup_email_form
        description: Submit the signup email step after recovering the forced direct OpenAI create-account email form from the retryable shell.
  - submit_prepare_login_email_after_forced_signup_email_timeout_retry:
      if: "${state.steps.refill_prepare_email_after_forced_signup_email_timeout_retry?.action?.ok === true && state.steps.refill_prepare_email_after_forced_signup_email_timeout_retry?.action?.skipped !== true && !/create-account/i.test(String(state.steps.classify_prepare_after_forced_signup_email_timeout_retry?.action?.current_url || ''))}"
      metadata:
        templateRef: submit_login_email_form
        description: Submit the login email step after recovering the forced direct OpenAI recovery email form from the retryable shell.
  - wait_after_prepare_forced_signup_email_timeout_retry_submit:
      if: "${(state.steps.submit_prepare_signup_email_after_forced_signup_email_timeout_retry?.action?.ok === true && state.steps.submit_prepare_signup_email_after_forced_signup_email_timeout_retry?.action?.skipped !== true) || (state.steps.submit_prepare_login_email_after_forced_signup_email_timeout_retry?.action?.ok === true && state.steps.submit_prepare_login_email_after_forced_signup_email_timeout_retry?.action?.skipped !== true)}"
      metadata:
        description: Allow the retried forced direct OpenAI recovery email submit to settle before classifying the next page.
      wait:
        milliseconds: 900
  - classify_prepare_after_forced_signup_email_timeout_retry_submit:
      if: "${(state.steps.submit_prepare_signup_email_after_forced_signup_email_timeout_retry?.action?.ok === true && state.steps.submit_prepare_signup_email_after_forced_signup_email_timeout_retry?.action?.skipped !== true) || (state.steps.submit_prepare_login_email_after_forced_signup_email_timeout_retry?.action?.ok === true && state.steps.submit_prepare_login_email_after_forced_signup_email_timeout_retry?.action?.skipped !== true)}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Refresh the prepare auth surface after resubmitting the recovered forced direct OpenAI recovery email form.
  - fallback_prepare_login_after_signup_invalid_state:
      if: "${((state.steps.classify_prepare_after_forced_signup_email_timeout_retry_submit?.action?.retryable_timeout === true && /create-account/i.test(String(state.steps.classify_prepare_after_forced_signup_email_timeout_retry_submit?.action?.current_url || ''))) || (state.steps.classify_prepare_after_forced_signup_recovery_email_submit?.action?.retryable_timeout === true && state.steps.click_prepare_forced_signup_email_timeout_retry?.action?.ok !== true && /create-account/i.test(String(state.steps.classify_prepare_after_forced_signup_recovery_email_submit?.action?.current_url || ''))))}"
      metadata:
        description: Pivot the prepare branch back to the direct OpenAI login page when repeated create-account invalid-state shells imply an already-created account.
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.navigate
      with:
        body:
          url: https://auth.openai.com/log-in
  - wait_after_fallback_prepare_login_after_signup_invalid_state:
      if: "${state.steps.fallback_prepare_login_after_signup_invalid_state?.action?.ok === true && state.steps.fallback_prepare_login_after_signup_invalid_state?.action?.skipped !== true}"
      metadata:
        description: Allow the direct OpenAI login fallback to render after repeated signup invalid-state shells.
      wait:
        milliseconds: 900
  - classify_prepare_login_entry_after_signup_invalid_state_fallback:
      if: "${state.steps.fallback_prepare_login_after_signup_invalid_state?.action?.ok === true && state.steps.fallback_prepare_login_after_signup_invalid_state?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Classify the direct OpenAI login fallback after repeated signup invalid-state shells.
  - fill_prepare_login_email_after_signup_invalid_state_fallback:
      if: "${state.steps.classify_prepare_login_entry_after_signup_invalid_state_fallback?.action?.stage === 'login_email' || (state.steps.classify_prepare_login_entry_after_signup_invalid_state_fallback?.action?.stage === 'auth_prompt' && state.steps.classify_prepare_login_entry_after_signup_invalid_state_fallback?.action?.email_input === true)}"
      metadata:
        templateRef: fill_openai_email_field
        description: Re-enter the target email on the direct OpenAI login fallback after repeated signup invalid-state shells.
  - submit_prepare_login_email_after_signup_invalid_state_fallback:
      if: "${state.steps.fill_prepare_login_email_after_signup_invalid_state_fallback?.action?.ok === true && state.steps.fill_prepare_login_email_after_signup_invalid_state_fallback?.action?.skipped !== true}"
      metadata:
        templateRef: submit_login_email_form
        description: Submit the login email on the direct OpenAI login fallback after repeated signup invalid-state shells.
  - wait_after_prepare_login_email_after_signup_invalid_state_fallback:
      if: "${state.steps.submit_prepare_login_email_after_signup_invalid_state_fallback?.action?.ok === true && state.steps.submit_prepare_login_email_after_signup_invalid_state_fallback?.action?.skipped !== true}"
      metadata:
        description: Allow the login fallback email submit to settle before classifying the next page.
      wait:
        milliseconds: 900
  - classify_prepare_after_login_email_signup_invalid_state_fallback:
      if: "${state.steps.submit_prepare_login_email_after_signup_invalid_state_fallback?.action?.ok === true && state.steps.submit_prepare_login_email_after_signup_invalid_state_fallback?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Refresh the prepare login surface after the signup invalid-state fallback submits the login email.
  - cache_effective_prepare_after_login_email_state:
      metadata:
        description: Cache the effective prepare auth state immediately after the email step and any retryable timeout recovery.
      run:
        script:
          language: javascript
          code: |
            const candidates = [
              state.steps?.classify_prepare_after_login_email_signup_invalid_state_fallback?.action,
              state.steps?.classify_prepare_login_entry_after_signup_invalid_state_fallback?.action,
              state.steps?.classify_prepare_after_forced_signup_email_timeout_retry_submit?.action,
              state.steps?.classify_prepare_after_forced_signup_email_timeout_retry?.action,
              state.steps?.classify_prepare_after_forced_signup_recovery_email_submit?.action,
              state.steps?.classify_prepare_signup_recovery_after_login_timeout?.action,
              state.steps?.classify_prepare_after_login_email_timeout_retry_submit?.action,
              state.steps?.classify_prepare_after_login_email_timeout_retry?.action,
              state.steps?.classify_prepare_after_login_email_retry?.action,
              state.steps?.classify_prepare_after_login_email?.action,
            ];
            const effective = candidates.find((candidate) => {
              return candidate
                && typeof candidate === "object"
                && candidate.skipped !== true
                && (candidate.stage || candidate.current_url);
            }) || {};
            return {
              effective_prepare_after_login_email_state: {
                ok: effective.ok === true,
                current_url: effective.current_url || null,
                headline: effective.headline || null,
                stage: effective.stage || null,
                email_verification: effective.email_verification === true,
                email_input: effective.email_input === true,
                password_input: effective.password_input === true,
                auth_prompt: effective.auth_prompt === true,
                retryable_timeout: effective.retryable_timeout === true,
                invalid_credentials: effective.invalid_credentials === true,
                reset_password_prompt: effective.reset_password_prompt === true,
                username_not_found: effective.username_not_found === true,
              },
            };
  - fill_prepare_login_password:
      if: "${state.steps.cache_effective_prepare_after_login_email_state?.action?.value?.effective_prepare_after_login_email_state?.stage === 'login_password' && String(inputs.prefer_password_login ?? 'true') === 'true'}"
      metadata:
        templateRef: fill_openai_password_field
        description: "Fill the stored password on the prepare login branch before falling back to recovery."
  - submit_prepare_login_password:
      if: "${state.steps.cache_effective_prepare_after_login_email_state?.action?.value?.effective_prepare_after_login_email_state?.stage === 'login_password' && String(inputs.prefer_password_login ?? 'true') === 'true' && state.steps.fill_prepare_login_password?.action?.ok === true && state.steps.fill_prepare_login_password?.action?.skipped !== true}"
      metadata:
        templateRef: submit_openai_password_form
        description: "Submit the stored password on the prepare login branch."
  - wait_after_prepare_login_password_submit:
      if: "${state.steps.submit_prepare_login_password?.action?.ok === true && state.steps.submit_prepare_login_password?.action?.skipped !== true}"
      metadata:
        description: "Allow the prepare login password submission to settle before classifying the next page."
      wait:
        milliseconds: 900
  - classify_prepare_after_login_password:
      if: "${state.steps.cache_effective_prepare_after_login_email_state?.action?.value?.effective_prepare_after_login_email_state?.stage === 'login_password' && String(inputs.prefer_password_login ?? 'true') === 'true'}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Classify the prepare login surface after submitting the stored password."
  - classify_prepare_after_login_password_gate:
      if: "${state.steps.cache_effective_prepare_after_login_email_state?.action?.value?.effective_prepare_after_login_email_state?.stage === 'login_password' && String(inputs.prefer_password_login ?? 'true') === 'true'}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the prepare login state after any password-step handling."
        browser:
          screenshot: true
  - wait_for_prepare_signup_password_field:
      if: "${state.steps.cache_effective_prepare_after_login_email_state?.action?.value?.effective_prepare_after_login_email_state?.stage === 'signup_password'}"
      metadata:
        description: "Wait for the signup password field to become visible before filling it."
        mode: headless_ok
        targetRef: primary
        browser:
          screenshot: true
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const selector = 'input[type="password"], input[name="new-password"], input[autocomplete="new-password"]';
            const ready = await page.waitForSelector(selector, { state: "visible", timeout: 10000 }).then(() => true).catch(() => false);
            return {
              ok: ready,
              current_url: page.url(),
            };
  - fill_prepare_signup_password:
      if: "${state.steps.wait_for_prepare_signup_password_field?.action?.ok === true && state.steps.wait_for_prepare_signup_password_field?.action?.skipped !== true}"
      metadata:
        templateRef: fill_openai_password_field
        description: "Fill the new account password on the prepare signup branch."
        browser:
          screenshot: true
          validation:
            pre:
              kind: playwright
              op: exists
              selector: 'input[type="password"], input[name="new-password"], input[autocomplete="new-password"]'
            post:
              kind: js
              expression: |
                const fields = Array.from(document.querySelectorAll('input[type="password"], input[name="new-password"], input[autocomplete="new-password"]'));
                const visibleFields = fields.filter((field) => {
                  if (!(field instanceof Element)) {
                    return false;
                  }
                  const style = window.getComputedStyle(field);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = field.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                });
                const values = visibleFields.map((field) => String(field.value || ""));
                const submit = Array.from(document.querySelectorAll('button[type="submit"], input[type="submit"], button, [role="button"]')).find((node) => {
                  if (!(node instanceof Element)) {
                    return false;
                  }
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = node.getBoundingClientRect();
                  if (!(rect.width > 0 && rect.height > 0)) {
                    return false;
                  }
                  const label = String(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || "")).replace(/\s+/g, " ").trim();
                  return /continue|next|create account|sign up|submit|verify/i.test(label);
                });
                const submitDisabled = submit instanceof Element ? (submit.hasAttribute("disabled") || submit.getAttribute("aria-disabled") === "true") : null;
                const hasValue = values.some((value) => value.length > 0);
                return {
                  ok: hasValue && submitDisabled === false,
                  has_value: hasValue,
                  value_lengths: values.map((value) => value.length),
                  submit_disabled: submitDisabled,
                };
  - submit_prepare_signup_password:
      if: "${state.steps.cache_effective_prepare_after_login_email_state?.action?.value?.effective_prepare_after_login_email_state?.stage === 'signup_password' && state.steps.fill_prepare_signup_password?.action?.ok === true && state.steps.fill_prepare_signup_password?.action?.skipped !== true}"
      metadata:
        templateRef: submit_openai_password_form
        description: "Submit the new account password on the prepare signup branch."
        browser:
          screenshot: true
  - wait_after_prepare_signup_password_submit:
      if: "${state.steps.submit_prepare_signup_password?.action?.ok === true && state.steps.submit_prepare_signup_password?.action?.skipped !== true}"
      metadata:
        description: "Allow the prepare signup password submission to settle before classifying the next page."
      wait:
        milliseconds: 900
  - classify_prepare_after_signup_password:
      if: "${state.steps.submit_prepare_signup_password?.action?.ok === true && state.steps.submit_prepare_signup_password?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Classify the prepare signup surface after submitting the new password."
  - click_prepare_signup_password_timeout_retry:
      if: "${state.steps.classify_prepare_after_signup_password?.action?.retryable_timeout === true && /create-account/i.test(String(state.steps.classify_prepare_after_signup_password?.action?.current_url || ''))}"
      metadata:
        templateRef: click_retryable_try_again
        description: Retry the prepare signup password step when OpenAI shows its retryable invalid-state shell.
  - wait_after_prepare_signup_password_timeout_retry:
      if: "${state.steps.click_prepare_signup_password_timeout_retry?.action?.ok === true && state.steps.click_prepare_signup_password_timeout_retry?.action?.skipped !== true}"
      metadata:
        description: Allow the retryable prepare signup password shell to return to the auth form.
      wait:
        milliseconds: 900
  - classify_prepare_after_signup_password_timeout_retry:
      if: "${state.steps.click_prepare_signup_password_timeout_retry?.action?.ok === true && state.steps.click_prepare_signup_password_timeout_retry?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Classify the prepare signup surface after clicking Try again from the retryable password shell.
  - refill_prepare_signup_password_after_timeout_retry:
      if: "${state.steps.classify_prepare_after_signup_password_timeout_retry?.action?.stage === 'signup_password'}"
      metadata:
        templateRef: fill_openai_password_field
        description: Refill the new account password after the retryable prepare signup password shell returns to the auth form.
  - submit_prepare_signup_password_after_timeout_retry:
      if: "${state.steps.refill_prepare_signup_password_after_timeout_retry?.action?.ok === true && state.steps.refill_prepare_signup_password_after_timeout_retry?.action?.skipped !== true}"
      metadata:
        templateRef: submit_openai_password_form
        description: Resubmit the new account password after recovering from a retryable prepare signup password shell.
        browser:
          screenshot: true
  - wait_after_prepare_signup_password_timeout_retry_submit:
      if: "${state.steps.submit_prepare_signup_password_after_timeout_retry?.action?.ok === true && state.steps.submit_prepare_signup_password_after_timeout_retry?.action?.skipped !== true}"
      metadata:
        description: Allow the recovered prepare signup password submit to settle before classifying the next page.
      wait:
        milliseconds: 900
  - classify_prepare_after_signup_password_timeout_retry_submit:
      if: "${state.steps.submit_prepare_signup_password_after_timeout_retry?.action?.ok === true && state.steps.submit_prepare_signup_password_after_timeout_retry?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Refresh the prepare signup surface after resubmitting the recovered password form.
  - classify_prepare_after_signup_password_gate:
      if: "${(state.steps.submit_prepare_signup_password?.action?.ok === true && state.steps.submit_prepare_signup_password?.action?.skipped !== true) || (state.steps.click_prepare_signup_password_timeout_retry?.action?.ok === true && state.steps.click_prepare_signup_password_timeout_retry?.action?.skipped !== true) || (state.steps.submit_prepare_signup_password_after_timeout_retry?.action?.ok === true && state.steps.submit_prepare_signup_password_after_timeout_retry?.action?.skipped !== true)}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the prepare signup state after the password step."
  - fallback_prepare_login_after_existing_account_prompt:
      if: "${state.steps.classify_prepare_after_signup_password_gate?.action?.existing_account_prompt === true || state.steps.classify_prepare_after_signup_password_gate?.action?.stage === 'existing_account_prompt'}"
      metadata:
        description: Pivot the prepare branch to the direct OpenAI login page when signup password confirms the account already exists.
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.navigate
      with:
        body:
          url: https://auth.openai.com/log-in
  - wait_after_fallback_prepare_login_after_existing_account_prompt:
      if: "${state.steps.fallback_prepare_login_after_existing_account_prompt?.action?.ok === true && state.steps.fallback_prepare_login_after_existing_account_prompt?.action?.skipped !== true}"
      metadata:
        description: Allow the direct OpenAI login fallback to render after the existing-account prompt.
      wait:
        milliseconds: 900
  - classify_prepare_login_entry_after_existing_account_prompt:
      if: "${state.steps.fallback_prepare_login_after_existing_account_prompt?.action?.ok === true && state.steps.fallback_prepare_login_after_existing_account_prompt?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Classify the direct OpenAI login fallback after the existing-account prompt.
  - fill_prepare_login_email_after_existing_account_prompt:
      if: "${state.steps.classify_prepare_login_entry_after_existing_account_prompt?.action?.stage === 'login_email' || (state.steps.classify_prepare_login_entry_after_existing_account_prompt?.action?.stage === 'auth_prompt' && state.steps.classify_prepare_login_entry_after_existing_account_prompt?.action?.email_input === true)}"
      metadata:
        templateRef: fill_openai_email_field
        description: Re-enter the target email on the direct OpenAI login fallback after the existing-account prompt.
  - submit_prepare_login_email_after_existing_account_prompt:
      if: "${state.steps.fill_prepare_login_email_after_existing_account_prompt?.action?.ok === true && state.steps.fill_prepare_login_email_after_existing_account_prompt?.action?.skipped !== true}"
      metadata:
        templateRef: submit_login_email_form
        description: Submit the login email on the direct OpenAI login fallback after the existing-account prompt.
  - wait_after_prepare_login_email_after_existing_account_prompt:
      if: "${state.steps.submit_prepare_login_email_after_existing_account_prompt?.action?.ok === true && state.steps.submit_prepare_login_email_after_existing_account_prompt?.action?.skipped !== true}"
      metadata:
        description: Allow the existing-account login fallback email submit to settle before classifying the next page.
      wait:
        milliseconds: 900
  - classify_prepare_after_login_email_existing_account_prompt:
      if: "${state.steps.submit_prepare_login_email_after_existing_account_prompt?.action?.ok === true && state.steps.submit_prepare_login_email_after_existing_account_prompt?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Refresh the prepare login surface after the existing-account fallback submits the login email.
  - compute_prepare_one_time_code_recovery_flag:
      metadata:
        description: "Compute whether the prepare branch should switch to one-time-code recovery."
      run:
        script:
          language: javascript
          code: |
            const existingAccountAfterEmailCandidates = [
              state.steps?.classify_prepare_after_login_email_existing_account_prompt?.action,
              state.steps?.classify_prepare_login_entry_after_existing_account_prompt?.action,
              state.steps?.cache_effective_prepare_after_login_email_state?.action?.value?.effective_prepare_after_login_email_state,
            ];
            const afterEmail = existingAccountAfterEmailCandidates.find((candidate) => {
              return candidate
                && typeof candidate === "object"
                && candidate.skipped !== true
                && (candidate.stage || candidate.current_url);
            }) || {};
            const afterPassword = state.steps?.classify_prepare_after_login_password_gate?.action || {};
            const hasLocator = args.account_login_locator != null;
            const preferPasswordLogin = String(args.prefer_password_login ?? "true").trim().toLowerCase() !== "false";
            const passwordStage = afterEmail.stage === "login_password";
            const passwordAuthPrompt =
              afterEmail.stage === "auth_prompt"
              && afterEmail.password_input === true;
            const existingAccountFallbackActive =
              state.steps?.classify_prepare_after_signup_password_gate?.action?.existing_account_prompt === true
              || state.steps?.classify_prepare_after_signup_password_gate?.action?.stage === "existing_account_prompt"
              || state.steps?.classify_prepare_after_login_email_existing_account_prompt?.action?.ok === true
              || state.steps?.classify_prepare_login_entry_after_existing_account_prompt?.action?.ok === true;
            const passwordAttempted =
              (state.steps?.fill_prepare_login_password?.action?.ok === true && state.steps?.fill_prepare_login_password?.action?.skipped !== true)
              || (state.steps?.submit_prepare_login_password?.action?.ok === true && state.steps?.submit_prepare_login_password?.action?.skipped !== true)
              || (state.steps?.classify_prepare_after_login_password_gate?.action?.ok === true && state.steps?.classify_prepare_after_login_password_gate?.action?.skipped !== true);
            const shouldRecover =
              ((!preferPasswordLogin) && (passwordStage || passwordAuthPrompt))
              || ((passwordStage || passwordAuthPrompt) && preferPasswordLogin && passwordAttempted !== true)
              || (existingAccountFallbackActive && (passwordStage || passwordAuthPrompt))
              || afterPassword.invalid_credentials === true
              || afterPassword.reset_password_prompt === true
              || afterPassword.username_not_found === true;
            return {
              ok: true,
              should_recover: shouldRecover,
              has_locator: hasLocator,
              prefer_password_login: preferPasswordLogin,
              existing_account_fallback_active: existingAccountFallbackActive,
              password_stage: passwordStage,
              password_auth_prompt: passwordAuthPrompt,
              password_attempted: passwordAttempted,
            };
  - choose_prepare_one_time_code_recovery:
      if: "${state.steps.compute_prepare_one_time_code_recovery_flag?.action?.value?.should_recover === true}"
      metadata:
        templateRef: click_one_time_code_button
        description: "Switch the local device-auth prepare login branch to one-time-code recovery."
  - classify_prepare_after_recovery_choice:
      if: "${state.steps.choose_prepare_one_time_code_recovery?.action?.ok === true && state.steps.choose_prepare_one_time_code_recovery?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the prepare login surface after selecting one-time-code recovery."
  - recover_prepare_login_recovery_timeout:
      if: "${state.steps.classify_prepare_after_recovery_choice?.action?.retryable_timeout === true}"
      metadata:
        templateRef: click_retryable_try_again
        description: "Retry the local device-auth prepare login branch when OpenAI shows a retryable timeout shell."
  - classify_prepare_after_recovery_timeout_retry:
      if: "${state.steps.recover_prepare_login_recovery_timeout?.action?.ok === true && state.steps.recover_prepare_login_recovery_timeout?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the prepare login surface after retrying one-time-code recovery."
  - reopen_prepare_one_time_code_after_timeout:
      if: "${state.steps.classify_prepare_after_recovery_timeout_retry?.action?.stage === 'login_password' || (state.steps.classify_prepare_after_recovery_timeout_retry?.action?.stage === 'auth_prompt' && state.steps.classify_prepare_after_recovery_timeout_retry?.action?.password_input === true)}"
      metadata:
        templateRef: click_one_time_code_button
        description: "Reopen one-time-code recovery when a retryable timeout returns the prepare login branch to the password prompt."
  - classify_prepare_after_timeout_reopen:
      if: "${state.steps.reopen_prepare_one_time_code_after_timeout?.action?.ok === true && state.steps.reopen_prepare_one_time_code_after_timeout?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the prepare login surface after reopening one-time-code recovery."
  - collect_prepare_login_verification_artifact:
      if: "${state.steps.cache_effective_prepare_after_login_email_state?.action?.value?.effective_prepare_after_login_email_state?.email_verification === true || state.steps.classify_prepare_after_login_email_existing_account_prompt?.action?.email_verification === true || state.steps.classify_prepare_login_entry_after_existing_account_prompt?.action?.email_verification === true || state.steps.classify_prepare_after_login_password_gate?.action?.email_verification === true || state.steps.classify_prepare_after_recovery_choice?.action?.email_verification === true || state.steps.classify_prepare_after_recovery_timeout_retry?.action?.email_verification === true || state.steps.classify_prepare_after_timeout_reopen?.action?.email_verification === true || state.steps.classify_prepare_after_signup_password_gate?.action?.email_verification === true}"
      metadata:
        description: "Collect the prepare-branch OTP from Gmail on the same managed profile."
        ref:
          digest: sha256:01e032fddc23788226829689d0b2161ad4b9acb108dac83395bfbdaef5f8974d
      call: workflow.sys.web.mail-google-com.collect-verification-artifact
      with:
        version: 1.0.0
        input:
          enabled: true
          search_query: "${'from:openai to:' + String(inputs.email || '').trim() + ' newer_than:7d'}"
          message_match_text: "${String(inputs.email || '').trim() || 'OpenAI'}"
          link_host_regex: auth\.openai\.com|chatgpt\.com
  - return_to_prepare_login_verification_page:
      if: "${state.steps.collect_prepare_login_verification_artifact?.action?.result != null}"
      metadata:
        description: "Return from Gmail to the active OpenAI email-verification page in the local device-auth prepare branch."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.navigate
      with:
        body:
          url: https://auth.openai.com/email-verification
  - stabilize_prepare_login_verification_surface:
      if: "${state.steps.return_to_prepare_login_verification_page?.action?.ok === true}"
      metadata:
        templateRef: wait_for_openai_surface_ready
        description: "Wait for the prepare login verification page to render after returning from Gmail."
  - classify_prepare_login_verification_ready:
      if: "${state.steps.return_to_prepare_login_verification_page?.action?.ok === true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Confirm the OpenAI verification surface after returning from Gmail during local device-auth prepare."
        browser:
          screenshot: true
  - refill_prepare_login_email_after_return:
      if: "${state.steps.classify_prepare_login_verification_ready?.action?.stage === 'login_email' && state.steps.classify_prepare_login_verification_ready?.action?.email_input_editable !== false}"
      metadata:
        templateRef: fill_openai_email_field
        description: "Re-enter the email when Gmail return resets the local prepare branch to the email step."
  - continue_prepare_login_email_after_return:
      if: "${state.steps.classify_prepare_login_verification_ready?.action?.stage === 'login_email' && state.steps.classify_prepare_login_verification_ready?.action?.email_input_editable === false && state.steps.classify_prepare_login_verification_ready?.action?.email_prefilled === true && state.steps.classify_prepare_login_verification_ready?.action?.continue_button_visible === true}"
      metadata:
        templateRef: click_primary_continue
        description: "Continue from the locked prefilled email step when Gmail return restores the local prepare branch to Welcome back."
  - submit_prepare_login_email_after_return:
      if: "${state.steps.refill_prepare_login_email_after_return?.action?.ok === true && state.steps.refill_prepare_login_email_after_return?.action?.skipped !== true}"
      metadata:
        templateRef: submit_login_email_form
        description: "Submit the restored prepare login email step before reopening one-time-code recovery."
  - wait_after_prepare_login_email_after_return:
      if: "${(state.steps.submit_prepare_login_email_after_return?.action?.ok === true && state.steps.submit_prepare_login_email_after_return?.action?.skipped !== true) || (state.steps.continue_prepare_login_email_after_return?.action?.ok === true && state.steps.continue_prepare_login_email_after_return?.action?.skipped !== true)}"
      metadata:
        description: "Allow the restored prepare login email step to settle before checking whether the password prompt reappeared."
      wait:
        milliseconds: 900
  - classify_prepare_login_after_return:
      if: "${(state.steps.submit_prepare_login_email_after_return?.action?.ok === true && state.steps.submit_prepare_login_email_after_return?.action?.skipped !== true) || (state.steps.continue_prepare_login_email_after_return?.action?.ok === true && state.steps.continue_prepare_login_email_after_return?.action?.skipped !== true)}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Classify the local prepare login surface after restoring the email step from the Gmail return path."
  - reopen_prepare_one_time_code_after_return:
      if: "${state.steps.classify_prepare_login_verification_ready?.action?.stage === 'login_password' || (state.steps.classify_prepare_login_verification_ready?.action?.stage === 'auth_prompt' && state.steps.classify_prepare_login_verification_ready?.action?.password_input === true) || state.steps.classify_prepare_login_after_return?.action?.stage === 'login_password' || (state.steps.classify_prepare_login_after_return?.action?.stage === 'auth_prompt' && state.steps.classify_prepare_login_after_return?.action?.password_input === true)}"
      metadata:
        templateRef: click_one_time_code_button
        description: "Reopen one-time-code recovery when the Gmail return path lands back on the password prompt during local device-auth prepare."
  - classify_prepare_after_return_recovery:
      if: "${state.steps.reopen_prepare_one_time_code_after_return?.action?.ok === true && state.steps.reopen_prepare_one_time_code_after_return?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the prepare login surface after rebuilding one-time-code recovery from the Gmail return path."
  - fill_prepare_login_verification_code:
      if: "${state.steps.cache_effective_prepare_after_login_email_state?.action?.value?.effective_prepare_after_login_email_state?.email_verification === true || state.steps.classify_prepare_login_verification_ready?.action?.stage === 'email_verification' || state.steps.classify_prepare_after_return_recovery?.action?.stage === 'email_verification' || state.steps.classify_prepare_after_recovery_choice?.action?.stage === 'email_verification' || state.steps.classify_prepare_after_timeout_reopen?.action?.stage === 'email_verification' || state.steps.classify_prepare_after_login_password_gate?.action?.stage === 'email_verification' || state.steps.classify_prepare_after_signup_password_gate?.action?.stage === 'email_verification' || state.steps.classify_prepare_after_login_email_existing_account_prompt?.action?.stage === 'email_verification' || state.steps.classify_prepare_login_entry_after_existing_account_prompt?.action?.stage === 'email_verification' || state.steps.classify_prepare_after_login_email_signup_invalid_state_fallback?.action?.stage === 'email_verification' || state.steps.classify_prepare_login_entry_after_signup_invalid_state_fallback?.action?.stage === 'email_verification' || state.steps.classify_prepare_after_login_email_retry?.action?.stage === 'email_verification' || state.steps.classify_prepare_after_login_email?.action?.stage === 'email_verification'}"
      metadata:
        description: "Fill the Gmail OTP into the OpenAI email-verification form during local device-auth prepare."
      call: afn.driver.browser.type
      with:
        body:
          selector: input[name*="code" i], input[autocomplete="one-time-code"], input[aria-label*="code" i], input[placeholder*="code" i], input[inputmode="numeric"]
          text: "${String(state.steps.collect_prepare_login_verification_artifact?.action?.result?.output?.code || state.steps.collect_prepare_login_verification_artifact?.action?.result?.state?.steps?.collect_verification_artifact?.action?.code || '').replace(/\\D+/g, '').slice(0, 6)}"
          dispatch_events_after_fill: true
          blur_after_fill: true
          settle_ms: 250
  - submit_prepare_login_verification_code:
      if: "${state.steps.fill_prepare_login_verification_code?.action?.ok === true && state.steps.fill_prepare_login_verification_code?.action?.skipped !== true}"
      metadata:
        description: "Submit the OpenAI verification OTP during local device-auth prepare."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const readSixDigitCode = (value) => {
              const match = String(value || "").match(/(?:^|\D)(\d{6})(?:\D|$)/);
              return match?.[1] || null;
            };
            const extractVerificationCode = (action) => {
              const outputs = [
                action?.result?.result?.output,
                action?.result?.output,
                action?.output,
              ].filter((value) => value && typeof value === "object");
              for (const output of outputs) {
                const directCode =
                  readSixDigitCode(output.code)
                  || readSixDigitCode(output.verification_code)
                  || readSixDigitCode(output.otp)
                  || readSixDigitCode(output.one_time_code);
                if (directCode) {
                  return directCode;
                }
              }
              for (const output of outputs) {
                const derivedCode =
                  readSixDigitCode(output.message_subject)
                  || readSixDigitCode(output.message_preview)
                  || readSixDigitCode(output.verification_link_text);
                if (derivedCode) {
                  return derivedCode;
                }
              }
              return readSixDigitCode(
                action?.result?.result?.state?.steps?.collect_verification_artifact?.action?.code
                || action?.result?.state?.steps?.collect_verification_artifact?.action?.code
                || ""
              );
            };
            const code = String(
              extractVerificationCode(state.steps.collect_prepare_login_verification_artifact?.action)
              || ""
            ).replace(/\D+/g, "").slice(0, 6);
            if (code.length < 6) {
              throw new Error("prepare-verification-code-missing");
            }
            const verificationSelector = 'input[name*="code" i], input[autocomplete="one-time-code"], input[aria-label*="code" i], input[placeholder*="code" i], input[inputmode="numeric"]';
            const detectStage = async () => await page.evaluate((selector) => {
              const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
              const hasVisible = (query) => Array.from(document.querySelectorAll(query)).some((node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              });
              const bodyText = normalize(document.body ? (document.body.innerText || document.body.textContent || "") : "");
              const title = normalize(document.title || "");
              const path = location.pathname;
              const verificationVisible = hasVisible(selector)
                && (/check your inbox|check your email|verify your email|verification code|enter the verification code|enter your code|temporary verification code|resend email/i.test(bodyText)
                  || /email-verification/i.test(path));
              let stage = "unknown";
              if (/^(localhost|127\.0\.0\.1)$/i.test(location.hostname)) {
                stage = "callback_complete";
              } else if (/just a moment/i.test(title) || /performing security verification|verify you are not a bot|verifying you are human|verify you are human/i.test(bodyText)) {
                stage = "security_verification";
              } else if (/rate limit exceeded|too many requests|please try again later/i.test(bodyText)) {
                stage = "rate_limit";
              } else if (/your session has ended/i.test(bodyText)) {
                stage = "session_ended";
              } else if (/oops, an error occurred|operation timed out|try again/i.test(bodyText)) {
                stage = "retryable_timeout";
              } else if (/add-phone/i.test(path) || /phone number required|add your phone number|verify your phone number|enter your phone number/i.test(bodyText)) {
                stage = "add_phone";
              } else if (/confirm your age|full name|birthday|finish creating account|what should we call you|tell us about yourself|about you/i.test(bodyText) || /about-you/i.test(path)) {
                stage = "about_you";
              } else if (verificationVisible) {
                stage = "email_verification";
              } else if (/an account for this email address already exists|this email address already exists|account already exists|already registered/i.test(bodyText)) {
                stage = "existing_account_prompt";
              } else if (/continue to codex|continue to openai|authorize|allow|accept/i.test(bodyText)) {
                stage = "oauth_consent";
              }
              return {
                stage,
                current_url: location.href,
                headline: bodyText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
                incorrect_code: /incorrect code/i.test(bodyText),
              };
            }, verificationSelector);
            const inputLocator = page.locator(verificationSelector);
            const inputCount = await inputLocator.count().catch(() => 0);
            const inputs = [];
            for (let index = 0; index < inputCount; index += 1) {
              const candidate = inputLocator.nth(index);
              if (await candidate.isVisible().catch(() => false)) {
                inputs.push(candidate);
              }
            }
            const input = inputs[0] || inputLocator.first();
            await input.waitFor({ state: "visible", timeout: 8000 }).catch(() => {});
            if ((await input.count().catch(() => 0)) === 0 || !(await input.isVisible().catch(() => false))) {
              const before = await detectStage();
              throw new Error("prepare-verification-input-not-ready:" + String(before.stage || "unknown") + ":" + String(before.current_url || page.url()));
            }
            const fillField = async (locator, value) => {
              await locator.scrollIntoViewIfNeeded().catch(() => {});
              await locator.click({ timeout: 1500 }).catch(() => {});
              await locator.fill("").catch(() => {});
              const filled = await locator.fill(value).then(() => true).catch(() => false);
              if (!filled) {
                await locator.press("Meta+A").catch(() => {});
                await locator.press("Control+A").catch(() => {});
                await locator.press("Backspace").catch(() => {});
                await locator.pressSequentially(value, { delay: 45 }).catch(() => {});
              }
            };
            const readCombinedValue = async () => {
              const parts = [];
              for (const locator of inputs.length ? inputs : [input]) {
                parts.push(String(await locator.inputValue().catch(() => "")).replace(/\D+/g, ""));
              }
              return parts.join("").slice(0, code.length);
            };
            if (inputs.length > 1) {
              for (let index = 0; index < inputs.length && index < code.length; index += 1) {
                await fillField(inputs[index], code[index]);
              }
            } else {
              await fillField(input, code);
            }
            await page.waitForTimeout(150).catch(() => {});
            let value = await readCombinedValue();
            if (value !== code && inputs.length > 1) {
              await input.click({ timeout: 1500 }).catch(() => {});
              await input.press("Meta+A").catch(() => {});
              await input.press("Control+A").catch(() => {});
              await input.press("Backspace").catch(() => {});
              await input.pressSequentially(code, { delay: 45 }).catch(() => {});
              await page.waitForTimeout(150).catch(() => {});
              value = await readCombinedValue();
            }
            if (value.length < 6 || value !== code) {
              throw new Error("prepare-verification-input-mismatch:" + (value || "empty"));
            }
            const clickVisibleContinue = async () => {
              return await input.evaluate((field) => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                const isVisible = (node) => {
                  if (!(node instanceof Element)) {
                    return false;
                  }
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                const readLabel = (node) => {
                  if (!(node instanceof Element)) {
                    return "";
                  }
                  if (node instanceof HTMLInputElement) {
                    return normalize(node.value || node.getAttribute("aria-label") || "");
                  }
                  return normalize(node.innerText || node.textContent || node.getAttribute("aria-label") || "");
                };
                const form = field instanceof Element ? field.closest("form") : null;
                const scope = form || document;
                const candidates = Array.from(scope.querySelectorAll("button, input[type='submit'], [role='button']"))
                  .filter((node) => isVisible(node) && node.getAttribute("aria-disabled") !== "true" && !node.hasAttribute("disabled"));
                const preferred = candidates.find((node) => {
                  const label = readLabel(node);
                  const isSubmitNode = (node instanceof HTMLButtonElement && node.type === "submit")
                    || (node instanceof HTMLInputElement && node.type === "submit");
                  return isSubmitNode && /continue|next|verify|submit|authorize|allow|accept/i.test(label);
                }) || candidates.find((node) => /continue|next|verify|submit|authorize|allow|accept/i.test(readLabel(node)));
                if (preferred instanceof HTMLElement) {
                  preferred.click();
                  return {
                    ok: true,
                    strategy: "form-dom-click",
                    label: readLabel(preferred) || null,
                  };
                }
                if (form instanceof HTMLFormElement) {
                  form.requestSubmit();
                  return {
                    ok: true,
                    strategy: "form-request-submit",
                    label: null,
                  };
                }
                return null;
              }).catch(() => null);
            };
            const attempts = [
              async () => clickVisibleContinue(),
              async () => {
                await input.press("Enter").catch(() => {});
                return { strategy: "press-enter", label: null };
              },
              async () => {
                await input.evaluate((field) => {
                  if (field instanceof HTMLElement) {
                    field.blur();
                  }
                  const form = field instanceof Element ? field.closest("form") : null;
                  if (form instanceof HTMLFormElement) {
                    form.requestSubmit();
                  }
                }).catch(() => {});
                return { strategy: "request-submit", label: null };
              },
            ];
            const transitions = [];
            for (const attempt of attempts) {
              const performed = await attempt();
              if (!performed) {
                continue;
              }
              await page.waitForTimeout(1200).catch(() => {});
              const after = await detectStage();
              transitions.push({ ...performed, ...after });
              if (after.stage !== "email_verification") {
                return {
                  ok: true,
                  code,
                  strategy: performed.strategy,
                  clicked_text: performed.label || null,
                  next_stage: after.stage,
                  current_url: after.current_url,
                  headline: after.headline,
                  transitions,
                };
              }
            }
            const finalState = await detectStage();
            if (finalState.incorrect_code) {
              throw new Error("prepare-verification-code-rejected:" + String(finalState.current_url || page.url()));
            }
            if (finalState.stage === "email_verification") {
              return {
                ok: true,
                code,
                strategy: "email-verification-still-open",
                clicked_text: null,
                next_stage: finalState.stage,
                current_url: finalState.current_url,
                headline: finalState.headline,
                incorrect_code: finalState.incorrect_code,
                transitions,
              };
            }
            throw new Error("prepare-verification-submit-stuck:" + String(finalState.stage || "unknown") + ":" + String(finalState.current_url || page.url()));
  - wait_after_prepare_login_verification_code_submit:
      if: "${state.steps.submit_prepare_login_verification_code?.action?.ok === true && state.steps.submit_prepare_login_verification_code?.action?.skipped !== true}"
      metadata:
        description: "Allow the prepare login verification submission to settle before opening ChatGPT settings."
      wait:
        milliseconds: 900
  - classify_prepare_after_login_verification_submit:
      if: "${state.steps.submit_prepare_login_verification_code?.action?.ok === true && state.steps.submit_prepare_login_verification_code?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Classify the prepare flow after submitting the verification OTP."
  - recollect_prepare_login_verification_artifact_after_incorrect_code:
      if: "${state.steps.classify_prepare_after_login_verification_submit?.action?.stage === 'email_verification'}"
      metadata:
        description: "Re-collect the prepare-branch OTP from Gmail once when OpenAI rejects the first verification code."
        ref:
          digest: sha256:01e032fddc23788226829689d0b2161ad4b9acb108dac83395bfbdaef5f8974d
      call: workflow.sys.web.mail-google-com.collect-verification-artifact
      with:
        version: 1.0.0
        input:
          enabled: true
          search_query: "${'from:openai to:' + String(inputs.email || '').trim() + ' newer_than:7d'}"
          message_match_text: "${String(inputs.email || '').trim() || 'OpenAI'}"
          link_host_regex: auth\.openai\.com|chatgpt\.com
  - cache_prepare_login_verification_artifact_after_incorrect_code:
      if: "${state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result != null}"
      metadata:
        description: Cache the replacement prepare-branch OTP into simple workflow state.
      set:
        prepare_login_verification_code: "${(String(state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.result?.output?.code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.output?.code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.output?.code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.result?.output?.verification_code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.output?.verification_code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.result?.output?.otp || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.output?.otp || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.result?.output?.one_time_code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.output?.one_time_code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.result?.output?.message_preview || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.output?.message_preview || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.result?.output?.message_subject || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.output?.message_subject || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.result?.state?.steps?.collect_verification_artifact?.action?.code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.state?.steps?.collect_verification_artifact?.action?.code || '')).match(/(?:^|\\D)(\\d{6})(?:\\D|$)/)?.[1] || ''}"
        prepare_login_verification_url: "${state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.result?.output?.verification_url || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.output?.verification_url || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.output?.verification_url || null}"
  - wait_for_prepare_login_verification_email_delivery_after_incorrect_code:
      if: "${state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.ok === true && state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.skipped !== true && String(state.vars.prepare_login_verification_code || '').length < 6}"
      metadata:
        description: Give Gmail one more chance to finish loading the replacement prepare-branch OTP before returning to OpenAI.
      wait:
        milliseconds: 4000
  - recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code:
      if: "${state.steps.wait_for_prepare_login_verification_email_delivery_after_incorrect_code?.action?.ok === true && state.steps.wait_for_prepare_login_verification_email_delivery_after_incorrect_code?.action?.skipped !== true && String(state.vars.prepare_login_verification_code || '').length < 6}"
      metadata:
        description: Re-collect the replacement prepare-branch OTP once more when Gmail still has not surfaced a usable code.
        ref:
          digest: sha256:01e032fddc23788226829689d0b2161ad4b9acb108dac83395bfbdaef5f8974d
      call: workflow.sys.web.mail-google-com.collect-verification-artifact
      with:
        version: 1.0.0
        input:
          enabled: true
          search_query: "${'from:openai to:' + String(inputs.email || '').trim() + ' newer_than:7d'}"
          message_match_text: "${String(inputs.email || '').trim() || 'OpenAI'}"
          link_host_regex: auth\.openai\.com|chatgpt\.com
  - cache_prepare_login_verification_artifact_after_incorrect_code_missing_code:
      if: "${state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result != null}"
      metadata:
        description: Overwrite the replacement prepare-branch OTP with the second recollection result when Gmail loaded late.
      set:
        prepare_login_verification_code: "${(String(state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.result?.output?.code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.output?.code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.output?.code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.result?.output?.verification_code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.output?.verification_code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.result?.output?.otp || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.output?.otp || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.result?.output?.one_time_code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.output?.one_time_code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.result?.output?.message_preview || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.output?.message_preview || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.result?.output?.message_subject || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.output?.message_subject || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.result?.state?.steps?.collect_verification_artifact?.action?.code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.state?.steps?.collect_verification_artifact?.action?.code || '')).match(/(?:^|\\D)(\\d{6})(?:\\D|$)/)?.[1] || ''}"
        prepare_login_verification_url: "${state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.result?.output?.verification_url || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.output?.verification_url || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.output?.verification_url || state.vars.prepare_login_verification_url || null}"
  - return_to_prepare_login_verification_page_after_incorrect_code:
      if: "${(state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.ok === true && state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.skipped !== true) || (state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.ok === true && state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.skipped !== true)}"
      metadata:
        description: "Return from Gmail to the prepare verification page after collecting a replacement code."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.navigate
      with:
        body:
          url: https://auth.openai.com/email-verification
  - stabilize_prepare_login_verification_surface_after_incorrect_code:
      if: "${state.steps.return_to_prepare_login_verification_page_after_incorrect_code?.action?.ok === true && state.steps.return_to_prepare_login_verification_page_after_incorrect_code?.action?.skipped !== true}"
      metadata:
        templateRef: wait_for_openai_surface_ready
        description: "Wait for the prepare verification page to settle after collecting a replacement code."
  - classify_prepare_login_verification_ready_after_incorrect_code:
      if: "${state.steps.return_to_prepare_login_verification_page_after_incorrect_code?.action?.ok === true && state.steps.return_to_prepare_login_verification_page_after_incorrect_code?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Confirm the prepare verification surface after collecting a replacement code."
  - fill_prepare_login_verification_code_after_incorrect_code:
      if: "${state.steps.classify_prepare_login_verification_ready_after_incorrect_code?.action?.stage === 'email_verification'}"
      metadata:
        description: "Fill the replacement Gmail OTP into the OpenAI email-verification form during local device-auth prepare."
      call: afn.driver.browser.type
      with:
        body:
          selector: 'input[name*="code" i], input[autocomplete="one-time-code"], input[aria-label*="code" i], input[placeholder*="code" i], input[inputmode="numeric"]'
          text: "${String(state.vars.prepare_login_verification_code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.output?.code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.state?.steps?.collect_verification_artifact?.action?.code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.output?.code || state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action?.result?.state?.steps?.collect_verification_artifact?.action?.code || '').replace(/\\D+/g, '').slice(0, 6)}"
          dispatch_events_after_fill: true
          blur_after_fill: true
          settle_ms: 250
  - submit_prepare_login_verification_code_after_incorrect_code:
      if: "${state.steps.fill_prepare_login_verification_code_after_incorrect_code?.action?.ok === true && state.steps.fill_prepare_login_verification_code_after_incorrect_code?.action?.skipped !== true}"
      metadata:
        description: "Submit the replacement OpenAI verification OTP during local device-auth prepare."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const readSixDigitCode = (value) => {
              const match = String(value || "").match(/(?:^|\D)(\d{6})(?:\D|$)/);
              return match?.[1] || null;
            };
            const extractVerificationCode = (action) => {
              const outputs = [
                action?.result?.result?.output,
                action?.result?.output,
                action?.output,
              ].filter((value) => value && typeof value === "object");
              for (const output of outputs) {
                const directCode =
                  readSixDigitCode(output.code)
                  || readSixDigitCode(output.verification_code)
                  || readSixDigitCode(output.otp)
                  || readSixDigitCode(output.one_time_code);
                if (directCode) {
                  return directCode;
                }
              }
              for (const output of outputs) {
                const derivedCode =
                  readSixDigitCode(output.message_subject)
                  || readSixDigitCode(output.message_preview)
                  || readSixDigitCode(output.verification_link_text);
                if (derivedCode) {
                  return derivedCode;
                }
              }
              return readSixDigitCode(
                action?.result?.result?.state?.steps?.collect_verification_artifact?.action?.code
                || action?.result?.state?.steps?.collect_verification_artifact?.action?.code
                || ""
              );
            };
            const code = String(
              state.vars.prepare_login_verification_code
              || extractVerificationCode(state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code_missing_code?.action)
              || extractVerificationCode(state.steps.recollect_prepare_login_verification_artifact_after_incorrect_code?.action)
              || ""
            ).replace(/\D+/g, "").slice(0, 6);
            if (code.length < 6) {
              throw new Error("prepare-verification-code-missing");
            }
            const verificationSelector = 'input[name*="code" i], input[autocomplete="one-time-code"], input[aria-label*="code" i], input[placeholder*="code" i], input[inputmode="numeric"]';
            const detectStage = async () => await page.evaluate((selector) => {
              const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
              const hasVisible = (query) => Array.from(document.querySelectorAll(query)).some((node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              });
              const bodyText = normalize(document.body ? (document.body.innerText || document.body.textContent || "") : "");
              const title = normalize(document.title || "");
              const path = location.pathname;
              const verificationVisible = hasVisible(selector)
                && (/check your inbox|check your email|verify your email|verification code|enter the verification code|enter your code|temporary verification code|resend email/i.test(bodyText)
                  || /email-verification/i.test(path));
              let stage = "unknown";
              if (/^(localhost|127\.0\.0\.1)$/i.test(location.hostname)) {
                stage = "callback_complete";
              } else if (/just a moment/i.test(title) || /performing security verification|verify you are not a bot|verifying you are human|verify you are human/i.test(bodyText)) {
                stage = "security_verification";
              } else if (/rate limit exceeded|too many requests|please try again later/i.test(bodyText)) {
                stage = "rate_limit";
              } else if (/your session has ended/i.test(bodyText)) {
                stage = "session_ended";
              } else if (/oops, an error occurred|operation timed out|try again/i.test(bodyText)) {
                stage = "retryable_timeout";
              } else if (/add-phone/i.test(path) || /phone number required|add your phone number|verify your phone number|enter your phone number/i.test(bodyText)) {
                stage = "add_phone";
              } else if (/confirm your age|full name|birthday|finish creating account|what should we call you|tell us about yourself|about you/i.test(bodyText) || /about-you/i.test(path)) {
                stage = "about_you";
              } else if (verificationVisible) {
                stage = "email_verification";
              } else if (/an account for this email address already exists|this email address already exists|account already exists|already registered/i.test(bodyText)) {
                stage = "existing_account_prompt";
              } else if (/continue to codex|continue to openai|authorize|allow|accept/i.test(bodyText)) {
                stage = "oauth_consent";
              }
              return {
                stage,
                current_url: location.href,
                headline: bodyText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
                incorrect_code: /incorrect code/i.test(bodyText),
              };
            }, verificationSelector);
            const inputLocator = page.locator(verificationSelector);
            const inputCount = await inputLocator.count().catch(() => 0);
            const inputs = [];
            for (let index = 0; index < inputCount; index += 1) {
              const candidate = inputLocator.nth(index);
              if (await candidate.isVisible().catch(() => false)) {
                inputs.push(candidate);
              }
            }
            const input = inputs[0] || inputLocator.first();
            await input.waitFor({ state: "visible", timeout: 8000 }).catch(() => {});
            if ((await input.count().catch(() => 0)) === 0 || !(await input.isVisible().catch(() => false))) {
              const before = await detectStage();
              throw new Error("prepare-verification-input-not-ready:" + String(before.stage || "unknown") + ":" + String(before.current_url || page.url()));
            }
            const fillField = async (locator, value) => {
              await locator.scrollIntoViewIfNeeded().catch(() => {});
              await locator.click({ timeout: 1500 }).catch(() => {});
              await locator.fill("").catch(() => {});
              const filled = await locator.fill(value).then(() => true).catch(() => false);
              if (!filled) {
                await locator.press("Meta+A").catch(() => {});
                await locator.press("Control+A").catch(() => {});
                await locator.press("Backspace").catch(() => {});
                await locator.pressSequentially(value, { delay: 45 }).catch(() => {});
              }
            };
            const readCombinedValue = async () => {
              const parts = [];
              for (const locator of inputs.length ? inputs : [input]) {
                parts.push(String(await locator.inputValue().catch(() => "")).replace(/\D+/g, ""));
              }
              return parts.join("").slice(0, code.length);
            };
            if (inputs.length > 1) {
              for (let index = 0; index < inputs.length && index < code.length; index += 1) {
                await fillField(inputs[index], code[index]);
              }
            } else {
              await fillField(input, code);
            }
            await page.waitForTimeout(150).catch(() => {});
            let value = await readCombinedValue();
            if (value !== code && inputs.length > 1) {
              await input.click({ timeout: 1500 }).catch(() => {});
              await input.press("Meta+A").catch(() => {});
              await input.press("Control+A").catch(() => {});
              await input.press("Backspace").catch(() => {});
              await input.pressSequentially(code, { delay: 45 }).catch(() => {});
              await page.waitForTimeout(150).catch(() => {});
              value = await readCombinedValue();
            }
            if (value.length < 6 || value !== code) {
              throw new Error("prepare-verification-input-mismatch:" + (value || "empty"));
            }
            const clickVisibleContinue = async () => {
              return await input.evaluate((field) => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                const isVisible = (node) => {
                  if (!(node instanceof Element)) {
                    return false;
                  }
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                const readLabel = (node) => {
                  if (!(node instanceof Element)) {
                    return "";
                  }
                  if (node instanceof HTMLInputElement) {
                    return normalize(node.value || node.getAttribute("aria-label") || "");
                  }
                  return normalize(node.innerText || node.textContent || node.getAttribute("aria-label") || "");
                };
                const form = field instanceof Element ? field.closest("form") : null;
                const scope = form || document;
                const candidates = Array.from(scope.querySelectorAll("button, input[type='submit'], [role='button']"))
                  .filter((node) => isVisible(node) && node.getAttribute("aria-disabled") !== "true" && !node.hasAttribute("disabled"));
                const preferred = candidates.find((node) => {
                  const label = readLabel(node);
                  const isSubmitNode = (node instanceof HTMLButtonElement && node.type === "submit")
                    || (node instanceof HTMLInputElement && node.type === "submit");
                  return isSubmitNode && /continue|next|verify|submit|authorize|allow|accept/i.test(label);
                }) || candidates.find((node) => /continue|next|verify|submit|authorize|allow|accept/i.test(readLabel(node)));
                if (preferred instanceof HTMLElement) {
                  preferred.click();
                  return {
                    ok: true,
                    strategy: "form-dom-click",
                    label: readLabel(preferred) || null,
                  };
                }
                if (form instanceof HTMLFormElement) {
                  form.requestSubmit();
                  return {
                    ok: true,
                    strategy: "form-request-submit",
                    label: null,
                  };
                }
                return null;
              }).catch(() => null);
            };
            const attempts = [
              async () => clickVisibleContinue(),
              async () => {
                await input.press("Enter").catch(() => {});
                return { strategy: "press-enter", label: null };
              },
              async () => {
                await input.evaluate((field) => {
                  if (field instanceof HTMLElement) {
                    field.blur();
                  }
                  const form = field instanceof Element ? field.closest("form") : null;
                  if (form instanceof HTMLFormElement) {
                    form.requestSubmit();
                  }
                }).catch(() => {});
                return { strategy: "request-submit", label: null };
              },
            ];
            const transitions = [];
            for (const attempt of attempts) {
              const performed = await attempt();
              if (!performed) {
                continue;
              }
              await page.waitForTimeout(1200).catch(() => {});
              const after = await detectStage();
              transitions.push({ ...performed, ...after });
              if (after.stage !== "email_verification") {
                return {
                  ok: true,
                  code,
                  strategy: performed.strategy,
                  clicked_text: performed.label || null,
                  next_stage: after.stage,
                  current_url: after.current_url,
                  headline: after.headline,
                  transitions,
                };
              }
            }
            const finalState = await detectStage();
            if (finalState.incorrect_code) {
              throw new Error("prepare-verification-code-rejected:" + String(finalState.current_url || page.url()));
            }
            throw new Error("prepare-verification-submit-stuck:" + String(finalState.stage || "unknown") + ":" + String(finalState.current_url || page.url()));
  - wait_after_prepare_login_verification_code_retry_submit:
      if: "${state.steps.submit_prepare_login_verification_code_after_incorrect_code?.action?.ok === true && state.steps.submit_prepare_login_verification_code_after_incorrect_code?.action?.skipped !== true}"
      metadata:
        description: "Allow the replacement prepare verification submission to settle before continuing."
      wait:
        milliseconds: 900
  - classify_prepare_after_login_verification_retry:
      if: "${state.steps.submit_prepare_login_verification_code_after_incorrect_code?.action?.ok === true && state.steps.submit_prepare_login_verification_code_after_incorrect_code?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Classify the prepare flow after retrying the verification OTP."
  - fill_prepare_signup_about_you:
      if: "${state.steps.classify_prepare_after_signup_password_gate?.action?.follow_up_step === true || state.steps.classify_prepare_after_login_verification_submit?.action?.follow_up_step === true || state.steps.classify_prepare_after_login_verification_retry?.action?.follow_up_step === true}"
      metadata:
        templateRef: fill_openai_about_you_form
        description: "Fill the OpenAI about-you form during the prepare branch with the proven shared helper."
        mode: headless_ok
        targetRef: primary
        browser:
          screenshot: true
  - submit_prepare_signup_about_you:
      if: "${state.steps.fill_prepare_signup_about_you?.action?.ok === true && state.steps.fill_prepare_signup_about_you?.action?.skipped !== true}"
      metadata:
        templateRef: submit_openai_about_you_form
        description: "Submit the OpenAI about-you form during the prepare signup branch."
        browser:
          screenshot: true
  - wait_after_prepare_signup_about_you_submit:
      if: "${state.steps.submit_prepare_signup_about_you?.action?.ok === true && state.steps.submit_prepare_signup_about_you?.action?.skipped !== true}"
      metadata:
        description: "Allow the prepare about-you submission to settle before classifying the next page."
      wait:
        milliseconds: 900
  - classify_prepare_after_signup_about_you:
      if: "${state.steps.submit_prepare_signup_about_you?.action?.ok === true && state.steps.submit_prepare_signup_about_you?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Classify the prepare flow after submitting the about-you form."
        browser:
          screenshot: true
  - compute_prepare_flow_result:
      metadata:
        description: "Compute the local device-auth prepare result before opening ChatGPT Security."
      run:
        script:
          language: javascript
          code: |
            if (state.vars?.reuse_device_auth_session === true) {
              return {
                prepare_flow_output: {
                  reused_device_auth_session: true,
                  next_action: "complete",
                  success: true,
                },
                prepare_flow_ready: true,
              };
            }
            const candidates = [
              state.steps?.classify_prepare_after_signup_about_you?.action,
              state.steps?.classify_prepare_after_login_verification_retry?.action,
              state.steps?.classify_prepare_after_login_verification_submit?.action,
              state.steps?.classify_prepare_after_return_recovery?.action,
              state.steps?.classify_prepare_login_after_return?.action,
              state.steps?.classify_prepare_login_verification_ready_after_incorrect_code?.action,
              state.steps?.classify_prepare_login_verification_ready?.action,
              state.steps?.classify_prepare_after_timeout_reopen?.action,
              state.steps?.classify_prepare_after_recovery_timeout_retry?.action,
              state.steps?.classify_prepare_after_recovery_choice?.action,
              state.steps?.classify_prepare_after_signup_password_gate?.action,
              state.steps?.classify_prepare_after_login_password_gate?.action,
              state.steps?.classify_prepare_after_login_email_retry?.action,
              state.steps?.classify_prepare_after_login_email?.action,
              state.steps?.classify_prepare_login_entry_after_auth_landing?.action,
              state.steps?.classify_prepare_login_entry?.action,
              state.steps?.inspect_prepare_public_chatgpt_entry_after_ui_logout?.action,
            ];
            const latest = candidates.find((candidate) => candidate && typeof candidate === "object" && candidate.skipped !== true) || {};
            const stage = String(latest.stage || "");
            const poisonedPendingFallback =
              state.steps?.classify_prepare_after_login_email_signup_invalid_state_fallback?.action || {};
            const poisonedPendingAccount =
              poisonedPendingFallback.retryable_timeout === true
              && /\/log-in(?:\/|$)/i.test(String(poisonedPendingFallback.current_url || ""));
            const publicPrepareState =
              latest.chatgpt_public_shell === true
              || stage === "public_chatgpt";
            const prepareStartedFromAuthenticatedShell = state.steps?.inspect_prepare_public_chatgpt_entry?.action?.stage === "app_shell";
            const executedPrepareInteraction = [
              state.steps?.open_prepare_direct_auth_entry_fallback?.action,
              state.steps?.click_prepare_signup_button?.action,
              state.steps?.click_prepare_login_button?.action,
              state.steps?.click_prepare_login_button_from_auth_landing?.action,
              state.steps?.fill_prepare_login_email?.action,
              state.steps?.submit_prepare_signup_email?.action,
              state.steps?.submit_prepare_login_email?.action,
              state.steps?.fill_prepare_signup_password?.action,
              state.steps?.submit_prepare_signup_password?.action,
              state.steps?.fill_prepare_login_password?.action,
              state.steps?.submit_prepare_login_password?.action,
              state.steps?.choose_prepare_one_time_code_recovery?.action,
              state.steps?.fill_prepare_login_verification_code?.action,
              state.steps?.fill_prepare_login_verification_code_after_incorrect_code?.action,
              state.steps?.fill_prepare_signup_about_you?.action,
              state.steps?.submit_prepare_signup_about_you?.action,
            ].some((action) => action && typeof action === "object" && action.skipped !== true);
            const staleAuthenticatedShellOnly =
              prepareStartedFromAuthenticatedShell
              && (stage === "app_shell" || latest.account_ready === true)
              && executedPrepareInteraction !== true;
            const authenticatedPrepareState =
              publicPrepareState !== true
              && (
                latest.account_ready === true
                || latest.chatgpt_app_shell === true
                || stage === "app_shell"
                || latest.add_phone_prompt === true
                || stage === "add_phone"
                || stage === "oauth_consent"
                || latest.oauth_continue_visible === true
                || latest.success === true
                || stage === "success"
                || stage === "callback_complete"
              );
            const prepareFlowReady =
              stage !== ""
              && staleAuthenticatedShellOnly !== true
              && authenticatedPrepareState === true;
            if (poisonedPendingAccount) {
              return {
                prepare_flow_output: {
                  ...(poisonedPendingFallback && typeof poisonedPendingFallback === "object" ? poisonedPendingFallback : {}),
                  next_action: "skip_account",
                  retry_reason: "prepare_invalid_state",
                  error_message: "OpenAI kept returning invalid_state while trying to reuse this pending account during device-auth preparation.",
                },
                prepare_flow_ready: false,
                stale_authenticated_shell_only: false,
              };
            }
            return {
              prepare_flow_output: latest,
              prepare_flow_ready: prepareFlowReady,
              stale_authenticated_shell_only: staleAuthenticatedShellOnly,
            };
  - prepare_with_original_flow_fallback:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.compute_prepare_flow_result?.action?.value?.prepare_flow_output?.next_action === 'skip_account' && state.steps.compute_prepare_flow_result?.action?.value?.prepare_flow_output?.retry_reason === 'prepare_invalid_state'}"
      metadata:
        description: Fall back to the proven original non-device auth workflow when repeated invalid_state responses poison the device-auth prepare branch.
      call: workflow.workspace.web.auth-openai-com.codex-rotate-account-flow
      with:
        version: 1.0.7
        input:
          enabled: true
          mode: codex_login
          codex_bin: "${inputs.codex_bin}"
          auth_url: "${inputs.auth_url}"
          callback_url: "${inputs.callback_url}"
          callback_port: "${inputs.callback_port}"
          codex_session_dir: "${inputs.codex_session_dir}"
          codex_login_pid: "${inputs.codex_login_pid}"
          codex_login_stdout_path: "${inputs.codex_login_stdout_path}"
          codex_login_stderr_path: "${inputs.codex_login_stderr_path}"
          codex_login_exit_path: "${inputs.codex_login_exit_path}"
          email: "${inputs.email}"
          account_login_locator: "${inputs.account_login_locator}"
          full_name: "${inputs.full_name}"
          birth_month: "${inputs.birth_month}"
          birth_day: "${inputs.birth_day}"
          birth_year: "${inputs.birth_year}"
          prefer_signup_recovery: "${inputs.prefer_signup_recovery}"
  - cache_prepare_flow_result:
      metadata:
        description: "Cache the local device-auth prepare result before opening ChatGPT Security."
      set:
        prepare_flow_output: "${state.steps.prepare_with_original_flow_fallback?.action?.result?.output || state.steps.compute_prepare_flow_result?.action?.value?.prepare_flow_output || {}}"
        prepare_flow_ready: "${(state.steps.prepare_with_original_flow_fallback?.action?.result?.output?.success === true || state.steps.prepare_with_original_flow_fallback?.action?.result?.output?.next_action === 'complete') || state.steps.compute_prepare_flow_result?.action?.value?.prepare_flow_ready === true}"
  - open_chatgpt_shell:
      if: "${false}"
      metadata:
        description: "Legacy no-op kept only for step-id stability; device-auth now opens settings directly after prepare succeeds."
      set:
        skipped: true
  - reopen_chatgpt_shell:
      if: "${false}"
      metadata:
        description: "Legacy no-op kept only for step-id stability; device-auth now opens settings directly after prepare succeeds."
      set:
        skipped: true
  - inspect_chatgpt_shell:
      if: "${false}"
      metadata:
        description: "Legacy no-op kept only for step-id stability; device-auth now verifies auth by opening settings directly."
        mode: headless_ok
        targetRef: primary
      set:
        skipped: true
  - open_authenticated_chatgpt_shell_before_security_settings:
      if: "${state.vars.reuse_device_auth_session !== true && state.vars.prepare_flow_ready === true}"
      metadata:
        description: Warm the authenticated ChatGPT app shell before opening the Security settings so the Security controls load in a logged-in state.
      call: afn.driver.browser.navigate
      with:
        body:
          url: https://chatgpt.com/?codex_rotate_device_auth_warmup=1
  - wait_for_authenticated_chatgpt_shell_before_security_settings:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.open_authenticated_chatgpt_shell_before_security_settings?.action?.ok === true && state.steps.open_authenticated_chatgpt_shell_before_security_settings?.action?.skipped !== true}"
      metadata:
        description: Require a genuinely logged-in ChatGPT shell before entering the Security settings.
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const snapshot = async () => {
              const title = await page.title().catch(() => "");
              return await page.evaluate((titleText) => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                const isVisible = (node) => {
                  if (!(node instanceof Element)) {
                    return false;
                  }
                  if (node.closest("script,style,noscript,template,[hidden],[aria-hidden='true']")) {
                    return false;
                  }
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                const labels = Array.from(document.querySelectorAll("button, a, [role='button'], [role='tab'], input[type='submit']"))
                  .filter((node) => isVisible(node))
                  .map((node) => {
                    if (node instanceof HTMLInputElement) {
                      return normalize(node.value || node.getAttribute("aria-label") || "");
                    }
                    return normalize(node.innerText || node.textContent || node.getAttribute("aria-label") || "");
                  })
                  .filter(Boolean)
                  .slice(0, 40);
                const bodyText = normalize(document.body ? (document.body.innerText || document.body.textContent || "") : "");
                const combinedText = normalize([titleText || document.title || "", bodyText, ...labels].join(" "));
                const appMarkers = /new chat|search chats|chat history|projects|what are you working on|upgrade plan|explore gpts|ask anything/i.test(combinedText);
                const settingsShell = /general|notifications|apps|data controls|security|account|appearance|language/i.test(combinedText);
                const securityControlVisible = labels.some((label) => /^security$/i.test(label));
                const authenticatedSecurityMarkers = /secure sign in with chatgpt|trusted devices|enable device code authorization for codex|log out of all devices|multi-factor authentication|security keys & passkeys/i.test(combinedText);
                const publicSignals = /log in to get smarter responses|get responses tailored to you|see plans and pricing|sign up for free|welcome to chatgpt|get started/i.test(combinedText);
                const loginVisible = labels.some((label) => /^(log in|sign in)$/i.test(label) || /log in/i.test(label));
                const signupVisible = labels.some((label) => /^sign up$/i.test(label) || /sign up for free|create account|get started/i.test(label));
                return {
                  current_url: location.href,
                  headline: bodyText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || normalize(titleText || document.title || "") || null,
                  app_markers: appMarkers,
                  settings_shell: settingsShell,
                  security_control_visible: securityControlVisible,
                  authenticated_security_markers: authenticatedSecurityMarkers,
                  public_shell: publicSignals || loginVisible || signupVisible,
                };
              }, title).catch(() => ({
                current_url: page.url(),
                headline: title || null,
                app_markers: false,
                settings_shell: false,
                security_control_visible: false,
                authenticated_security_markers: false,
                public_shell: false,
              }));
            };
            const deadline = Date.now() + 15000;
            let latest = await snapshot();
            while (Date.now() < deadline) {
              const authenticated =
                latest.public_shell !== true
                && (latest.app_markers === true
                  || latest.authenticated_security_markers === true
                  || (latest.settings_shell === true && latest.security_control_visible === true));
              if (authenticated) {
                return {
                  ok: true,
                  authenticated_chatgpt_shell: true,
                  ...latest,
                };
              }
              await page.waitForTimeout(750).catch(() => {});
              latest = await snapshot();
            }
            return {
              ok: false,
              authenticated_chatgpt_shell: false,
              ...latest,
              error: latest.public_shell === true
                ? "chatgpt-shell-not-authenticated"
                : "chatgpt-shell-did-not-become-authenticated",
            };
  - open_chatgpt_login_entry_before_security_settings:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.wait_for_authenticated_chatgpt_shell_before_security_settings?.action?.ok !== true}"
      metadata:
        description: Open the explicit ChatGPT auth-login entry when the warmup shell is still public so Security can be reached from a genuinely logged-in state.
      call: afn.driver.browser.navigate
      with:
        body:
          url: https://chatgpt.com/auth/login?next=%2F%23settings%2FSecurity
  - stabilize_chatgpt_login_surface_before_security_settings:
      if: "${state.steps.open_chatgpt_login_entry_before_security_settings?.action?.ok === true && state.steps.open_chatgpt_login_entry_before_security_settings?.action?.skipped !== true}"
      metadata:
        templateRef: wait_for_openai_surface_ready
        description: Wait for the explicit ChatGPT login surface to render before driving the login branch for Security settings.
  - classify_chatgpt_login_entry_before_security_settings:
      if: "${state.steps.stabilize_chatgpt_login_surface_before_security_settings?.action?.ok === true && state.steps.stabilize_chatgpt_login_surface_before_security_settings?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Classify the explicit ChatGPT login surface before continuing toward the authenticated Security settings.
  - click_chatgpt_login_button_before_security_settings_from_auth_landing:
      if: "${state.steps.classify_chatgpt_login_entry_before_security_settings?.action?.stage === 'public_chatgpt' && /chatgpt\\.com\\/auth\\/login/i.test(String(state.steps.classify_chatgpt_login_entry_before_security_settings?.action?.current_url || '')) && state.steps.classify_chatgpt_login_entry_before_security_settings?.action?.login_cta_visible === true}"
      metadata:
        templateRef: click_chatgpt_login_button
        description: Click Log in again on the ChatGPT auth landing shell so the Security-settings login branch reaches the actual email form.
  - wait_after_chatgpt_login_button_before_security_settings_from_auth_landing:
      if: "${state.steps.click_chatgpt_login_button_before_security_settings_from_auth_landing?.action?.ok === true && state.steps.click_chatgpt_login_button_before_security_settings_from_auth_landing?.action?.skipped !== true}"
      metadata:
        description: Let the explicit ChatGPT auth-landing login click settle before classifying the Security-settings login surface again.
      wait:
        milliseconds: 900
  - classify_chatgpt_login_entry_before_security_settings_after_auth_landing:
      if: "${state.steps.click_chatgpt_login_button_before_security_settings_from_auth_landing?.action?.ok === true && state.steps.click_chatgpt_login_button_before_security_settings_from_auth_landing?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Reclassify the explicit ChatGPT login surface after the auth landing's secondary login step.
  - cache_effective_chatgpt_login_entry_before_security_settings:
      if: "${state.steps.open_chatgpt_login_entry_before_security_settings?.action?.ok === true && state.steps.open_chatgpt_login_entry_before_security_settings?.action?.skipped !== true}"
      metadata:
        description: Cache the effective explicit ChatGPT login-entry state before the Security-settings login branch continues.
      set:
        pre_settings_login_entry: "${(state.steps.classify_chatgpt_login_entry_before_security_settings_after_auth_landing?.action?.stage || state.steps.classify_chatgpt_login_entry_before_security_settings_after_auth_landing?.action?.current_url) ? state.steps.classify_chatgpt_login_entry_before_security_settings_after_auth_landing?.action : (state.steps.classify_chatgpt_login_entry_before_security_settings?.action || {})}"
  - fill_chatgpt_login_email_before_security_settings:
      if: "${state.vars.reuse_device_auth_session !== true && (state.steps.cache_effective_chatgpt_login_entry_before_security_settings?.action?.value?.pre_settings_login_entry?.stage === 'login_email' || (state.steps.cache_effective_chatgpt_login_entry_before_security_settings?.action?.value?.pre_settings_login_entry?.stage === 'auth_prompt' && state.steps.cache_effective_chatgpt_login_entry_before_security_settings?.action?.value?.pre_settings_login_entry?.email_input === true))}"
      metadata:
        templateRef: fill_openai_email_field
        description: Fill the account email on the explicit ChatGPT login branch before opening Security settings.
  - submit_chatgpt_login_email_before_security_settings:
      if: "${state.steps.fill_chatgpt_login_email_before_security_settings?.action?.ok === true && state.steps.fill_chatgpt_login_email_before_security_settings?.action?.skipped !== true}"
      metadata:
        templateRef: submit_login_email_form
        description: Submit the explicit ChatGPT login email step before opening Security settings.
  - wait_after_chatgpt_login_email_before_security_settings:
      if: "${state.steps.submit_chatgpt_login_email_before_security_settings?.action?.ok === true && state.steps.submit_chatgpt_login_email_before_security_settings?.action?.skipped !== true}"
      metadata:
        description: Allow the explicit ChatGPT login email step to settle before switching to one-time-code recovery.
      wait:
        milliseconds: 900
  - classify_chatgpt_login_after_email_before_security_settings:
      if: "${state.steps.submit_chatgpt_login_email_before_security_settings?.action?.ok === true && state.steps.submit_chatgpt_login_email_before_security_settings?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Refresh the explicit ChatGPT login surface after the email step.
  - retry_chatgpt_login_email_submit_before_security_settings:
      if: "${state.steps.classify_chatgpt_login_after_email_before_security_settings?.action?.stage === 'login_email' || (state.steps.classify_chatgpt_login_after_email_before_security_settings?.action?.stage === 'auth_prompt' && state.steps.classify_chatgpt_login_after_email_before_security_settings?.action?.email_input === true)}"
      metadata:
        templateRef: click_primary_continue
        description: Retry the explicit ChatGPT login email submit when OpenAI leaves the email step unchanged.
  - wait_after_chatgpt_login_email_retry_before_security_settings:
      if: "${state.steps.retry_chatgpt_login_email_submit_before_security_settings?.action?.ok === true && state.steps.retry_chatgpt_login_email_submit_before_security_settings?.action?.skipped !== true}"
      metadata:
        description: Allow the retried explicit ChatGPT login email submit to settle before classifying the next page.
      wait:
        milliseconds: 900
  - classify_chatgpt_login_after_email_retry_before_security_settings:
      if: "${state.steps.retry_chatgpt_login_email_submit_before_security_settings?.action?.ok === true && state.steps.retry_chatgpt_login_email_submit_before_security_settings?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Refresh the explicit ChatGPT login surface after retrying the email submit.
  - choose_chatgpt_one_time_code_before_security_settings:
      if: "${state.steps.classify_chatgpt_login_after_email_retry_before_security_settings?.action?.stage === 'login_password' || (state.steps.classify_chatgpt_login_after_email_retry_before_security_settings?.action?.stage === 'auth_prompt' && state.steps.classify_chatgpt_login_after_email_retry_before_security_settings?.action?.password_input === true) || state.steps.classify_chatgpt_login_after_email_retry_before_security_settings?.action?.one_time_code_cta === true || state.steps.classify_chatgpt_login_after_email_before_security_settings?.action?.stage === 'login_password' || (state.steps.classify_chatgpt_login_after_email_before_security_settings?.action?.stage === 'auth_prompt' && state.steps.classify_chatgpt_login_after_email_before_security_settings?.action?.password_input === true) || state.steps.classify_chatgpt_login_after_email_before_security_settings?.action?.one_time_code_cta === true}"
      metadata:
        templateRef: click_one_time_code_button
        description: Switch the explicit ChatGPT login branch to one-time-code recovery before opening Security settings.
  - classify_chatgpt_login_after_recovery_choice_before_security_settings:
      if: "${state.steps.choose_chatgpt_one_time_code_before_security_settings?.action?.ok === true && state.steps.choose_chatgpt_one_time_code_before_security_settings?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Refresh the explicit ChatGPT login surface after selecting one-time-code recovery.
  - collect_chatgpt_login_verification_artifact_before_security_settings:
      if: "${state.steps.classify_chatgpt_login_after_email_before_security_settings?.action?.stage === 'email_verification' || state.steps.classify_chatgpt_login_after_email_retry_before_security_settings?.action?.stage === 'email_verification' || state.steps.classify_chatgpt_login_after_recovery_choice_before_security_settings?.action?.stage === 'email_verification'}"
      metadata:
        description: Collect the explicit ChatGPT login OTP from Gmail on the same managed profile before opening Security settings.
        ref:
          digest: sha256:01e032fddc23788226829689d0b2161ad4b9acb108dac83395bfbdaef5f8974d
      call: workflow.sys.web.mail-google-com.collect-verification-artifact
      with:
        version: 1.0.0
        input:
          enabled: true
          search_query: "${'from:openai to:' + String(inputs.email || '').trim() + ' newer_than:7d'}"
          message_match_text: "${String(inputs.email || '').trim() || 'OpenAI'}"
          link_host_regex: auth\.openai\.com|chatgpt\.com
  - cache_chatgpt_login_verification_artifact_before_security_settings:
      if: "${state.steps.collect_chatgpt_login_verification_artifact_before_security_settings?.action?.result != null}"
      metadata:
        description: Cache the collected explicit ChatGPT login verification artifact into workflow state.
      set:
        pre_settings_login_verification_code: "${(String(state.steps.collect_chatgpt_login_verification_artifact_before_security_settings?.action?.result?.result?.output?.code || state.steps.collect_chatgpt_login_verification_artifact_before_security_settings?.action?.result?.output?.code || state.steps.collect_chatgpt_login_verification_artifact_before_security_settings?.action?.result?.result?.output?.verification_code || state.steps.collect_chatgpt_login_verification_artifact_before_security_settings?.action?.result?.output?.verification_code || state.steps.collect_chatgpt_login_verification_artifact_before_security_settings?.action?.result?.result?.output?.otp || state.steps.collect_chatgpt_login_verification_artifact_before_security_settings?.action?.result?.output?.otp || state.steps.collect_chatgpt_login_verification_artifact_before_security_settings?.action?.result?.result?.state?.steps?.collect_verification_artifact?.action?.code || state.steps.collect_chatgpt_login_verification_artifact_before_security_settings?.action?.result?.state?.steps?.collect_verification_artifact?.action?.code || '')).match(/(?:^|\\D)(\\d{6})(?:\\D|$)/)?.[1] || ''}"
  - wait_for_chatgpt_login_verification_email_delivery_before_security_settings:
      if: "${state.steps.collect_chatgpt_login_verification_artifact_before_security_settings?.action?.ok === true && state.steps.collect_chatgpt_login_verification_artifact_before_security_settings?.action?.skipped !== true && String(state.vars.pre_settings_login_verification_code || '').length < 6}"
      metadata:
        description: Give Gmail one more chance to finish loading the explicit ChatGPT login OTP before leaving the mailbox.
      wait:
        milliseconds: 4000
  - recollect_chatgpt_login_verification_artifact_before_security_settings:
      if: "${state.steps.wait_for_chatgpt_login_verification_email_delivery_before_security_settings?.action?.ok === true && state.steps.wait_for_chatgpt_login_verification_email_delivery_before_security_settings?.action?.skipped !== true && String(state.vars.pre_settings_login_verification_code || '').length < 6}"
      metadata:
        description: Re-collect the explicit ChatGPT login OTP once when Gmail has not surfaced a usable code yet.
        ref:
          digest: sha256:01e032fddc23788226829689d0b2161ad4b9acb108dac83395bfbdaef5f8974d
      call: workflow.sys.web.mail-google-com.collect-verification-artifact
      with:
        version: 1.0.0
        input:
          enabled: true
          search_query: "${'from:openai to:' + String(inputs.email || '').trim() + ' newer_than:7d'}"
          message_match_text: "${String(inputs.email || '').trim() || 'OpenAI'}"
          link_host_regex: auth\.openai\.com|chatgpt\.com
  - cache_recollected_chatgpt_login_verification_artifact_before_security_settings:
      if: "${state.steps.recollect_chatgpt_login_verification_artifact_before_security_settings?.action?.result != null}"
      metadata:
        description: Replace the explicit ChatGPT login verification artifact after waiting for Gmail to finish loading.
      set:
        pre_settings_login_verification_code: "${(String(state.steps.recollect_chatgpt_login_verification_artifact_before_security_settings?.action?.result?.result?.output?.code || state.steps.recollect_chatgpt_login_verification_artifact_before_security_settings?.action?.result?.output?.code || state.steps.recollect_chatgpt_login_verification_artifact_before_security_settings?.action?.result?.result?.output?.verification_code || state.steps.recollect_chatgpt_login_verification_artifact_before_security_settings?.action?.result?.output?.verification_code || state.steps.recollect_chatgpt_login_verification_artifact_before_security_settings?.action?.result?.result?.output?.otp || state.steps.recollect_chatgpt_login_verification_artifact_before_security_settings?.action?.result?.output?.otp || state.steps.recollect_chatgpt_login_verification_artifact_before_security_settings?.action?.result?.result?.state?.steps?.collect_verification_artifact?.action?.code || state.steps.recollect_chatgpt_login_verification_artifact_before_security_settings?.action?.result?.state?.steps?.collect_verification_artifact?.action?.code || state.vars.pre_settings_login_verification_code || '')).match(/(?:^|\\D)(\\d{6})(?:\\D|$)/)?.[1] || ''}"
  - return_to_chatgpt_login_verification_page_before_security_settings:
      if: "${(state.steps.collect_chatgpt_login_verification_artifact_before_security_settings?.action?.ok === true && state.steps.collect_chatgpt_login_verification_artifact_before_security_settings?.action?.skipped !== true) || (state.steps.recollect_chatgpt_login_verification_artifact_before_security_settings?.action?.ok === true && state.steps.recollect_chatgpt_login_verification_artifact_before_security_settings?.action?.skipped !== true)}"
      metadata:
        description: Return from Gmail to the active OpenAI email-verification page on the explicit ChatGPT login branch.
      call: afn.driver.browser.navigate
      with:
        body:
          url: "${state.steps.classify_chatgpt_login_after_recovery_choice_before_security_settings?.action?.current_url || state.steps.classify_chatgpt_login_after_email_retry_before_security_settings?.action?.current_url || state.steps.classify_chatgpt_login_after_email_before_security_settings?.action?.current_url || 'https://auth.openai.com/email-verification'}"
  - stabilize_chatgpt_login_verification_surface_before_security_settings:
      if: "${state.steps.return_to_chatgpt_login_verification_page_before_security_settings?.action?.ok === true && state.steps.return_to_chatgpt_login_verification_page_before_security_settings?.action?.skipped !== true}"
      metadata:
        templateRef: wait_for_openai_surface_ready
        description: Wait for the explicit ChatGPT login verification page to render after returning from Gmail.
  - classify_chatgpt_login_verification_ready_before_security_settings:
      if: "${state.steps.return_to_chatgpt_login_verification_page_before_security_settings?.action?.ok === true && state.steps.return_to_chatgpt_login_verification_page_before_security_settings?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Confirm the OpenAI email-verification surface after returning from Gmail on the explicit ChatGPT login branch.
  - reopen_chatgpt_one_time_code_after_return_before_security_settings:
      if: "${state.steps.classify_chatgpt_login_verification_ready_before_security_settings?.action?.stage === 'login_password' || (state.steps.classify_chatgpt_login_verification_ready_before_security_settings?.action?.stage === 'auth_prompt' && state.steps.classify_chatgpt_login_verification_ready_before_security_settings?.action?.password_input === true)}"
      metadata:
        templateRef: click_one_time_code_button
        description: Reopen one-time-code recovery when the Gmail return path lands back on the password prompt before opening Security settings.
  - classify_chatgpt_after_return_recovery_before_security_settings:
      if: "${state.steps.reopen_chatgpt_one_time_code_after_return_before_security_settings?.action?.ok === true && state.steps.reopen_chatgpt_one_time_code_after_return_before_security_settings?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Refresh the explicit ChatGPT login surface after rebuilding one-time-code recovery from the Gmail return path.
  - fill_chatgpt_login_verification_code_before_security_settings:
      if: "${state.steps.classify_chatgpt_login_verification_ready_before_security_settings?.action?.stage === 'email_verification' || state.steps.classify_chatgpt_after_return_recovery_before_security_settings?.action?.stage === 'email_verification' || state.steps.classify_chatgpt_login_after_recovery_choice_before_security_settings?.action?.stage === 'email_verification' || state.steps.classify_chatgpt_login_after_email_retry_before_security_settings?.action?.stage === 'email_verification' || state.steps.classify_chatgpt_login_after_email_before_security_settings?.action?.stage === 'email_verification'}"
      metadata:
        description: Fill the Gmail OTP into the OpenAI email-verification form before opening Security settings.
      call: afn.driver.browser.type
      with:
        body:
          selector: >-
            input[name*="code" i], input[autocomplete="one-time-code"], input[aria-label*="code" i], input[placeholder*="code" i], input[inputmode="numeric"]
          text: "${String(state.vars.pre_settings_login_verification_code || '').replace(/\\D+/g, '').slice(0, 6)}"
          dispatch_events_after_fill: true
          blur_after_fill: true
          settle_ms: 250
  - submit_chatgpt_login_verification_code_before_security_settings:
      if: "${state.steps.fill_chatgpt_login_verification_code_before_security_settings?.action?.ok === true && state.steps.fill_chatgpt_login_verification_code_before_security_settings?.action?.skipped !== true}"
      metadata:
        templateRef: click_primary_continue
        description: Submit the OpenAI email-verification OTP before opening Security settings.
  - wait_after_chatgpt_login_verification_submit_before_security_settings:
      if: "${state.steps.submit_chatgpt_login_verification_code_before_security_settings?.action?.ok === true && state.steps.submit_chatgpt_login_verification_code_before_security_settings?.action?.skipped !== true}"
      metadata:
        description: Allow the explicit ChatGPT login verification submission to settle before classifying the next page.
      wait:
        milliseconds: 900
  - classify_chatgpt_after_login_verification_submit_before_security_settings:
      if: "${state.steps.submit_chatgpt_login_verification_code_before_security_settings?.action?.ok === true && state.steps.submit_chatgpt_login_verification_code_before_security_settings?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Classify the explicit ChatGPT login flow after submitting the verification OTP.
  - wait_on_chatgpt_add_phone_before_security_settings_short:
      if: "${state.steps.classify_chatgpt_after_login_verification_submit_before_security_settings?.action?.stage === 'add_phone'}"
      metadata:
        description: Wait briefly when ChatGPT shows the transient add-phone blocker before Security settings.
      wait:
        milliseconds: 5000
  - classify_chatgpt_after_add_phone_wait_before_security_settings_short:
      if: "${state.steps.wait_on_chatgpt_add_phone_before_security_settings_short?.action?.ok === true && state.steps.wait_on_chatgpt_add_phone_before_security_settings_short?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Reclassify the explicit ChatGPT login surface after the short add-phone wait.
  - wait_on_chatgpt_add_phone_before_security_settings_medium:
      if: "${state.steps.classify_chatgpt_after_add_phone_wait_before_security_settings_short?.action?.stage === 'add_phone'}"
      metadata:
        description: Wait longer when the transient add-phone blocker still persists before Security settings.
      wait:
        milliseconds: 10000
  - classify_chatgpt_after_add_phone_wait_before_security_settings_medium:
      if: "${state.steps.wait_on_chatgpt_add_phone_before_security_settings_medium?.action?.ok === true && state.steps.wait_on_chatgpt_add_phone_before_security_settings_medium?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Reclassify the explicit ChatGPT login surface after the medium add-phone wait.
  - wait_on_chatgpt_add_phone_before_security_settings_long:
      if: "${state.steps.classify_chatgpt_after_add_phone_wait_before_security_settings_medium?.action?.stage === 'add_phone'}"
      metadata:
        description: Give the transient add-phone blocker one final long wait before declaring the pre-settings login branch blocked.
      wait:
        milliseconds: 20000
  - classify_chatgpt_after_add_phone_wait_before_security_settings_long:
      if: "${state.steps.wait_on_chatgpt_add_phone_before_security_settings_long?.action?.ok === true && state.steps.wait_on_chatgpt_add_phone_before_security_settings_long?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Reclassify the explicit ChatGPT login surface after the final add-phone wait.
  - cache_effective_authenticated_chatgpt_shell_before_security_settings:
      if: "${state.vars.reuse_device_auth_session !== true}"
      metadata:
        description: Cache the effective authenticated ChatGPT shell state before opening Security settings.
      run:
        script:
          language: javascript
          code: |
            const candidates = [
              state.steps?.wait_for_authenticated_chatgpt_shell_before_security_settings?.action?.ok === true
                ? {
                    ready: state.steps.wait_for_authenticated_chatgpt_shell_before_security_settings.action.authenticated_chatgpt_shell === true,
                    current_url: state.steps.wait_for_authenticated_chatgpt_shell_before_security_settings.action.current_url || null,
                    headline: state.steps.wait_for_authenticated_chatgpt_shell_before_security_settings.action.headline || null,
                    stage: "app_shell",
                  }
                : null,
              state.steps?.classify_chatgpt_after_add_phone_wait_before_security_settings_long?.action,
              state.steps?.classify_chatgpt_after_add_phone_wait_before_security_settings_medium?.action,
              state.steps?.classify_chatgpt_after_add_phone_wait_before_security_settings_short?.action,
              state.steps?.classify_chatgpt_after_login_verification_submit_before_security_settings?.action,
              state.steps?.classify_chatgpt_after_return_recovery_before_security_settings?.action,
              state.steps?.classify_chatgpt_login_verification_ready_before_security_settings?.action,
              state.steps?.classify_chatgpt_login_after_recovery_choice_before_security_settings?.action,
              state.steps?.classify_chatgpt_login_after_email_retry_before_security_settings?.action,
              state.steps?.classify_chatgpt_login_after_email_before_security_settings?.action,
            ].filter(Boolean);
            const effective = candidates.find((candidate) => {
              return candidate
                && typeof candidate === "object"
                && candidate.skipped !== true
                && (candidate.ready === true || candidate.stage === "app_shell");
            }) || {};
            return {
              authenticated_chatgpt_shell_before_security_settings: {
                ready: effective.ready === true || effective.stage === "app_shell",
                current_url: effective.current_url || null,
                headline: effective.headline || null,
                stage: effective.stage || null,
              },
            };
  - open_chatgpt_security_settings:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.cache_effective_authenticated_chatgpt_shell_before_security_settings?.action?.value?.authenticated_chatgpt_shell_before_security_settings?.ready === true}"
      metadata:
        description: "Open the authenticated ChatGPT Security settings directly after prepare succeeds."
      call: afn.driver.browser.navigate
      with:
        body:
          url: "https://chatgpt.com/#settings/Security"
  - wait_for_chatgpt_settings_shell:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.open_chatgpt_security_settings?.action?.ok === true && state.steps.open_chatgpt_security_settings?.action?.skipped !== true}"
      metadata:
        description: "Wait for the ChatGPT settings shell to actually render before clicking Security."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const inspectShell = async () => {
              const title = await page.title().catch(() => "");
              return await page.evaluate((titleText) => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                const isVisible = (node) => {
                  if (!(node instanceof Element)) {
                    return false;
                  }
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                const dialog = Array.from(document.querySelectorAll("[role='dialog']")).find((node) => isVisible(node));
                const root = dialog || document.body;
                const labels = Array.from((root || document.body).querySelectorAll("button, a, [role='button'], [role='tab'], input[type='submit']"))
                  .filter((node) => isVisible(node))
                  .map((node) => {
                    if (node instanceof HTMLInputElement) {
                      return normalize(node.value || node.getAttribute("aria-label") || "");
                    }
                    return normalize(node.innerText || node.textContent || node.getAttribute("aria-label") || "");
                  })
                  .filter(Boolean)
                  .slice(0, 40);
                const text = normalize(root ? (root.innerText || root.textContent || "") : "");
                const combinedText = normalize([titleText || document.title || "", text, ...labels].join(" "));
                const authenticatedSecurityMarkers = /secure sign in with chatgpt|trusted devices|enable device code authorization for codex|log out of all devices|multi-factor authentication|security keys & passkeys/i.test(combinedText);
                const securityControlVisible = labels.some((label) => /^security$/i.test(label));
                const publicSignals = /log in to get smarter responses|get responses tailored to you|see plans and pricing|sign up for free|welcome to chatgpt|get started/i.test(combinedText);
                const loginVisible = labels.some((label) => /^(log in|sign in)$/i.test(label) || /log in/i.test(label));
                const signupVisible = labels.some((label) => /^sign up$/i.test(label) || /sign up for free|create account|get started/i.test(label));
                return {
                  current_url: location.href,
                  headline: text.split("\n").map((value) => value.trim()).filter(Boolean)[0] || normalize(titleText || document.title || "") || null,
                  dialog_visible: Boolean(dialog),
                  settings_shell: /general|notifications|apps|data controls|security|account|appearance|language/i.test(combinedText),
                  security_control_visible: securityControlVisible,
                  authenticated_security_markers: authenticatedSecurityMarkers,
                  public_shell: publicSignals || loginVisible || signupVisible,
                };
              }, title).catch(() => ({
                current_url: page.url(),
                headline: title || null,
                dialog_visible: false,
                settings_shell: false,
                security_control_visible: false,
                authenticated_security_markers: false,
                public_shell: false,
              }));
            };
            const deadline = Date.now() + 12000;
            let latest = await inspectShell();
            while (Date.now() < deadline) {
              const ok = latest.settings_shell === true
                && latest.public_shell !== true
                && (latest.authenticated_security_markers === true || latest.security_control_visible === true);
              if (ok) {
                return {
                  ok: true,
                  logged_in_settings_shell: true,
                  ...latest,
                };
              }
              await page.waitForTimeout(750).catch(() => {});
              latest = await inspectShell();
            }
            return {
              ok: false,
              logged_in_settings_shell: false,
              ...latest,
              error: latest.public_shell === true
                ? "chatgpt-settings-shell-not-authenticated"
                : "chatgpt-security-controls-not-visible",
            };
  - activate_chatgpt_security_settings_tab:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.wait_for_chatgpt_settings_shell?.action?.ok === true && state.steps.wait_for_chatgpt_settings_shell?.action?.skipped !== true}"
      metadata:
        description: "Open the Security section inside ChatGPT settings before enabling the Codex device-authorization toggle."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const inspectPanel = async () => {
              const title = await page.title().catch(() => "");
              const panel = await page.evaluate(() => {
                const isVisible = (node) => {
                  if (!(node instanceof Element)) {
                    return false;
                  }
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                const dialog = Array.from(document.querySelectorAll("[role='dialog']")).find((node) => isVisible(node));
                const root = dialog || document.body;
                return {
                  dialog_visible: Boolean(dialog),
                  text: String(root ? (root.innerText || root.textContent || "") : "").replace(/\s+/g, " ").trim(),
                };
              }).catch(() => ({ dialog_visible: false, text: "" }));
              const normalizedText = String(panel.text || "").replace(/\s+/g, " ").trim();
              return {
                current_url: page.url(),
                headline: normalizedText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
                dialog_visible: panel.dialog_visible === true,
                settings_shell: /general|notifications|apps|data controls|security|account|appearance/i.test(normalizedText),
                logged_in_settings_shell: /secure sign in with chatgpt|trusted devices|enable device code authorization for codex|log out of all devices|multi-factor authentication|security keys & passkeys/i.test(normalizedText) || /new chat|search chats|projects|chat history/i.test(normalizedText),
                security_open: /secure sign in with chatgpt|trusted devices|enable device code authorization for codex|log out of all devices/i.test(normalizedText),
              };
            };
            const clickSecurity = async () => {
              const deadline = Date.now() + 10000;
              while (Date.now() < deadline) {
                const panelBefore = await inspectPanel();
                if (panelBefore.logged_in_settings_shell !== true && panelBefore.security_open !== true) {
                  await page.waitForTimeout(500).catch(() => {});
                  continue;
                }
                if (panelBefore.security_open === true) {
                  return { ok: true, clicked: false, panel: panelBefore };
                }
                const candidates = [
                  page.getByRole("tab", { name: /^Security$/i }).first(),
                  page.getByRole("button", { name: /^Security$/i }).first(),
                  page.getByRole("link", { name: /^Security$/i }).first(),
                  page.locator("button,a,[role='tab'],[role='button']").filter({ hasText: /Security/i }).first(),
                  page.getByText(/^Security$/i).first(),
                ];
                for (const candidate of candidates) {
                  if ((await candidate.count().catch(() => 0)) === 0) {
                    continue;
                  }
                  if (!(await candidate.isVisible().catch(() => false))) {
                    continue;
                  }
                  await candidate.scrollIntoViewIfNeeded().catch(() => {});
                  await candidate.click().catch(() => {});
                  await page.waitForTimeout(900).catch(() => {});
                  const panelAfter = await inspectPanel();
                  if (panelAfter.security_open === true) {
                    return { ok: true, clicked: true, panel: panelAfter };
                  }
                }
                await page.waitForTimeout(500).catch(() => {});
              }
              return { ok: false, clicked: false, panel: await inspectPanel() };
            };
            const result = await clickSecurity();
            const panel = result.panel || await inspectPanel();
            return {
              ok: panel.security_open === true,
              clicked: result.clicked === true,
              ...panel,
            };
  - ensure_device_code_authorization_enabled:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.activate_chatgpt_security_settings_tab?.action?.ok === true && state.steps.activate_chatgpt_security_settings_tab?.action?.skipped !== true}"
      metadata:
        templateRef: ensure_codex_device_auth_enabled
        description: "Enable the ChatGPT security toggle that allows Codex device-code authorization."
  - wait_after_device_code_toggle_enable:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.ensure_device_code_authorization_enabled?.action?.ok === true && state.steps.ensure_device_code_authorization_enabled?.action?.changed === true}"
      metadata:
        description: "Give ChatGPT a moment to persist the Codex device-code authorization toggle before reloading settings."
      wait:
        milliseconds: 6000
  - reopen_chatgpt_security_settings_after_toggle:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.ensure_device_code_authorization_enabled?.action?.ok === true && state.steps.ensure_device_code_authorization_enabled?.action?.skipped !== true}"
      metadata:
        description: "Reopen the ChatGPT Security settings after the initial toggle action so the device-auth enablement is re-read from a fresh settings dialog."
        browser:
          screenshot: true
      call: afn.driver.browser.navigate
      with:
        body:
          url: "https://chatgpt.com/#settings/Security"
  - wait_for_chatgpt_settings_shell_after_toggle:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.reopen_chatgpt_security_settings_after_toggle?.action?.ok === true && state.steps.reopen_chatgpt_security_settings_after_toggle?.action?.skipped !== true}"
      metadata:
        description: "Wait for the reopened ChatGPT settings dialog before re-checking the Codex device-auth toggle."
        mode: headless_ok
        targetRef: primary
        browser:
          screenshot: true
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const inspectShell = async () => {
              const title = await page.title().catch(() => "");
              return await page.evaluate((titleText) => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                const isVisible = (node) => {
                  if (!(node instanceof Element)) {
                    return false;
                  }
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                const dialog = Array.from(document.querySelectorAll("[role='dialog']")).find((node) => isVisible(node));
                const root = dialog || document.body;
                const labels = Array.from((root || document.body).querySelectorAll("button, a, [role='button'], [role='tab'], input[type='submit']"))
                  .filter((node) => isVisible(node))
                  .map((node) => {
                    if (node instanceof HTMLInputElement) {
                      return normalize(node.value || node.getAttribute("aria-label") || "");
                    }
                    return normalize(node.innerText || node.textContent || node.getAttribute("aria-label") || "");
                  })
                  .filter(Boolean)
                  .slice(0, 40);
                const text = normalize(root ? (root.innerText || root.textContent || "") : "");
                const combinedText = normalize([titleText || document.title || "", text, ...labels].join(" "));
                const authenticatedSecurityMarkers = /secure sign in with chatgpt|trusted devices|enable device code authorization for codex|log out of all devices|multi-factor authentication|security keys & passkeys/i.test(combinedText);
                const securityControlVisible = labels.some((label) => /^security$/i.test(label));
                const publicSignals = /log in to get smarter responses|get responses tailored to you|see plans and pricing|sign up for free|welcome to chatgpt|get started/i.test(combinedText);
                const loginVisible = labels.some((label) => /^(log in|sign in)$/i.test(label) || /log in/i.test(label));
                const signupVisible = labels.some((label) => /^sign up$/i.test(label) || /sign up for free|create account|get started/i.test(label));
                return {
                  current_url: location.href,
                  headline: text.split("\n").map((value) => value.trim()).filter(Boolean)[0] || normalize(titleText || document.title || "") || null,
                  dialog_visible: Boolean(dialog),
                  settings_shell: /general|notifications|apps|data controls|security|account|appearance|language/i.test(combinedText),
                  security_control_visible: securityControlVisible,
                  authenticated_security_markers: authenticatedSecurityMarkers,
                  public_shell: publicSignals || loginVisible || signupVisible,
                };
              }, title).catch(() => ({
                current_url: page.url(),
                headline: title || null,
                dialog_visible: false,
                settings_shell: false,
                security_control_visible: false,
                authenticated_security_markers: false,
                public_shell: false,
              }));
            };
            const deadline = Date.now() + 12000;
            let latest = await inspectShell();
            while (Date.now() < deadline) {
              const ok = latest.settings_shell === true
                && latest.public_shell !== true
                && (latest.authenticated_security_markers === true || latest.security_control_visible === true);
              if (ok) {
                return {
                  ok: true,
                  logged_in_settings_shell: true,
                  ...latest,
                };
              }
              await page.waitForTimeout(750).catch(() => {});
              latest = await inspectShell();
            }
            return {
              ok: false,
              logged_in_settings_shell: false,
              ...latest,
              error: latest.public_shell === true
                ? "chatgpt-settings-shell-not-authenticated"
                : "chatgpt-security-controls-not-visible",
            };
  - reactivate_chatgpt_security_settings_tab_after_toggle:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.wait_for_chatgpt_settings_shell_after_toggle?.action?.ok === true && state.steps.wait_for_chatgpt_settings_shell_after_toggle?.action?.skipped !== true}"
      metadata:
        description: "Re-open the Security tab inside the refreshed ChatGPT settings dialog before reconfirming the Codex device-auth toggle."
        mode: headless_ok
        targetRef: primary
        browser:
          screenshot: true
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const inspectPanel = async () => {
              const title = await page.title().catch(() => "");
              const panel = await page.evaluate(() => {
                const isVisible = (node) => {
                  if (!(node instanceof Element)) {
                    return false;
                  }
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                const dialog = Array.from(document.querySelectorAll("[role='dialog']")).find((node) => isVisible(node));
                const root = dialog || document.body;
                return {
                  dialog_visible: Boolean(dialog),
                  text: String(root ? (root.innerText || root.textContent || "") : "").replace(/\s+/g, " ").trim(),
                };
              }).catch(() => ({ dialog_visible: false, text: "" }));
              const normalizedText = String(panel.text || "").replace(/\s+/g, " ").trim();
              return {
                current_url: page.url(),
                headline: normalizedText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
                dialog_visible: panel.dialog_visible === true,
                settings_shell: /general|notifications|apps|data controls|security|account|appearance/i.test(normalizedText),
                logged_in_settings_shell: /secure sign in with chatgpt|trusted devices|enable device code authorization for codex|log out of all devices|multi-factor authentication|security keys & passkeys/i.test(normalizedText) || /new chat|search chats|projects|chat history/i.test(normalizedText),
                security_open: /secure sign in with chatgpt|trusted devices|enable device code authorization for codex|log out of all devices/i.test(normalizedText),
              };
            };
            const clickSecurity = async () => {
              const deadline = Date.now() + 10000;
              while (Date.now() < deadline) {
                const panelBefore = await inspectPanel();
                if (panelBefore.logged_in_settings_shell !== true && panelBefore.security_open !== true) {
                  await page.waitForTimeout(500).catch(() => {});
                  continue;
                }
                if (panelBefore.security_open === true) {
                  return { ok: true, clicked: false, panel: panelBefore };
                }
                const candidates = [
                  page.getByRole("tab", { name: /^Security$/i }).first(),
                  page.getByRole("button", { name: /^Security$/i }).first(),
                  page.getByRole("link", { name: /^Security$/i }).first(),
                  page.locator("button,a,[role='tab'],[role='button']").filter({ hasText: /Security/i }).first(),
                  page.getByText(/^Security$/i).first(),
                ];
                for (const candidate of candidates) {
                  if ((await candidate.count().catch(() => 0)) === 0) {
                    continue;
                  }
                  if (!(await candidate.isVisible().catch(() => false))) {
                    continue;
                  }
                  await candidate.scrollIntoViewIfNeeded().catch(() => {});
                  await candidate.click().catch(() => {});
                  await page.waitForTimeout(900).catch(() => {});
                  const panelAfter = await inspectPanel();
                  if (panelAfter.security_open === true) {
                    return { ok: true, clicked: true, panel: panelAfter };
                  }
                }
                await page.waitForTimeout(500).catch(() => {});
              }
              return { ok: false, clicked: false, panel: await inspectPanel() };
            };
            const result = await clickSecurity();
            const panel = result.panel || await inspectPanel();
            return {
              ok: panel.security_open === true,
              clicked: result.clicked === true,
              ...panel,
            };
  - reconfirm_device_code_authorization_enabled:
      if: "${state.vars.reuse_device_auth_session !== true && state.steps.reactivate_chatgpt_security_settings_tab_after_toggle?.action?.ok === true && state.steps.reactivate_chatgpt_security_settings_tab_after_toggle?.action?.skipped !== true}"
      metadata:
        templateRef: ensure_codex_device_auth_enabled
        description: "Reconfirm that the ChatGPT Security toggle for Codex device-code authorization remains enabled before starting device auth."
  - wait_after_device_code_toggle_reconfirm:
      if: "${state.vars.reuse_device_auth_session !== true && (state.steps.reconfirm_device_code_authorization_enabled?.action?.enabled === true || state.steps.ensure_device_code_authorization_enabled?.action?.enabled === true)}"
      metadata:
        description: "Allow the confirmed Codex device-code authorization setting to propagate before starting the detached Codex device-auth session."
      wait:
        milliseconds: 10000
  - seed_existing_codex_login_session:
      if: "${state.vars.prepare_flow_ready === true && state.vars.reuse_device_auth_session !== true && (String(inputs.auth_url || '').trim() !== '' || String(inputs.device_code || '').trim() !== '' || String(inputs.codex_login_stdout_path || '').trim() !== '' || String(inputs.codex_login_stderr_path || '').trim() !== '' || String(inputs.codex_session_dir || '').trim() !== '')}"
      metadata:
        description: Normalize any caller-provided detached Codex device-auth session before deciding whether a fresh one must be started.
      run:
        script:
          language: javascript
          code: |
            const fs = await import("node:fs");
            const stripAnsi = (raw) => String(raw || "").replace(/\u001b\[[0-9;]*m/g, "");
            const readText = (filePath) =>
              (!filePath || !fs.existsSync(filePath)) ? "" : fs.readFileSync(filePath, "utf8");
            const extractDeviceChallenge = (raw) => {
              const cleaned = stripAnsi(raw);
              const authMatch = cleaned.match(/https:\/\/auth\.openai\.com\/codex\/device\b[^\s"'<>]*/);
              const codeMatch = cleaned.match(/\b[A-Z0-9]{4}-[A-Z0-9]{5}\b/);
              return {
                auth_url: authMatch ? authMatch[0] : null,
                device_code: codeMatch ? codeMatch[0] : null,
              };
            };
            const explicitAuthUrl = String(args.auth_url || "").trim();
            const explicitDeviceCode = String(args.device_code || "").trim().toUpperCase();
            const stdoutPath = String(args.codex_login_stdout_path || "").trim();
            const stderrPath = String(args.codex_login_stderr_path || "").trim();
            const exitPath = String(args.codex_login_exit_path || "").trim();
            const sessionDir = String(args.codex_session_dir || "").trim() || null;
            const codexHomePath = String(args.codex_home_path || "").trim() || null;
            const authFilePath = String(args.codex_auth_file_path || "").trim() || null;
            const pidValue = Number(args.codex_login_pid || 0);
            const recovered = extractDeviceChallenge(
              readText(stdoutPath) + "\n" + readText(stderrPath)
            );
            const authUrl = explicitAuthUrl || recovered.auth_url || null;
            const deviceCode = explicitDeviceCode || recovered.device_code || null;
            return {
              ok: true,
              reusable_challenge: Boolean(authUrl && deviceCode),
              auth_url: authUrl,
              device_code: deviceCode,
              session_dir: sessionDir,
              codex_home_path: codexHomePath,
              auth_file_path: authFilePath,
              pid: Number.isInteger(pidValue) && pidValue > 0 ? pidValue : null,
              stdout_path: stdoutPath || null,
              stderr_path: stderrPath || null,
              exit_path: exitPath || null,
            };
  - start_codex_login_session:
      if: "${state.vars.prepare_flow_ready === true && state.vars.reuse_device_auth_session !== true && state.steps.seed_existing_codex_login_session?.action?.value?.reusable_challenge !== true}"
      metadata:
        description: "Start a detached codex device-auth session on the host and capture its stdout/stderr paths."
      run:
        script:
          language: javascript
          code: |
            const fs = await import("node:fs");
            const path = await import("node:path");
            const { spawn } = await import("node:child_process");
            const shell = process.env.SHELL || "/bin/sh";
            const codexBin = String(args.codex_bin || "codex").trim() || "codex";
            const sessionRoot = path.join(env.projectArtifactsRoot, "codex-login-sessions");
            fs.mkdirSync(sessionRoot, { recursive: true });
            const sessionDir = fs.mkdtempSync(path.join(sessionRoot, String(env.workflowRunStamp || "run") + "-"));
            const codexHomePath = path.join(sessionDir, "codex-home");
            fs.mkdirSync(codexHomePath, { recursive: true });
            const stdoutPath = path.join(sessionDir, "stdout.log");
            const stderrPath = path.join(sessionDir, "stderr.log");
            const exitPath = path.join(sessionDir, "exit.code");
            const authPath = path.join(codexHomePath, "auth.json");
            const quote = (value) => "'" + String(value).replace(/'/g, "'\"'\"'") + "'";
            const command = quote(codexBin) + " login --device-auth >" + quote(stdoutPath) + " 2>" + quote(stderrPath) + "; printf '%s' \"$?\" >" + quote(exitPath);
            const child = spawn(shell, ["-lc", command], {
              cwd: env.workspaceRoot,
              env: {
                ...process.env,
                CODEX_HOME: codexHomePath,
              },
              detached: true,
              stdio: "ignore",
            });
            child.unref();
            return {
              ok: true,
              started: true,
              codex_bin: codexBin,
              session_dir: sessionDir,
              codex_home_path: codexHomePath,
              auth_file_path: authPath,
              pid: child.pid ?? null,
              stdout_path: stdoutPath,
              stderr_path: stderrPath,
              exit_path: exitPath,
            };
  - capture_codex_login_url:
      if: "${state.vars.prepare_flow_ready === true}"
      metadata:
        description: "Capture or normalize the Codex device-auth URL and one-time code for this workflow run."
      run:
        script:
          language: javascript
          code: |
            const fs = await import("node:fs");
            const { setTimeout: delay } = await import("node:timers/promises");
            const seededSession =
              state.steps?.seed_existing_codex_login_session?.action?.value
              && typeof state.steps.seed_existing_codex_login_session.action.value === "object"
              && !Array.isArray(state.steps.seed_existing_codex_login_session.action.value)
                ? state.steps.seed_existing_codex_login_session.action.value
                : {};
            const explicitAuthUrl = String(args.auth_url || seededSession.auth_url || "").trim();
            const explicitDeviceCode = String(args.device_code || seededSession.device_code || "").trim();
            const stdoutPath = String(args.codex_login_stdout_path || seededSession.stdout_path || state.steps.start_codex_login_session?.action?.value?.stdout_path || "").trim();
            const stderrPath = String(args.codex_login_stderr_path || seededSession.stderr_path || state.steps.start_codex_login_session?.action?.value?.stderr_path || "").trim();
            const exitPath = String(args.codex_login_exit_path || seededSession.exit_path || state.steps.start_codex_login_session?.action?.value?.exit_path || "").trim();
            const sessionDir = String(args.codex_session_dir || seededSession.session_dir || state.steps.start_codex_login_session?.action?.value?.session_dir || "").trim() || null;
            const codexHomePath = String(args.codex_home_path || seededSession.codex_home_path || state.steps.start_codex_login_session?.action?.value?.codex_home_path || "").trim() || null;
            const authFilePath = String(args.codex_auth_file_path || seededSession.auth_file_path || state.steps.start_codex_login_session?.action?.value?.auth_file_path || "").trim() || null;
            const pidValue = Number(args.codex_login_pid || seededSession.pid || state.steps.start_codex_login_session?.action?.value?.pid || 0);
            const stripAnsi = (raw) => String(raw || "").replace(/\u001b\[[0-9;]*m/g, "");
            const extractDeviceChallenge = (raw) => {
              const cleaned = stripAnsi(raw);
              const authMatch = cleaned.match(/https:\/\/auth\.openai\.com\/codex\/device\b[^\s"'<>]*/);
              const codeMatch = cleaned.match(/\b[A-Z0-9]{4}-[A-Z0-9]{5}\b/);
              return { auth_url: authMatch ? authMatch[0] : null, device_code: codeMatch ? codeMatch[0] : null };
            };
            const parseCapture = (authUrl, deviceCode) => {
              if (!authUrl) throw new Error("captured-device-auth-url-missing");
              if (!deviceCode) throw new Error("captured-device-auth-code-missing");
              return { auth_url: authUrl, callback_url: null, callback_port: null, device_code: deviceCode };
            };
            const readText = (filePath) => (!filePath || !fs.existsSync(filePath)) ? "" : fs.readFileSync(filePath, "utf8");
            const tail = (filePath, maxChars = 1200) => {
              const text = readText(filePath);
              return text.length > maxChars ? text.slice(-maxChars) : text;
            };
            if (explicitAuthUrl && explicitDeviceCode) {
              return {
                ok: true,
                reused: true,
                ...parseCapture(explicitAuthUrl, explicitDeviceCode),
                session_dir: sessionDir,
                codex_home_path: codexHomePath,
                auth_file_path: authFilePath,
                pid: Number.isInteger(pidValue) && pidValue > 0 ? pidValue : null,
                stdout_path: stdoutPath || null,
                stderr_path: stderrPath || null,
                exit_path: exitPath || null,
              };
            }
            const timeoutMs = 45000;
            const startedAt = Date.now();
            while (Date.now() - startedAt < timeoutMs) {
              const combined = readText(stdoutPath) + "\n" + readText(stderrPath);
              const challenge = extractDeviceChallenge(combined);
              if (challenge.auth_url && challenge.device_code) {
                return {
                  ok: true,
                  reused: false,
                  ...parseCapture(challenge.auth_url, challenge.device_code),
                  session_dir: sessionDir,
                  codex_home_path: codexHomePath,
                  auth_file_path: authFilePath,
                  pid: Number.isInteger(pidValue) && pidValue > 0 ? pidValue : null,
                  stdout_path: stdoutPath || null,
                  stderr_path: stderrPath || null,
                  exit_path: exitPath || null,
                };
              }
              if (exitPath && fs.existsSync(exitPath)) {
                await delay(400);
                const exitCodeRaw = readText(exitPath).trim();
                const combinedAfterExit = readText(stdoutPath) + "\n" + readText(stderrPath);
                const challengeAfterExit = extractDeviceChallenge(combinedAfterExit);
                const stderrTail = tail(stderrPath) || null;
                const stdoutTail = tail(stdoutPath) || null;
                const combinedTail = String(stderrTail || "") + "\n" + String(stdoutTail || "");
                const deviceAuthRateLimited = /device code request failed with status 429|device auth failed with status 429|429 too many requests/i.test(combinedTail);
                if (challengeAfterExit.auth_url && challengeAfterExit.device_code) {
                  return {
                    ok: true,
                    reused: false,
                    ...parseCapture(challengeAfterExit.auth_url, challengeAfterExit.device_code),
                    issued_before_exit: true,
                    retry_reason: deviceAuthRateLimited ? "device_auth_rate_limit" : null,
                    error_message: deviceAuthRateLimited
                      ? (stderrTail || stdoutTail || "Codex device authorization was rate limited after issuing a device code.")
                      : null,
                    exit_code: exitCodeRaw === "" ? null : Number.parseInt(exitCodeRaw, 10),
                    stdout_tail: stdoutTail,
                    stderr_tail: stderrTail,
                    session_dir: sessionDir,
                    codex_home_path: codexHomePath,
                    auth_file_path: authFilePath,
                    pid: Number.isInteger(pidValue) && pidValue > 0 ? pidValue : null,
                    stdout_path: stdoutPath || null,
                    stderr_path: stderrPath || null,
                    exit_path: exitPath || null,
                  };
                }
                return {
                  ok: false,
                  error: "codex-login-exited-before-auth-url",
                  retry_reason: deviceAuthRateLimited ? "device_auth_rate_limit" : null,
                  error_message: stderrTail || stdoutTail || ("codex login exited before issuing a device authorization URL (exit=" + (exitCodeRaw || "unknown") + ")"),
                  exit_code: exitCodeRaw === "" ? null : Number.parseInt(exitCodeRaw, 10),
                  stdout_tail: stdoutTail,
                  stderr_tail: stderrTail,
                  session_dir: sessionDir,
                  codex_home_path: codexHomePath,
                  auth_file_path: authFilePath,
                  pid: Number.isInteger(pidValue) && pidValue > 0 ? pidValue : null,
                  stdout_path: stdoutPath || null,
                  stderr_path: stderrPath || null,
                  exit_path: exitPath || null,
                };
              }
              await delay(250);
            }
            return {
              ok: false,
              error: "codex-login-auth-url-timeout",
              retry_reason: null,
              error_message: tail(stderrPath) || tail(stdoutPath) || "codex login did not emit a device authorization URL before timing out",
              session_dir: sessionDir,
              codex_home_path: codexHomePath,
              auth_file_path: authFilePath,
              pid: Number.isInteger(pidValue) && pidValue > 0 ? pidValue : null,
              stdout_path: stdoutPath || null,
              stderr_path: stderrPath || null,
              exit_path: exitPath || null,
            };
  - cache_codex_login_session:
      if: "${state.steps.seed_existing_codex_login_session?.action?.ok === true || state.steps.start_codex_login_session?.action?.ok === true || state.steps.capture_codex_login_url?.action?.ok === true || String(inputs.auth_url || '').trim() !== '' || String(inputs.codex_login_stdout_path || '').trim() !== '' || String(inputs.codex_login_stderr_path || '').trim() !== '' || String(inputs.codex_session_dir || '').trim() !== ''}"
      metadata:
        description: "Cache the current Codex login session metadata for later exit handling."
      set:
        codex_login: "${state.steps.capture_codex_login_url?.action?.value || state.steps.seed_existing_codex_login_session?.action?.value || state.steps.start_codex_login_session?.action?.value || { auth_url: String(inputs.auth_url || '').trim() || null, device_code: String(inputs.device_code || '').trim() || null, session_dir: String(inputs.codex_session_dir || '').trim() || null, codex_home_path: String(inputs.codex_home_path || '').trim() || null, auth_file_path: String(inputs.codex_auth_file_path || '').trim() || null, pid: (inputs.codex_login_pid ?? null), stdout_path: String(inputs.codex_login_stdout_path || '').trim() || null, stderr_path: String(inputs.codex_login_stderr_path || '').trim() || null, exit_path: String(inputs.codex_login_exit_path || '').trim() || null }}"
  - restore_codex_login_challenge_from_artifacts:
      if: "${(String(state.vars.codex_login?.auth_url || '').trim() === '' || String(state.vars.codex_login?.device_code || '').trim() === '') && (String(state.vars.codex_login?.stdout_path || inputs.codex_login_stdout_path || '').trim() !== '' || String(state.vars.codex_login?.stderr_path || inputs.codex_login_stderr_path || '').trim() !== '')}"
      metadata:
        description: "Rebuild the saved device-auth URL/code from the detached Codex session logs when retrying a preserved device-auth session."
      run:
        script:
          language: javascript
          code: |
            const fs = await import("node:fs");
            const session = state.vars?.codex_login && typeof state.vars.codex_login === "object" && !Array.isArray(state.vars.codex_login)
              ? { ...state.vars.codex_login }
              : {};
            const stdoutPath = String(session.stdout_path || args.codex_login_stdout_path || "").trim();
            const stderrPath = String(session.stderr_path || args.codex_login_stderr_path || "").trim();
            const stripAnsi = (raw) => String(raw || "").replace(/\u001b\[[0-9;]*m/g, "");
            const readText = (filePath) => (!filePath || !fs.existsSync(filePath)) ? "" : fs.readFileSync(filePath, "utf8");
            const combined = stripAnsi(readText(stdoutPath) + "\n" + readText(stderrPath));
            const authMatch = combined.match(/https:\/\/auth\.openai\.com\/codex\/device\b[^\s"'<>]*/);
            const codeMatch = combined.match(/\b[A-Z0-9]{4}-[A-Z0-9]{5}\b/);
            return {
              ok: Boolean(authMatch?.[0] || session.auth_url || args.auth_url || ""),
              auth_url: String(session.auth_url || args.auth_url || authMatch?.[0] || "").trim() || null,
              device_code: String(session.device_code || args.device_code || codeMatch?.[0] || "").trim() || null,
              session_dir: String(session.session_dir || args.codex_session_dir || "").trim() || null,
              codex_home_path: String(session.codex_home_path || args.codex_home_path || "").trim() || null,
              auth_file_path: String(session.auth_file_path || args.codex_auth_file_path || "").trim() || null,
              pid: session.pid ?? args.codex_login_pid ?? null,
              stdout_path: stdoutPath || null,
              stderr_path: stderrPath || null,
              exit_path: String(session.exit_path || args.codex_login_exit_path || "").trim() || null,
            };
  - merge_restored_codex_login_session:
      if: "${String(state.steps.restore_codex_login_challenge_from_artifacts?.action?.value?.auth_url || '').trim() !== '' && String(state.steps.restore_codex_login_challenge_from_artifacts?.action?.value?.device_code || '').trim() !== ''}"
      metadata:
        description: "Promote the restored detached Codex device-auth session into workflow state."
      set:
        codex_login: "${state.steps.restore_codex_login_challenge_from_artifacts?.action?.value || state.vars.codex_login || {}}"
  - open_device_authorization_entry:
      if: "${state.vars.prepare_flow_ready === true && String(state.vars.codex_login?.auth_url || state.steps.capture_codex_login_url?.action?.value?.auth_url || state.steps.restore_codex_login_challenge_from_artifacts?.action?.value?.auth_url || '').trim() !== ''}"
      metadata:
        templateRef: replay_captured_auth_url
        description: "Open the captured Codex device-authorization URL on the already authenticated managed browser profile."
  - stabilize_device_authorization_surface:
      if: "${state.steps.open_device_authorization_entry?.action?.ok === true && state.steps.open_device_authorization_entry?.action?.skipped !== true}"
      metadata:
        templateRef: wait_for_openai_surface_ready
        description: "Wait for the device-authorization surface to render."
  - inspect_device_authorization_surface:
      if: "${state.steps.open_device_authorization_entry?.action?.ok === true && state.steps.open_device_authorization_entry?.action?.skipped !== true}"
      metadata:
        description: "Inspect the device-authorization surface for a code prompt, consent page, success page, or unexpected auth prompt."
        mode: headless_ok
        targetRef: primary
        browser:
          screenshot: true
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const title = await page.title().catch(() => "");
            const bodyText = await page.locator("body").innerText().catch(() => "");
            const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
            const normalizedText = normalize(bodyText);
            const firstLine = normalizedText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || null;
            const visibleButtons = await page.evaluate(() => {
              const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
              const isVisible = (node) => {
                if (!(node instanceof Element)) return false;
                if (node.closest("script,style,noscript,template,[hidden],[aria-hidden=\"true\"]")) return false;
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") return false;
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              };
              return Array.from(document.querySelectorAll("button,a,[role=\"button\"],input[type=\"submit\"]"))
                .filter((node) => isVisible(node))
                .map((node) => node instanceof HTMLInputElement ? normalize(node.value || node.getAttribute("aria-label") || "") : normalize(node.innerText || node.textContent || node.getAttribute("aria-label") || ""))
                .filter(Boolean)
                .slice(0, 16);
            }).catch(() => []);
            const verificationInput = await page.locator("input[name*=\"code\" i], input[autocomplete=\"one-time-code\"], input[aria-label*=\"code\" i], input[placeholder*=\"code\" i], input[inputmode=\"numeric\"]").count().catch(() => 0) > 0;
            const emailInput = await page.locator("input[type=\"email\"], input[name=\"email\"], input[autocomplete=\"email\"], input[autocomplete=\"username\"]").count().catch(() => 0) > 0;
            const passwordInput = await page.locator("input[type=\"password\"], input[name=\"password\"], input[autocomplete=\"current-password\"]").count().catch(() => 0) > 0;
            const surfaceText = String(title || "") + "\n" + normalizedText;
            let host = "";
            let path = "";
            try {
              const parsed = new URL(page.url());
              host = parsed.hostname;
              path = parsed.pathname;
            } catch {}
            const deviceCodePrompt = verificationInput && /device code|one-time code|authorization code|enter (?:the |your )?(?:\d+-character )?code|code displayed in your terminal/i.test(surfaceText);
            const stateMismatch = /state mismatch/i.test(title) || /state mismatch/i.test(normalizedText);
            const sawOauthConsent = /sign in to codex with chatgpt|share your name, email, and profile picture with codex|link your account/i.test(normalizedText) || /sign in to codex/i.test(title);
            const deviceAuthDisabled = /enable device code authorization for codex in chatgpt security settings/i.test(normalizedText);
            const oauthContinueVisible = sawOauthConsent && visibleButtons.some((text) => /continue to codex|continue to openai|authorize|allow|accept/i.test(text) || /^continue$/i.test(text));
            const success = /you can close this window|successfully logged in|signed in|device authorized|authorization complete/i.test(normalizedText) || /close this window/i.test(title);
            const chatGptHost = /(?:^|\.)chatgpt\.com$/i.test(host);
            const appShell = chatGptHost && !/^\/auth\//i.test(path) && /\bnew chat\b|\bsearch chats\b|\bimages\b|\bapps\b|\bprojects\b|\bdeep research\b|what['’]s on your mind today\?|ask anything/i.test(normalizedText);
            const securityShell = chatGptHost && !/^\/auth\//i.test(path) && /secure sign in with chatgpt|trusted devices|enable device code authorization for codex|log out of all devices|multi-factor authentication|security keys & passkeys/i.test(normalizedText);
            const publicShell = chatGptHost && !appShell && !securityShell && !emailInput && !passwordInput && !verificationInput && /log in to get smarter responses|log in|sign up for free|welcome to chatgpt|get started/i.test(normalizedText);
            const authPrompt = !chatGptHost && (emailInput || passwordInput || /welcome back|log in|enter your password|check your inbox/i.test(normalizedText));
            return {
              ok: true,
              current_url: page.url(),
              headline: firstLine || title || null,
              device_code_prompt: deviceCodePrompt,
              saw_oauth_consent: sawOauthConsent,
              device_auth_disabled: deviceAuthDisabled,
              oauth_continue_visible: oauthContinueVisible,
              success,
              state_mismatch: stateMismatch,
              chatgpt_app_shell: appShell || securityShell,
              chatgpt_public_shell: publicShell,
              auth_prompt: authPrompt,
            };
  - advance_device_authorization_login:
      if: "${state.steps.inspect_device_authorization_surface?.action?.ok === true && state.steps.inspect_device_authorization_surface?.action?.skipped !== true && state.steps.inspect_device_authorization_surface?.action?.auth_prompt === true}"
      metadata:
        description: "Mark the start of the local existing-account login branch for device authorization."
      set:
        device_auth_login_branch_started: "true"
  - classify_device_auth_login_entry:
      if: "${state.steps.advance_device_authorization_login?.action?.ok === true && state.steps.advance_device_authorization_login?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Classify the current OpenAI login surface before continuing the device-auth account login locally."
  - click_device_auth_login_button_from_auth_landing:
      if: "${state.steps.classify_device_auth_login_entry?.action?.stage === 'public_chatgpt' && /chatgpt\\.com\\/auth\\/login/i.test(String(state.steps.classify_device_auth_login_entry?.action?.current_url || '')) && state.steps.classify_device_auth_login_entry?.action?.login_cta_visible === true}"
      metadata:
        templateRef: click_chatgpt_login_button
        description: "Click Log in again on the ChatGPT auth landing shell so the device-auth login branch reaches the actual email form."
  - wait_after_device_auth_login_button_from_auth_landing:
      if: "${state.steps.click_device_auth_login_button_from_auth_landing?.action?.ok === true && state.steps.click_device_auth_login_button_from_auth_landing?.action?.skipped !== true}"
      metadata:
        description: "Let the secondary auth-landing login click settle before choosing the email login provider for device auth."
      wait:
        milliseconds: 900
  - choose_device_auth_email_login_option_after_auth_landing:
      if: "${false}"
      metadata:
        templateRef: click_email_login_option
        description: "Legacy no-op kept for step-id stability; device-auth now uses the visible email form directly instead of choosing a provider."
  - wait_after_device_auth_email_login_option_after_auth_landing:
      if: "${state.steps.choose_device_auth_email_login_option_after_auth_landing?.action?.ok === true && state.steps.choose_device_auth_email_login_option_after_auth_landing?.action?.skipped !== true}"
      metadata:
        description: "Allow the secondary device-auth email-login provider transition to settle before classifying the login surface again."
      wait:
        milliseconds: 900
  - classify_device_auth_login_entry_after_auth_landing:
      if: "${(state.steps.click_device_auth_login_button_from_auth_landing?.action?.ok === true && state.steps.click_device_auth_login_button_from_auth_landing?.action?.skipped !== true) || (state.steps.choose_device_auth_email_login_option_after_auth_landing?.action?.ok === true && state.steps.choose_device_auth_email_login_option_after_auth_landing?.action?.skipped !== true)}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Reclassify the device-auth login surface after the ChatGPT auth landing's secondary login step."
  - wait_after_device_auth_login_entry_unknown:
      if: "${state.steps.classify_device_auth_login_entry_after_auth_landing?.action?.skipped !== true && state.steps.classify_device_auth_login_entry_after_auth_landing?.action?.stage === 'unknown'}"
      metadata:
        description: Give the device-auth login shell a moment to finish rendering when the secondary auth-landing classification returns unknown.
      wait:
        milliseconds: 900
  - wait_after_device_auth_login_entry_initial_unknown:
      if: "${(state.steps.classify_device_auth_login_entry_after_auth_landing?.action?.skipped === true || !state.steps.classify_device_auth_login_entry_after_auth_landing?.action?.stage) && state.steps.classify_device_auth_login_entry?.action?.stage === 'unknown'}"
      metadata:
        description: Give the initial device-auth login shell a moment to finish rendering when the first classification returns unknown.
      wait:
        milliseconds: 900
  - classify_device_auth_login_entry_retry:
      if: "${(state.steps.wait_after_device_auth_login_entry_unknown?.action?.ok === true && state.steps.wait_after_device_auth_login_entry_unknown?.action?.skipped !== true) || (state.steps.wait_after_device_auth_login_entry_initial_unknown?.action?.ok === true && state.steps.wait_after_device_auth_login_entry_initial_unknown?.action?.skipped !== true)}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: Retry the device-auth login classification after a short settle when OpenAI first reports an unknown auth surface.
  - cache_effective_device_auth_login_entry_state:
      if: "${state.steps.advance_device_authorization_login?.action?.ok === true && state.steps.advance_device_authorization_login?.action?.skipped !== true}"
      metadata:
        description: "Cache the effective device-auth login-entry state after any secondary ChatGPT auth-landing recovery."
      set:
        effective_device_auth_login_entry_state: "${(state.steps.classify_device_auth_login_entry_retry?.action?.skipped !== true && (state.steps.classify_device_auth_login_entry_retry?.action?.stage || state.steps.classify_device_auth_login_entry_retry?.action?.current_url)) ? state.steps.classify_device_auth_login_entry_retry?.action : ((state.steps.classify_device_auth_login_entry_after_auth_landing?.action?.skipped !== true && (state.steps.classify_device_auth_login_entry_after_auth_landing?.action?.stage || state.steps.classify_device_auth_login_entry_after_auth_landing?.action?.current_url)) ? state.steps.classify_device_auth_login_entry_after_auth_landing?.action : (((state.steps.classify_device_auth_login_entry?.action?.skipped !== true) && (state.steps.classify_device_auth_login_entry?.action?.stage || state.steps.classify_device_auth_login_entry?.action?.current_url)) ? state.steps.classify_device_auth_login_entry?.action : {}))}"
  - fill_device_auth_login_email:
      if: "${state.vars.device_auth_login_branch_started === 'true' && (state.vars.effective_device_auth_login_entry_state?.stage === 'login_email' || (state.vars.effective_device_auth_login_entry_state?.stage === 'auth_prompt' && state.vars.effective_device_auth_login_entry_state?.email_input === true))}"
      metadata:
        templateRef: fill_openai_email_field
        description: "Fill the account email on the device-auth login entry."
  - submit_device_auth_login_email:
      if: "${state.vars.device_auth_login_branch_started === 'true' && (state.vars.effective_device_auth_login_entry_state?.stage === 'login_email' || (state.vars.effective_device_auth_login_entry_state?.stage === 'auth_prompt' && state.vars.effective_device_auth_login_entry_state?.email_input === true)) && state.steps.fill_device_auth_login_email?.action?.ok === true && state.steps.fill_device_auth_login_email?.action?.skipped !== true}"
      metadata:
        templateRef: submit_login_email_form
        description: "Submit the device-auth login email step."
  - wait_after_device_auth_login_email_submit:
      if: "${state.vars.device_auth_login_branch_started === 'true' && state.steps.submit_device_auth_login_email?.action?.ok === true && state.steps.submit_device_auth_login_email?.action?.skipped !== true}"
      metadata:
        description: "Allow the device-auth login email step to settle before choosing the recovery branch."
      wait:
        milliseconds: 900
  - classify_device_auth_after_login_email:
      if: "${state.vars.device_auth_login_branch_started === 'true' && ((state.steps.submit_device_auth_login_email?.action?.ok === true && state.steps.submit_device_auth_login_email?.action?.skipped !== true) || state.vars.effective_device_auth_login_entry_state?.ok === true)}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the device-auth login surface after the email step."
  - retry_device_auth_login_email_submit:
      if: "${state.vars.device_auth_login_branch_started === 'true' && state.steps.submit_device_auth_login_email?.action?.ok === true && state.steps.submit_device_auth_login_email?.action?.skipped !== true && (state.steps.classify_device_auth_after_login_email?.action?.stage === 'login_email' || (state.steps.classify_device_auth_after_login_email?.action?.stage === 'auth_prompt' && state.steps.classify_device_auth_after_login_email?.action?.email_input === true))}"
      metadata:
        templateRef: click_primary_continue
        description: "Retry the device-auth login email submit when OpenAI remains on the email step."
  - wait_after_device_auth_login_email_retry:
      if: "${state.vars.device_auth_login_branch_started === 'true' && state.steps.retry_device_auth_login_email_submit?.action?.ok === true && state.steps.retry_device_auth_login_email_submit?.action?.skipped !== true}"
      metadata:
        description: "Allow the retried device-auth login email submit to settle before classifying the next page."
      wait:
        milliseconds: 1500
  - classify_device_auth_after_login_email_retry:
      if: "${state.vars.device_auth_login_branch_started === 'true' && state.steps.retry_device_auth_login_email_submit?.action?.ok === true && state.steps.retry_device_auth_login_email_submit?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the device-auth login surface after retrying the email submit."
  - fill_device_auth_login_password:
      if: "${state.vars.device_auth_login_branch_started === 'true' && (state.steps.classify_device_auth_after_login_email_retry?.action?.stage === 'login_password' || state.steps.classify_device_auth_after_login_email?.action?.stage === 'login_password') && String(inputs.prefer_password_login ?? 'true') === 'true'}"
      metadata:
        templateRef: fill_openai_password_field
        description: "Fill the stored password on the device-auth login branch before falling back to recovery."
  - submit_device_auth_login_password:
      if: "${state.vars.device_auth_login_branch_started === 'true' && (state.steps.classify_device_auth_after_login_email_retry?.action?.stage === 'login_password' || state.steps.classify_device_auth_after_login_email?.action?.stage === 'login_password') && String(inputs.prefer_password_login ?? 'true') === 'true' && state.steps.fill_device_auth_login_password?.action?.ok === true && state.steps.fill_device_auth_login_password?.action?.skipped !== true}"
      metadata:
        templateRef: submit_openai_password_form
        description: "Submit the stored password on the device-auth login branch."
  - wait_after_device_auth_login_password_submit:
      if: "${state.vars.device_auth_login_branch_started === 'true' && state.steps.submit_device_auth_login_password?.action?.ok === true && state.steps.submit_device_auth_login_password?.action?.skipped !== true}"
      metadata:
        description: "Allow the device-auth login password submission to settle before classifying the next page."
      wait:
        milliseconds: 900
  - classify_device_auth_after_login_password:
      if: "${state.vars.device_auth_login_branch_started === 'true' && (state.steps.classify_device_auth_after_login_email_retry?.action?.stage === 'login_password' || state.steps.classify_device_auth_after_login_email?.action?.stage === 'login_password') && String(inputs.prefer_password_login ?? 'true') === 'true'}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Classify the device-auth login surface after submitting the stored password."
  - classify_device_auth_after_login_password_gate:
      if: "${state.vars.device_auth_login_branch_started === 'true' && (state.steps.classify_device_auth_after_login_email_retry?.action?.stage === 'login_password' || state.steps.classify_device_auth_after_login_email?.action?.stage === 'login_password') && String(inputs.prefer_password_login ?? 'true') === 'true'}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the device-auth login state after any password-step handling."
  - compute_device_auth_one_time_code_recovery_flag:
      if: "${state.vars.device_auth_login_branch_started === 'true'}"
      metadata:
        description: "Compute whether the device-auth login branch should switch to one-time-code recovery."
      run:
        script:
          language: javascript
          code: |
            const afterRetry = state.steps?.classify_device_auth_after_login_email_retry?.action || {};
            const afterEmail = state.steps?.classify_device_auth_after_login_email?.action || {};
            const afterPassword = state.steps?.classify_device_auth_after_login_password_gate?.action || {};
            const hasLocator = args.account_login_locator != null;
            const preferPasswordLogin = String(args.prefer_password_login ?? "true").trim().toLowerCase() !== "false";
            const passwordStage = afterRetry.stage === "login_password" || afterEmail.stage === "login_password";
            const passwordAuthPrompt =
              (afterRetry.stage === "auth_prompt" || afterEmail.stage === "auth_prompt")
              && (afterRetry.password_input === true || afterEmail.password_input === true);
            const passwordAttempted =
              (state.steps?.fill_device_auth_login_password?.action?.ok === true && state.steps?.fill_device_auth_login_password?.action?.skipped !== true)
              || (state.steps?.submit_device_auth_login_password?.action?.ok === true && state.steps?.submit_device_auth_login_password?.action?.skipped !== true)
              || (state.steps?.classify_device_auth_after_login_password_gate?.action?.ok === true && state.steps?.classify_device_auth_after_login_password_gate?.action?.skipped !== true);
            const shouldRecover =
              ((!preferPasswordLogin) && (passwordStage || passwordAuthPrompt))
              || ((passwordStage || passwordAuthPrompt) && preferPasswordLogin && passwordAttempted !== true)
              || afterPassword.invalid_credentials === true
              || afterPassword.reset_password_prompt === true
              || afterPassword.username_not_found === true;
            return {
              ok: true,
              should_recover: shouldRecover,
              has_locator: hasLocator,
              prefer_password_login: preferPasswordLogin,
              password_stage: passwordStage,
              password_auth_prompt: passwordAuthPrompt,
              password_attempted: passwordAttempted,
            };
  - choose_device_auth_one_time_code_recovery:
      if: "${state.steps.compute_device_auth_one_time_code_recovery_flag?.action?.value?.should_recover === true}"
      metadata:
        templateRef: click_one_time_code_button
        description: "Switch the device-auth login branch to one-time-code recovery instead of using the stored password."
  - classify_device_auth_after_recovery_choice:
      if: "${state.vars.device_auth_login_branch_started === 'true' && state.steps.choose_device_auth_one_time_code_recovery?.action?.ok === true && state.steps.choose_device_auth_one_time_code_recovery?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the device-auth login surface after selecting the one-time-code branch."
  - recover_device_auth_login_recovery_timeout:
      if: "${state.vars.device_auth_login_branch_started === 'true' && state.steps.classify_device_auth_after_recovery_choice?.action?.retryable_timeout === true}"
      metadata:
        templateRef: click_retryable_try_again
        description: "Retry the one-time-code branch when OpenAI renders its retryable timeout shell."
  - classify_device_auth_after_recovery_timeout_retry:
      if: "${state.vars.device_auth_login_branch_started === 'true' && state.steps.recover_device_auth_login_recovery_timeout?.action?.ok === true && state.steps.recover_device_auth_login_recovery_timeout?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the device-auth login surface after retrying the one-time-code branch."
  - reopen_device_auth_one_time_code_after_timeout:
      if: "${state.vars.device_auth_login_branch_started === 'true' && (state.steps.classify_device_auth_after_recovery_timeout_retry?.action?.stage === 'login_password' || (state.steps.classify_device_auth_after_recovery_timeout_retry?.action?.stage === 'auth_prompt' && state.steps.classify_device_auth_after_recovery_timeout_retry?.action?.password_input === true))}"
      metadata:
        templateRef: click_one_time_code_button
        description: "Reopen the one-time-code recovery branch when a retryable timeout returns the device-auth flow to the password prompt."
  - classify_device_auth_after_timeout_reopen:
      if: "${state.vars.device_auth_login_branch_started === 'true' && state.steps.reopen_device_auth_one_time_code_after_timeout?.action?.ok === true && state.steps.reopen_device_auth_one_time_code_after_timeout?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the device-auth login surface after reopening the one-time-code branch."
  - resend_device_auth_login_verification_email:
      if: "${state.vars.device_auth_login_branch_started === 'true' && (state.steps.classify_device_auth_after_login_email?.action?.email_verification === true || state.steps.classify_device_auth_after_login_email_retry?.action?.email_verification === true || state.steps.classify_device_auth_after_login_password_gate?.action?.email_verification === true || state.steps.classify_device_auth_after_recovery_choice?.action?.email_verification === true || state.steps.classify_device_auth_after_recovery_timeout_retry?.action?.email_verification === true || state.steps.classify_device_auth_after_timeout_reopen?.action?.email_verification === true)}"
      metadata:
        description: "Request a fresh device-auth login verification email before collecting the OTP from Gmail."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const result = await page.evaluate(() => {
              const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
              const isVisible = (node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              };
              const candidates = Array.from(document.querySelectorAll("button, input[type='submit'], [role='button']"))
                .filter((node) => isVisible(node) && node.getAttribute("aria-disabled") !== "true" && !node.hasAttribute("disabled"));
              const resend = candidates.find((node) => /resend email/i.test(normalize(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || ""))));
              if (!(resend instanceof HTMLElement)) {
                return { ok: false, reason: "resend-email-button-not-found" };
              }
              resend.click();
              return {
                ok: true,
                label: normalize(resend instanceof HTMLInputElement ? resend.value : (resend.innerText || resend.textContent || "")) || null,
              };
            }).catch((error) => ({
              ok: false,
              reason: error instanceof Error ? error.message : String(error || "resend-email-click-failed"),
            }));
            if (!result || result.ok !== true) {
              throw new Error(String(result && result.reason ? result.reason : "resend-email-click-failed"));
            }
            await page.waitForTimeout(1800).catch(() => {});
            return result;
  - collect_device_auth_login_verification_artifact:
      if: "${state.vars.device_auth_login_branch_started === 'true' && (state.steps.classify_device_auth_after_login_email?.action?.email_verification === true || state.steps.classify_device_auth_after_login_email_retry?.action?.email_verification === true || state.steps.classify_device_auth_after_login_password_gate?.action?.email_verification === true || state.steps.classify_device_auth_after_recovery_choice?.action?.email_verification === true || state.steps.classify_device_auth_after_recovery_timeout_retry?.action?.email_verification === true || state.steps.classify_device_auth_after_timeout_reopen?.action?.email_verification === true)}"
      metadata:
        description: "Collect the device-auth login OTP from Gmail on the same managed profile."
        ref:
          digest: sha256:01e032fddc23788226829689d0b2161ad4b9acb108dac83395bfbdaef5f8974d
      call: workflow.sys.web.mail-google-com.collect-verification-artifact
      with:
        version: 1.0.0
        input:
          enabled: true
          search_query: "${'from:openai to:' + String(inputs.email || '').trim() + ' newer_than:7d'}"
          message_match_text: "${String(inputs.email || '').trim() || 'OpenAI'}"
          link_host_regex: auth\.openai\.com|chatgpt\.com
  - cache_device_auth_login_verification_artifact:
      if: "${state.steps.collect_device_auth_login_verification_artifact?.action?.result != null}"
      metadata:
        description: Cache the collected device-auth login verification artifact into simple workflow state.
      set:
        device_auth_login_verification_code: "${(String(state.steps.collect_device_auth_login_verification_artifact?.action?.result?.result?.output?.code || state.steps.collect_device_auth_login_verification_artifact?.action?.result?.output?.code || state.steps.collect_device_auth_login_verification_artifact?.action?.output?.code || state.steps.collect_device_auth_login_verification_artifact?.action?.result?.result?.output?.verification_code || state.steps.collect_device_auth_login_verification_artifact?.action?.result?.output?.verification_code || state.steps.collect_device_auth_login_verification_artifact?.action?.result?.result?.output?.otp || state.steps.collect_device_auth_login_verification_artifact?.action?.result?.output?.otp || state.steps.collect_device_auth_login_verification_artifact?.action?.result?.result?.output?.one_time_code || state.steps.collect_device_auth_login_verification_artifact?.action?.result?.output?.one_time_code || state.steps.collect_device_auth_login_verification_artifact?.action?.result?.result?.output?.message_preview || state.steps.collect_device_auth_login_verification_artifact?.action?.result?.output?.message_preview || state.steps.collect_device_auth_login_verification_artifact?.action?.result?.result?.output?.message_subject || state.steps.collect_device_auth_login_verification_artifact?.action?.result?.output?.message_subject || state.steps.collect_device_auth_login_verification_artifact?.action?.result?.result?.state?.steps?.collect_verification_artifact?.action?.code || state.steps.collect_device_auth_login_verification_artifact?.action?.result?.state?.steps?.collect_verification_artifact?.action?.code || '')).match(/(?:^|\\D)(\\d{6})(?:\\D|$)/)?.[1] || ''}"
        device_auth_login_verification_url: "${state.steps.collect_device_auth_login_verification_artifact?.action?.result?.result?.output?.verification_url || state.steps.collect_device_auth_login_verification_artifact?.action?.result?.output?.verification_url || state.steps.collect_device_auth_login_verification_artifact?.action?.output?.verification_url || null}"
  - wait_for_device_auth_login_verification_email_delivery:
      if: "${state.vars.device_auth_login_branch_started === 'true' && state.steps.collect_device_auth_login_verification_artifact?.action?.ok === true && state.steps.collect_device_auth_login_verification_artifact?.action?.skipped !== true && String(state.vars.device_auth_login_verification_code || '').length < 6}"
      metadata:
        description: Give Gmail one more chance to finish loading the device-auth OTP before leaving the mailbox.
      wait:
        milliseconds: 4000
  - recollect_device_auth_login_verification_artifact_after_missing_code:
      if: "${state.steps.wait_for_device_auth_login_verification_email_delivery?.action?.ok === true && state.steps.wait_for_device_auth_login_verification_email_delivery?.action?.skipped !== true && String(state.vars.device_auth_login_verification_code || '').length < 6}"
      metadata:
        description: Re-collect the device-auth login OTP once when Gmail has not surfaced a usable code yet.
        ref:
          digest: sha256:01e032fddc23788226829689d0b2161ad4b9acb108dac83395bfbdaef5f8974d
      call: workflow.sys.web.mail-google-com.collect-verification-artifact
      with:
        version: 1.0.0
        input:
          enabled: true
          search_query: "${'from:openai to:' + String(inputs.email || '').trim() + ' newer_than:7d'}"
          message_match_text: "${String(inputs.email || '').trim() || 'OpenAI'}"
          link_host_regex: auth\.openai\.com|chatgpt\.com
  - cache_device_auth_login_verification_artifact_after_missing_code:
      if: "${state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.result != null}"
      metadata:
        description: Replace the device-auth login verification artifact after waiting for Gmail to finish loading.
      set:
        device_auth_login_verification_code: "${(String(state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.result?.result?.output?.code || state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.result?.output?.code || state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.output?.code || state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.result?.result?.output?.verification_code || state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.result?.output?.verification_code || state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.result?.result?.output?.otp || state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.result?.output?.otp || state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.result?.result?.output?.one_time_code || state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.result?.output?.one_time_code || state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.result?.result?.output?.message_preview || state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.result?.output?.message_preview || state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.result?.result?.output?.message_subject || state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.result?.output?.message_subject || state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.result?.result?.state?.steps?.collect_verification_artifact?.action?.code || state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.result?.state?.steps?.collect_verification_artifact?.action?.code || state.vars.device_auth_login_verification_code || '')).match(/(?:^|\\D)(\\d{6})(?:\\D|$)/)?.[1] || ''}"
        device_auth_login_verification_url: "${state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.result?.result?.output?.verification_url || state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.result?.output?.verification_url || state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.output?.verification_url || state.vars.device_auth_login_verification_url || null}"
  - return_to_device_auth_login_verification_page:
      if: "${state.vars.device_auth_login_branch_started === 'true' && ((state.steps.collect_device_auth_login_verification_artifact?.action?.ok === true && state.steps.collect_device_auth_login_verification_artifact?.action?.skipped !== true) || (state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.ok === true && state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action?.skipped !== true))}"
      metadata:
        description: "Return from Gmail to the active OpenAI email-verification page in the device-auth login branch."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.navigate
      with:
        body:
          url: "${state.steps.classify_device_auth_after_return_recovery?.action?.current_url || state.steps.classify_device_auth_after_timeout_reopen?.action?.current_url || state.steps.classify_device_auth_after_recovery_timeout_retry?.action?.current_url || state.steps.classify_device_auth_after_recovery_choice?.action?.current_url || state.steps.classify_device_auth_after_login_password_gate?.action?.current_url || state.steps.classify_device_auth_after_login_email_retry?.action?.current_url || state.steps.classify_device_auth_after_login_email?.action?.current_url || 'https://auth.openai.com/email-verification'}"
  - stabilize_device_auth_login_verification_surface:
      if: "${state.steps.return_to_device_auth_login_verification_page?.action?.ok === true && state.steps.return_to_device_auth_login_verification_page?.action?.skipped !== true}"
      metadata:
        templateRef: wait_for_openai_surface_ready
        description: "Wait for the device-auth login verification page to render after returning from Gmail."
  - classify_device_auth_login_verification_ready:
      if: "${state.steps.return_to_device_auth_login_verification_page?.action?.ok === true && state.steps.return_to_device_auth_login_verification_page?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Confirm the OpenAI email-verification surface after returning from Gmail."
  - refill_device_auth_login_email_after_return:
      if: "${state.steps.classify_device_auth_login_verification_ready?.action?.stage === 'login_email' && state.steps.classify_device_auth_login_verification_ready?.action?.email_input_editable !== false}"
      metadata:
        templateRef: fill_openai_email_field
        description: "Re-enter the account email when returning from Gmail resets the device-auth login flow to the email step."
  - continue_device_auth_login_email_after_return:
      if: "${state.steps.classify_device_auth_login_verification_ready?.action?.stage === 'login_email' && state.steps.classify_device_auth_login_verification_ready?.action?.email_input_editable === false && state.steps.classify_device_auth_login_verification_ready?.action?.email_prefilled === true && state.steps.classify_device_auth_login_verification_ready?.action?.continue_button_visible === true}"
      metadata:
        templateRef: click_primary_continue
        description: "Continue from the locked prefilled email step when returning from Gmail restores the device-auth login flow to Welcome back."
  - submit_device_auth_login_email_after_return:
      if: "${state.steps.refill_device_auth_login_email_after_return?.action?.ok === true && state.steps.refill_device_auth_login_email_after_return?.action?.skipped !== true}"
      metadata:
        templateRef: submit_login_email_form
        description: "Submit the restored device-auth login email step before reopening one-time-code recovery."
  - wait_after_device_auth_login_email_after_return:
      if: "${(state.steps.submit_device_auth_login_email_after_return?.action?.ok === true && state.steps.submit_device_auth_login_email_after_return?.action?.skipped !== true) || (state.steps.continue_device_auth_login_email_after_return?.action?.ok === true && state.steps.continue_device_auth_login_email_after_return?.action?.skipped !== true)}"
      metadata:
        description: "Allow the restored device-auth login email step to settle before checking whether the password prompt reappeared."
      wait:
        milliseconds: 900
  - classify_device_auth_login_after_return:
      if: "${(state.steps.submit_device_auth_login_email_after_return?.action?.ok === true && state.steps.submit_device_auth_login_email_after_return?.action?.skipped !== true) || (state.steps.continue_device_auth_login_email_after_return?.action?.ok === true && state.steps.continue_device_auth_login_email_after_return?.action?.skipped !== true)}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Classify the device-auth login surface after restoring the email step from the Gmail return path."
  - reopen_device_auth_one_time_code_after_return:
      if: "${state.steps.classify_device_auth_login_verification_ready?.action?.stage === 'login_password' || (state.steps.classify_device_auth_login_verification_ready?.action?.stage === 'auth_prompt' && state.steps.classify_device_auth_login_verification_ready?.action?.password_input === true) || state.steps.classify_device_auth_login_after_return?.action?.stage === 'login_password' || (state.steps.classify_device_auth_login_after_return?.action?.stage === 'auth_prompt' && state.steps.classify_device_auth_login_after_return?.action?.password_input === true)}"
      metadata:
        templateRef: click_one_time_code_button
        description: "Reopen one-time-code recovery when the Gmail return path lands back on the password prompt."
  - classify_device_auth_after_return_recovery:
      if: "${state.steps.reopen_device_auth_one_time_code_after_return?.action?.ok === true && state.steps.reopen_device_auth_one_time_code_after_return?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the device-auth login surface after rebuilding one-time-code recovery from the Gmail return path."
  - fill_device_auth_login_verification_code:
      if: "${state.steps.classify_device_auth_login_verification_ready?.action?.stage === 'email_verification' || state.steps.classify_device_auth_after_return_recovery?.action?.stage === 'email_verification' || state.steps.classify_device_auth_after_login_password_gate?.action?.stage === 'email_verification' || state.steps.classify_device_auth_after_recovery_choice?.action?.stage === 'email_verification' || state.steps.classify_device_auth_after_timeout_reopen?.action?.stage === 'email_verification' || state.steps.classify_device_auth_after_login_email_retry?.action?.stage === 'email_verification' || state.steps.classify_device_auth_after_login_email?.action?.stage === 'email_verification'}"
      metadata:
        description: "Fill the Gmail OTP into the OpenAI email-verification form for device auth."
      call: afn.driver.browser.type
      with:
        body:
          selector: 'input[name*="code" i], input[autocomplete="one-time-code"], input[aria-label*="code" i], input[placeholder*="code" i], input[inputmode="numeric"]'
          text: "${String(state.vars.device_auth_login_verification_code || state.steps.collect_device_auth_login_verification_artifact?.action?.result?.result?.output?.code || state.steps.collect_device_auth_login_verification_artifact?.action?.result?.output?.code || state.steps.collect_device_auth_login_verification_artifact?.action?.result?.state?.steps?.collect_verification_artifact?.action?.code || '').replace(/\\D+/g, '').slice(0, 6)}"
          dispatch_events_after_fill: true
          blur_after_fill: true
          settle_ms: 250
  - submit_device_auth_login_verification_code:
      if: "${state.steps.fill_device_auth_login_verification_code?.action?.ok === true && state.steps.fill_device_auth_login_verification_code?.action?.skipped !== true}"
      metadata:
        description: "Submit the OpenAI email-verification OTP for device auth."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const verificationSelector = 'input[name*="code" i], input[autocomplete="one-time-code"], input[aria-label*="code" i], input[placeholder*="code" i], input[inputmode="numeric"]';
            const readSixDigitCode = (value) => {
              const match = String(value || "").match(/(?:^|\D)(\d{6})(?:\D|$)/);
              return match?.[1] || null;
            };
            const extractVerificationCode = (action) => {
              const outputs = [
                action?.result?.result?.output,
                action?.result?.output,
                action?.output,
              ].filter((value) => value && typeof value === "object");
              for (const output of outputs) {
                const directCode =
                  readSixDigitCode(output.code)
                  || readSixDigitCode(output.verification_code)
                  || readSixDigitCode(output.otp)
                  || readSixDigitCode(output.one_time_code);
                if (directCode) {
                  return directCode;
                }
              }
              for (const output of outputs) {
                const derivedCode =
                  readSixDigitCode(output.message_subject)
                  || readSixDigitCode(output.message_preview)
                  || readSixDigitCode(output.verification_link_text);
                if (derivedCode) {
                  return derivedCode;
                }
              }
              return readSixDigitCode(
                action?.result?.result?.state?.steps?.collect_verification_artifact?.action?.code
                || action?.result?.state?.steps?.collect_verification_artifact?.action?.code
                || ""
              );
            };
            const readCurrentPageVerificationCode = async () => await page.evaluate((selector) => {
              const readSixDigitCode = (value) => {
                const match = String(value || "").match(/(?:^|\D)(\d{6})(?:\D|$)/);
                return match?.[1] || null;
              };
              const inputs = Array.from(document.querySelectorAll(selector)).filter((node) =>
                node instanceof HTMLInputElement || node instanceof HTMLTextAreaElement
              );
              for (const input of inputs) {
                const directCode = readSixDigitCode(input.value || input.getAttribute("value") || "");
                if (directCode) {
                  return directCode;
                }
              }
              const joinedDigits = inputs
                .map((input) => String(input.value || input.getAttribute("value") || "").replace(/\D+/g, ""))
                .filter(Boolean)
                .join("")
                .slice(0, 6);
              return joinedDigits.length === 6 ? joinedDigits : null;
            }, verificationSelector);
            const existingCode = await readCurrentPageVerificationCode();
            const code = String(
              existingCode
              || state.vars.device_auth_login_verification_code
              || extractVerificationCode(state.steps.collect_device_auth_login_verification_artifact?.action)
              || extractVerificationCode(state.steps.recollect_device_auth_login_verification_artifact_after_missing_code?.action)
              || ""
            ).replace(/\D+/g, "").slice(0, 6);
            if (code.length < 6) {
              throw new Error("device-auth-verification-code-missing");
            }
            const detectStage = async () => await page.evaluate((selector) => {
              const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
              const hasVisible = (query) => Array.from(document.querySelectorAll(query)).some((node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              });
              const bodyText = normalize(document.body ? (document.body.innerText || document.body.textContent || "") : "");
              const title = normalize(document.title || "");
              const path = location.pathname;
              const verificationVisible = hasVisible(selector)
                && (/check your inbox|check your email|verify your email|verification code|enter the verification code|enter your code|temporary verification code|resend email/i.test(bodyText)
                  || /email-verification/i.test(path));
              let stage = "unknown";
              if (/^(localhost|127\.0\.0\.1)$/i.test(location.hostname)) {
                stage = "callback_complete";
              } else if (/just a moment/i.test(title) || /performing security verification|verify you are not a bot|verifying you are human|verify you are human/i.test(bodyText)) {
                stage = "security_verification";
              } else if (/rate limit exceeded|too many requests|please try again later/i.test(bodyText)) {
                stage = "rate_limit";
              } else if (/your session has ended/i.test(bodyText)) {
                stage = "session_ended";
              } else if (/oops, an error occurred|operation timed out|try again/i.test(bodyText)) {
                stage = "retryable_timeout";
              } else if (/add-phone/i.test(path) || /phone number required|add your phone number|verify your phone number|enter your phone number/i.test(bodyText)) {
                stage = "add_phone";
              } else if (/confirm your age|full name|birthday|finish creating account|what should we call you|tell us about yourself|about you/i.test(bodyText) || /about-you/i.test(path)) {
                stage = "about_you";
              } else if (verificationVisible) {
                stage = "email_verification";
              } else if (/an account for this email address already exists|this email address already exists|account already exists|already registered/i.test(bodyText)) {
                stage = "existing_account_prompt";
              } else if (/continue to codex|continue to openai|authorize|allow|accept/i.test(bodyText)) {
                stage = "oauth_consent";
              }
              return {
                stage,
                current_url: location.href,
                headline: bodyText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
                incorrect_code: /incorrect code/i.test(bodyText),
              };
            }, verificationSelector);
            const inputLocator = page.locator(verificationSelector);
            const inputCount = await inputLocator.count().catch(() => 0);
            const inputs = [];
            for (let index = 0; index < inputCount; index += 1) {
              const candidate = inputLocator.nth(index);
              if (await candidate.isVisible().catch(() => false)) {
                inputs.push(candidate);
              }
            }
            const input = inputs[0] || inputLocator.first();
            await input.waitFor({ state: "visible", timeout: 8000 }).catch(() => {});
            if ((await input.count().catch(() => 0)) === 0 || !(await input.isVisible().catch(() => false))) {
              const before = await detectStage();
              throw new Error("device-auth-verification-input-not-ready:" + String(before.stage || "unknown") + ":" + String(before.current_url || page.url()));
            }
            const fillField = async (locator, value) => {
              await locator.scrollIntoViewIfNeeded().catch(() => {});
              await locator.click({ timeout: 1500 }).catch(() => {});
              await locator.fill("").catch(() => {});
              const filled = await locator.fill(value).then(() => true).catch(() => false);
              if (!filled) {
                await locator.press("Meta+A").catch(() => {});
                await locator.press("Control+A").catch(() => {});
                await locator.press("Backspace").catch(() => {});
                await locator.pressSequentially(value, { delay: 45 }).catch(() => {});
              }
            };
            const readCombinedValue = async () => {
              const parts = [];
              for (const locator of inputs.length ? inputs : [input]) {
                parts.push(String(await locator.inputValue().catch(() => "")).replace(/\D+/g, ""));
              }
              return parts.join("").slice(0, code.length);
            };
            if (inputs.length > 1) {
              for (let index = 0; index < inputs.length && index < code.length; index += 1) {
                await fillField(inputs[index], code[index]);
              }
            } else {
              await fillField(input, code);
            }
            await page.waitForTimeout(150).catch(() => {});
            let value = await readCombinedValue();
            if (value !== code && inputs.length > 1) {
              await input.click({ timeout: 1500 }).catch(() => {});
              await input.press("Meta+A").catch(() => {});
              await input.press("Control+A").catch(() => {});
              await input.press("Backspace").catch(() => {});
              await input.pressSequentially(code, { delay: 45 }).catch(() => {});
              await page.waitForTimeout(150).catch(() => {});
              value = await readCombinedValue();
            }
            if (value.length < 6 || value !== code) {
              throw new Error("device-auth-verification-input-mismatch:" + (value || "empty"));
            }
            const clickVisibleContinue = async () => {
              return await input.evaluate((field) => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                const isVisible = (node) => {
                  if (!(node instanceof Element)) {
                    return false;
                  }
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                const readLabel = (node) => {
                  if (!(node instanceof Element)) {
                    return "";
                  }
                  if (node instanceof HTMLInputElement) {
                    return normalize(node.value || node.getAttribute("aria-label") || "");
                  }
                  return normalize(node.innerText || node.textContent || node.getAttribute("aria-label") || "");
                };
                const form = field instanceof Element ? field.closest("form") : null;
                const scope = form || document;
                const candidates = Array.from(scope.querySelectorAll("button, input[type='submit'], [role='button']"))
                  .filter((node) => isVisible(node) && node.getAttribute("aria-disabled") !== "true" && !node.hasAttribute("disabled"));
                const preferred = candidates.find((node) => {
                  const label = readLabel(node);
                  const isSubmitNode = (node instanceof HTMLButtonElement && node.type === "submit")
                    || (node instanceof HTMLInputElement && node.type === "submit");
                  return isSubmitNode && /continue|next|verify|submit|authorize|allow|accept/i.test(label);
                }) || candidates.find((node) => /continue|next|verify|submit|authorize|allow|accept/i.test(readLabel(node)));
                if (preferred instanceof HTMLElement) {
                  preferred.click();
                  return {
                    ok: true,
                    strategy: "form-dom-click",
                    label: readLabel(preferred) || null,
                  };
                }
                if (form instanceof HTMLFormElement) {
                  form.requestSubmit();
                  return {
                    ok: true,
                    strategy: "form-request-submit",
                    label: null,
                  };
                }
                return null;
              }).catch(() => null);
            };
            const attempts = [
              async () => clickVisibleContinue(),
              async () => {
                await input.press("Enter").catch(() => {});
                return { strategy: "press-enter", label: null };
              },
              async () => {
                await input.evaluate((field) => {
                  if (field instanceof HTMLElement) {
                    field.blur();
                  }
                  const form = field instanceof Element ? field.closest("form") : null;
                  if (form instanceof HTMLFormElement) {
                    form.requestSubmit();
                  }
                }).catch(() => {});
                return { strategy: "request-submit", label: null };
              },
            ];
            const transitions = [];
            for (const attempt of attempts) {
              const performed = await attempt();
              if (!performed) {
                continue;
              }
              await page.waitForTimeout(1200).catch(() => {});
              const after = await detectStage();
              transitions.push({ ...performed, ...after });
              if (after.stage !== "email_verification") {
                return {
                  ok: true,
                  code,
                  strategy: performed.strategy,
                  clicked_text: performed.label || null,
                  next_stage: after.stage,
                  current_url: after.current_url,
                  headline: after.headline,
                  transitions,
                };
              }
            }
            const finalState = await detectStage();
            if (finalState.incorrect_code) {
              throw new Error("device-auth-verification-code-rejected:" + String(finalState.current_url || page.url()));
            }
            if (finalState.stage === "email_verification") {
              return {
                ok: true,
                code,
                strategy: "email-verification-still-open",
                clicked_text: null,
                next_stage: finalState.stage,
                current_url: finalState.current_url,
                headline: finalState.headline,
                incorrect_code: finalState.incorrect_code,
                transitions,
              };
            }
            throw new Error("device-auth-verification-submit-stuck:" + String(finalState.stage || "unknown") + ":" + String(finalState.current_url || page.url()));
  - wait_after_device_auth_login_verification_code_submit:
      if: "${state.steps.submit_device_auth_login_verification_code?.action?.ok === true && state.steps.submit_device_auth_login_verification_code?.action?.skipped !== true}"
      metadata:
        description: "Allow the device-auth email-verification submission to settle before resuming the device challenge."
      wait:
        milliseconds: 900
  - classify_device_auth_after_login_verification_submit:
      if: "${state.steps.submit_device_auth_login_verification_code?.action?.ok === true && state.steps.submit_device_auth_login_verification_code?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Classify the device-auth login flow after submitting the verification OTP."
  - recover_device_auth_login_verification_timeout:
      if: "${state.steps.classify_device_auth_after_login_verification_submit?.action?.retryable_timeout === true}"
      metadata:
        templateRef: click_retryable_try_again
        description: "Retry the device-auth verification flow when OpenAI renders a retryable timeout after OTP submission."
  - classify_device_auth_after_login_verification_timeout_retry:
      if: "${state.steps.recover_device_auth_login_verification_timeout?.action?.ok === true && state.steps.recover_device_auth_login_verification_timeout?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the device-auth login surface after retrying the post-OTP timeout shell."
  - reopen_device_auth_one_time_code_after_login_verification_timeout:
      if: "${state.steps.classify_device_auth_after_login_verification_timeout_retry?.action?.stage === 'login_password' || (state.steps.classify_device_auth_after_login_verification_timeout_retry?.action?.stage === 'auth_prompt' && state.steps.classify_device_auth_after_login_verification_timeout_retry?.action?.password_input === true)}"
      metadata:
        templateRef: click_one_time_code_button
        description: "Reopen one-time-code recovery when a post-OTP timeout returns the device-auth flow to the password prompt."
  - classify_device_auth_after_login_verification_timeout_reopen:
      if: "${state.steps.reopen_device_auth_one_time_code_after_login_verification_timeout?.action?.ok === true && state.steps.reopen_device_auth_one_time_code_after_login_verification_timeout?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the device-auth login surface after reopening one-time-code recovery from the post-OTP timeout branch."
  - resend_device_auth_login_verification_email_after_incorrect_code:
      if: "${state.steps.classify_device_auth_after_login_verification_submit?.action?.stage === 'email_verification' || state.steps.classify_device_auth_after_login_verification_timeout_retry?.action?.stage === 'email_verification' || state.steps.classify_device_auth_after_login_verification_timeout_reopen?.action?.stage === 'email_verification'}"
      metadata:
        description: "Request a fresh device-auth verification email before collecting a replacement OTP."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const result = await page.evaluate(() => {
              const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
              const isVisible = (node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              };
              const candidates = Array.from(document.querySelectorAll("button, input[type='submit'], [role='button']"))
                .filter((node) => isVisible(node) && node.getAttribute("aria-disabled") !== "true" && !node.hasAttribute("disabled"));
              const resend = candidates.find((node) => /resend email/i.test(normalize(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || ""))));
              if (!(resend instanceof HTMLElement)) {
                return { ok: false, reason: "resend-email-button-not-found" };
              }
              resend.click();
              return {
                ok: true,
                label: normalize(resend instanceof HTMLInputElement ? resend.value : (resend.innerText || resend.textContent || "")) || null,
              };
            }).catch((error) => ({
              ok: false,
              reason: error instanceof Error ? error.message : String(error || "resend-email-click-failed"),
            }));
            if (!result || result.ok !== true) {
              throw new Error(String(result && result.reason ? result.reason : "resend-email-click-failed"));
            }
            await page.waitForTimeout(1800).catch(() => {});
            return result;
  - recollect_device_auth_login_verification_artifact_after_incorrect_code:
      if: "${state.steps.resend_device_auth_login_verification_email_after_incorrect_code?.action?.ok === true && state.steps.resend_device_auth_login_verification_email_after_incorrect_code?.action?.skipped !== true}"
      metadata:
        description: "Re-collect the device-auth login OTP from Gmail once when OpenAI rejects the first verification code."
        ref:
          digest: sha256:01e032fddc23788226829689d0b2161ad4b9acb108dac83395bfbdaef5f8974d
      call: workflow.sys.web.mail-google-com.collect-verification-artifact
      with:
        version: 1.0.0
        input:
          enabled: true
          search_query: "${'from:openai to:' + String(inputs.email || '').trim() + ' newer_than:7d'}"
          message_match_text: "${String(inputs.email || '').trim() || 'OpenAI'}"
          link_host_regex: auth\.openai\.com|chatgpt\.com
  - cache_device_auth_login_verification_artifact_after_incorrect_code:
      if: "${state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result != null}"
      metadata:
        description: Cache the replacement device-auth login verification artifact into simple workflow state.
      set:
        device_auth_login_verification_code_retry: "${(String(state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.result?.output?.code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.output?.code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.output?.code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.result?.output?.verification_code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.output?.verification_code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.result?.output?.otp || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.output?.otp || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.result?.output?.one_time_code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.output?.one_time_code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.result?.output?.message_preview || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.output?.message_preview || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.result?.output?.message_subject || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.output?.message_subject || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.result?.state?.steps?.collect_verification_artifact?.action?.code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.state?.steps?.collect_verification_artifact?.action?.code || '')).match(/(?:^|\\D)(\\d{6})(?:\\D|$)/)?.[1] || ''}"
        device_auth_login_verification_url_retry: "${state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.result?.output?.verification_url || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.output?.verification_url || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.output?.verification_url || null}"
  - wait_for_device_auth_login_verification_email_delivery_after_incorrect_code:
      if: "${state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.ok === true && state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.skipped !== true && String(state.vars.device_auth_login_verification_code_retry || '').length < 6}"
      metadata:
        description: Give Gmail one more chance to finish loading a replacement device-auth OTP after OpenAI rejects the first code.
      wait:
        milliseconds: 4000
  - recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code:
      if: "${state.steps.wait_for_device_auth_login_verification_email_delivery_after_incorrect_code?.action?.ok === true && state.steps.wait_for_device_auth_login_verification_email_delivery_after_incorrect_code?.action?.skipped !== true && String(state.vars.device_auth_login_verification_code_retry || '').length < 6}"
      metadata:
        description: Re-collect the replacement device-auth login OTP once when Gmail still has not surfaced a usable code.
        ref:
          digest: sha256:01e032fddc23788226829689d0b2161ad4b9acb108dac83395bfbdaef5f8974d
      call: workflow.sys.web.mail-google-com.collect-verification-artifact
      with:
        version: 1.0.0
        input:
          enabled: true
          search_query: "${'from:openai to:' + String(inputs.email || '').trim() + ' newer_than:7d'}"
          message_match_text: "${String(inputs.email || '').trim() || 'OpenAI'}"
          link_host_regex: auth\.openai\.com|chatgpt\.com
  - cache_device_auth_login_verification_artifact_after_incorrect_code_missing_code:
      if: "${state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.result != null}"
      metadata:
        description: Replace the retry device-auth login verification artifact after waiting for Gmail to finish loading.
      set:
        device_auth_login_verification_code_retry: "${(String(state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.result?.output?.code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.output?.code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.output?.code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.result?.output?.verification_code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.output?.verification_code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.result?.output?.otp || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.output?.otp || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.result?.output?.one_time_code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.output?.one_time_code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.result?.output?.message_preview || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.output?.message_preview || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.result?.output?.message_subject || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.output?.message_subject || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.result?.state?.steps?.collect_verification_artifact?.action?.code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.state?.steps?.collect_verification_artifact?.action?.code || state.vars.device_auth_login_verification_code_retry || '')).match(/(?:^|\\D)(\\d{6})(?:\\D|$)/)?.[1] || ''}"
        device_auth_login_verification_url_retry: "${state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.result?.output?.verification_url || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.result?.output?.verification_url || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.output?.verification_url || state.vars.device_auth_login_verification_url_retry || null}"
  - return_to_device_auth_login_verification_page_after_incorrect_code:
      if: "${(state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.ok === true && state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.skipped !== true) || (state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.ok === true && state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action?.skipped !== true)}"
      metadata:
        description: "Return from Gmail to the device-auth verification page after collecting a replacement code."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.navigate
      with:
        body:
          url: "${state.steps.classify_device_auth_after_login_verification_timeout_reopen?.action?.current_url || state.steps.classify_device_auth_after_login_verification_timeout_retry?.action?.current_url || state.steps.classify_device_auth_after_login_verification_submit?.action?.current_url || state.steps.classify_device_auth_after_return_recovery?.action?.current_url || state.steps.classify_device_auth_after_timeout_reopen?.action?.current_url || state.steps.classify_device_auth_after_recovery_timeout_retry?.action?.current_url || state.steps.classify_device_auth_after_recovery_choice?.action?.current_url || state.steps.classify_device_auth_after_login_password_gate?.action?.current_url || state.steps.classify_device_auth_after_login_email_retry?.action?.current_url || state.steps.classify_device_auth_after_login_email?.action?.current_url || 'https://auth.openai.com/email-verification'}"
  - stabilize_device_auth_login_verification_surface_after_incorrect_code:
      if: "${state.steps.return_to_device_auth_login_verification_page_after_incorrect_code?.action?.ok === true && state.steps.return_to_device_auth_login_verification_page_after_incorrect_code?.action?.skipped !== true}"
      metadata:
        templateRef: wait_for_openai_surface_ready
        description: "Wait for the device-auth verification page to settle after collecting a replacement code."
  - classify_device_auth_login_verification_ready_after_incorrect_code:
      if: "${state.steps.return_to_device_auth_login_verification_page_after_incorrect_code?.action?.ok === true && state.steps.return_to_device_auth_login_verification_page_after_incorrect_code?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Confirm the device-auth verification surface after collecting a replacement code."
  - fill_device_auth_login_verification_code_after_incorrect_code:
      if: "${state.steps.classify_device_auth_login_verification_ready_after_incorrect_code?.action?.stage === 'email_verification'}"
      metadata:
        description: "Fill the replacement Gmail OTP into the OpenAI email-verification form for device auth."
      call: afn.driver.browser.type
      with:
        body:
          selector: 'input[name*="code" i], input[autocomplete="one-time-code"], input[aria-label*="code" i], input[placeholder*="code" i], input[inputmode="numeric"]'
          text: "${String(state.vars.device_auth_login_verification_code_retry || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.result?.output?.code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.output?.code || state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action?.result?.state?.steps?.collect_verification_artifact?.action?.code || '').replace(/\\D+/g, '').slice(0, 6)}"
          dispatch_events_after_fill: true
          blur_after_fill: true
          settle_ms: 250
  - submit_device_auth_login_verification_code_after_incorrect_code:
      if: "${state.steps.fill_device_auth_login_verification_code_after_incorrect_code?.action?.ok === true && state.steps.fill_device_auth_login_verification_code_after_incorrect_code?.action?.skipped !== true}"
      metadata:
        description: "Submit the replacement OpenAI verification OTP for device auth."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const verificationSelector = 'input[name*="code" i], input[autocomplete="one-time-code"], input[aria-label*="code" i], input[placeholder*="code" i], input[inputmode="numeric"]';
            const readSixDigitCode = (value) => {
              const match = String(value || "").match(/(?:^|\D)(\d{6})(?:\D|$)/);
              return match?.[1] || null;
            };
            const extractVerificationCode = (action) => {
              const outputs = [
                action?.result?.result?.output,
                action?.result?.output,
                action?.output,
              ].filter((value) => value && typeof value === "object");
              for (const output of outputs) {
                const directCode =
                  readSixDigitCode(output.code)
                  || readSixDigitCode(output.verification_code)
                  || readSixDigitCode(output.otp)
                  || readSixDigitCode(output.one_time_code);
                if (directCode) {
                  return directCode;
                }
              }
              for (const output of outputs) {
                const derivedCode =
                  readSixDigitCode(output.message_subject)
                  || readSixDigitCode(output.message_preview)
                  || readSixDigitCode(output.verification_link_text);
                if (derivedCode) {
                  return derivedCode;
                }
              }
              return readSixDigitCode(
                action?.result?.result?.state?.steps?.collect_verification_artifact?.action?.code
                || action?.result?.state?.steps?.collect_verification_artifact?.action?.code
                || ""
              );
            };
            const readCurrentPageVerificationCode = async () => await page.evaluate((selector) => {
              const readSixDigitCode = (value) => {
                const match = String(value || "").match(/(?:^|\D)(\d{6})(?:\D|$)/);
                return match?.[1] || null;
              };
              const inputs = Array.from(document.querySelectorAll(selector)).filter((node) =>
                node instanceof HTMLInputElement || node instanceof HTMLTextAreaElement
              );
              for (const input of inputs) {
                const directCode = readSixDigitCode(input.value || input.getAttribute("value") || "");
                if (directCode) {
                  return directCode;
                }
              }
              const joinedDigits = inputs
                .map((input) => String(input.value || input.getAttribute("value") || "").replace(/\D+/g, ""))
                .filter(Boolean)
                .join("")
                .slice(0, 6);
              return joinedDigits.length === 6 ? joinedDigits : null;
            }, verificationSelector);
            const existingCode = await readCurrentPageVerificationCode();
            const code = String(
              existingCode
              || state.vars.device_auth_login_verification_code_retry
              || extractVerificationCode(state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code?.action)
              || extractVerificationCode(state.steps.recollect_device_auth_login_verification_artifact_after_incorrect_code_missing_code?.action)
              || ""
            ).replace(/\D+/g, "").slice(0, 6);
            if (code.length < 6) {
              throw new Error("device-auth-verification-code-missing");
            }
            const detectStage = async () => await page.evaluate((selector) => {
              const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
              const hasVisible = (query) => Array.from(document.querySelectorAll(query)).some((node) => {
                if (!(node instanceof Element)) {
                  return false;
                }
                const style = window.getComputedStyle(node);
                if (!style || style.display === "none" || style.visibility === "hidden") {
                  return false;
                }
                const rect = node.getBoundingClientRect();
                return rect.width > 0 && rect.height > 0;
              });
              const bodyText = normalize(document.body ? (document.body.innerText || document.body.textContent || "") : "");
              const title = normalize(document.title || "");
              const path = location.pathname;
              const verificationVisible = hasVisible(selector)
                && (/check your inbox|check your email|verify your email|verification code|enter the verification code|enter your code|temporary verification code|resend email/i.test(bodyText)
                  || /email-verification/i.test(path));
              let stage = "unknown";
              if (/^(localhost|127\.0\.0\.1)$/i.test(location.hostname)) {
                stage = "callback_complete";
              } else if (/just a moment/i.test(title) || /performing security verification|verify you are not a bot|verifying you are human|verify you are human/i.test(bodyText)) {
                stage = "security_verification";
              } else if (/rate limit exceeded|too many requests|please try again later/i.test(bodyText)) {
                stage = "rate_limit";
              } else if (/your session has ended/i.test(bodyText)) {
                stage = "session_ended";
              } else if (/oops, an error occurred|operation timed out|try again/i.test(bodyText)) {
                stage = "retryable_timeout";
              } else if (/add-phone/i.test(path) || /phone number required|add your phone number|verify your phone number|enter your phone number/i.test(bodyText)) {
                stage = "add_phone";
              } else if (/confirm your age|full name|birthday|finish creating account|what should we call you|tell us about yourself|about you/i.test(bodyText) || /about-you/i.test(path)) {
                stage = "about_you";
              } else if (verificationVisible) {
                stage = "email_verification";
              } else if (/an account for this email address already exists|this email address already exists|account already exists|already registered/i.test(bodyText)) {
                stage = "existing_account_prompt";
              } else if (/continue to codex|continue to openai|authorize|allow|accept/i.test(bodyText)) {
                stage = "oauth_consent";
              }
              return {
                stage,
                current_url: location.href,
                headline: bodyText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
                incorrect_code: /incorrect code/i.test(bodyText),
              };
            }, verificationSelector);
            const inputLocator = page.locator(verificationSelector);
            const inputCount = await inputLocator.count().catch(() => 0);
            const inputs = [];
            for (let index = 0; index < inputCount; index += 1) {
              const candidate = inputLocator.nth(index);
              if (await candidate.isVisible().catch(() => false)) {
                inputs.push(candidate);
              }
            }
            const input = inputs[0] || inputLocator.first();
            await input.waitFor({ state: "visible", timeout: 8000 }).catch(() => {});
            if ((await input.count().catch(() => 0)) === 0 || !(await input.isVisible().catch(() => false))) {
              const before = await detectStage();
              throw new Error("device-auth-verification-input-not-ready:" + String(before.stage || "unknown") + ":" + String(before.current_url || page.url()));
            }
            const fillField = async (locator, value) => {
              await locator.scrollIntoViewIfNeeded().catch(() => {});
              await locator.click({ timeout: 1500 }).catch(() => {});
              await locator.fill("").catch(() => {});
              const filled = await locator.fill(value).then(() => true).catch(() => false);
              if (!filled) {
                await locator.press("Meta+A").catch(() => {});
                await locator.press("Control+A").catch(() => {});
                await locator.press("Backspace").catch(() => {});
                await locator.pressSequentially(value, { delay: 45 }).catch(() => {});
              }
            };
            const readCombinedValue = async () => {
              const parts = [];
              for (const locator of inputs.length ? inputs : [input]) {
                parts.push(String(await locator.inputValue().catch(() => "")).replace(/\D+/g, ""));
              }
              return parts.join("").slice(0, code.length);
            };
            if (inputs.length > 1) {
              for (let index = 0; index < inputs.length && index < code.length; index += 1) {
                await fillField(inputs[index], code[index]);
              }
            } else {
              await fillField(input, code);
            }
            await page.waitForTimeout(150).catch(() => {});
            let value = await readCombinedValue();
            if (value !== code && inputs.length > 1) {
              await input.click({ timeout: 1500 }).catch(() => {});
              await input.press("Meta+A").catch(() => {});
              await input.press("Control+A").catch(() => {});
              await input.press("Backspace").catch(() => {});
              await input.pressSequentially(code, { delay: 45 }).catch(() => {});
              await page.waitForTimeout(150).catch(() => {});
              value = await readCombinedValue();
            }
            if (value.length < 6 || value !== code) {
              throw new Error("device-auth-verification-input-mismatch:" + (value || "empty"));
            }
            const clickVisibleContinue = async () => {
              return await input.evaluate((field) => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                const isVisible = (node) => {
                  if (!(node instanceof Element)) {
                    return false;
                  }
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") {
                    return false;
                  }
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                const readLabel = (node) => {
                  if (!(node instanceof Element)) {
                    return "";
                  }
                  if (node instanceof HTMLInputElement) {
                    return normalize(node.value || node.getAttribute("aria-label") || "");
                  }
                  return normalize(node.innerText || node.textContent || node.getAttribute("aria-label") || "");
                };
                const form = field instanceof Element ? field.closest("form") : null;
                const scope = form || document;
                const candidates = Array.from(scope.querySelectorAll("button, input[type='submit'], [role='button']"))
                  .filter((node) => isVisible(node) && node.getAttribute("aria-disabled") !== "true" && !node.hasAttribute("disabled"));
                const preferred = candidates.find((node) => {
                  const label = readLabel(node);
                  const isSubmitNode = (node instanceof HTMLButtonElement && node.type === "submit")
                    || (node instanceof HTMLInputElement && node.type === "submit");
                  return isSubmitNode && /continue|next|verify|submit|authorize|allow|accept/i.test(label);
                }) || candidates.find((node) => /continue|next|verify|submit|authorize|allow|accept/i.test(readLabel(node)));
                if (preferred instanceof HTMLElement) {
                  preferred.click();
                  return {
                    ok: true,
                    strategy: "form-dom-click",
                    label: readLabel(preferred) || null,
                  };
                }
                if (form instanceof HTMLFormElement) {
                  form.requestSubmit();
                  return {
                    ok: true,
                    strategy: "form-request-submit",
                    label: null,
                  };
                }
                return null;
              }).catch(() => null);
            };
            const attempts = [
              async () => clickVisibleContinue(),
              async () => {
                await input.press("Enter").catch(() => {});
                return { strategy: "press-enter", label: null };
              },
              async () => {
                await input.evaluate((field) => {
                  if (field instanceof HTMLElement) {
                    field.blur();
                  }
                  const form = field instanceof Element ? field.closest("form") : null;
                  if (form instanceof HTMLFormElement) {
                    form.requestSubmit();
                  }
                }).catch(() => {});
                return { strategy: "request-submit", label: null };
              },
            ];
            const transitions = [];
            for (const attempt of attempts) {
              const performed = await attempt();
              if (!performed) {
                continue;
              }
              await page.waitForTimeout(1200).catch(() => {});
              const after = await detectStage();
              transitions.push({ ...performed, ...after });
              if (after.stage !== "email_verification") {
                return {
                  ok: true,
                  code,
                  strategy: performed.strategy,
                  clicked_text: performed.label || null,
                  next_stage: after.stage,
                  current_url: after.current_url,
                  headline: after.headline,
                  transitions,
                };
              }
            }
            const finalState = await detectStage();
            if (finalState.incorrect_code) {
              throw new Error("device-auth-verification-code-rejected:" + String(finalState.current_url || page.url()));
            }
            throw new Error("device-auth-verification-submit-stuck:" + String(finalState.stage || "unknown") + ":" + String(finalState.current_url || page.url()));
  - wait_after_device_auth_login_verification_code_retry_submit:
      if: "${state.steps.submit_device_auth_login_verification_code_after_incorrect_code?.action?.ok === true && state.steps.submit_device_auth_login_verification_code_after_incorrect_code?.action?.skipped !== true}"
      metadata:
        description: "Allow the replacement device-auth verification submission to settle before resuming the device challenge."
      wait:
        milliseconds: 900
  - classify_device_auth_after_login_verification_retry:
      if: "${state.steps.submit_device_auth_login_verification_code_after_incorrect_code?.action?.ok === true && state.steps.submit_device_auth_login_verification_code_after_incorrect_code?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Classify the device-auth login flow after retrying the verification OTP."
  - recover_device_auth_login_verification_retry_timeout:
      if: "${state.steps.classify_device_auth_after_login_verification_retry?.action?.retryable_timeout === true}"
      metadata:
        templateRef: click_retryable_try_again
        description: "Retry the device-auth verification flow when OpenAI renders a retryable timeout after the replacement OTP submission."
  - classify_device_auth_after_login_verification_retry_timeout_retry:
      if: "${state.steps.recover_device_auth_login_verification_retry_timeout?.action?.ok === true && state.steps.recover_device_auth_login_verification_retry_timeout?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the device-auth login surface after retrying the replacement-OTP timeout shell."
  - reopen_device_auth_one_time_code_after_login_verification_retry_timeout:
      if: "${state.steps.classify_device_auth_after_login_verification_retry_timeout_retry?.action?.stage === 'login_password' || (state.steps.classify_device_auth_after_login_verification_retry_timeout_retry?.action?.stage === 'auth_prompt' && state.steps.classify_device_auth_after_login_verification_retry_timeout_retry?.action?.password_input === true)}"
      metadata:
        templateRef: click_one_time_code_button
        description: "Reopen one-time-code recovery when the replacement-OTP timeout branch returns the device-auth flow to the password prompt."
  - classify_device_auth_after_login_verification_retry_timeout_reopen:
      if: "${state.steps.reopen_device_auth_one_time_code_after_login_verification_retry_timeout?.action?.ok === true && state.steps.reopen_device_auth_one_time_code_after_login_verification_retry_timeout?.action?.skipped !== true}"
      metadata:
        templateRef: classify_openai_auth_surface
        description: "Refresh the device-auth login surface after reopening one-time-code recovery from the replacement-OTP timeout branch."
  - stabilize_device_authorization_after_login:
      if: "${state.steps.advance_device_authorization_login?.action?.ok === true && state.steps.advance_device_authorization_login?.action?.skipped !== true}"
      metadata:
        templateRef: wait_for_openai_surface_ready
        description: "Wait for the device-authorization surface to settle after the reused OpenAI login branch completes."
  - inspect_device_authorization_after_login:
      if: "${state.steps.advance_device_authorization_login?.action?.ok === true && state.steps.advance_device_authorization_login?.action?.skipped !== true}"
      metadata:
        description: "Inspect the device-authorization surface again after the reused OpenAI login branch completes."
        mode: headless_ok
        targetRef: primary
        browser:
          screenshot: true
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
            const readSurface = async () => {
              const title = await page.title().catch(() => "");
              const bodyText = await page.locator("body").innerText().catch(() => "");
              const normalizedText = normalize(bodyText);
              const firstLine = normalizedText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || null;
              const visibleButtons = await page.evaluate(() => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                const isVisible = (node) => {
                  if (!(node instanceof Element)) return false;
                  if (node.closest("script,style,noscript,template,[hidden],[aria-hidden=\"true\"]")) return false;
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") return false;
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                return Array.from(document.querySelectorAll("button,a,[role=\"button\"],input[type=\"submit\"]"))
                  .filter((node) => isVisible(node))
                  .map((node) => node instanceof HTMLInputElement ? normalize(node.value || node.getAttribute("aria-label") || "") : normalize(node.innerText || node.textContent || node.getAttribute("aria-label") || ""))
                  .filter(Boolean)
                  .slice(0, 16);
              }).catch(() => []);
              const verificationInput = await page.locator("input[name*=\"code\" i], input[autocomplete=\"one-time-code\"], input[aria-label*=\"code\" i], input[placeholder*=\"code\" i], input[inputmode=\"numeric\"]").count().catch(() => 0) > 0;
              const emailInput = await page.locator("input[type=\"email\"], input[name=\"email\"], input[autocomplete=\"email\"], input[autocomplete=\"username\"]").count().catch(() => 0) > 0;
              const passwordInput = await page.locator("input[type=\"password\"], input[name=\"password\"], input[autocomplete=\"current-password\"]").count().catch(() => 0) > 0;
              const emailVerification = verificationInput && /check your inbox|enter the verification code|verification code we just sent|resend email/i.test(normalizedText);
              const surfaceText = String(title || "") + "\n" + normalizedText;
              let host = "";
              let path = "";
              try {
                const parsed = new URL(page.url());
                host = parsed.hostname;
                path = parsed.pathname;
              } catch {}
              const deviceCodePrompt = verificationInput && !emailVerification && /device code|one-time code|authorization code|enter (?:the |your )?(?:\d+-character )?code|code displayed in your terminal/i.test(surfaceText);
              const stateMismatch = /state mismatch/i.test(title) || /state mismatch/i.test(normalizedText);
              const sawOauthConsent = /sign in to codex with chatgpt|share your name, email, and profile picture with codex|link your account/i.test(normalizedText) || /sign in to codex/i.test(title);
              const oauthContinueVisible = sawOauthConsent && visibleButtons.some((text) => /continue to codex|continue to openai|authorize|allow|accept/i.test(text) || /^continue$/i.test(text));
              const success = /you can close this window|successfully logged in|signed in|device authorized|authorization complete/i.test(normalizedText) || /close this window/i.test(title);
              const chatGptHost = /(?:^|\.)chatgpt\.com$/i.test(host);
              const settingsMenuCount = [
                "general",
                "notifications",
                "security",
                "account",
                "data controls",
                "apps",
              ].reduce((count, label) => count + (normalizedText.toLowerCase().includes(label) ? 1 : 0), 0);
              const settingsShell = chatGptHost && !/^\/auth\//i.test(path) && settingsMenuCount >= 2;
              const appShell = chatGptHost && !/^\/auth\//i.test(path) && /\bnew chat\b|\bsearch chats\b|\bimages\b|\bapps\b|\bprojects\b|\bdeep research\b|what['’]s on your mind today\?|ask anything/i.test(normalizedText);
              const securityShell = chatGptHost && !/^\/auth\//i.test(path) && /secure sign in with chatgpt|trusted devices|enable device code authorization for codex|log out of all devices|multi-factor authentication|security keys & passkeys/i.test(normalizedText);
              const publicShell = chatGptHost && !appShell && !securityShell && !settingsShell && !emailInput && !passwordInput && !verificationInput && /log in to get smarter responses|log in|sign up for free|welcome to chatgpt|get started/i.test(normalizedText);
              const authPrompt = emailInput || passwordInput || /welcome back|log in|enter your password/i.test(normalizedText);
              const retryableTimeout = /oops, an error occurred|operation timed out|try again/i.test(normalizedText) || /oops, an error occurred|operation timed out/i.test(title);
              const rateLimitExceeded = /rate limit exceeded|too many requests|please try again later/i.test(normalizedText);
              const addPhonePrompt = /\/add-phone(?:[/?#]|$)/i.test(path) || /phone number required|add your phone number|verify your phone number|enter your phone number|add phone/i.test(normalizedText);
              return {
                ok: true,
                current_url: page.url(),
                headline: firstLine || title || null,
                device_code_prompt: deviceCodePrompt,
                email_verification: emailVerification,
                saw_oauth_consent: sawOauthConsent,
                oauth_continue_visible: oauthContinueVisible,
                success,
                state_mismatch: stateMismatch,
                chatgpt_app_shell: appShell || securityShell || settingsShell,
                chatgpt_settings_shell: settingsShell,
                chatgpt_security_shell: securityShell,
                chatgpt_public_shell: publicShell,
                auth_prompt: authPrompt,
                retryable_timeout: retryableTimeout,
                rate_limit_exceeded: rateLimitExceeded,
                add_phone_prompt: addPhonePrompt,
              };
            };

            let surface = await readSurface();
            if (surface.email_verification === true && surface.success !== true && surface.saw_oauth_consent !== true && surface.device_code_prompt !== true && surface.chatgpt_app_shell !== true && surface.chatgpt_public_shell !== true) {
              for (const waitMs of [1000, 1500, 2500, 4000, 6000]) {
                await page.waitForTimeout(waitMs).catch(() => {});
                surface = await readSurface();
                if (
                  surface.email_verification !== true
                  || surface.success === true
                  || surface.saw_oauth_consent === true
                  || surface.device_code_prompt === true
                  || surface.chatgpt_app_shell === true
                  || surface.chatgpt_public_shell === true
                  || surface.auth_prompt === true
                  || surface.state_mismatch === true
                  || surface.retryable_timeout === true
                  || surface.rate_limit_exceeded === true
                ) {
                  break;
                }
              }
            }
            if ((surface.chatgpt_app_shell === true || surface.chatgpt_public_shell === true) && surface.success !== true && surface.saw_oauth_consent !== true && surface.device_code_prompt !== true) {
              for (const waitMs of [1000, 1500, 2500, 4000]) {
                await page.waitForTimeout(waitMs).catch(() => {});
                surface = await readSurface();
                if (
                  surface.chatgpt_app_shell !== true
                  && surface.chatgpt_public_shell !== true
                  || surface.success === true
                  || surface.saw_oauth_consent === true
                  || surface.device_code_prompt === true
                  || surface.auth_prompt === true
                  || surface.state_mismatch === true
                  || surface.retryable_timeout === true
                  || surface.rate_limit_exceeded === true
                ) {
                  break;
                }
              }
            }
            const authUrl = String(state.vars?.codex_login?.auth_url || args.auth_url || "").trim();
            if (surface.add_phone_prompt === true && authUrl) {
              for (const waitMs of [2000, 4000, 8000, 12000]) {
                await page.waitForTimeout(waitMs).catch(() => {});
                await page.goto(authUrl, { waitUntil: "domcontentloaded", timeout: 30000 }).catch(() => {});
                await page.waitForTimeout(1200).catch(() => {});
                surface = await readSurface();
                if (
                  surface.add_phone_prompt !== true
                  || surface.success === true
                  || surface.saw_oauth_consent === true
                  || surface.device_code_prompt === true
                  || surface.auth_prompt === true
                  || surface.state_mismatch === true
                  || surface.retryable_timeout === true
                  || surface.rate_limit_exceeded === true
                ) {
                  break;
                }
              }
            }

            return surface;
  - reopen_device_authorization_entry_after_login:
      if: "${state.steps.inspect_device_authorization_after_login?.action?.success !== true && state.steps.inspect_device_authorization_after_login?.action?.device_code_prompt !== true && state.steps.inspect_device_authorization_after_login?.action?.oauth_continue_visible !== true && String(state.vars.codex_login?.auth_url || inputs.auth_url || '').trim() !== '' && (state.steps.inspect_device_authorization_after_login?.action?.auth_prompt === true || state.steps.inspect_device_authorization_after_login?.action?.retryable_timeout === true || state.steps.inspect_device_authorization_after_login?.action?.state_mismatch === true || state.steps.inspect_device_authorization_after_login?.action?.email_verification === true || state.steps.inspect_device_authorization_after_login?.action?.chatgpt_app_shell === true || state.steps.inspect_device_authorization_after_login?.action?.chatgpt_public_shell === true)}"
      metadata:
        description: "Reopen the original device-authorization challenge URL after the reused login branch so OpenAI can resume the pending device flow on the now-authenticated session."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.navigate
      with:
        body:
          url: "${state.vars.codex_login?.auth_url || inputs.auth_url || ''}"
  - stabilize_device_authorization_after_login_reopen:
      if: "${state.steps.reopen_device_authorization_entry_after_login?.action?.ok === true && state.steps.reopen_device_authorization_entry_after_login?.action?.skipped !== true}"
      metadata:
        templateRef: wait_for_openai_surface_ready
        description: "Wait for the reopened device-authorization challenge to render on the authenticated browser session."
  - inspect_device_authorization_after_login_reopen:
      if: "${state.steps.reopen_device_authorization_entry_after_login?.action?.ok === true && state.steps.reopen_device_authorization_entry_after_login?.action?.skipped !== true}"
      metadata:
        description: "Inspect the reopened device-authorization challenge after reusing the authenticated browser session."
        mode: headless_ok
        targetRef: primary
        browser:
          screenshot: true
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
            const readSurface = async () => {
              const title = await page.title().catch(() => "");
              const bodyText = await page.locator("body").innerText().catch(() => "");
              const normalizedText = normalize(bodyText);
              const firstLine = normalizedText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || null;
              const visibleButtons = await page.evaluate(() => {
                const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
                const isVisible = (node) => {
                  if (!(node instanceof Element)) return false;
                  if (node.closest("script,style,noscript,template,[hidden],[aria-hidden=\"true\"]")) return false;
                  const style = window.getComputedStyle(node);
                  if (!style || style.display === "none" || style.visibility === "hidden") return false;
                  const rect = node.getBoundingClientRect();
                  return rect.width > 0 && rect.height > 0;
                };
                return Array.from(document.querySelectorAll("button,a,[role=\"button\"],input[type=\"submit\"]"))
                  .filter((node) => isVisible(node))
                  .map((node) => node instanceof HTMLInputElement ? normalize(node.value || node.getAttribute("aria-label") || "") : normalize(node.innerText || node.textContent || node.getAttribute("aria-label") || ""))
                  .filter(Boolean)
                  .slice(0, 16);
              }).catch(() => []);
              const verificationInput = await page.locator("input[name*=\"code\" i], input[autocomplete=\"one-time-code\"], input[aria-label*=\"code\" i], input[placeholder*=\"code\" i], input[inputmode=\"numeric\"]").count().catch(() => 0) > 0;
              const emailInput = await page.locator("input[type=\"email\"], input[name=\"email\"], input[autocomplete=\"email\"], input[autocomplete=\"username\"]").count().catch(() => 0) > 0;
              const passwordInput = await page.locator("input[type=\"password\"], input[name=\"password\"], input[autocomplete=\"current-password\"]").count().catch(() => 0) > 0;
              const emailVerification = verificationInput && /check your inbox|enter the verification code|verification code we just sent|resend email/i.test(normalizedText);
              const surfaceText = String(title || "") + "\n" + normalizedText;
              const deviceCodePrompt = verificationInput && !emailVerification && /device code|one-time code|authorization code|enter (?:the |your )?(?:\d+-character )?code|code displayed in your terminal/i.test(surfaceText);
              const stateMismatch = /state mismatch/i.test(title) || /state mismatch/i.test(normalizedText);
              const sawOauthConsent = /sign in to codex with chatgpt|share your name, email, and profile picture with codex|link your account/i.test(normalizedText) || /sign in to codex/i.test(title);
              const oauthContinueVisible = sawOauthConsent && visibleButtons.some((text) => /continue to codex|continue to openai|authorize|allow|accept/i.test(text) || /^continue$/i.test(text));
              const success = /you can close this window|successfully logged in|signed in|device authorized|authorization complete/i.test(normalizedText) || /close this window/i.test(title);
              const authPrompt = emailInput || passwordInput || /welcome back|log in|enter your password/i.test(normalizedText);
              const retryableTimeout = /oops, an error occurred|operation timed out|try again/i.test(normalizedText) || /oops, an error occurred|operation timed out/i.test(title);
              const rateLimitExceeded = /rate limit exceeded|too many requests|please try again later/i.test(normalizedText);
              const addPhonePrompt = /\/add-phone(?:[/?#]|$)/i.test(page.url()) || /phone number required|add your phone number|verify your phone number|enter your phone number|add phone/i.test(normalizedText);
              return {
                ok: true,
                current_url: page.url(),
                headline: firstLine || title || null,
                device_code_prompt: deviceCodePrompt,
                email_verification: emailVerification,
                saw_oauth_consent: sawOauthConsent,
                oauth_continue_visible: oauthContinueVisible,
                success,
                state_mismatch: stateMismatch,
                auth_prompt: authPrompt,
                retryable_timeout: retryableTimeout,
                rate_limit_exceeded: rateLimitExceeded,
                add_phone_prompt: addPhonePrompt,
              };
            };
            let surface = await readSurface();
            const authUrl = String(state.vars?.codex_login?.auth_url || args.auth_url || "").trim();
            if (surface.add_phone_prompt === true && authUrl) {
              for (const waitMs of [2000, 4000, 8000, 12000]) {
                await page.waitForTimeout(waitMs).catch(() => {});
                await page.goto(authUrl, { waitUntil: "domcontentloaded", timeout: 30000 }).catch(() => {});
                await page.waitForTimeout(1200).catch(() => {});
                surface = await readSurface();
                if (
                  surface.add_phone_prompt !== true
                  || surface.success === true
                  || surface.saw_oauth_consent === true
                  || surface.device_code_prompt === true
                  || surface.auth_prompt === true
                  || surface.state_mismatch === true
                  || surface.retryable_timeout === true
                  || surface.rate_limit_exceeded === true
                ) {
                  break;
                }
              }
            }
            return surface;
  - cache_effective_device_authorization_surface:
      metadata:
        description: "Cache the current device-authorization surface after any reused login branch."
      set:
        device_authorization_surface: "${state.steps.inspect_device_authorization_after_login_reopen?.action?.current_url ? state.steps.inspect_device_authorization_after_login_reopen?.action : (state.steps.inspect_device_authorization_after_login?.action?.current_url ? state.steps.inspect_device_authorization_after_login?.action : (state.steps.inspect_device_authorization_surface?.action || {}))}"
  - fill_device_authorization_code:
      if: "${state.vars.device_authorization_surface?.device_code_prompt === true && String(state.vars.codex_login?.device_code || '').trim() !== ''}"
      metadata:
        description: "Fill the Codex device code into the OpenAI device-authorization entry form."
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const rawCode = String(state.vars?.codex_login?.device_code || args.device_code || "").trim().toUpperCase();
            const selector = 'input[name*="code" i], input[autocomplete="one-time-code"], input[aria-label*="code" i], input[placeholder*="code" i], input[inputmode="numeric"]';
            if (!rawCode) {
              return { ok: false, error: "device-code-missing", current_url: page.url() };
            }
            const plainCode = rawCode.replace(/[^A-Z0-9]/g, "");
            const locateInputs = async () => {
              const locator = page.locator(selector);
              const count = await locator.count().catch(() => 0);
              const visible = [];
              for (let index = 0; index < count; index += 1) {
                const candidate = locator.nth(index);
                const isVisible = await candidate.isVisible().catch(() => false);
                const isDisabled = await candidate.isDisabled().catch(() => false);
                if (isVisible && !isDisabled) {
                  visible.push(candidate);
                }
              }
              return visible;
            };
            const readValues = async (inputs) => {
              return await Promise.all(inputs.map(async (input) => String(await input.inputValue().catch(() => "")).replace(/\s+/g, "").trim()));
            };
            let typed = { ok: false, error: "device-code-input-not-found" };
            const inputs = await locateInputs();
            if (!inputs.length) {
              typed = { ok: false, error: "device-code-input-not-found" };
            } else {
              try {
                if (inputs.length === 1) {
                  await inputs[0].click({ force: true });
                  await inputs[0].fill("");
                  await page.keyboard.type(plainCode, { delay: 35 });
                } else {
                  await inputs[0].click({ force: true });
                  await page.keyboard.type(plainCode, { delay: 35 });
                }
                await page.waitForTimeout(150).catch(() => {});
                let values = await readValues(inputs);
                let filled = values.join("");
                if (filled !== plainCode) {
                  await page.evaluate(({ selector, plainCode }) => {
                    const normalize = (value) => String(value || "").replace(/\s+/g, "").trim();
                    const isVisible = (node) => {
                      if (!(node instanceof Element)) return false;
                      const style = window.getComputedStyle(node);
                      if (!style || style.display === "none" || style.visibility === "hidden") return false;
                      const rect = node.getBoundingClientRect();
                      return rect.width > 0 && rect.height > 0;
                    };
                    const dispatch = (node) => {
                      node.dispatchEvent(new Event("input", { bubbles: true }));
                      node.dispatchEvent(new Event("change", { bubbles: true }));
                    };
                    const domInputs = Array.from(document.querySelectorAll(selector))
                      .filter((node) => node instanceof HTMLInputElement)
                      .filter((node) => isVisible(node) && !node.disabled);
                    if (domInputs.length === 1) {
                      domInputs[0].focus();
                      domInputs[0].value = plainCode;
                      dispatch(domInputs[0]);
                      return;
                    }
                    const chars = plainCode.split("");
                    domInputs.forEach((input, index) => {
                      input.focus();
                      input.value = chars[index] || "";
                      dispatch(input);
                    });
                  }, { selector, plainCode }).catch(() => {});
                  await page.waitForTimeout(150).catch(() => {});
                  values = await readValues(inputs);
                  filled = values.join("");
                }
                typed = {
                  ok: filled === plainCode,
                  input_count: inputs.length,
                  values,
                  filled,
                };
              } catch (error) {
                typed = {
                  ok: false,
                  error: String(error?.message || error || "device-code-fill-failed"),
                };
              }
            }
            await page.waitForTimeout(250).catch(() => {});
            return {
              ...typed,
              current_url: page.url(),
            };
  - submit_device_authorization_code:
      if: "${state.vars.device_authorization_surface?.device_code_prompt === true && state.steps.fill_device_authorization_code?.action?.ok === true}"
      metadata:
        templateRef: click_primary_continue
        description: "Submit the populated device-authorization code form."
  - inspect_device_authorization_after_code:
      if: "${state.vars.device_authorization_surface?.device_code_prompt === true || state.vars.device_authorization_surface?.oauth_continue_visible === true || state.vars.device_authorization_surface?.saw_oauth_consent === true || state.vars.device_authorization_surface?.success === true}"
      metadata:
        description: "Refresh the device-authorization surface after any code entry."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const title = await page.title().catch(() => "");
            const bodyText = await page.locator("body").innerText().catch(() => "");
            const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
            const normalizedText = normalize(bodyText);
            const visibleButtons = await page.evaluate(() => Array.from(document.querySelectorAll("button,a,[role=\"button\"],input[type=\"submit\"]")).map((node) => String(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || "")).replace(/\s+/g, " ").trim()).filter(Boolean).slice(0, 16)).catch(() => []);
            const sawOauthConsent = /sign in to codex with chatgpt|share your name, email, and profile picture with codex|link your account/i.test(normalizedText) || /sign in to codex/i.test(title);
            const deviceAuthDisabled = /enable device code authorization for codex in chatgpt security settings/i.test(normalizedText);
            const oauthContinueVisible = sawOauthConsent && visibleButtons.some((text) => /continue to codex|continue to openai|authorize|allow|accept/i.test(text) || /^continue$/i.test(text));
            const success = /you can close this window|successfully logged in|signed in|device authorized|authorization complete/i.test(normalizedText) || /close this window/i.test(title);
            const retryableTimeout = /oops, an error occurred|operation timed out|try again/i.test(normalizedText) || /oops, an error occurred|operation timed out/i.test(title);
            const rateLimitExceeded = /rate limit exceeded|too many requests|please try again later/i.test(normalizedText);
            return {
              ok: true,
              current_url: page.url(),
              headline: normalizedText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
              saw_oauth_consent: sawOauthConsent,
              device_auth_disabled: deviceAuthDisabled,
              oauth_continue_visible: oauthContinueVisible,
              success,
              retryable_timeout: retryableTimeout,
              rate_limit_exceeded: rateLimitExceeded,
            };
  - submit_device_authorization_consent:
      if: "${(state.steps.inspect_device_authorization_after_code?.action?.oauth_continue_visible === true || state.steps.inspect_device_authorization_after_code?.action?.saw_oauth_consent === true || state.vars.device_authorization_surface?.oauth_continue_visible === true || state.vars.device_authorization_surface?.saw_oauth_consent === true) && state.steps.inspect_device_authorization_after_code?.action?.device_auth_disabled !== true && state.vars.device_authorization_surface?.device_auth_disabled !== true}"
      metadata:
        templateRef: click_oauth_consent_continue
        description: "Approve the final Codex device-authorization consent step."
  - inspect_device_authorization_final:
      if: "${(state.steps.submit_device_authorization_consent?.action?.ok === true && state.steps.submit_device_authorization_consent?.action?.skipped !== true) || state.steps.inspect_device_authorization_after_code?.action?.success === true || state.steps.inspect_device_authorization_surface?.action?.success === true}"
      metadata:
        description: "Inspect the final surface after any device-authorization consent action."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const readSurface = async () => {
              const title = await page.title().catch(() => "");
              const bodyText = await page.locator("body").innerText().catch(() => "");
              const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
              const normalizedText = normalize(bodyText);
              const visibleButtons = await page.evaluate(() => Array.from(document.querySelectorAll("button,a,[role=\"button\"],input[type=\"submit\"]")).map((node) => String(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || "")).replace(/\s+/g, " ").trim()).filter(Boolean).slice(0, 16)).catch(() => []);
              const verificationInput = await page.locator("input[name*=\"code\" i], input[autocomplete=\"one-time-code\"], input[aria-label*=\"code\" i], input[placeholder*=\"code\" i], input[inputmode=\"numeric\"]").count().catch(() => 0) > 0;
              const surfaceText = String(title || "") + "\n" + normalizedText;
              const deviceCodePrompt = verificationInput && /device code|one-time code|authorization code|enter (?:the |your )?(?:\d+-character )?code|code displayed in your terminal/i.test(surfaceText);
            const sawOauthConsent = /sign in to codex with chatgpt|share your name, email, and profile picture with codex|link your account/i.test(normalizedText) || /sign in to codex/i.test(title);
            const oauthContinueVisible = sawOauthConsent && visibleButtons.some((text) => /continue to codex|continue to openai|authorize|allow|accept|continue/i.test(text));
              return {
                ok: true,
                current_url: page.url(),
                headline: normalizedText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
                device_code_prompt: deviceCodePrompt,
                oauth_continue_visible: oauthContinueVisible,
                success: /you can close this window|successfully logged in|signed in|device authorized|authorization complete/i.test(normalizedText) || /close this window/i.test(title),
              };
            };

            let surface = await readSurface();
            if (/\/deviceauth\/callback(?:[/?#]|$)/i.test(String(surface.current_url || "")) && surface.device_code_prompt !== true && surface.success !== true && surface.oauth_continue_visible !== true) {
              for (const waitMs of [500, 1000, 1500, 2500, 4000]) {
                await page.waitForTimeout(waitMs).catch(() => {});
                surface = await readSurface();
                if (surface.device_code_prompt === true || surface.success === true || surface.oauth_continue_visible === true || surface.headline) {
                  break;
                }
              }
            }

            return surface;
  - fill_callback_device_authorization_code:
      if: "${state.steps.inspect_device_authorization_final?.action?.device_code_prompt === true && String(state.vars.codex_login?.device_code || '').trim() !== ''}"
      metadata:
        description: "Fill the Codex device code on the post-consent callback page when OpenAI asks for it after consent."
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const rawCode = String(state.vars?.codex_login?.device_code || args.device_code || "").trim().toUpperCase();
            const selector = 'input[name*="code" i], input[autocomplete="one-time-code"], input[aria-label*="code" i], input[placeholder*="code" i], input[inputmode="numeric"]';
            if (!rawCode) {
              return { ok: false, error: "device-code-missing", current_url: page.url() };
            }
            const plainCode = rawCode.replace(/[^A-Z0-9]/g, "");
            const locateInputs = async () => {
              const locator = page.locator(selector);
              const count = await locator.count().catch(() => 0);
              const visible = [];
              for (let index = 0; index < count; index += 1) {
                const candidate = locator.nth(index);
                const isVisible = await candidate.isVisible().catch(() => false);
                const isDisabled = await candidate.isDisabled().catch(() => false);
                if (isVisible && !isDisabled) {
                  visible.push(candidate);
                }
              }
              return visible;
            };
            const readValues = async (inputs) => {
              return await Promise.all(inputs.map(async (input) => String(await input.inputValue().catch(() => "")).replace(/\s+/g, "").trim()));
            };
            let typed = { ok: false, error: "device-code-input-not-found" };
            const inputs = await locateInputs();
            if (!inputs.length) {
              typed = { ok: false, error: "device-code-input-not-found" };
            } else {
              try {
                if (inputs.length === 1) {
                  await inputs[0].click({ force: true });
                  await inputs[0].fill("");
                  await page.keyboard.type(plainCode, { delay: 35 });
                } else {
                  await inputs[0].click({ force: true });
                  await page.keyboard.type(plainCode, { delay: 35 });
                }
                await page.waitForTimeout(150).catch(() => {});
                let values = await readValues(inputs);
                let filled = values.join("");
                if (filled !== plainCode) {
                  await page.evaluate(({ selector, plainCode }) => {
                    const normalize = (value) => String(value || "").replace(/\s+/g, "").trim();
                    const isVisible = (node) => {
                      if (!(node instanceof Element)) return false;
                      const style = window.getComputedStyle(node);
                      if (!style || style.display === "none" || style.visibility === "hidden") return false;
                      const rect = node.getBoundingClientRect();
                      return rect.width > 0 && rect.height > 0;
                    };
                    const dispatch = (node) => {
                      node.dispatchEvent(new Event("input", { bubbles: true }));
                      node.dispatchEvent(new Event("change", { bubbles: true }));
                    };
                    const domInputs = Array.from(document.querySelectorAll(selector))
                      .filter((node) => node instanceof HTMLInputElement)
                      .filter((node) => isVisible(node) && !node.disabled);
                    if (domInputs.length === 1) {
                      domInputs[0].focus();
                      domInputs[0].value = plainCode;
                      dispatch(domInputs[0]);
                      return;
                    }
                    const chars = plainCode.split("");
                    domInputs.forEach((input, index) => {
                      input.focus();
                      input.value = chars[index] || "";
                      dispatch(input);
                    });
                  }, { selector, plainCode }).catch(() => {});
                  await page.waitForTimeout(150).catch(() => {});
                  values = await readValues(inputs);
                  filled = values.join("");
                }
                typed = {
                  ok: filled === plainCode,
                  input_count: inputs.length,
                  values,
                  filled,
                };
              } catch (error) {
                typed = {
                  ok: false,
                  error: String(error?.message || error || "device-code-fill-failed"),
                };
              }
            }
            await page.waitForTimeout(250).catch(() => {});
            return {
              ...typed,
              current_url: page.url(),
            };
  - submit_callback_device_authorization_code:
      if: "${state.steps.inspect_device_authorization_final?.action?.device_code_prompt === true && state.steps.fill_callback_device_authorization_code?.action?.ok === true}"
      metadata:
        templateRef: click_primary_continue
        description: "Submit the device code from the post-consent callback page."
  - inspect_device_authorization_after_callback_code:
      if: "${state.steps.submit_callback_device_authorization_code?.action?.ok === true && state.steps.submit_callback_device_authorization_code?.action?.skipped !== true}"
      metadata:
        description: "Inspect the device-authorization callback page after submitting the device code."
        mode: headless_ok
        targetRef: primary
      call: afn.driver.browser.exec_script
      with:
        body:
          script: |
            const readSurface = async () => {
              const title = await page.title().catch(() => "");
              const bodyText = await page.locator("body").innerText().catch(() => "");
              const normalize = (value) => String(value || "").replace(/\s+/g, " ").trim();
              const normalizedText = normalize(bodyText);
              const visibleButtons = await page.evaluate(() => Array.from(document.querySelectorAll("button,a,[role=\"button\"],input[type=\"submit\"]")).map((node) => String(node instanceof HTMLInputElement ? node.value : (node.innerText || node.textContent || "")).replace(/\s+/g, " ").trim()).filter(Boolean).slice(0, 16)).catch(() => []);
              const verificationInput = await page.locator("input[name*=\"code\" i], input[autocomplete=\"one-time-code\"], input[aria-label*=\"code\" i], input[placeholder*=\"code\" i], input[inputmode=\"numeric\"]").count().catch(() => 0) > 0;
              const surfaceText = String(title || "") + "\n" + normalizedText;
              const deviceCodePrompt = verificationInput && /device code|one-time code|authorization code|enter (?:the |your )?(?:\d+-character )?code|code displayed in your terminal/i.test(surfaceText);
              const sawOauthConsent = /sign in to codex with chatgpt|share your name, email, and profile picture with codex|link your account/i.test(normalizedText) || /sign in to codex/i.test(title);
              const oauthContinueVisible = sawOauthConsent && visibleButtons.some((text) => /continue to codex|continue to openai|authorize|allow|accept|continue/i.test(text));
              return {
                ok: true,
                current_url: page.url(),
                headline: normalizedText.split("\n").map((value) => value.trim()).filter(Boolean)[0] || title || null,
                device_code_prompt: deviceCodePrompt,
                oauth_continue_visible: oauthContinueVisible,
                success: /you can close this window|successfully logged in|signed in|device authorized|authorization complete/i.test(normalizedText) || /close this window/i.test(title),
              };
            };

            let surface = await readSurface();
            if (/\/deviceauth\/callback(?:[/?#]|$)/i.test(String(surface.current_url || "")) && surface.device_code_prompt !== true && surface.success !== true && surface.oauth_continue_visible !== true) {
              for (const waitMs of [500, 1000, 1500, 2500, 4000]) {
                await page.waitForTimeout(waitMs).catch(() => {});
                surface = await readSurface();
                if (surface.device_code_prompt === true || surface.success === true || surface.oauth_continue_visible === true || surface.headline) {
                  break;
                }
              }
            }

            return surface;
  - wait_for_codex_login_exit:
      if: "${(state.vars.codex_login?.exit_path || inputs.codex_login_exit_path) && (state.steps.open_device_authorization_entry?.action?.ok === true && state.steps.open_device_authorization_entry?.action?.skipped !== true) && state.steps.inspect_device_authorization_after_code?.action?.device_auth_disabled !== true && state.vars.device_authorization_surface?.device_auth_disabled !== true && (state.steps.inspect_device_authorization_final?.action?.device_code_prompt !== true || (state.steps.submit_callback_device_authorization_code?.action?.ok === true && state.steps.submit_callback_device_authorization_code?.action?.skipped !== true))}"
      metadata:
        description: "Wait for the detached codex device-auth process to exit after browser authorization completes."
      run:
        script:
          language: javascript
          code: |
            const fs = await import("node:fs");
            const { setTimeout: delay } = await import("node:timers/promises");
            const exitPath = String(state.vars?.codex_login?.exit_path || args.codex_login_exit_path || "").trim();
            const stdoutPath = String(state.vars?.codex_login?.stdout_path || args.codex_login_stdout_path || "").trim();
            const stderrPath = String(state.vars?.codex_login?.stderr_path || args.codex_login_stderr_path || "").trim();
            if (!exitPath) {
              return { ok: true, skipped: true };
            }
            const readText = (filePath) => (!filePath || !fs.existsSync(filePath)) ? "" : fs.readFileSync(filePath, "utf8");
            const tail = (filePath, maxChars = 1200) => {
              const text = readText(filePath);
              return text.length > maxChars ? text.slice(-maxChars) : text;
            };
            const timeoutMs = 45000;
            const startedAt = Date.now();
            while (Date.now() - startedAt < timeoutMs) {
              if (fs.existsSync(exitPath)) {
                const raw = readText(exitPath).trim();
                const parsed = raw === "" ? null : Number.parseInt(raw, 10);
                const exitCode = Number.isInteger(parsed) ? parsed : null;
                return {
                  ok: exitCode === 0,
                  exit_code: exitCode,
                  stdout_tail: tail(stdoutPath) || null,
                  stderr_tail: tail(stderrPath) || null,
                };
              }
              await delay(250);
            }
            return {
              ok: false,
              timeout: true,
              exit_code: null,
              stdout_tail: tail(stdoutPath) || null,
              stderr_tail: tail(stderrPath) || null,
            };
  - finalize_device_auth_tail_summary:
      if: "${state.vars.prepare_flow_ready === true && (String(state.vars.codex_login?.auth_url || '').trim() !== '' || String(state.vars.codex_login?.stdout_path || '').trim() !== '' || String(state.vars.codex_login?.stderr_path || '').trim() !== '' || String(inputs.codex_login_stdout_path || '').trim() !== '' || String(inputs.codex_login_stderr_path || '').trim() !== '')}"
      metadata:
        description: "Build the final device-authorization outcome from the browser surface and detached Codex session."
      run:
        script:
          language: javascript
          code: |
            const fs = await import("node:fs");
            const prepare = state.vars.prepare_flow_output || {};
            const existingShell = state.steps.inspect_existing_chatgpt_session?.action || {};
            const shell = state.steps.inspect_chatgpt_shell?.action || {};
            const inspectInitial = state.steps.inspect_device_authorization_surface?.action || {};
            const inspectAfterLogin = state.steps.inspect_device_authorization_after_login?.action || {};
            const inspect = inspectAfterLogin.current_url ? inspectAfterLogin : inspectInitial;
            const afterCode = state.steps.inspect_device_authorization_after_code?.action || {};
            const finalInspect = state.steps.inspect_device_authorization_final?.action || {};
            const afterCallbackCode = state.steps.inspect_device_authorization_after_callback_code?.action || {};
            const exitResult = state.steps.wait_for_codex_login_exit?.action?.value || {};
            const session = state.vars?.codex_login && typeof state.vars.codex_login === "object" && !Array.isArray(state.vars.codex_login) ? state.vars.codex_login : {};
            const readText = (filePath) => (!filePath || !fs.existsSync(filePath)) ? "" : fs.readFileSync(filePath, "utf8");
            const stripAnsi = (raw) => String(raw || "").replace(/\u001b\[[0-9;]*m/g, "");
            const extractDeviceChallenge = (raw) => {
              const cleaned = stripAnsi(raw);
              const authMatch = cleaned.match(/https:\/\/auth\.openai\.com\/codex\/device\b[^\s"'<>]*/);
              const codeMatch = cleaned.match(/\b[A-Z0-9]{4}-[A-Z0-9]{5}\b/);
              return {
                auth_url: authMatch ? authMatch[0] : null,
                device_code: codeMatch ? codeMatch[0] : null,
              };
            };
            const recoveredChallenge = extractDeviceChallenge(
              readText(String(session.stdout_path || "")) + "\n" + readText(String(session.stderr_path || ""))
            );
            const resolvedAuthUrl = String(session.auth_url || recoveredChallenge.auth_url || "").trim() || null;
            const resolvedDeviceCode = String(session.device_code || recoveredChallenge.device_code || "").trim() || null;
            const lastSurface = afterCallbackCode.current_url ? afterCallbackCode : (finalInspect.current_url ? finalInspect : (afterCode.current_url ? afterCode : inspect));
            const callbackComplete = /\/deviceauth\/callback(?:[/?#]|$)/i.test(
              String(lastSurface.current_url || inspect.current_url || "")
            ) && lastSurface.device_code_prompt !== true;
            const deviceCodePrompt = afterCallbackCode.device_code_prompt === true
              || finalInspect.device_code_prompt === true
              || afterCode.device_code_prompt === true
              || inspect.device_code_prompt === true;
            const oauthContinueVisible = afterCallbackCode.oauth_continue_visible === true
              || finalInspect.oauth_continue_visible === true
              || afterCode.oauth_continue_visible === true
              || inspect.oauth_continue_visible === true;
            const finalSuccess = afterCallbackCode.success === true
              || finalInspect.success === true
              || afterCode.success === true
              || inspect.success === true;
            const emailVerification = inspect.email_verification === true || inspectInitial.email_verification === true || afterCode.email_verification === true;
            const authPrompt = inspect.auth_prompt === true && emailVerification !== true;
            const addPhonePrompt = inspect.add_phone_prompt === true
              || inspectInitial.add_phone_prompt === true
              || afterCode.add_phone_prompt === true
              || finalInspect.add_phone_prompt === true
              || afterCallbackCode.add_phone_prompt === true;
            const stateMismatch = inspect.state_mismatch === true || inspectInitial.state_mismatch === true;
            const deviceAuthDisabled = afterCode.device_auth_disabled === true
              || state.vars.device_authorization_surface?.device_auth_disabled === true;
            const preDeviceSessionConfirmed = state.vars.reuse_device_auth_session === true
              || state.steps.activate_chatgpt_security_settings_tab?.action?.ok === true
              || state.steps.ensure_device_code_authorization_enabled?.action?.enabled === true
              || state.vars.prepare_flow_ready === true
              || prepare.account_ready === true
              || prepare.stage === "app_shell"
              || existingShell.account_ready === true
              || existingShell.stage === "app_shell"
              || shell.account_ready === true
              || shell.stage === "app_shell";
            const issuedDeviceChallenge = Boolean(resolvedAuthUrl && resolvedDeviceCode);
            const cliCompleted = exitResult.ok === true
              && exitResult.skipped !== true
              && exitResult.timeout !== true
              && (exitResult.exit_code === 0 || exitResult.exit_code == null);
            const success = preDeviceSessionConfirmed
              && issuedDeviceChallenge
              && deviceAuthDisabled !== true
              && (finalSuccess || cliCompleted);
            const retryableTimeout = inspect.retryable_timeout === true || inspectInitial.retryable_timeout === true || afterCode.retryable_timeout === true;
            const rateLimitExceeded = inspect.rate_limit_exceeded === true || inspectInitial.rate_limit_exceeded === true || afterCode.rate_limit_exceeded === true;
            const codexStderrTail = String(exitResult.stderr_tail || session.stderr_tail || "").trim();
            const codexStdoutTail = String(exitResult.stdout_tail || session.stdout_tail || "").trim();
            const deviceAuthRateLimited = /device code request failed with status 429|device auth failed with status 429|429 too many requests/i.test(codexStderrTail || codexStdoutTail);
            let errorMessage = null;
            let nextAction = success ? "complete" : "fail_terminal";
            let retryReason = null;
            if (deviceAuthRateLimited) {
              nextAction = "retry_attempt";
              retryReason = "device_auth_rate_limit";
              errorMessage = codexStderrTail || codexStdoutTail || "Codex device authorization is rate limited.";
            } else if (rateLimitExceeded) {
              nextAction = "retry_attempt";
              retryReason = "rate_limit";
              errorMessage = "OpenAI rate-limited the device authorization flow.";
            } else if (retryableTimeout) {
              nextAction = "retry_attempt";
              retryReason = "retryable_timeout";
              errorMessage = "OpenAI timed out while advancing the device authorization flow.";
            } else if (addPhonePrompt) {
              nextAction = "retry_attempt";
              retryReason = "device_auth_add_phone";
              errorMessage = "OpenAI blocked the final device-authorization login with an add-phone prompt.";
            } else if (deviceAuthDisabled) {
              errorMessage = "ChatGPT did not expose or enable the Codex device-code authorization toggle before device authorization started.";
            } else if (issuedDeviceChallenge !== true) {
              errorMessage = "Codex device authorization never issued a device URL and code after ChatGPT account preparation.";
            } else if (preDeviceSessionConfirmed !== true) {
              errorMessage = "Managed browser session could not be confirmed in ChatGPT before device authorization started.";
            } else if (stateMismatch) {
              errorMessage = "OpenAI returned a state mismatch during Codex device authorization.";
            } else if (emailVerification) {
              errorMessage = "OpenAI remained on email verification instead of advancing to the device authorization surface.";
            } else if (authPrompt) {
              errorMessage = "OpenAI redirected the device-authorization entry back to a login prompt instead of showing the device authorization screen.";
            } else if (deviceCodePrompt) {
              errorMessage = "OpenAI still requires the Codex device code before authorization can complete.";
            } else if (oauthContinueVisible && success !== true) {
              errorMessage = "OpenAI still requires consent before Codex device authorization can complete.";
            } else if (success !== true) {
              errorMessage = "Codex device authorization did not complete.";
            }
            return {
              stage: success ? "success" : (retryReason === "device_auth_rate_limit" ? "device_auth_rate_limit" : (retryReason === "retryable_timeout" ? "retryable_timeout" : "device_authorization_failed")),
              current_url: lastSurface.current_url || inspect.current_url || shell.current_url || null,
              headline: lastSurface.headline || inspect.headline || shell.headline || null,
              callback_complete: callbackComplete || cliCompleted,
              success,
              device_code_prompt: deviceCodePrompt,
              add_phone_prompt: addPhonePrompt,
              auth_prompt: authPrompt,
              email_verification: emailVerification,
              state_mismatch: stateMismatch,
              retryable_timeout: retryableTimeout,
              rate_limit_exceeded: rateLimitExceeded,
              next_action: nextAction,
              retry_reason: retryReason,
              error_message: success ? null : errorMessage,
              verified_account_email: success ? (String(args.email || "").trim().toLowerCase() || null) : null,
              codex_session: {
                auth_url: resolvedAuthUrl,
                callback_url: session.callback_url || null,
                callback_port: session.callback_port ?? null,
                device_code: resolvedDeviceCode,
                session_dir: session.session_dir || null,
                codex_home_path: session.codex_home_path || null,
                auth_file_path: session.auth_file_path || null,
                pid: session.pid ?? null,
                stdout_path: session.stdout_path || null,
                stderr_path: session.stderr_path || null,
                exit_path: session.exit_path || null,
              },
              codex_login_exit_ok: exitResult.ok === true,
              codex_login_exit_code: exitResult.exit_code ?? session.exit_code ?? null,
              codex_login_stdout_tail: exitResult.stdout_tail || session.stdout_tail || null,
              codex_login_stderr_tail: exitResult.stderr_tail || session.stderr_tail || null,
            };
  - finalize_selected_flow:
      metadata:
        description: "Return the merged device-auth outcome when preparation succeeded, otherwise surface the preparation failure."
      run:
        script:
          language: javascript
          code: |
            const prepare =
              state.vars.prepare_flow_output || {};
            const tailAction =
              state.steps.finalize_device_auth_tail_summary?.action || null;
            const tail =
              tailAction && tailAction.skipped !== true
                ? (tailAction.value || null)
                : null;
            const prepareComplete =
              state.vars.prepare_flow_ready === true
              || prepare.account_ready === true
              || prepare.stage === "app_shell";
            const selected = tail || {
              ...(prepare && typeof prepare === "object" ? prepare : {}),
              success: false,
              callback_complete: false,
              codex_login_exit_ok: false,
              codex_login_exit_code: null,
              next_action: prepareComplete ? "fail_terminal" : ((prepare && prepare.next_action) || "fail_terminal"),
              error_message:
                (prepare && prepare.error_message)
                || (prepareComplete
                    ? "Codex device authorization never started after ChatGPT account preparation."
                    : "ChatGPT account preparation did not reach an authenticated session for device authorization."),
            };
            const tailAttempted = Boolean(tail);
            const tailSelected = Boolean(tail);

            return {
              ...(selected && typeof selected === "object" ? selected : {}),
              selected_flow:
                "workspace.web.auth-openai-com.codex-rotate-account-flow-device-auth",
              prepare_flow:
                "workspace.web.auth-openai-com.codex-rotate-account-flow-device-auth",
              prepare_completed: prepareComplete,
              tail_attempted: tailAttempted,
              tail_selected: tailSelected,
              merged_tail: tailSelected,
              selection_strategy:
                "single-device-auth-workflow",
            };
  - teardown_abandoned_codex_login_session:
      if: "${state.steps.finalize_selected_flow?.action?.value?.next_action != 'complete' && (state.steps.finalize_selected_flow?.action?.value?.codex_session?.pid ?? 0) > 0}"
      metadata:
        description: "Terminate the detached device-auth Codex login session when this flow exits without completing."
      run:
        script:
          language: javascript
          code: |
            const pid = Number(
              state.steps.finalize_selected_flow?.action?.value?.codex_session?.pid || 0
            );

            if (!Number.isInteger(pid) || pid <= 0) {
              return {
                ok: true,
                skipped: true,
              };
            }

            const sendSignal = (signal) => {
              try {
                process.kill(-pid, signal);
                return true;
              } catch (error) {
                if (error && typeof error === "object" && error.code === "ESRCH") {
                  return false;
                }
                throw error;
              }
            };

            const terminated = sendSignal("SIGTERM");
            return {
              ok: true,
              pid,
              terminated,
              signal: "SIGTERM",
            };
output:
  as: "${state.steps.finalize_selected_flow?.action?.value || {}}"
